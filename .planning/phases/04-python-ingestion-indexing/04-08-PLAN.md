---
phase: "04-python-ingestion-indexing"
plan: "08"
type: "execute"
wave: 8
depends_on: ["07"]
files_modified:
  - "ai-service/README.md"
  - "infra/README.md"
  - ".planning/phases/04-python-ingestion-indexing/04-UAT.md"
  - "ai-service/tests"
autonomous: true
requirements: ["ING-01", "ING-02", "ING-03", "ING-04", "ING-05", "ING-06", "ING-07"]
user_setup:
  - "For full UAT, provide enough Docker Desktop memory for local bge-m3 and set GEMINI_API_KEY."
must_haves:
  truths:
    - "End-of-phase UAT runs on retained Docker volumes and consumes accumulated Phase 3 messages."
    - "UAT proves success, forced parsing failure, graph-upsert rollback, duplicate redelivery, and optional delete cleanup."
---

<objective>
Harden documentation and execute the Phase 4 end-to-end UAT script.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@.planning/phases/04-python-ingestion-indexing/RESEARCH.md
@infra/docker-compose.yml
@ai-service/README.md
@infra/README.md
@CLAUDE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add operational docs and UAT checklist</name>
  <files>ai-service/README.md, infra/README.md, .planning/phases/04-python-ingestion-indexing/04-UAT.md</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, .planning/phases/04-python-ingestion-indexing/RESEARCH.md</read_first>
  <action>Document local FlagEmbedding model cache, memory expectations, first-run model download, Gemini key requirement, integration test markers, and how to start the stack without clearing retained RabbitMQ volumes. Create `04-UAT.md` with the locked scenarios: P1 FlagEmbedding smoke, P2 Gemini structured-output smoke, P3 Docker stack startup, accumulated Phase 3 messages, fresh PDF happy path, broken PDF parsing failure, Neo4j-down graph rollback, duplicate redelivery, optional delete cleanup.</action>
  <verify>
    <automated>Get-Content .planning/phases/04-python-ingestion-indexing/04-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - UAT doc has concrete commands/queries for Qdrant, Neo4j, Java status, AI Postgres, and RabbitMQ checks.
    - Docs explicitly say not to clear retained volumes before Scenario 1.
    - HF sparse-output preflight is absent; FlagEmbedding smoke is present.
  </acceptance_criteria>
  <done>Humans have a runnable UAT script.</done>
</task>

<task type="auto">
  <name>Task 2: Add final integration markers and smoke helpers</name>
  <files>ai-service/tests, ai-service/README.md</files>
  <read_first>ai-service/tests, .planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Add or refine pytest markers for slow/live integration tests: local FlagEmbedding smoke, Qdrant live smoke, Neo4j live smoke, Gemini live extraction. Default test run must skip live external tests unless env flags/API keys are present. README must show exact `uv run pytest -m integration` style command and required env vars.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Default pytest does not require model download or Gemini key.
    - Integration-marked tests skip clearly when prerequisites are missing.
    - Live tests can be selected intentionally for UAT.
  </acceptance_criteria>
  <done>Final verification separates CI-safe tests from live UAT tests.</done>
</task>

<task type="manual">
  <name>Task 3: Execute Phase 4 UAT on retained volumes</name>
  <files>.planning/phases/04-python-ingestion-indexing/04-UAT.md, .planning/phases/04-python-ingestion-indexing/04-08-SUMMARY.md</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-UAT.md</read_first>
  <action>Run the UAT scenarios exactly from `04-UAT.md` after all automated tests pass. Use retained Docker volumes from Phase 3. Confirm accumulated messages are consumed, one retained document indexes, the delete race ends deleted/cleaned, fresh PDF indexes, broken PDF fails PARSING, Neo4j-down upload fails GRAPH_UPSERT with Qdrant rollback, duplicate event redelivery is skipped, and optional delete cleanup works. Record exact document IDs, event IDs, and command outputs in the summary.</action>
  <verify>
    <manual>Run P1/P2/P3 preflights and Scenarios 1-5 from 04-UAT.md; Scenario 6 optional.</manual>
  </verify>
  <acceptance_criteria>
    - P1, P2, and P3 preflights pass.
    - Scenarios 1 through 5 pass.
    - Scenario 4 proves Qdrant has no points for the GRAPH_UPSERT failed document after rollback.
    - Any UAT deviation is documented with follow-up fix or explicit blocker.
  </acceptance_criteria>
  <done>Phase 4 has end-to-end UAT evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local developer environment | Missing RAM/API keys/model cache can invalidate UAT. |
| Manual verification | Vague checks can pass despite partial state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-08-01 | Reliability | UAT retained queues | mitigate | Explicit no-clean-state precondition and row/message count checks. |
| T-04-08-02 | Repudiation | UAT evidence | mitigate | Record document IDs, event IDs, queue counts, and store queries in summary. |
| T-04-08-03 | Reliability | live prerequisites | mitigate | Preflight FlagEmbedding, Gemini, Docker stack before scenarios. |
</threat_model>

<verification>
- [ ] `python scripts/verify-contracts.py` passes.
- [ ] `cd ai-service; uv run pytest tests` passes.
- [ ] Docker stack starts with retained volumes.
- [ ] UAT Scenarios 1-5 pass; Scenario 6 is documented if skipped.
</verification>

<success_criteria>
- Phase 4 is ready for verify-work with automated and human UAT evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python-ingestion-indexing/04-08-SUMMARY.md`.
</output>
