---
phase: "04-python-ingestion-indexing"
plan: "09"
type: "execute"
wave: 9
depends_on: ["07", "08"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/uv.lock"
  - "ai-service/src/corp_rag_ai/config.py"
  - "ai-service/src/corp_rag_ai/main.py"
  - "ai-service/src/corp_rag_ai/domain/exceptions.py"
  - "ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py"
  - "ai-service/tests/test_entity_extractor.py"
  - "ai-service/tests/test_entity_extraction_fixture.py"
  - "ai-service/tests/test_deepseek_extraction_live.py"
  - "ai-service/tests/test_live_smokes.py"
  - "docs/ARCHITECTURE.md"
  - "docs/decisions/ADR-001-embedding-model.md"
  - "docs/decisions/ADR-004-llm-provider-deepseek-openrouter.md"
  - "docs/decisions/README.md"
  - ".planning/phases/04-python-ingestion-indexing/04-CONTEXT.md"
  - ".planning/phases/04-python-ingestion-indexing/04-UAT.md"
  - ".planning/phases/04-python-ingestion-indexing/04-USER-SETUP.md"
  - ".env.example"
  - "infra/.env.example"
  - "infra/docker-compose.yml"
  - "infra/README.md"
  - "ai-service/README.md"
autonomous: true
requirements: ["ING-05", "ING-06", "ING-07"]
user_setup:
  - "Set OPENROUTER_API_KEY in the shell and infra/.env before live DeepSeek/OpenRouter tests."
must_haves:
  truths:
    - "Gemini and google-genai are fully removed from runtime code and tests."
    - "All LLM use cases use one default model ID: deepseek/deepseek-v4-flash:free through OpenRouter."
    - "Entity extraction uses OpenRouter strict json_schema mode with response-healing and bounded retry."
---

<objective>
Pivot the Phase 4 LLM provider from Gemini to DeepSeek V4 Flash through OpenRouter so blocked UAT P2 can be retried with one OpenAI-compatible SDK, one API key, and one model ID.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@.planning/phases/04-python-ingestion-indexing/04-UAT.md
@docs/ARCHITECTURE.md
@docs/decisions
@ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py
@ai-service/tests
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace Gemini SDK with OpenRouter client</name>
  <files>ai-service/pyproject.toml, ai-service/uv.lock, ai-service/src/corp_rag_ai/config.py, ai-service/src/corp_rag_ai/main.py, ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py</files>
  <action>Remove `google-genai`, add `openai>=1.40,<2.0`, add OpenRouter settings, and rewrite `GeminiEntityExtractor` into `DeepSeekEntityExtractor` using `openai.AsyncOpenAI` with `base_url`, `response_format.type=json_schema`, `strict=true`, sanitized Pydantic schema, `extra_body.plugins=[{"id":"response-healing"}]`, `temperature=0.1`, and `max_tokens=2000`.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_entity_extractor.py tests/test_entity_extraction_fixture.py</automated>
  </verify>
  <acceptance_criteria>
    - Runtime code has no `google.genai`, `google-genai`, or `GEMINI_API_KEY` references.
    - Retry policy is 3 attempts for 429/5xx/timeouts/connection errors, no retry for 401/403, and one malformed JSON retry.
    - Schema sanitization still removes `additionalProperties` recursively.
  </acceptance_criteria>
  <done>DeepSeek/OpenRouter is the only Phase 4 entity extraction provider.</done>
</task>

<task type="auto">
  <name>Task 2: Update failure matrix and StageFailure wording</name>
  <files>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, ai-service/src/corp_rag_ai/domain/exceptions.py, ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py</files>
  <action>Replace provider-specific failure rows with OpenRouter HTTP semantics: 401/403 auth, 429 rate limit, 5xx, timeout/connection, and malformed JSON. Keep Russian user message plus English technical detail, replacing provider text with DeepSeek/OpenRouter.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_stage_failure.py tests/test_entity_extractor.py</automated>
  </verify>
  <acceptance_criteria>
    - Auth failures are `DEPENDENCY_UNAVAILABLE`, non-retryable.
    - Rate limit, timeout, connection, and 5xx failures become retryable dependency failures after bounded retry.
    - 400/schema/malformed output failures are non-retryable indexing pipeline failures.
  </acceptance_criteria>
  <done>Failed indexing events expose safe DeepSeek/OpenRouter diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests and live preflight</name>
  <files>ai-service/tests/test_entity_extractor.py, ai-service/tests/test_entity_extraction_fixture.py, ai-service/tests/test_deepseek_extraction_live.py, ai-service/tests/test_live_smokes.py</files>
  <action>Rewrite mocks from Google Gen AI to OpenAI-compatible chat completions, rename the live entity extraction test, add a minimal DeepSeek/OpenRouter smoke in `test_live_smokes.py`, and skip live calls when `OPENROUTER_API_KEY` is absent.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Default tests stay CI-safe and skip live external calls.
    - New P2 preflight uses `OPENROUTER_API_KEY` and one chat completion call.
    - Existing golden fixture and StageFailure regression coverage remain.
  </acceptance_criteria>
  <done>Automated tests cover the OpenRouter pivot.</done>
</task>

<task type="auto">
  <name>Task 4: Update architecture and ADRs</name>
  <files>docs/ARCHITECTURE.md, docs/decisions/ADR-001-embedding-model.md, docs/decisions/ADR-004-llm-provider-deepseek-openrouter.md, docs/decisions/README.md</files>
  <action>Rewrite the LLM model table, Python LLM client row, Phase 5 schema references, structured output section, roadmap references, and ADR rationale around DeepSeek V4 Flash through OpenRouter.</action>
  <verify>
    <automated>rg -n "google-genai|GEMINI_API_KEY" docs</automated>
  </verify>
  <acceptance_criteria>
    - Architecture describes one LLM provider/model ID for generation, extraction, router, guards, and synthesizer.
    - ADR-004 records the provider decision and consequences.
    - ADR-001 keeps the free/open-source constraint aligned with the new LLM decision.
  </acceptance_criteria>
  <done>Planning documentation reflects the provider pivot.</done>
</task>

<task type="auto">
  <name>Task 5: Update UAT and operational docs</name>
  <files>.planning/phases/04-python-ingestion-indexing/04-UAT.md, .planning/phases/04-python-ingestion-indexing/04-USER-SETUP.md, .env.example, infra/.env.example, infra/docker-compose.yml, infra/README.md, ai-service/README.md</files>
  <action>Replace the P2 smoke and setup instructions with DeepSeek/OpenRouter commands, document `OPENROUTER_API_KEY`, wire compose env, and remove the old provider key surface.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
    <automated>cd ai-service; uv run pytest tests</automated>
    <manual>cd ai-service; uv run pytest tests/test_deepseek_extraction_live.py -m integration -q -s with OPENROUTER_API_KEY set</manual>
  </verify>
  <acceptance_criteria>
    - UAT P2 command targets `test_deepseek_extraction_live.py::test_live_deepseek_extracts_expected_hr_policy_subset`.
    - Setup docs point to https://openrouter.ai/keys.
    - Grep confirms no old provider key or SDK remains in runtime code/tests.
  </acceptance_criteria>
  <done>UAT can retry P2 against DeepSeek/OpenRouter.</done>
</task>
</tasks>

<verification>
- `python scripts/verify-contracts.py`
- `cd ai-service; uv run pytest tests`
- `cd ai-service; uv run pytest tests/test_deepseek_extraction_live.py -m integration -q -s` when `OPENROUTER_API_KEY` is set.
- `rg -n "gemini|google-genai|GEMINI_API_KEY" ai-service/src ai-service/tests docs`
- `rg -n "deepseek-v4-flash" ai-service/src`
</verification>

<success_criteria>
- The Python AI service uses `deepseek/deepseek-v4-flash:free` via OpenRouter as its default LLM provider.
- Phase 4 UAT P2 is rewritten from the blocked provider to DeepSeek/OpenRouter.
- One atomic commit records the hot-fix pivot.
- `04-09-SUMMARY.md` documents tasks, commit, and verification.
</success_criteria>
