---
phase: "04-python-ingestion-indexing"
plan: "04"
type: "execute"
wave: 4
depends_on: ["02", "03"]
files_modified:
  - "ai-service/src/corp_rag_ai/pipeline/ingestion/chunker.py"
  - "ai-service/src/corp_rag_ai/pipeline/ingestion/sentence_boundary.py"
  - "ai-service/src/corp_rag_ai/pipeline/ingestion/corpus_sanitizer.py"
  - "ai-service/src/corp_rag_ai/domain"
  - "ai-service/tests"
autonomous: true
requirements: ["ING-03", "ING-04"]
user_setup: []
must_haves:
  truths:
    - "Chunking is deterministic structural+size, not SemanticSplitter."
    - "Sanitizer flags suspicious chunks but drops only empty/garbage chunks."
    - "Parent content is persisted separately; Qdrant remains child-only."
  decision_refs:
    - "D-38 through D-60: chunking, IDs, parent storage, text serialization."
    - "D-104 through D-116: Tier-0 sanitizer."
---

<objective>
Implement deterministic parent/child chunking and Tier-0 child sanitizer.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@ai-service/src/corp_rag_ai/domain/document.py
@ai-service/src/corp_rag_ai/repositories
@CLAUDE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement token counter and sentence-boundary splitter</name>
  <files>ai-service/pyproject.toml, ai-service/src/corp_rag_ai/pipeline/ingestion/sentence_boundary.py, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Add `tiktoken>=0.13.0,<1.0.0`. Implement `cl100k_base` token counter and deterministic child splitter with target 350, max 500, overlap 50, paragraph/sentence/newline preference, Russian/English abbreviation whitelist, number and initials guards, and hard-cut fallback. Hard cuts should emit a warning/debug flag for logs, not Qdrant payload.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Golden texts split byte-identically across runs.
    - Abbreviations such as `т.е.`, `и т.д.`, `Dr.`, `U.S.` do not create false boundaries.
    - Overlap never crosses parent boundary.
  </acceptance_criteria>
  <done>Sentence-aware deterministic child splitting is testable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement parent-child chunker and serialization</name>
  <files>ai-service/src/corp_rag_ai/pipeline/ingestion/chunker.py, ai-service/src/corp_rag_ai/domain, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, ai-service/src/corp_rag_ai/domain/document.py</read_first>
  <action>Implement parent grouping by `section_path`, parent target/max token limits, table atomic behavior, deterministic UUID v5 parent/child IDs, global child position, parent body content without breadcrumb, child display content without breadcrumb, and child `content_for_embedding` with title + section breadcrumb using U+203A. Do not embed/store parents. Prepare parent rows for `document_chunks_parent` replacement in orchestration.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Same ParsedDocument fixture yields same parent IDs, child IDs, content, and content_for_embedding.
    - Table block is never split; oversized table remains one child/parent with warning.
    - Chunk model keeps `position_in_parent` and global `position` separate: `position_in_parent` is 0-based within parent and used for UUID v5 child name, while `position` is 0-based globally across the document and used in Qdrant payload.
    - Qdrant child payload candidate has `sectionPath` array and global child `position`.
  </acceptance_criteria>
  <done>Chunker produces deterministic retrieval/context units.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Tier-0 corpus sanitizer</name>
  <files>ai-service/src/corp_rag_ai/pipeline/ingestion/corpus_sanitizer.py, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Implement cleanup and regex flags exactly from D-104..D-116: preserve paragraph breaks, remove zero-width/control chars, drop only empty/punctuation-only/repeated-character chunks, flag prompt injection and secret-like patterns with locked codes, and leave flagged chunks indexable with `isSanitized=false`. Add all-dropped failure helper for `SANITIZATION / INVALID_FILE_FORMAT`.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Prompt injection and secret fixtures are flagged but not dropped.
    - Empty/garbage fixtures are dropped.
    - At least one surviving flagged chunk allows indexing to proceed.
  </acceptance_criteria>
  <done>Chunk sanitation behavior is explicit and reusable for Phase 5 guard code.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Parsed document to retrieval corpus | Bad boundaries or unsafe chunks can degrade retrieval or expose instructions. |
| Chunker to Qdrant payload | Debug fields must not pollute production payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-04-01 | Tampering | chunk IDs | mitigate | UUID v5 deterministic IDs based on document/parent positions. |
| T-04-04-02 | Information Disclosure | sanitizer logs | mitigate | Log only first 100 chars for flagged/dropped chunks. |
| T-04-04-03 | Reliability | all dropped chunks | mitigate | Fail SANITIZATION deterministically rather than publishing empty indexes. |
</threat_model>

<verification>
- [ ] `cd ai-service; uv run pytest tests` passes.
- [ ] Golden chunk fixture proves deterministic IDs/texts.
- [ ] Sanitizer regex fixtures cover every locked pattern family.
</verification>

<success_criteria>
- ING-03 and ING-04 are implemented with deterministic, unit-level evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python-ingestion-indexing/04-04-SUMMARY.md`.
</output>
