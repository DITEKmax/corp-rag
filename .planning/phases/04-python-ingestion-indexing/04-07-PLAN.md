---
phase: "04-python-ingestion-indexing"
plan: "07"
type: "execute"
wave: 7
depends_on: ["02", "03", "04", "05", "06"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/src/corp_rag_ai/pipeline/ingestion"
  - "ai-service/src/corp_rag_ai/adapters/minio"
  - "ai-service/src/corp_rag_ai/adapters/amqp"
  - "ai-service/src/corp_rag_ai/main.py"
  - "ai-service/tests"
autonomous: true
requirements: ["ING-01", "ING-02", "ING-03", "ING-04", "ING-05", "ING-06", "ING-07"]
user_setup: []
must_haves:
  truths:
    - "Upload and delete handlers implement terminal-after-outcome processed_events semantics."
    - "Failures after Qdrant upsert roll back Qdrant before publishing failed events."
    - "Delete-before-upload silently skips late upload and preserves DELETED tombstone."
  decision_refs:
    - "All Phase 4 decisions D-11 through D-148."
---

<objective>
Wire the full ingestion service orchestration for upload/delete events.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@contracts/asyncapi/events-v1.yaml
@contracts/constants.yaml
@ai-service/src/corp_rag_ai
@backend/corp-rag-app/src/main/java/com/corprag/config/AmqpConfig.java
@CLAUDE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement MinIO fetch adapter and upload event mapping</name>
  <files>ai-service/pyproject.toml, ai-service/src/corp_rag_ai/adapters/minio, ai-service/src/corp_rag_ai/pipeline/ingestion, ai-service/tests</files>
  <read_first>contracts/asyncapi/events-v1.yaml, .planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Add official `minio>=7.2.0,<8.0.0` Python SDK and wrap blocking calls in `asyncio.to_thread()` for async orchestration. Do not use aiobotocore or aioboto3. Map generated `DocumentUploadedPayload` fields into an internal metadata object for title, language, docType, department, accessLevel, mimeType, bucket/key/object reference. Implement fetch timeouts and map timeout/network to `FETCHING / DEPENDENCY_UNAVAILABLE / retryable=true`, 403 to retryable=false, and object 404 through the locked delete-race re-check.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Fetch adapter never queries Java for metadata.
    - Use official `minio>=7.2.0,<8.0.0` Python SDK wrapped in `asyncio.to_thread()` for async orchestration. Do not use aiobotocore or aioboto3.
    - MinIO 404 during fetch re-checks `document_index_state`. If `status=DELETED`, silently skip because the delete event already processed. If no DELETED tombstone exists, publish `DOCUMENT_NOT_FOUND` failed event with `retryable=false` because Java/object lifecycle is inconsistent. Both paths still insert terminal `processed_events` and ACK.
    - MinIO object lifecycle remains Java-owned.
  </acceptance_criteria>
  <done>Upload events can fetch source bytes safely.</done>
</task>

<task type="auto">
  <name>Task 2: Implement upload handler with terminal success/failure semantics</name>
  <files>ai-service/src/corp_rag_ai/pipeline/ingestion, ai-service/src/corp_rag_ai/adapters/amqp, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, ai-service/src/corp_rag_ai/repositories, ai-service/src/corp_rag_ai/pipeline/indexing</read_first>
  <action>Implement upload orchestration: early processed_events duplicate check; early DELETED tombstone skip; set status INDEXING; fetch; parse; chunk; sanitize; fail PARSING/SANITIZATION if empty; replace parent chunks in Postgres; Qdrant delete-by-filter then upsert; parent-level entity extraction; entity embedding; Neo4j write; publish `document.indexed`; terminal update status INDEXED with last_indexed_event_id; insert processed_events; ACK. In except StageFailure, rollback Qdrant for any failure after vector upsert has started, including suspected partial VECTOR_UPSERT, then publish failed event, update status FAILED with last_failure fields, insert processed_events, ACK. If failed-event publish fails, do not insert processed_events and let RabbitMQ redeliver.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Success path publishes indexed before terminal processed_events insert.
    - Failed path publishes failed before terminal processed_events insert.
    - Crash windows match D-17 through D-19 by construction.
    - On any StageFailure after vector upsert started, including VECTOR_UPSERT itself if partial success is possible, execute Qdrant delete-by-filter `documentId` as defensive rollback before publishing failed event. This combines with D-74 retry-start delete-before-upsert for rollback-as-soon-as-detected and replace-all-on-next-attempt semantics.
  </acceptance_criteria>
  <done>Upload ingestion produces terminal Java-visible outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Implement delete handler and integration-style pipeline tests</name>
  <files>ai-service/src/corp_rag_ai/pipeline/ingestion, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Implement delete handler: early duplicate check; Qdrant delete-by-filter; Neo4j `DETACH DELETE d`; parent chunk delete by document_id; UPSERT DELETED tombstone; insert processed_events; ACK. Add integration-style tests with mocked MinIO/Qdrant/Neo4j/Gemini/FlagEmbedding/AMQP proving happy path, parsing failure, graph failure with Qdrant rollback, delete-before-upload, duplicate redelivery, and delete idempotency.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Delete does not delete MinIO.
    - Delete is no-op safe when no Qdrant/Neo4j data exists.
    - Mocked end-to-end tests assert exact state transitions and AMQP publish ordering.
  </acceptance_criteria>
  <done>Python upload/delete consumers are behavior-complete under mocks.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External stores | MinIO, Qdrant, Gemini, Neo4j can fail at different stages. |
| Side effects to terminal state | processed_events must only record terminal outcomes. |
| Delete/upload race | Late upload must not resurrect deleted document. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-07-01 | Reliability | terminal ordering | mitigate | Publish result before final state+processed_events transaction; ACK last. |
| T-04-07-02 | Tampering | partial indexes | mitigate | Delete-by-filter before upsert and rollback after graph failures. |
| T-04-07-03 | Information Disclosure | deleted docs | mitigate | DELETED tombstone skip and cleanup on document.deleted. |
</threat_model>

<verification>
- [ ] `python scripts/verify-contracts.py` passes.
- [ ] `cd ai-service; uv run pytest tests` passes.
- [ ] Mocked full pipeline covers success, parsing failure, graph rollback failure, delete race, duplicate redelivery, and delete cleanup.
</verification>

<success_criteria>
- ING-01 through ING-07 are connected in one orchestration path with terminal outcome semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python-ingestion-indexing/04-07-SUMMARY.md`.
</output>
