---
phase: "04-python-ingestion-indexing"
plan: "05"
type: "execute"
wave: 5
depends_on: ["02", "04"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/src/corp_rag_ai/pipeline/indexing/embedding.py"
  - "ai-service/src/corp_rag_ai/pipeline/indexing/vector_indexer.py"
  - "ai-service/src/corp_rag_ai/main.py"
  - "ai-service/src/corp_rag_ai/config.py"
  - "ai-service/tests"
autonomous: true
requirements: ["ING-05"]
user_setup: []
must_haves:
  truths:
    - "Local FlagEmbedding produces bge-m3 dense+sparse vectors for child chunks."
    - "Qdrant collection uses named dense and sparse vectors with locked payload indexes."
    - "Vector upsert is replace-all per document through delete-by-filter before upsert."
  decision_refs:
    - "D-61 through D-75 and D-142 through D-148: Qdrant schema and local embedding pivot."
    - "RESEARCH.md sections A and B: FlagEmbedding and qdrant-client API."
---

<objective>
Implement local bge-m3 embedding and Qdrant vector indexing.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@.planning/phases/04-python-ingestion-indexing/RESEARCH.md
@ai-service/src/corp_rag_ai/pipeline/ingestion/chunker.py
@infra/docker-compose.yml
@CLAUDE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add local FlagEmbedding adapter with smoke fallback</name>
  <files>ai-service/pyproject.toml, ai-service/src/corp_rag_ai/pipeline/indexing/embedding.py, ai-service/src/corp_rag_ai/config.py, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, .planning/phases/04-python-ingestion-indexing/RESEARCH.md</read_first>
  <action>Add `FlagEmbedding>=1.4.0,<2.0.0` unless execution documents a compatibility rollback to `>=1.3.0,<2.0.0`. Implement lazy `BGEM3FlagModel("BAAI/bge-m3", use_fp16=True)` loading with CPU fallback to `use_fp16=False` if smoke fails. Encode batches of 32 texts with `return_dense=True`, `return_sparse=True`, `return_colbert_vecs=False`. Add a startup or explicit preflight function that verifies dense dimension 1024 and non-empty sparse lexical weights for one short text. Do not use HF API or `HF_API_TOKEN`.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests can mock the FlagEmbedding model without downloading weights.
    - Live smoke test is skipped unless explicitly enabled or model cache is available.
    - Missing sparse lexical weights maps to `EMBEDDING / INDEXING_PIPELINE_ERROR / retryable=false`.
  </acceptance_criteria>
  <done>Embedding adapter has deterministic local dense+sparse behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Qdrant collection initializer and payload indexes</name>
  <files>ai-service/pyproject.toml, ai-service/src/corp_rag_ai/pipeline/indexing/vector_indexer.py, ai-service/src/corp_rag_ai/main.py, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/RESEARCH.md, infra/docker-compose.yml</read_first>
  <action>Add `qdrant-client>=1.12.2,<1.18.0`. Implement `AsyncQdrantClient` wrapper with constants `COLLECTION_NAME=documents_chunks`, `DENSE_VECTOR_NAME=dense`, `SPARSE_VECTOR_NAME=sparse`. Startup `ensure_collection_exists()` runs when `AI_QDRANT_INITIALIZE_COLLECTION=true`: get collection, create with dense 1024 cosine and sparse vector config if absent, create payload indexes for documentId/language/docType/department/accessLevel, and raise on incompatible existing schema. Incompatibility checks must explicitly validate dense vector size, dense distance, and named sparse vector presence. Do not auto-drop data.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Tests verify create/no-op/incompatible-schema behavior with mocked client.
    - `ensure_collection_exists()` checks all three schema invariants: dense vector size = 1024, dense distance = COSINE, and sparse vector config present. Mismatch on any raises with the explicit field name in the error.
    - Payload index set excludes parentChunkId and isSanitized.
    - Startup can skip Qdrant in unit tests by default.
  </acceptance_criteria>
  <done>Qdrant collection lifecycle is explicit and safe.</done>
</task>

<task type="auto">
  <name>Task 3: Implement vector upsert and delete-by-document</name>
  <files>ai-service/src/corp_rag_ai/pipeline/indexing/vector_indexer.py, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, ai-service/src/corp_rag_ai/pipeline/ingestion/chunker.py</read_first>
  <action>Implement conversion from bge lexical weights to sorted Qdrant `SparseVector`. Implement `replace_document_chunks(document_id, chunks)` that first deletes by filter `documentId`, embeds sanitized child `content_for_embedding` in batches, and upserts one `PointStruct` per child with named vectors and locked payload fields. Implement `delete_document(document_id)` with the locked Qdrant filter selector.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Sparse indices are sorted in unit test.
    - Point payload has all locked fields and no `content_for_embedding`.
    - Delete-by-filter uses `documentId` and is idempotent.
  </acceptance_criteria>
  <done>Child chunks can be embedded and written to Qdrant with replace-all semantics.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local model runtime | Heavy model load can fail due to disk/RAM/dependency issues. |
| Embedding output to Qdrant | Wrong vector shape or sparse conversion breaks retrieval. |
| Reindex retry to existing points | Old points can remain if replace-all cleanup is missing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-05-01 | Denial of Service | FlagEmbedding model | mitigate | Lazy load, batch size 32, smoke check, compose memory limit. |
| T-04-05-02 | Tampering | Qdrant schema | mitigate | Raise on incompatible collection; never auto-drop. |
| T-04-05-03 | Reliability | retries/reindex | mitigate | Delete-by-filter before every document upsert. |
</threat_model>

<verification>
- [ ] `cd ai-service; uv run pytest tests` passes.
- [ ] Live FlagEmbedding smoke verifies 1024 dense + sparse lexical weights when model cache is available.
- [ ] Qdrant initializer tests cover collection creation and incompatible schema.
</verification>

<success_criteria>
- ING-05 is implemented through local bge-m3 dense+sparse embeddings and Qdrant child points.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python-ingestion-indexing/04-05-SUMMARY.md`.
</output>
