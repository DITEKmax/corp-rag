---
phase: "04-python-ingestion-indexing"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/migrations/versions"
  - "ai-service/src/corp_rag_ai/config.py"
  - "ai-service/src/corp_rag_ai/domain"
  - "ai-service/src/corp_rag_ai/repositories"
  - "ai-service/src/corp_rag_ai/adapters/amqp"
  - "ai-service/src/corp_rag_ai/main.py"
  - "ai-service/tests"
autonomous: true
requirements: ["ING-01", "ING-07"]
user_setup: []
must_haves:
  truths:
    - "AI Postgres has processed_events, document_index_state, and document_chunks_parent tables."
    - "AMQP consumers use manual ACK and terminal-after-outcome idempotency primitives."
    - "StageFailure centralizes failed-event message sanitization."
  decision_refs:
    - "D-11 through D-24: event intake, idempotency, delete tombstones, terminal processed_events."
    - "D-117 through D-129: failure matrix, failed event envelope, sanitized error messages."
---

<objective>
Create the ingestion control-plane foundation: database state, AMQP adapters, and stage-aware failures.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@.planning/phases/04-python-ingestion-indexing/RESEARCH.md
@contracts/asyncapi/events-v1.yaml
@contracts/constants.yaml
@ai-service/migrations/env.py
@ai-service/src/corp_rag_ai/main.py
@ai-service/src/corp_rag_ai/config.py
@backend/corp-rag-app/src/main/java/com/corprag/config/AmqpConfig.java
@backend/corp-rag-app/src/main/java/com/corprag/service/outbox/EventEnvelopeFactory.java
@CLAUDE.md

<dependency_context>
Requires Plan 04-01 because generated constants/models must exist in Docker/local verification. Downstream plans use these repositories and failure primitives.
</dependency_context>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add AI ingestion state migrations and repositories</name>
  <files>ai-service/migrations/versions, ai-service/src/corp_rag_ai/repositories, ai-service/tests</files>
  <read_first>ai-service/migrations/env.py, backend/corp-rag-app/src/main/resources/db/migration/V13__create_processed_events_table.sql, .planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Add Alembic migration for `processed_events`, `document_index_state`, and `document_chunks_parent`. Match locked columns exactly: processed event id/type/consumed_at; document state status and failure/indexed columns with timestamps; parent chunks with UUID primary key, document_id index, section_path text array, content, position, token_count. Implement async SQLAlchemy repositories for early duplicate check, terminal insert, state upsert/update, and parent chunk replace/delete-by-document.</action>
  <verify>
    <automated>cd ai-service; uv run alembic upgrade head</automated>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies cleanly on empty AI database.
    - Repository tests cover duplicate check, terminal insert unique conflict, INDEXING update, FAILED update, DELETED tombstone upsert, and parent chunk delete by document_id.
    - No Java-owned document metadata is added to `document_index_state`.
  </acceptance_criteria>
  <done>AI database state is ready for ingestion semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add AMQP consumer/publisher foundation with manual ACK</name>
  <files>ai-service/pyproject.toml, ai-service/src/corp_rag_ai/adapters/amqp, ai-service/src/corp_rag_ai/main.py, ai-service/tests</files>
  <read_first>contracts/constants.yaml, contracts/asyncapi/events-v1.yaml, .planning/phases/04-python-ingestion-indexing/RESEARCH.md</read_first>
  <action>Add `aio-pika>=9.6.2,<10.0.0`. Implement robust connection lifecycle, passive queue binding for Java-declared queues, `prefetch_count=1`, manual `ack()` after handler success, and `nack(requeue=True)` only for infrastructure failures before terminal outcome. Implement result publisher for `document.indexed` and `document.indexing.failed` using generated constants and AMQP headers `x-correlation-id`, `x-event-type`, and `x-event-version`. Wire startup/shutdown hooks, with a setting that can disable consumers for tests.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Consumers do not auto-ack.
    - Queue declarations are passive, not topology-owning.
    - Publisher preserves incoming correlation ID with documented fallback order.
    - Tests prove duplicate processed event path ACKs without invoking business work.
  </acceptance_criteria>
  <done>Python can connect to RabbitMQ safely without business pipeline code yet.</done>
</task>

<task type="auto">
  <name>Task 3: Implement StageFailure and failed-event message formatting</name>
  <files>ai-service/src/corp_rag_ai/domain, ai-service/src/corp_rag_ai/adapters/amqp, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, contracts/asyncapi/events-v1.yaml</read_first>
  <action>Implement `StageFailure` with locked fields and `to_error_message(max_len=2048)`. Encode stage-specific templates from 04-CONTEXT, updated for local FlagEmbedding rather than HF. Ensure template vars use safe values only: exception class name, HTTP status, literal timeout, MIME type, parser name, truncated Qdrant/Neo4j structured summaries. Implement helper functions to build `DocumentIndexingFailedPayload` without changing AsyncAPI contracts.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - No formatter path uses `str(exception)` or traceback in event payload.
    - `retryCount` defaults to 0.
    - Existing event contracts/constants are unchanged after verification.
  </acceptance_criteria>
  <done>Failure reporting has one safe implementation path.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| RabbitMQ to Python | Events may duplicate, redeliver, or arrive out of order. |
| Python internal errors to Java audit | Error details can leak document content if not sanitized. |
| Python DB transaction to AMQP ACK | Wrong ordering can lose terminal outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-02-01 | Reliability | processed_events | mitigate | Early duplicate SELECT plus terminal insert after outcome. |
| T-04-02-02 | Information Disclosure | failed event messages | mitigate | Central StageFailure safe templates and max length. |
| T-04-02-03 | Repudiation | correlation chain | mitigate | Preserve `x-correlation-id` on outbound events. |
</threat_model>

<verification>
- [ ] `python scripts/verify-contracts.py` passes.
- [ ] `cd ai-service; uv run alembic upgrade head` passes.
- [ ] `cd ai-service; uv run pytest tests` passes.
- [ ] Tests cover idempotency duplicate ACK, state transitions, and safe error formatting.
</verification>

<success_criteria>
- Python has durable state and AMQP control-plane primitives for all later ingestion plans.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python-ingestion-indexing/04-02-SUMMARY.md`.
</output>
