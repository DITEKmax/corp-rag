---
phase: "04-python-ingestion-indexing"
plan: "03"
type: "execute"
wave: 3
depends_on: ["02"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/src/corp_rag_ai/domain/document.py"
  - "ai-service/src/corp_rag_ai/pipeline/ingestion/parsers"
  - "ai-service/tests"
autonomous: true
requirements: ["ING-02"]
user_setup: []
must_haves:
  truths:
    - "Supported MIME types produce a normalized ParsedDocument with section_path assigned before chunking."
    - "Docling implementation starts with a bounded spike and falls back to Markdown export if structured traversal is not reliable."
  decision_refs:
    - "D-25 through D-37 and D-141: parser dispatch and normalized document model."
---

<objective>
Implement normalized parsing for PDF, DOCX, HTML, Markdown, and plain text.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@.planning/phases/04-python-ingestion-indexing/RESEARCH.md
@ai-service/src/corp_rag_ai
@contracts/asyncapi/events-v1.yaml
@CLAUDE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add ParsedDocument domain model and parser registry</name>
  <files>ai-service/src/corp_rag_ai/domain/document.py, ai-service/src/corp_rag_ai/pipeline/ingestion/parsers, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Define Pydantic models `ParsedBlock` and `ParsedDocument` exactly as locked. Add `DocumentParser` protocol and MIME dispatcher for PDF, DOCX, HTML, Markdown, and plain text. Implement normalization helper that computes `section_path` in one sequential pass and assigns deterministic 0-based positions. Unsupported MIME raises `StageFailure(PARSING, UNSUPPORTED_FILE_TYPE, retryable=false)`.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Section path for nested headings matches locked algorithm.
    - Unsupported MIME maps to `UNSUPPORTED_FILE_TYPE`.
    - No parser-native format fields leak into `ParsedBlock`.
  </acceptance_criteria>
  <done>Parser contract and dispatcher are stable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Markdown, HTML, and plain-text parsers</name>
  <files>ai-service/pyproject.toml, ai-service/src/corp_rag_ai/pipeline/ingestion/parsers, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, .planning/phases/04-python-ingestion-indexing/RESEARCH.md</read_first>
  <action>Add `markdown-it-py>=4.2.0,<5.0.0` and `trafilatura>=2.0.0,<3.0.0`. Markdown parser builds a flat block list from AST with headings, paragraphs, list items, preformatted blocks, and tables. HTML parser uses trafilatura main-content extraction and simple h1/h2/h3 handling where recoverable; boilerplate is dropped. Plain text parser returns a single paragraph block when non-empty. Empty parser output raises `PARSING / INVALID_FILE_FORMAT`.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Markdown headings set levels 1..6 and list items remain separate blocks.
    - HTML boilerplate fixture does not appear in parsed blocks.
    - Plain text with content yields one paragraph; whitespace-only fails parsing.
  </acceptance_criteria>
  <done>Non-Docling parser paths are deterministic and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Spike and implement Docling parser path</name>
  <files>ai-service/pyproject.toml, ai-service/src/corp_rag_ai/pipeline/ingestion/parsers, ai-service/tests/fixtures, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/RESEARCH.md</read_first>
  <action>Add `docling>=2.93.0,<3.0.0`. Start with a 2-4 hour spike using a representative PDF/DOCX fixture. Prefer structured `DoclingDocument` traversal for headings, paragraphs, tables, and page metadata. Timebox the spike to 4 hours maximum; if fallback triggers fire, implement `document.export_to_markdown()` plus the Markdown normalizer and document the page-number tradeoff in the summary. Catch Docling conversion errors at adapter boundary and map to `PARSING / INVALID_FILE_FORMAT` with sanitized parser name `docling`.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - PDF/DOCX parser returns ordered blocks for a fixture with heading, paragraph, and table.
    - Corrupt/encrypted-like fixture maps to `PARSING / INVALID_FILE_FORMAT`.
    - Spike is timeboxed at 4 hours maximum. Switch to `export_to_markdown()` fallback if any of: DoclingDocument traversal does not yield heading levels for a test PDF, table extraction requires more than 50 lines of adapter code, or page numbers cannot be reliably attached to blocks. Document chosen path and rationale in `04-03-SUMMARY.md`.
    - If fallback path is used, it is explicit in summary and reuses Markdown normalizer.
  </acceptance_criteria>
  <done>PDF/DOCX parsing is implemented with a known page-fidelity posture.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Uploaded file bytes to parser | Files may be corrupt, encrypted, huge, or malformed. |
| Parser-native structures to domain | Format-specific metadata can leak into downstream contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-03-01 | Denial of Service | parsers | mitigate | Bounded parser errors, no OCR, fixture tests for empty/corrupt files. |
| T-04-03-02 | Information Disclosure | parser warnings | mitigate | Warnings describe simplification, not raw sensitive content. |
| T-04-03-03 | Tampering | MIME trust | accept | Trust Java Tika MIME per D-37; unsupported values fail safely. |
</threat_model>

<verification>
- [ ] `cd ai-service; uv run pytest tests` passes.
- [ ] Parser fixtures cover PDF/DOCX, HTML, Markdown, plain text, unsupported MIME, and empty output.
</verification>

<success_criteria>
- ING-02 has normalized parsing coverage for all supported MVP MIME types.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python-ingestion-indexing/04-03-SUMMARY.md`.
</output>
