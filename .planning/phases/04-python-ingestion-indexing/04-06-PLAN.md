---
phase: "04-python-ingestion-indexing"
plan: "06"
type: "execute"
wave: 6
depends_on: ["05"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py"
  - "ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py"
  - "ai-service/src/corp_rag_ai/pipeline/indexing/prompts/entity_extraction_v1.md"
  - "ai-service/tests/fixtures/entity_extraction/01_hr_policy_basic.json"
  - "ai-service/tests"
  - "ai-service/README.md"
autonomous: true
requirements: ["ING-06"]
user_setup:
  - "Set GEMINI_API_KEY only for skipped-by-default live integration tests and end-of-phase UAT."
must_haves:
  truths:
    - "Neo4j graph uses provenance-first Document/Entity/RelationMention schema without Chunk nodes."
    - "Gemini extraction is parent-level, sequential, structured output through google-genai."
    - "Graph writes are one Neo4j transaction per document."
  decision_refs:
    - "D-76 through D-103 and D-147: Neo4j schema, extraction, prompt lifecycle, google-genai."
    - "RESEARCH.md sections C and D: google-genai and Neo4j async/vector index."
---

<objective>
Implement parent-level entity/relation extraction and Neo4j graph indexing.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@.planning/phases/04-python-ingestion-indexing/RESEARCH.md
@ai-service/src/corp_rag_ai/pipeline/indexing/embedding.py
@infra/docker-compose.yml
@CLAUDE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Neo4j async schema initializer and graph indexer</name>
  <files>ai-service/pyproject.toml, ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py, ai-service/src/corp_rag_ai/main.py, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/RESEARCH.md, .planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Add `neo4j>=5.28.4,<6.0.0`. Implement async driver lifecycle and `ensure_graph_schema()` with indexes for Entity normalizedName/type, Document id/accessLevel, RelationMention type, and vector index `entity_embedding_idx` over `Entity.embedding` as `LIST<FLOAT>` with 1024 cosine. After creating/checking the vector index, inspect `SHOW VECTOR INDEXES` and tolerate background build state. Implement cleanup `MATCH (d:Document {id: $documentId}) DETACH DELETE d`. Implement one-document write transaction that MERGEs Document, Entity, RelationMention, SOURCE/TARGET, MENTIONED_IN, and EVIDENCE according to locked schema. Do not create Chunk nodes or delete shared Entity/RelationMention nodes during document cleanup.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Cypher uses Community-compatible list embeddings, not native VECTOR properties.
    - `ensure_graph_schema()` polls `SHOW VECTOR INDEXES` with 5-second timeout waiting for `entity_embedding_idx` state in `{ONLINE, POPULATING}`. `POPULATING` is acceptable because Neo4j builds in background and queries can degrade gracefully. `FAILED` state raises. Timeout raises with an explicit message.
    - Cleanup only deletes Document node and attached edges.
    - Transaction callback has no external side effects.
  </acceptance_criteria>
  <done>Neo4j graph storage shape is implemented safely.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Gemini structured entity extractor and prompt artifact</name>
  <files>ai-service/pyproject.toml, ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py, ai-service/src/corp_rag_ai/pipeline/indexing/prompts/entity_extraction_v1.md, ai-service/tests</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/RESEARCH.md, .planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Add `google-genai>=2.0.0,<3.0.0`. Create prompt file with `## System prompt` and `## User template`. Implement cached prompt loading, `ENTITY_EXTRACTION_PROMPT_VERSION`, Pydantic structured output schema, Gemini 2.0 Flash call with temperature 0.1 and max_output_tokens around 2000, entity type whitelist enforcement, relation open vocabulary, malformed JSON/schema validation retry once, and backoff for 429/5xx/timeouts up to 3 attempts. Empty entities is valid.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests mock google-genai and do not require API key.
    - Unknown entity types are dropped with warning, not mapped silently.
    - Malformed structured output has one retry total across `ValidationError` and JSON decode failures. If first attempt fails validation and retry fails JSON decoding, fail immediately after that retry.
    - Malformed structured output fails `ENTITY_EXTRACTION / INDEXING_PIPELINE_ERROR / retryable=false` after one retry.
  </acceptance_criteria>
  <done>Gemini extraction is prompt-versioned and testable.</done>
</task>

<task type="auto">
  <name>Task 3: Add golden fixture, entity embedding, and live integration test</name>
  <files>ai-service/tests/fixtures/entity_extraction/01_hr_policy_basic.json, ai-service/tests, ai-service/README.md</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, ai-service/src/corp_rag_ai/pipeline/indexing/embedding.py</read_first>
  <action>Add locked golden fixture shape. Unit test maps mocked Gemini response to domain objects, deduplicates entities by normalizedName/type UUID v5, deduplicates relations by source/target/type UUID v5, and embeds unique entity texts dense-only through the local FlagEmbedding adapter. Add skipped-by-default integration test marked `integration` that uses real `GEMINI_API_KEY` and asserts expected entities/relations are a subset. Update README with how to run it.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests</automated>
  </verify>
  <acceptance_criteria>
    - Live test skips when `GEMINI_API_KEY` is missing.
    - Mocked unit tests always run.
    - Entity description is set on create and not overwritten on match.
  </acceptance_criteria>
  <done>Graph extraction has a quality fixture and CI-safe tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Parent text to Gemini | Prompt can leak or malformed model output can break indexing. |
| Gemini output to Neo4j | Entity/relation data must be normalized and idempotent. |
| Graph cleanup | Document deletes must not delete shared entities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-06-01 | Information Disclosure | failed event | mitigate | Sanitized StageFailure; raw Gemini responses only in Python logs. |
| T-04-06-02 | Tampering | entity types | mitigate | Enforce whitelist and drop unknown types. |
| T-04-06-03 | Reliability | Neo4j transaction | mitigate | Single document transaction and cleanup safety net. |
</threat_model>

<verification>
- [ ] `cd ai-service; uv run pytest tests` passes.
- [ ] Skipped live Gemini test is documented.
- [ ] Neo4j Cypher tests/mocks prove no Chunk nodes and cleanup does not delete Entity/RelationMention.
</verification>

<success_criteria>
- ING-06 is implemented with provenance-first graph storage and prompt-versioned extraction.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python-ingestion-indexing/04-06-SUMMARY.md`.
</output>
