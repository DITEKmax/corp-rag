---
phase: "04-python-ingestion-indexing"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "infra/docker-compose.yml"
  - ".dockerignore"
  - "ai-service/Dockerfile"
  - "ai-service/README.md"
  - "ai-service/src/corp_rag_ai/config.py"
  - "ai-service/src/corp_rag_ai/main.py"
autonomous: true
requirements: ["ING-05"]
user_setup: []
must_haves:
  truths:
    - "python-ai Docker builds use repository-root context and generate Python contracts inside the build without host pre-generation."
    - "Generated contract artifacts in the final image come only from the builder stage, never from stale local host state."
    - "Compose reserves local bge-m3 runtime resources without starting ingestion consumers yet."
  artifacts:
    - path: "ai-service/Dockerfile"
      provides: "repo-root multi-stage Python image"
      contains: "builder codegen, generated directory cleanup, runtime without PyYAML"
    - path: "infra/docker-compose.yml"
      provides: "python-ai build/runtime wiring"
      contains: "context: .., dockerfile: ai-service/Dockerfile, bge-m3-cache volume, 4GB mem limit"
    - path: ".dockerignore"
      provides: "root context pruning"
      contains: "generated contract exclusion and large unrelated directory exclusions"
  decision_refs:
    - "D-01 through D-10: repo-root Docker codegen contract."
    - "D-145 and D-146: bge-m3 cache volume and memory budget."
    - "D-148: Phase 4 adds FlagEmbedding package later, not reranker model."
---

<objective>
Make the Python image build reproducibly from a clean repo-root Docker context.

Purpose: Phase 4 cannot safely execute while Docker builds depend on host-generated Python contracts. This plan removes that preflight risk and prepares compose resource wiring for local bge-m3.
Output: root-context Dockerfile, `.dockerignore`, compose build/runtime wiring, README note, and smoke evidence.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@.planning/phases/04-python-ingestion-indexing/RESEARCH.md
@.planning/phases/03-documents-events-audit/03-HANDOFF.md
@scripts/generate_python_contracts.py
@scripts/generate_constants.py
@ai-service/Dockerfile
@infra/docker-compose.yml
@ai-service/README.md
@CLAUDE.md

<dependency_context>
No Phase 4 implementation dependency. This plan must complete before any plan that relies on generated Python contracts inside Docker.
</dependency_context>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Convert python-ai to repo-root multi-stage Docker build</name>
  <files>ai-service/Dockerfile, infra/docker-compose.yml, .dockerignore</files>
  <read_first>ai-service/Dockerfile, infra/docker-compose.yml, scripts/generate_python_contracts.py, scripts/generate_constants.py</read_first>
  <action>Change `infra/docker-compose.yml` python-ai build to `context: ..` and `dockerfile: ai-service/Dockerfile`. Rewrite the Dockerfile as a multi-stage build with `ghcr.io/astral-sh/uv:0.5.26-python3.12-bookworm` for both builder/runtime. Builder uses `WORKDIR /repo`, copies `contracts/`, `scripts/`, and `ai-service/`, installs PyYAML only in builder, runs `rm -rf /repo/ai-service/src/corp_rag_ai/contracts/generated`, then runs the two generator scripts. Runtime uses `WORKDIR /app`, copies pyproject/readme first, runs `uv sync --no-dev --no-install-project`, then copies generated `src`, migrations/alembic files, and runs final `uv sync --no-dev`. Do not modify generator scripts.</action>
  <verify>
    <automated>docker compose -f infra/docker-compose.yml build python-ai</automated>
  </verify>
  <acceptance_criteria>
    - Docker build succeeds after local `ai-service/src/corp_rag_ai/contracts/generated` is absent.
    - Dockerfile has builder `WORKDIR /repo` and runtime `WORKDIR /app`.
    - Runtime image does not install PyYAML as a production dependency.
    - `scripts/generate_python_contracts.py` and `scripts/generate_constants.py` are unchanged.
  </acceptance_criteria>
  <done>python-ai clean Docker builds no longer depend on host codegen.</done>
</task>

<task type="auto">
  <name>Task 2: Add root .dockerignore and local embedding runtime contour</name>
  <files>.dockerignore, infra/docker-compose.yml, ai-service/src/corp_rag_ai/config.py</files>
  <read_first>.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md, infra/docker-compose.yml, ai-service/src/corp_rag_ai/config.py</read_first>
  <action>Create or update root `.dockerignore` to exclude `.git`, `.planning/`, `backend/target/`, `**/__pycache__`, `**/.pytest_cache`, `ai-service/.venv`, `frontend/node_modules`, and `ai-service/src/corp_rag_ai/contracts/generated/`. Add compose volume `bge-m3-cache` mounted at `/root/.cache/huggingface` for python-ai. Add an explicit 4GB memory limit/reservation compatible with the compose version in use. Extend settings with model/cache knobs only if useful for later plans, but do not load FlagEmbedding in this plan.</action>
  <verify>
    <automated>docker compose -f infra/docker-compose.yml config</automated>
  </verify>
  <acceptance_criteria>
    - Docker config renders with `bge-m3-cache` volume.
    - `.dockerignore` excludes generated Python contracts from build context.
    - Compose does not require external secrets just to render.
  </acceptance_criteria>
  <done>Compose is ready for local model cache without changing service behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Verify stale generated files do not enter the image</name>
  <files>ai-service/README.md</files>
  <read_first>ai-service/README.md, .planning/phases/04-python-ingestion-indexing/04-CONTEXT.md</read_first>
  <action>Document local-vs-Docker codegen behavior in `ai-service/README.md`. Run the stale-file scenario: create `ai-service/src/corp_rag_ai/contracts/generated/zombie.py`, build python-ai, inspect the image/container filesystem, and prove `/app/src/corp_rag_ai/contracts/generated/zombie.py` is absent while generated `.py` contract files are present. Remove local `zombie.py` after the smoke. Also prove `/health` and `/ready` still respond when the stack starts.</action>
  <verify>
    <automated>docker compose -f infra/docker-compose.yml up -d python-ai</automated>
    <automated>docker exec corp-rag-python-ai-1 python -c "import importlib.util; import sys; sys.exit(0 if importlib.util.find_spec('yaml') is None else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Final image has generated contract modules under `/app/src/corp_rag_ai/contracts/generated/`.
    - Final image does not contain `zombie.py`.
    - Final image check for `yaml` import fails or proves PyYAML is absent from runtime.
    - `GET /health` and `GET /ready` return HTTP 200.
  </acceptance_criteria>
  <done>Wave 1 Docker/codegen preflight is closed.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host workspace to Docker image | Host-generated files may be stale or hand-edited. |
| Repo-root build context to Python image | Root context can accidentally send large or sensitive directories. |
| Build-only tooling to runtime | PyYAML/codegen tooling must not leak into runtime deps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01-01 | Tampering | generated contracts | mitigate | Delete generated directory before builder codegen and exclude host generated files from context. |
| T-04-01-02 | Denial of Service | Docker build context | mitigate | Root `.dockerignore` excludes large unrelated directories and caches. |
| T-04-01-03 | Supply Chain | runtime image | mitigate | Install PyYAML only in builder; runtime sync excludes dev deps. |
</threat_model>

<verification>
- [ ] `docker compose -f infra/docker-compose.yml config` passes.
- [ ] `docker compose -f infra/docker-compose.yml build python-ai` passes from clean generated-contract state.
- [ ] Stale `zombie.py` does not enter final image.
- [ ] Runtime image lacks PyYAML.
- [ ] `/health` and `/ready` return 200.
</verification>

<success_criteria>
- Phase 4 Docker builds are clean, deterministic, and symmetric with Java root-context codegen.
- Compose has local bge-m3 resource contour without starting ingestion behavior yet.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python-ingestion-indexing/04-01-SUMMARY.md`.
</output>
