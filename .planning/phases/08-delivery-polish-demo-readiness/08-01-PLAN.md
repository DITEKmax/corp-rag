---
phase: "08-delivery-polish-demo-readiness"
plan: "01"
type: "tooling"
wave: 1
depends_on: []
files_modified:
  - "ai-service/eval/seed_corpus.py"
  - "ai-service/tests/test_eval_seed_corpus.py"
  - "scripts/seed-demo-corpus.ps1"
  - ".planning/phases/08-delivery-polish-demo-readiness/08-01-SUMMARY.md"
autonomous: true
requirements: ["DEL-01"]
user_setup:
  - "Requires a running local compose stack and admin credentials supplied through ignored `infra/.env` or explicit environment variables."
must_haves:
  truths:
    - "D-421: The seed path must not weaken access filters, output guard, citation validation, weak-evidence thresholds, or refusal behavior."
    - "D-422: The seed path must not mutate frozen corpus files, golden questions, reference answers, or expected document UUID assumptions except by recording a fresh local Java document id map as evidence."
    - "D-428: The seed script is an idempotent reset, not append-only and not verify-only."
    - "D-429: Reset brings the local demo corpus to exactly the 16 documents in `ai-service/eval/corpus/documents/` and `manifest.json`."
    - "D-430: Reset uses the normal Java browser-facing document APIs under an admin user."
    - "D-431: Reset does not manually clean Qdrant, Neo4j, MinIO, Java Postgres, AI Postgres, RabbitMQ, or Docker volumes."
    - "D-432: Deleting through Java is required so the ordinary document delete event path cleans Qdrant and Neo4j."
    - "D-433: Seed verification checks backend document state plus Qdrant and Neo4j cleanliness."
    - "D-434: Seed identity uses stable corpus metadata and writes a stable marker in `description` if supported by the API."
    - "D-435: Seed upload preserves user-facing titles from the manifest for polished citation document titles."
    - "D-436: Seed waits for all 16 documents to reach successful terminal indexing and surfaces failures, chunks, and indexed timestamps."
    - "D-437: Seed documentation explains credentials/env values without committing secrets."
  artifacts:
    - path: "ai-service/eval/seed_corpus.py"
      provides: "idempotent demo corpus reset"
      contains: "login, list/delete by seed marker, upload 16 manifest docs, poll indexing, and write evidence"
    - path: "ai-service/tests/test_eval_seed_corpus.py"
      provides: "seed tooling unit coverage"
      contains: "mocked Java API client, marker matching, multipart metadata, polling, and evidence formatting tests"
    - path: "scripts/seed-demo-corpus.ps1"
      provides: "Windows-friendly seed command wrapper"
      contains: "thin uv invocation without secret values"
  decision_refs:
    - "D-421"
    - "D-422"
    - "D-428"
    - "D-429"
    - "D-430"
    - "D-431"
    - "D-432"
    - "D-433"
    - "D-434"
    - "D-435"
    - "D-436"
    - "D-437"
---

<objective>
Create an idempotent demo corpus reset tool that uses the real Java document lifecycle and produces reviewable seed/index evidence for the 16-document Russian corpus.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-delivery-polish-demo-readiness/08-CONTEXT.md
@.planning/phases/08-delivery-polish-demo-readiness/08-RESEARCH.md
@contracts/openapi/api-v1.yaml
@backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/DocumentController.java
@backend/corp-rag-app/src/main/java/com/corprag/service/document/DocumentUploadService.java
@backend/corp-rag-app/src/main/java/com/corprag/service/document/DocumentDeletionService.java
@ai-service/eval/corpus/manifest.json
@ai-service/eval/graph_corpus_state.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add seed client and evidence tests</name>
  <files>ai-service/tests/test_eval_seed_corpus.py</files>
  <behavior>
    - Test marker generation from manifest `corpus_version` and document `doc_id`.
    - Test matching previous seed docs by description marker, title, original filename, language, department, and doc type.
    - Test multipart upload fields preserve manifest title/accessLevel/department/docType/language and write the stable seed marker in description.
    - Test polling treats all 16 `INDEXED` documents as success and surfaces failed or timed-out documents.
    - Test evidence output contains Java document ids, titles, statuses, chunk counts, indexed timestamps, and store-cleanliness summary without secrets.
  </behavior>
  <action>Create focused pytest coverage for the seed corpus module before implementation. Use mocked `httpx` transports or small fake client classes; do not require the Docker stack for unit tests. Reuse manifest shape from `ai-service/eval/corpus/manifest.json` and comparison helpers from `graph_corpus_state.py` where possible.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_seed_corpus.py</automated>
  </verify>
  <done>Failing-then-passing tests specify the reset, upload, wait, and evidence contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Java-API seed reset tool</name>
  <files>ai-service/eval/seed_corpus.py, scripts/seed-demo-corpus.ps1, ai-service/tests/test_eval_seed_corpus.py</files>
  <read_first>ai-service/eval/query_client.py, ai-service/eval/io.py, ai-service/eval/validate_corpus.py</read_first>
  <action>Implement `ai-service/eval/seed_corpus.py` as an opt-in CLI. It must authenticate with `POST /api/v1/auth/login`, keep the session cookies, list documents through `/api/v1/documents`, delete matching seed documents through `DELETE /api/v1/documents/{id}`, upload all 16 manifest files through multipart `POST /api/v1/documents`, and poll Java document status until every uploaded seed doc is `INDEXED` or a clear failure/timeout occurs. Use environment variables or CLI flags for Java base URL, admin username/password, poll interval, timeout, manifest path, corpus dir, and evidence output path. The PowerShell wrapper must only delegate to the uv command and must not contain credentials.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_seed_corpus.py</automated>
    <automated>uv run --project ai-service python eval/seed_corpus.py --help</automated>
  </verify>
  <acceptance_criteria>
    - The seed marker includes corpus version and manifest `doc_id` and is written into `description` without replacing the manifest title.
    - Existing seed docs are deleted through Java before upload; no Docker volumes or AI stores are directly cleared by the reset path.
    - Upload metadata matches the manifest exactly for title, access level, department, doc type, and language.
    - Secret values are read from environment/CLI and never written to evidence output.
  </acceptance_criteria>
  <done>The script can reset and reupload the demo corpus through the Java product API.</done>
</task>

<task type="auto">
  <name>Task 3: Add store-cleanliness and seed evidence output</name>
  <files>ai-service/eval/seed_corpus.py, ai-service/tests/test_eval_seed_corpus.py, .planning/phases/08-delivery-polish-demo-readiness/08-01-SUMMARY.md</files>
  <read_first>ai-service/eval/graph_corpus_state.py, ai-service/eval/golden/golden_ru.meta.json, ai-service/eval/query_client.py</read_first>
  <action>Extend the seed tool to write JSON and Markdown evidence under the Phase 8 directory by default. Evidence must include compose target URLs, Java document records, a fresh local document-id map compatible with final eval metadata, Qdrant count/check status for the seeded document ids, Neo4j document/entity status using the existing graph comparison helper, and any failure reasons. If Qdrant or Neo4j checks cannot run, mark the evidence blocked rather than silently passing.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_seed_corpus.py</automated>
    <manual>Run the seed CLI against the local stack and confirm the evidence reports exactly 16 indexed documents with clean Java/Qdrant/Neo4j state.</manual>
  </verify>
  <acceptance_criteria>
    - Evidence records exactly 16 uploaded seed documents when successful.
    - Evidence includes a fresh document id map for final eval and does not rewrite corpus files or golden question/reference text.
    - Failed indexing, orphan store state, or missing store checks produce non-zero exit and a clear blocker.
  </acceptance_criteria>
  <done>Seed reset is reproducible and produces the evidence later plans consume.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Seed reset bypasses product access controls | Seed tooling | Use only Java login/list/delete/upload APIs for mutations. |
| Secrets leak into committed evidence | CLI/env handling | Redact credentials and never print env values. |
| Store orphan state is hidden | Qdrant/Neo4j checks | Fail evidence if cleanliness checks are missing or dirty. |
| Eval document ids become stale | Reupload flow | Write fresh local id map and require final eval to use it explicitly. |
</threat_model>

<verification>
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_eval_seed_corpus.py`
- [ ] `uv run --project ai-service python eval/seed_corpus.py --help`
- [ ] Live seed reset evidence shows exactly 16 indexed docs and clean Java/Qdrant/Neo4j state.
</verification>

<success_criteria>
- The demo corpus reset is idempotent and uses the Java document lifecycle.
- All 16 manifest documents are uploaded with polished titles and indexed successfully.
- Seed evidence includes a fresh document id map and store cleanliness results.
- No Docker volume wipe or direct store mutation is part of the seed reset.
</success_criteria>

<output>
After completion, create `.planning/phases/08-delivery-polish-demo-readiness/08-01-SUMMARY.md`.
</output>
