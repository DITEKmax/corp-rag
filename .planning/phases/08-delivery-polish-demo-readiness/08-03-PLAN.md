---
phase: "08-delivery-polish-demo-readiness"
plan: "03"
type: "validation"
wave: 2
depends_on: ["01", "02"]
files_modified:
  - "ai-service/eval/final_regression.py"
  - "ai-service/tests/test_eval_final_regression.py"
  - ".planning/phases/08-delivery-polish-demo-readiness/08-FINAL-REGRESSION.md"
  - ".planning/phases/08-delivery-polish-demo-readiness/08-03-SUMMARY.md"
autonomous: false
requirements: ["DEL-01"]
user_setup:
  - "Requires healthy compose stack, completed seed reset evidence, admin credentials, OpenRouter judge credentials, and user review before committing stochastic eval reports."
must_haves:
  truths:
    - "D-418: Multi-hop graph retrieval for `ru-multihop-002/003/005/006` is waived for Phase 8 and documented as a known limitation."
    - "D-419: Phase 8 must not implement text-conditioned multi-document graph retrieval or broad graph redesign."
    - "D-420: The waiver is acceptable because the system safely refuses rather than fabricating unsupported multi-hop answers."
    - "D-421: Final regression does not weaken access filters, guards, citation validation, weak-evidence thresholds, or refusal behavior."
    - "D-422: Final regression does not mutate frozen corpus or golden data except using the fresh local seed metadata map as evidence."
    - "D-438: The mandatory final regression gate is compose health, seed/index reset, one live chat-to-citation path, and one production-path RAGAS/eval run."
    - "D-439: Smoke-only is insufficient; chat/citation/evaluation path must be proven."
    - "D-440: Full replay of RAGAS plus ablation plus injection plus Langfuse is out of scope for the required gate."
    - "D-441: The single final RAGAS/eval run is report-only evidence, not a hard CI gate."
    - "D-442: Final eval uses production `/v1/query`, topK 10 where applicable, concurrency=1, limited RAGAS retries/wait, and local bge-m3 embeddings unless a concrete incompatibility is found."
    - "D-443: `reranker_degraded_count=0` is tracked as a key regression signal."
    - "D-444: Final regression captures compose health, seed summary, corpus/index counts, chat question/answer/citation proof, eval summary, and known limitations."
    - "D-445: Stochastic generated eval reports are not committed until the user reviews the run."
  artifacts:
    - path: "ai-service/eval/final_regression.py"
      provides: "final regression evidence runner"
      contains: "compose/seed evidence loading, Java chat probe, diagnostics delta, and RAGAS command orchestration"
    - path: ".planning/phases/08-delivery-polish-demo-readiness/08-FINAL-REGRESSION.md"
      provides: "human-readable final evidence"
      contains: "health, seed, chat/citation, eval, reranker degradation, and known-limit summary"
  decision_refs:
    - "D-418"
    - "D-419"
    - "D-420"
    - "D-421"
    - "D-422"
    - "D-438"
    - "D-439"
    - "D-440"
    - "D-441"
    - "D-442"
    - "D-443"
    - "D-444"
    - "D-445"
---

<objective>
Prove the final demo path end to end: healthy compose, seeded/indexed corpus, live Java chat with citations, and one production-path RAGAS/eval run with honest limitations.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-delivery-polish-demo-readiness/08-CONTEXT.md
@.planning/phases/08-delivery-polish-demo-readiness/08-RESEARCH.md
@.planning/phases/08-delivery-polish-demo-readiness/08-01-SUMMARY.md
@.planning/phases/08-delivery-polish-demo-readiness/08-02-SUMMARY.md
@ai-service/eval/ragas_runner.py
@ai-service/eval/query_client.py
@contracts/openapi/api-v1.yaml
@backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryService.java
@ai-service/eval/reports/ragas_ru.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add final regression runner tests</name>
  <files>ai-service/tests/test_eval_final_regression.py</files>
  <behavior>
    - Test evidence loader rejects missing compose or seed evidence.
    - Test Java chat probe requires answered response with at least one citation and documentTitle.
    - Test diagnostics delta records `reranker_degraded_count` before/after and fails when it increases.
    - Test RAGAS command uses metadata from the seed evidence, `--top-k 10`, `--ragas-max-retries 1`, and `--ragas-max-wait 5`.
    - Test report rendering includes known limitation for `ru-multihop-002/003/005/006`.
  </behavior>
  <action>Create focused pytest coverage for a new final regression module. Use mocked HTTP and subprocess boundaries; do not call the live stack in unit tests.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_final_regression.py</automated>
  </verify>
  <done>The final regression behavior is specified before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement final regression evidence runner</name>
  <files>ai-service/eval/final_regression.py, ai-service/tests/test_eval_final_regression.py</files>
  <read_first>ai-service/eval/ragas_runner.py, ai-service/eval/query_client.py, frontend/js/components/chat/diagnostics-panel.js</read_first>
  <action>Implement an opt-in CLI that consumes `08-COMPOSE-EVIDENCE.md` and seed evidence, checks `/diagnostics` before/after, authenticates through Java, creates or uses a conversation, sends one reliable Russian factual/demo question through Java chat API, verifies the assistant response has valid citations with `documentTitle`, and optionally launches the existing RAGAS runner with the seed-generated metadata. The runner must write Markdown and JSON evidence and must fail if `reranker_degraded_count` increases.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_final_regression.py</automated>
    <automated>uv run --project ai-service python eval/final_regression.py --help</automated>
  </verify>
  <acceptance_criteria>
    - Chat proof goes through Java `/api/v1/chat/query`, not Python directly.
    - Evidence includes answer text or transcript excerpt, citation titles, route/retrieval metadata when present, diagnostics before/after, and trace id when present.
    - RAGAS invocation uses production `/v1/query` via the existing runner with `concurrency=1` and limited retry/wait controls.
  </acceptance_criteria>
  <done>The final regression CLI can collect the required evidence chain.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Run live final regression and review generated reports</name>
  <files>.planning/phases/08-delivery-polish-demo-readiness/08-FINAL-REGRESSION.md, .planning/phases/08-delivery-polish-demo-readiness/08-03-SUMMARY.md, ai-service/eval/reports/ragas_ru.md, ai-service/eval/reports/ragas_ru.json, ai-service/eval/reports/ragas_ru.csv</files>
  <read_first>.planning/phases/08-delivery-polish-demo-readiness/08-COMPOSE-EVIDENCE.md, .planning/phases/08-delivery-polish-demo-readiness/08-01-SUMMARY.md</read_first>
  <action>Run the final regression against the live stack after Plans 01 and 02 pass. Record exact commands and outputs. Do not commit regenerated `ragas_ru.*` files until the user reviews that the run is clean and comparable. If the run has judge instability, no citations, dirty seed state, or reranker degradation, record the blocker instead of marking demo-ready.</action>
  <verify>
    <manual>User reviews `08-FINAL-REGRESSION.md` and generated `ragas_ru.*` report files, then approves commit or asks for rerun/fix.</manual>
  </verify>
  <acceptance_criteria>
    - Final evidence covers compose health, seed/index reset, chat/citation proof, one RAGAS/eval summary, diagnostics, and known limitations.
    - `reranker_degraded_count` remains 0 for the final evidence window or the blocker is recorded.
    - Generated stochastic reports are either approved for commit or left uncommitted with clear paths.
  </acceptance_criteria>
  <done>Final regression evidence is ready for demo review.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Smoke test hides broken eval path | Regression scope | Require compose, seed, chat/citation, and RAGAS/eval evidence. |
| Metrics are gamed by changing corpus or guard behavior | Eval inputs | Use existing corpus/golden and no guard/access/citation weakening. |
| Reranker degradation hidden | Diagnostics | Compare before/after `reranker_degraded_count` and fail on increase. |
| Stochastic report committed prematurely | Git/report artifacts | Human review checkpoint before committing generated reports. |
</threat_model>

<verification>
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_eval_final_regression.py`
- [ ] `uv run --project ai-service python eval/final_regression.py --help`
- [ ] Live final regression evidence reviewed and accepted or blocker recorded.
</verification>

<success_criteria>
- Final evidence proves the core chat/citation/evaluation path still works.
- Multi-hop graph debt is explicitly waived and not hidden.
- RAGAS remains report-only evidence and generated reports are reviewed before commit.
</success_criteria>

<output>
After completion, create `.planning/phases/08-delivery-polish-demo-readiness/08-03-SUMMARY.md`.
</output>
