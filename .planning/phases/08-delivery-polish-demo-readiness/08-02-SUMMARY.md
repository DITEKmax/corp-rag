---
phase: 08-delivery-polish-demo-readiness
plan: "02"
subsystem: infra-runbook
tags: [docker-compose, runbook, diagnostics, memory, demo-readiness]

requires:
  - phase: 08-delivery-polish-demo-readiness
    provides: Clean 16-document seed corpus evidence
provides:
  - Read-only demo stack health checker with self-test and diagnostics capture
  - Local demo runbook for startup, health, seed, diagnostics, troubleshooting, and no-volume-wipe rules
  - Compose readiness evidence showing 9/9 healthy services
affects: [phase-8-regression, demo-runbook, compose-evidence]

tech-stack:
  added: []
  patterns:
    - Read-only evidence helpers that fail on missing or unhealthy services
    - Root-level Makefile wrappers around existing compose, seed, and check commands

key-files:
  created:
    - scripts/check_demo_stack.py
    - .planning/phases/08-delivery-polish-demo-readiness/08-COMPOSE-EVIDENCE.md
    - .planning/phases/08-delivery-polish-demo-readiness/08-02-SUMMARY.md
  modified:
    - infra/README.md
    - infra/docker-compose.yml
    - .env.example
    - infra/.env.example
    - Makefile

key-decisions:
  - "Keep `infra/docker-compose.yml` as the only local demo topology; do not add a production compose file."
  - "Document the actual live memory contour: WSL 12GB, `PYTHON_AI_MEMORY_LIMIT=10g`, and `PYTHON_AI_MEMORY_RESERVATION=8g`."
  - "Preserve the Hugging Face `bge-m3-cache` volume and explicitly forbid `docker compose down -v` for normal demo reset."

patterns-established:
  - "Compose readiness evidence is generated by `scripts/check_demo_stack.py` without starting, stopping, or mutating services."
  - "Readiness means exactly 9/9 named services are running and healthy."

requirements-completed: ["DEL-01"]

duration: 1h
completed: 2026-06-02
---

# Phase 08 Plan 02: Compose Runbook Readiness Summary

**The existing local compose stack is documented and verified as a 9-service healthy demo topology with 10g/8g Python AI memory guidance.**

## Performance

- **Duration:** 1h
- **Started:** 2026-06-02T00:30:00+03:00
- **Completed:** 2026-06-02T01:35:44+03:00
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Added `scripts/check_demo_stack.py`, a read-only checker that parses Docker Compose JSON, verifies the exact 9 expected services, captures `/diagnostics`, redacts secret-like keys, records Docker/Python AI memory, and writes Markdown or JSON evidence.
- Rewrote `infra/README.md` as a practical local demo runbook covering prerequisites, startup, 9/9 health, diagnostics, seed reset, troubleshooting, Hugging Face cache retention, and the no-volume-wipe rule.
- Updated committed memory defaults/examples to match the live working contour: `PYTHON_AI_MEMORY_LIMIT=10g`, `PYTHON_AI_MEMORY_RESERVATION=8g`, and WSL memory guidance `12GB`.
- Added Makefile wrappers for demo health evidence and seed reset without introducing a second compose topology.
- Captured `.planning/phases/08-delivery-polish-demo-readiness/08-COMPOSE-EVIDENCE.md` from the running stack.

## Task Commits

This plan is committed as a single scoped plan commit after verification.

## Files Created/Modified

- `scripts/check_demo_stack.py` - read-only compose health, diagnostics, memory, self-test, and evidence helper.
- `infra/README.md` - Phase 8 local demo runbook.
- `infra/docker-compose.yml` - Python AI default limit raised to `10g` and reservation to `8g`.
- `.env.example` - root memory example updated to `10g` / `8g`.
- `infra/.env.example` - infra memory example updated to `10g` / `8g`.
- `Makefile` - `demo-check` and `demo-seed` wrappers.
- `.planning/phases/08-delivery-polish-demo-readiness/08-COMPOSE-EVIDENCE.md` - live 9/9 healthy compose evidence.

## Verification

- `python scripts/check_demo_stack.py --help` - passed.
- `python scripts/check_demo_stack.py --self-test` - passed.
- Documentation check for `docker compose`, `seed`, `diagnostics`, `9/9`, `Hugging Face cache`, and `do not run \`docker compose down -v\`` - passed.
- `docker compose --env-file infra\.env -f infra\docker-compose.yml ps` - all services healthy.
- `python scripts/check_demo_stack.py --compose-file infra/docker-compose.yml --env-file infra/.env --output .planning/phases/08-delivery-polish-demo-readiness/08-COMPOSE-EVIDENCE.md` - `services_healthy=9/9`.

## Decisions Made

- The committed compose default now reflects the proven demo requirement instead of the stale 8g contour.
- The runbook describes local laptop delivery and explicitly avoids real production deployment language.
- The stack checker is intentionally read-only; it does not start, stop, restart, delete, or recreate Docker resources.

## Deviations from Plan

### Auto-fixed Issues

**1. Memory guidance mismatch**
- **Found during:** Task 2 (runbook and env examples)
- **Issue:** The plan text expected older 8 GiB wording, but the live stack requires `PYTHON_AI_MEMORY_LIMIT=10g`, WSL `12GB`, and actual reservation `8g`.
- **Fix:** Updated runbook, examples, compose defaults, and evidence helper to document `10g` / `8g` / `12GB`.
- **Files modified:** `infra/README.md`, `infra/docker-compose.yml`, `.env.example`, `infra/.env.example`, `scripts/check_demo_stack.py`.
- **Verification:** Live evidence reports `python_ai_limit_bytes=10737418240`, `python_ai_reservation_bytes=8589934592`, and documented `12GB`.

---

**Total deviations:** 1 auto-fixed.
**Impact on plan:** The deviation preserves real stack readiness and avoids downgrading memory config to satisfy stale text.

## Issues Encountered

Python command execution occasionally hit the Windows sandbox spawn setup issue; rerunning with the approved scoped checker prefix resolved it. The checker and live Docker evidence passed.

## User Setup Required

None for committed artifacts. Live demo runs still require ignored `infra/.env` with admin credentials and OpenRouter/Langfuse keys as described in the runbook.

## Next Phase Readiness

Phase 8 Wave 1 can continue with polish/traceability work. Wave 2 regression can consume `08-SEED-EVIDENCE.md` and `08-COMPOSE-EVIDENCE.md` as readiness inputs.

---
*Phase: 08-delivery-polish-demo-readiness*
*Completed: 2026-06-02*
