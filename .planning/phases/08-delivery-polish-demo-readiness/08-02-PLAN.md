---
phase: "08-delivery-polish-demo-readiness"
plan: "02"
type: "docs"
wave: 1
depends_on: []
files_modified:
  - "scripts/check_demo_stack.py"
  - "infra/README.md"
  - ".env.example"
  - "infra/.env.example"
  - "Makefile"
  - ".planning/phases/08-delivery-polish-demo-readiness/08-02-SUMMARY.md"
autonomous: true
requirements: ["DEL-01"]
user_setup:
  - "Live compose checks require Docker Desktop and the ignored local `infra/.env` with real local secrets where needed."
must_haves:
  truths:
    - "D-423: Do not create `prod-compose`, `docker-compose.prod.yml`, or another deployment file."
    - "D-424: The compose success criterion is 9/9 healthy for PostgreSQL, MinIO, RabbitMQ, Qdrant, Neo4j, Langfuse, Java backend, Python AI service, and frontend."
    - "D-425: The demo run is local laptop delivery, not deployment; README may call it production-like only as a full local stack."
    - "D-426: Python AI memory guidance is explicit, and committed memory defaults are changed only if live verification proves the current default is insufficient."
    - "D-427: Keep the Hugging Face cache volume and do not instruct normal demo users to clear model/cache volumes."
    - "D-437: Documentation explains required admin credentials and env values without committing secrets."
  artifacts:
    - path: "scripts/check_demo_stack.py"
      provides: "deterministic stack health summary"
      contains: "compose ps parsing, service count, diagnostics snapshot, and redacted Markdown output"
    - path: "infra/README.md"
      provides: "local demo runbook"
      contains: "startup, memory, health, seed, diagnostics, and troubleshooting commands"
    - path: ".env.example"
      provides: "root env reference"
      contains: "non-secret demo defaults and memory guidance"
  decision_refs:
    - "D-423"
    - "D-424"
    - "D-425"
    - "D-426"
    - "D-427"
    - "D-437"
---

<objective>
Make the existing compose stack demonstrably production-like for a local demo without adding a second deployment topology.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-delivery-polish-demo-readiness/08-CONTEXT.md
@.planning/phases/08-delivery-polish-demo-readiness/08-RESEARCH.md
@infra/docker-compose.yml
@infra/README.md
@.env.example
@infra/.env.example
@Makefile
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add compose health evidence helper</name>
  <files>scripts/check_demo_stack.py</files>
  <behavior>
    - Given a compose ps JSON/text fixture with nine healthy services, report 9/9 healthy and exit 0.
    - Given a missing or unhealthy service, report the exact service and exit non-zero.
    - Given diagnostics JSON, include readiness booleans and counters while redacting secret-like values.
  </behavior>
  <action>Create a small Python helper that shells out to Docker Compose or accepts fixture input for tests, verifies the nine expected services by service name, captures optional `http://localhost:8000/diagnostics`, and writes a Markdown/JSON evidence file under the Phase 8 directory. Keep it read-only; it must not start, stop, restart, or remove services.</action>
  <verify>
    <automated>python scripts/check_demo_stack.py --help</automated>
    <automated>python scripts/check_demo_stack.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - Helper has a dry-run or fixture mode for deterministic tests.
    - Helper knows the exact nine expected services from D-424.
    - Helper redacts values matching key/token/password names.
  </acceptance_criteria>
  <done>Stack health can be captured reproducibly without mutating Docker state.</done>
</task>

<task type="auto">
  <name>Task 2: Update local runbook and env examples</name>
  <files>infra/README.md, .env.example, infra/.env.example, Makefile</files>
  <read_first>infra/docker-compose.yml, .planning/BACKLOG.md, .planning/phases/07-evaluation-observability/07-EVAL-SUMMARY.md</read_first>
  <action>Update documentation and thin command targets for the Phase 8 demo runbook: prerequisites, Docker Desktop memory guidance, exact compose startup, admin credential setup via ignored env files, seed reset command, health checks, diagnostics, final regression, troubleshooting, known model cache guidance, and no-volume-wipe rule. Add Makefile targets only as wrappers around existing compose/seed/check commands; do not introduce a new compose file. Keep committed env examples secret-free.</action>
  <verify>
    <automated>python scripts/check_demo_stack.py --help</automated>
    <automated>python -c "from pathlib import Path; t=Path('infra/README.md').read_text(encoding='utf-8'); required=['docker compose','seed','diagnostics','9/9','Hugging Face cache','do not run `docker compose down -v`']; missing=[x for x in required if x not in t]; assert not missing, missing"</automated>
  </verify>
  <acceptance_criteria>
    - Docs mention local laptop delivery, not real production deployment.
    - Docs keep `bge-m3-cache` and do not instruct normal users to clear model/cache volumes.
    - Memory guidance explains 8 GiB committed default and local override path; compose defaults change only if verified necessary.
    - No secret values are committed.
  </acceptance_criteria>
  <done>The local runbook is sufficient to start, inspect, seed, and troubleshoot the demo stack.</done>
</task>

<task type="auto">
  <name>Task 3: Capture compose readiness evidence</name>
  <files>.planning/phases/08-delivery-polish-demo-readiness/08-COMPOSE-EVIDENCE.md, .planning/phases/08-delivery-polish-demo-readiness/08-02-SUMMARY.md</files>
  <read_first>scripts/check_demo_stack.py, infra/README.md</read_first>
  <action>Run the read-only stack checker against the local Docker stack. Record service health, diagnostics readiness/counters, Docker memory summary if available, and any local override notes in `08-COMPOSE-EVIDENCE.md`. If any service is unhealthy, record the blocker and do not claim compose readiness.</action>
  <verify>
    <automated>docker compose --env-file infra/.env -f infra/docker-compose.yml ps</automated>
    <automated>python scripts/check_demo_stack.py --compose-file infra/docker-compose.yml --env-file infra/.env --output .planning/phases/08-delivery-polish-demo-readiness/08-COMPOSE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Evidence states whether all nine services are healthy.
    - Evidence includes `/diagnostics` values when reachable.
    - Evidence contains no secret values.
  </acceptance_criteria>
  <done>Compose readiness is proven or blocked with concrete service names.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Documentation causes data loss | Runbook | Explicitly forbid `docker compose down -v` for demo reset. |
| Secret leakage | Env docs/evidence | Use examples/placeholders and redaction. |
| Hidden service failure | Compose evidence | Check all nine named services and fail on missing/unhealthy. |
| Memory overfit | Compose config | Prefer documented local overrides unless live evidence requires default change. |
</threat_model>

<verification>
- [ ] `python scripts/check_demo_stack.py --help`
- [ ] `docker compose --env-file infra/.env -f infra/docker-compose.yml ps`
- [ ] `08-COMPOSE-EVIDENCE.md` reports 9/9 healthy or a concrete blocker.
</verification>

<success_criteria>
- Existing compose file remains the only local demo topology.
- The runbook explains startup, memory, seed reset, health, diagnostics, and troubleshooting.
- Compose evidence verifies 9/9 service health without clearing volumes or leaking secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/08-delivery-polish-demo-readiness/08-02-SUMMARY.md`.
</output>
