---
phase: "08-delivery-polish-demo-readiness"
plan: "06"
type: "bugfix"
wave: 1
depends_on: []
files_modified:
  - "ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py"
  - "ai-service/src/corp_rag_ai/pipeline/ingestion/orchestrator.py"
  - "ai-service/tests/test_entity_extractor.py"
  - "ai-service/tests/test_ingestion_orchestrator.py"
  - ".planning/phases/08-delivery-polish-demo-readiness/08-06-SUMMARY.md"
autonomous: true
requirements: ["DEL-01"]
must_haves:
  truths:
    - "This is a narrow Phase 8 insertion to stabilize entity-extraction parsing and bounded retries after live 08-01 seed evidence showed Java 16/16 and Qdrant 16/16 but Neo4j 9/16."
    - "Do not change the entity extraction schema, entity/relation contract, normalized graph model, or extraction-quality policy."
    - "Do not broaden the prompt beyond the minimum instruction needed to request strict valid JSON on malformed-output retries."
    - "D-421: Do not weaken output guard, citation validation, access filters, weak-evidence thresholds, or refusal behavior."
    - "D-419: Do not implement multi-hop graph retrieval, Cypher/path ranking, semantic path ranking, or entity-linking redesign."
    - "D-416/D-422: Do not mutate frozen corpus files, golden questions, reference answers, or expected UUID assumptions."
    - "Keep best-effort graph indexing semantics: vector indexing remains successful when entity extraction exhausts retries."
    - "Path-limited scope: production changes stay inside the ai-service entity-extraction/ingestion warning layer and focused tests."
  artifacts:
    - path: "ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py"
      provides: "tolerant DeepSeek/OpenRouter structured-output parsing and bounded malformed-output retries"
      contains: "markdown fence stripping, JSON object extraction from pre/postamble text, retry prompt hardening, attempt logging, and final malformed StageFailure"
    - path: "ai-service/tests/test_entity_extractor.py"
      provides: "unit coverage for fenced JSON, preamble JSON, retry success, and retry exhaustion"
      contains: "mocked OpenRouter responses with no live model or stack dependency"
    - path: "ai-service/src/corp_rag_ai/pipeline/ingestion/orchestrator.py"
      provides: "explicit best-effort graph skip warning context"
      contains: "document id, stage, error code, and failure detail when graph indexing is skipped after vector success"
    - path: "ai-service/tests/test_ingestion_orchestrator.py"
      provides: "unit coverage that exhausted entity extraction logs doc id and reason while preserving indexed state"
      contains: "mocked ingestion service with caplog assertions"
  decision_refs:
    - "D-416"
    - "D-419"
    - "D-421"
    - "D-422"
---

<objective>
Harden DeepSeek/OpenRouter entity-extraction response handling so valid JSON wrapped in markdown or prose is parsed without retry, truly malformed output gets bounded repair retries, and unrecoverable failures still gracefully skip graph indexing with explicit warning evidence.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-delivery-polish-demo-readiness/08-CONTEXT.md
@.planning/phases/08-delivery-polish-demo-readiness/08-RESEARCH.md
@ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py
@ai-service/src/corp_rag_ai/pipeline/indexing/prompts/entity_extraction_v1.md
@ai-service/src/corp_rag_ai/pipeline/ingestion/orchestrator.py
@ai-service/tests/test_entity_extractor.py
@ai-service/tests/test_ingestion_orchestrator.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify tolerant parsing and malformed retry behavior</name>
  <files>ai-service/tests/test_entity_extractor.py, ai-service/tests/test_ingestion_orchestrator.py</files>
  <behavior>
    - A DeepSeek response containing valid JSON inside a ```json markdown fence parses without retry.
    - A DeepSeek response with prose preamble/postamble around the JSON object parses without retry.
    - A fully invalid response retries with a stricter JSON-only instruction and succeeds on the second attempt.
    - Exhausted malformed attempts raise the existing entity-extraction StageFailure while logging the final retry outcome.
    - The ingestion orchestrator preserves vector-success best-effort behavior and logs a WARNING with document id and failure reason when graph indexing is skipped.
  </behavior>
  <action>Add focused pytest coverage using the existing fake OpenRouter client and ingestion service doubles. Keep tests unit-only; do not require Docker, OpenRouter, Qdrant, Neo4j, Java, or seeded corpus state.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_entity_extractor.py ai-service/tests/test_ingestion_orchestrator.py</automated>
  </verify>
  <done>Failing tests encode all four requested malformed-output scenarios and the explicit skip warning.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tolerant JSON extraction before malformed classification</name>
  <files>ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py, ai-service/tests/test_entity_extractor.py</files>
  <read_first>ai-service/src/corp_rag_ai/pipeline/indexing/prompts/entity_extraction_v1.md</read_first>
  <action>Update response parsing so `_parse_response` first honors parsed responses, then extracts and validates the JSON payload from raw text. Handle common DeepSeek/OpenRouter wrappers: ```json fences, unlabeled fences, prose preambles/postambles, and nested JSON object boundaries. Preserve the `EntityExtractionResponse` pydantic schema and existing mapping logic.</action>
  <acceptance_criteria>
    - Valid fenced JSON is parsed on the first API call.
    - Valid preamble/postamble JSON is parsed on the first API call.
    - Schema-invalid JSON remains malformed and is not silently coerced.
    - No entity/relation schema fields or graph normalization behavior change.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_entity_extractor.py</automated>
  </verify>
  <done>Tolerant parsing recovers wrapped JSON without retry or contract changes.</done>
</task>

<task type="auto">
  <name>Task 3: Add bounded malformed-output retries and explicit skip warning detail</name>
  <files>ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py, ai-service/src/corp_rag_ai/pipeline/ingestion/orchestrator.py, ai-service/tests/test_entity_extractor.py, ai-service/tests/test_ingestion_orchestrator.py</files>
  <action>Replace the single malformed retry flag with a bounded malformed-attempt loop, defaulting to three total malformed-output attempts. On retry attempts, append a minimal repair instruction that demands strictly valid JSON matching the existing schema with no prose or markdown. Log attempt number and outcome. Keep dependency retry behavior separate. Add an explicit per-request timeout for OpenRouter completion calls to prevent hanging. In the orchestrator warning, include the failure detail so skipped graph indexing records document id and reason.</action>
  <acceptance_criteria>
    - Malformed text can recover on the second attempt without changing extraction output contract.
    - All malformed attempts exhausted raises `malformed_structured_output` as before.
    - Retry count and call timeout are bounded by constructor defaults and are testable.
    - Graph skip WARNING includes `document_id`, `stage`, `error_code`, and `detail`.
    - Best-effort indexing state remains unchanged: vector succeeds, graph skip does not mark document failed.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_entity_extractor.py ai-service/tests/test_ingestion_orchestrator.py</automated>
  </verify>
  <done>Malformed-output instability is bounded and observable while preserving existing fallback semantics.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 4: Live reseed and record Phase 8 seed evidence</name>
  <files>.planning/phases/08-delivery-polish-demo-readiness/08-SEED-EVIDENCE.md, .planning/phases/08-delivery-polish-demo-readiness/08-SEED-EVIDENCE.json</files>
  <action>After the code fix and unit/regression tests are green, delete only non-seed documents through the standard Java DELETE API, then rerun the 08-01 seed corpus CLI. Do not manually clean Docker volumes or mutate identity records. Confirm Java has exactly 16 seed documents, Qdrant has 16/16, and Neo4j has 16/16 Document nodes, then update the existing Phase 8 seed evidence files.</action>
  <manual_gate>Requires the local compose stack, admin credentials, and explicit user approval before deleting non-seed documents.</manual_gate>
  <done>Live evidence shows Java 16/16, Qdrant 16/16, and Neo4j 16/16 after the reliability fix.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Extraction contract drift | Parser/retry implementation | Keep the existing pydantic schema, response_format schema, and graph mapping untouched. |
| Demo quality improves by weakening safety | Adjacent RAG guard/retrieval code | Path-limit edits to entity extraction and explicit ingestion warning detail only. |
| Infinite or expensive model retry loop | Malformed-output retries | Bound total malformed attempts and per-call timeout; log attempt outcomes. |
| Silent Neo4j under-indexing remains hidden | Best-effort graph skip | Preserve skip behavior but log document id, stage, error code, and detail. |
</threat_model>

<verification>
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_entity_extractor.py`
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_ingestion_orchestrator.py`
- [ ] Live reseed evidence shows Java 16/16, Qdrant 16/16, and Neo4j 16/16 after code/tests pass and deletion is approved.
</verification>

<success_criteria>
- Wrapped JSON from DeepSeek/OpenRouter no longer fails entity extraction.
- Truly malformed output is retried a bounded number of times with a minimal JSON-only repair instruction.
- Exhausted malformed output keeps current best-effort ingestion semantics and emits explicit warning evidence.
- No guard, retrieval, frozen corpus, golden, UUID, or graph retrieval behavior is changed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-delivery-polish-demo-readiness/08-06-SUMMARY.md`.
</output>
