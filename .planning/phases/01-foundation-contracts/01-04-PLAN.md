---
phase: "01-foundation-contracts"
plan: "04"
type: "execute"
wave: 3
depends_on: ["02"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/Dockerfile"
  - "ai-service/.dockerignore"
  - "ai-service/alembic.ini"
  - "ai-service/migrations/env.py"
  - "ai-service/migrations/versions/0001_empty_baseline.py"
  - "ai-service/src/corp_rag_ai/__init__.py"
  - "ai-service/src/corp_rag_ai/main.py"
  - "ai-service/src/corp_rag_ai/config.py"
autonomous: true
requirements: ["FND-01", "FND-03"]
user_setup: []
must_haves:
  truths:
    - "Python AI service has a runnable FastAPI application on port 8000."
    - "Python generated contract modules can be created under the documented package path and imported by tests."
    - "Python database migration infrastructure exists with an empty Alembic baseline for the AI-owned Postgres database."
  artifacts:
    - path: "ai-service/pyproject.toml"
      provides: "uv-managed Python project"
      contains: "FastAPI, pydantic-settings, SQLAlchemy async, asyncpg, Alembic, pytest"
    - path: "ai-service/src/corp_rag_ai/main.py"
      provides: "FastAPI app with /health and /ready"
      exports: ["app"]
    - path: "ai-service/src/corp_rag_ai/config.py"
      provides: "Typed settings from environment variables"
      exports: ["Settings", "get_settings"]
    - path: "ai-service/alembic.ini"
      provides: "Alembic command configuration"
    - path: "ai-service/migrations/versions/0001_empty_baseline.py"
      provides: "AI DB empty baseline migration"
    - path: "ai-service/Dockerfile"
      provides: "Docker image for compose"
  key_links:
    - from: "scripts/generate_python_contracts.py"
      to: "ai-service/src/corp_rag_ai/contracts/generated/"
      via: "Plan 02 generation target imported by this service"
    - from: "ai-service/src/corp_rag_ai/config.py"
      to: "infra/docker-compose.yml"
      via: "environment variable names planned in Plan 06"
    - from: "ai-service/alembic.ini"
      to: "Makefile migrate-python"
      via: "Plan 06 migration target"
  decision_refs:
    - "D-12: Python generated contract code lives under ai-service/src/corp_rag_ai/contracts/generated/."
    - "D-17: Python generated outputs are explicitly ignored."
    - "D-22: Python work proves runnable shape, not feature behavior."
    - "D-25: Python scope is FastAPI on port 8000 with /health, /ready, uv, generated contract imports, and no retrieval, embeddings, AI logic, or business routers."
    - "D-31: Python uses SQLAlchemy 2.0 async, asyncpg, and Alembic with an empty baseline migration against corp_rag_ai."
---

<objective>
Create the Python AI service foundation.

Purpose: The RAG service needs a runnable FastAPI and migration shape before ingestion, retrieval, guards, and generation are implemented in later phases.
Output: uv project, FastAPI health/readiness app, typed config, Alembic baseline, Docker image definition, and generated-contract package placement.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@docs/ARCHITECTURE.md
@docs/PATTERNS.md
@ai-service/README.md
@CLAUDE.md

<dependency_context>
Requires Plan 02 for Python contract generation scripts. Generated Python contract outputs are not committed per D-15 and D-17.
</dependency_context>

<decision_coverage>
- D-12: Python generated contract code lives under `ai-service/src/corp_rag_ai/contracts/generated/`.
- D-17: Python generated outputs are explicitly ignored.
- D-22: Python work proves runnable shape, not feature behavior.
- D-25: Python scope is FastAPI on port 8000 with `/health`, `/ready`, uv, generated contract imports, and no retrieval, embeddings, AI logic, or business routers.
- D-31: Python uses SQLAlchemy 2.0 async, asyncpg, and Alembic with an empty baseline migration against `corp_rag_ai`.
</decision_coverage>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add uv FastAPI service skeleton</name>
  <files>ai-service/pyproject.toml, ai-service/Dockerfile, ai-service/.dockerignore, ai-service/src/corp_rag_ai/__init__.py, ai-service/src/corp_rag_ai/main.py, ai-service/src/corp_rag_ai/config.py</files>
  <action>Create the Python project required by D-12, D-17, D-22, D-25, D-27, D-28, and D-34. `pyproject.toml` must use Python 3.12 and uv-compatible dependencies for FastAPI, Uvicorn, Pydantic v2, pydantic-settings, SQLAlchemy 2.0 async, asyncpg, Alembic, pytest, and pytest-asyncio. `config.py` must expose typed settings for AI database URL, RabbitMQ URL, Qdrant URL, Neo4j URI/auth, MinIO endpoint placeholders, and Langfuse placeholders using environment variables. `main.py` must create `app`, register `GET /health` returning healthy status and `GET /ready` returning ready status, and avoid retrieval, embeddings, AI logic, business routers, and AMQP consumers in this phase. Add a Dockerfile that runs `uvicorn corp_rag_ai.main:app` on port 8000.</action>
  <verify>
    <automated>cd ai-service; uv run python -c "from corp_rag_ai.main import app; print(app.title)"</automated>
  </verify>
  <done>Python FastAPI app imports successfully, has health/readiness routes, reads typed environment settings, and runs from a Docker image definition.</done>
</task>

<task type="auto">
  <name>Task 2: Add Alembic baseline for AI database</name>
  <files>ai-service/alembic.ini, ai-service/migrations/env.py, ai-service/migrations/versions/0001_empty_baseline.py</files>
  <action>Add migration infrastructure required by D-25, D-31, and D-32. Configure Alembic to read the AI database URL from the same settings/env names used by `config.py`. Create an empty baseline revision named `0001_empty_baseline.py` with no domain tables, because idempotency and business persistence arrive in later Python phases. Keep SQLAlchemy async configuration ready for `make migrate-python`, but do not add parsing, embedding, Qdrant, Neo4j, or graph code.</action>
  <verify>
    <automated>cd ai-service; uv run alembic upgrade head --sql</automated>
  </verify>
  <done>Alembic can render the empty baseline migration and uses the configured AI database URL without adding domain tables.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Java backend to Python AI API | Internal API contracts are generated from Plan 02 and imported by Python later. |
| Python AI service to AI-owned Postgres | Migration/config uses scoped database credentials from compose. |
| Python service to generated code | Generated Pydantic models/constants are trusted build artifacts from root contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `config.py` | mitigate | Use environment settings and placeholders only; do not commit real secrets. |
| T-04-02 | Tampering | Alembic baseline | mitigate | Create explicit empty baseline so later DB changes are ordered and reviewable. |
| T-04-03 | Denial of Service | `/ready` endpoint | accept | Readiness is deliberately light in Phase 1; dependency-deep readiness belongs to later service phases. |
</threat_model>

<verification>
Run `cd ai-service; uv run python -c "from corp_rag_ai.main import app; print(app.title)"` and `cd ai-service; uv run alembic upgrade head --sql`.
</verification>

<success_criteria>
- `uv run` can import the FastAPI app.
- `/health` and `/ready` are registered by the app.
- Alembic has an empty baseline for `corp_rag_ai`.
- Generated Python contract package path is reserved for Plan 02 outputs and ignored by git.
- No retrieval, embeddings, graph, guard, AMQP consumer, or business router code is added.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-contracts/01-04-SUMMARY.md`.
</output>
