---
phase: "01-foundation-contracts"
plan: "05"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "frontend/index.html"
  - "frontend/src/styles/base.css"
  - "frontend/nginx.conf"
  - "frontend/Dockerfile"
  - "frontend/.dockerignore"
autonomous: true
requirements: ["FND-01"]
user_setup: []
must_haves:
  truths:
    - "Frontend has a static browser entrypoint that nginx can serve in the local stack."
    - "The first browser screen clearly identifies the product as Corp RAG without adding app routing or API clients."
    - "Frontend Docker image can be built independently for compose."
  artifacts:
    - path: "frontend/index.html"
      provides: "Static browser entrypoint"
      contains: "Corp RAG - coming soon"
    - path: "frontend/src/styles/base.css"
      provides: "Plain CSS foundation"
      contains: "CSS custom properties and BEM-friendly classes"
    - path: "frontend/nginx.conf"
      provides: "nginx static file configuration"
    - path: "frontend/Dockerfile"
      provides: "Frontend Docker image"
  key_links:
    - from: "frontend/Dockerfile"
      to: "frontend/nginx.conf"
      via: "Docker image copies nginx config"
    - from: "frontend/Dockerfile"
      to: "frontend/index.html"
      via: "nginx document root copy"
    - from: "frontend/Dockerfile"
      to: "infra/docker-compose.yml"
      via: "compose build context planned in Plan 06"
  decision_refs:
    - "D-22: Frontend work proves served shape, not feature behavior."
    - "D-26: nginx serves static index.html saying Corp RAG - coming soon with no JS modules, routing, or API client."
---

<objective>
Create the static frontend foundation.

Purpose: Phase 1 only needs a served browser shell so the full local stack has a visible frontend surface.
Output: Static HTML/CSS, nginx configuration, and frontend Docker image definition.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@docs/ARCHITECTURE.md
@docs/PATTERNS.md
@frontend/README.md
@CLAUDE.md

<phase_decisions>
Implement D-22 and D-26 exactly: a static nginx-served page with text `Corp RAG - coming soon`. Do not add JS modules, routing, API client, auth guard, or frontend workflows in Phase 1.
</phase_decisions>

<decision_coverage>
- D-22: Frontend work proves served shape, not feature behavior.
- D-26: nginx serves static `index.html` saying `Corp RAG - coming soon` with no JS modules, routing, or API client.
</decision_coverage>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add static frontend shell</name>
  <files>frontend/index.html, frontend/src/styles/base.css</files>
  <action>Create the static browser shell required by D-22 and D-26. `index.html` must be semantic HTML, load only plain CSS, and display the exact product text `Corp RAG - coming soon`. `base.css` must use CSS custom properties and BEM-compatible class names without Tailwind, Bootstrap utility classes, React, Vue, Svelte, routing, API client code, or session guard code. Keep the visual simple and production-neutral because real UI arrives in Phase 6.</action>
  <verify>
    <automated>python -c "from pathlib import Path; html=Path('frontend/index.html').read_text(encoding='utf-8'); css=Path('frontend/src/styles/base.css').read_text(encoding='utf-8'); assert 'Corp RAG - coming soon' in html; assert 'script type=\"module\"' not in html; assert '--' in css; print('frontend shell present')"</automated>
  </verify>
  <done>The static page loads CSS, shows `Corp RAG - coming soon`, and contains no app routing, API client, or framework dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Add nginx packaging for frontend</name>
  <files>frontend/nginx.conf, frontend/Dockerfile, frontend/.dockerignore</files>
  <action>Create nginx packaging required by D-26, D-27, and D-28. `frontend/nginx.conf` must serve `index.html` and static assets from nginx on port 80, include a simple health location for Docker healthchecks, and avoid proxying to Java because Phase 1 frontend is only static. `frontend/Dockerfile` must use `nginx:alpine`, copy `index.html`, CSS assets, and `nginx.conf`, and expose port 80. `.dockerignore` must exclude local/editor/generated noise while keeping source assets available to the build.</action>
  <verify>
    <automated>docker build -t corp-rag-frontend:phase1 frontend</automated>
  </verify>
  <done>Frontend image builds successfully and serves the static shell through nginx.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to nginx | Static assets are served without user input or API calls in Phase 1. |
| nginx image to compose | Compose uses this image as the frontend service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `frontend/index.html` | accept | Page contains no secrets, user data, or API calls. |
| T-05-02 | Tampering | nginx static assets | mitigate | Docker image copies only tracked frontend files and excludes local noise with `.dockerignore`. |
| T-05-03 | Denial of Service | nginx health | accept | Phase 1 uses standard nginx health/static serving; compose healthcheck catches container failure. |
</threat_model>

<verification>
Run `docker build -t corp-rag-frontend:phase1 frontend` and inspect the static page content check from Task 1.
</verification>

<success_criteria>
- Frontend static page exists and shows `Corp RAG - coming soon`.
- No frontend framework, router, API client, or session guard is introduced.
- nginx configuration and Dockerfile build a static frontend image.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-contracts/01-05-SUMMARY.md`.
</output>
