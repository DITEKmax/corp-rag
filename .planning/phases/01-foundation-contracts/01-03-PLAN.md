---
phase: "01-foundation-contracts"
plan: "03"
type: "execute"
wave: 3
depends_on: ["02"]
files_modified:
  - "backend/pom.xml"
  - "backend/corp-rag-app/pom.xml"
  - "backend/corp-rag-app/src/main/java/com/corprag/CorpRagApplication.java"
  - "backend/corp-rag-app/src/main/resources/application.yml"
  - "backend/corp-rag-app/src/main/resources/db/migration/V1__baseline.sql"
  - "backend/corp-rag-app/src/test/java/com/corprag/CorpRagApplicationTests.java"
  - "backend/corp-rag-app/Dockerfile"
  - "backend/corp-rag-app/.dockerignore"
autonomous: true
requirements: ["FND-01", "FND-03"]
user_setup: []
must_haves:
  truths:
    - "Java backend has a runnable Spring Boot application with Actuator health on port 8080."
    - "Java backend compiles against the generated contract module without handwritten contract DTOs."
    - "Java database migration infrastructure exists with an empty baseline for the Java-owned Postgres database."
  artifacts:
    - path: "backend/corp-rag-app/pom.xml"
      provides: "Spring Boot app module"
      contains: "actuator, web, flyway, PostgreSQL, corp-rag-contracts dependency"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/CorpRagApplication.java"
      provides: "Java application entrypoint"
      exports: ["main"]
    - path: "backend/corp-rag-app/src/main/resources/application.yml"
      provides: "Java app runtime configuration"
      contains: "server.port=8080, actuator exposure, datasource, Flyway"
    - path: "backend/corp-rag-app/src/main/resources/db/migration/V1__baseline.sql"
      provides: "Flyway baseline migration"
    - path: "backend/corp-rag-app/Dockerfile"
      provides: "Docker image for compose"
  key_links:
    - from: "backend/corp-rag-app/pom.xml"
      to: "backend/corp-rag-contracts"
      via: "Maven module dependency"
    - from: "backend/corp-rag-app/src/main/resources/application.yml"
      to: "infra/postgres/init.sql"
      via: "database name and credentials planned in Plan 06"
    - from: "backend/corp-rag-app/Dockerfile"
      to: "infra/docker-compose.yml"
      via: "compose build context planned in Plan 06"
  decision_refs:
    - "D-22: Java work proves runnable shape, not feature behavior."
    - "D-23: Java scope is Spring Boot 3.3 in backend/corp-rag-app with Actuator on port 8080 and no security, JWT, business endpoints, or domain DB tables."
    - "D-24: Maven structure includes parent POM, corp-rag-contracts, and corp-rag-app; generated DTOs compile."
    - "D-30: Java uses Flyway with an empty baseline migration against corp_rag_java."
---

<objective>
Create the Java backend foundation.

Purpose: The enterprise backend needs a runnable Spring Boot shape before auth, documents, events, and audit behavior are implemented in later phases.
Output: Maven app module, Actuator health, Flyway baseline, generated-contract dependency, and Docker image definition.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@docs/ARCHITECTURE.md
@docs/PATTERNS.md
@backend/README.md
@CLAUDE.md

<dependency_context>
Requires Plan 02 for `backend/pom.xml` and `backend/corp-rag-contracts`. This plan may modify `backend/pom.xml` only to add the app module and dependencies needed by the app.
</dependency_context>

<decision_coverage>
- D-22: Java work proves runnable shape, not feature behavior.
- D-23: Java scope is Spring Boot 3.3 in `backend/corp-rag-app`, Actuator on port 8080, and no security, JWT, business endpoints, or domain DB tables.
- D-24: Maven structure includes parent POM, `corp-rag-contracts`, and `corp-rag-app`; generated DTOs compile.
- D-30: Java uses Flyway with an empty baseline migration against `corp_rag_java`.
</decision_coverage>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Spring Boot app module</name>
  <files>backend/pom.xml, backend/corp-rag-app/pom.xml, backend/corp-rag-app/src/main/java/com/corprag/CorpRagApplication.java, backend/corp-rag-app/Dockerfile, backend/corp-rag-app/.dockerignore</files>
  <action>Extend the Maven structure from Plan 02 with `corp-rag-app` per D-22, D-23, D-24, and D-30. Add the module to `backend/pom.xml`. Create `backend/corp-rag-app/pom.xml` using Java 21 and Spring Boot 3.3 compatible dependencies for web, actuator, validation, Flyway, JDBC/PostgreSQL, and a dependency on `corp-rag-contracts`. Create `CorpRagApplication` in package `com.corprag`. Do not add Spring Security, JWT, business endpoints, domain tables, AMQP runtime declarations, or feature helpers because those are explicitly out of Phase 1. Add a multi-stage Dockerfile that builds the Maven app and runs it on port 8080, using generated contract code from the Maven build.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am -DskipTests package</automated>
  </verify>
  <done>The Java app module packages successfully, imports the contract module through Maven, and exposes a Docker build target without business behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add Java runtime config and migration baseline</name>
  <files>backend/corp-rag-app/src/main/resources/application.yml, backend/corp-rag-app/src/main/resources/db/migration/V1__baseline.sql, backend/corp-rag-app/src/test/java/com/corprag/CorpRagApplicationTests.java</files>
  <action>Add runtime configuration required by D-23, D-27, D-28, D-29, D-30, and D-32. `application.yml` must set port 8080, expose Actuator health for Docker healthchecks, read datasource URL/user/password from environment variables with local defaults matching Plan 06, and enable Flyway against the Java database. `V1__baseline.sql` must be an empty baseline migration with a comment explaining that domain tables are deferred. Add a light test that loads the application context with Flyway disabled or with a test-safe datasource configuration, so Java packaging can prove the skeleton without requiring Docker.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test</automated>
  </verify>
  <done>`GET /actuator/health` is provided by Actuator when the app runs, the Java database has a Flyway baseline, and local tests prove the app skeleton compiles and starts in a test-safe profile.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/clients to Java app | Phase 1 exposes only Actuator health, but later public API contracts already require protected behavior. |
| Java app to PostgreSQL | The app receives database credentials from environment variables in compose. |
| Java app to generated contracts | Generated DTOs/constants are trusted compile-time inputs from Plan 02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `application.yml` | mitigate | Read credentials from environment variables and keep only local defaults/placeholders in source. |
| T-03-02 | Tampering | Flyway baseline | mitigate | Use an explicit empty baseline migration so later schema changes are ordered and reviewable. |
| T-03-03 | Elevation of Privilege | Java app surface | accept | No auth/business endpoints are exposed in Phase 1; Spring Security is deferred by explicit phase scope. |
</threat_model>

<verification>
Run `cd backend; mvn -q -pl corp-rag-app -am test` after Plan 02 generation succeeds.
</verification>

<success_criteria>
- Java app module packages and tests through Maven.
- App uses generated contract module as a compile dependency.
- Actuator health is configured for Docker healthchecks.
- Flyway empty baseline exists for `corp_rag_java`.
- No auth, JWT, domain tables, AMQP declarations, or business endpoints are added.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-contracts/01-03-SUMMARY.md`.
</output>
