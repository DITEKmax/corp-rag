---
context: phase
phase: 02-identity-users-access-control
task: 3
total_tasks: 3
status: paused
last_updated: 2026-05-12T06:35:59.478Z
---

<current_state>
Paused during Phase 02, Plan 07 (`02-07-PLAN.md`). Implementation and tests for access policy administration, access filter resolution/cache, downstream mapper, and audit integration are committed. Final `mvn verify` was started but manually interrupted by the user before completion.
</current_state>

<completed_work>

Completed commits:
- `90b2dd5 feat(02-07): implement access policy enforcement`
- `70ed818 test(02-07): cover identity access policy flows`

Completed implementation:
- Added `AccessPolicyController` for `/api/v1/access-policies`.
- Added `AccessPolicyService` with role uniqueness, ETag/If-Match replacement, system-role policy edits, validation, cache eviction, audit writes, and last-admin visibility protection.
- Added `AccessFilterResolver`, `AccessFilterCache`, and `AccessFilterCacheInvalidator`.
- Added `QueryAccessFilterMapper` for generated AI `AccessFilter`.
- Extended `AccessPolicyRepository` with list/delete/count helpers.
- Integrated cache invalidation into role/user-role mutations.
- Integrated audit events for user, role, access-policy, and password lifecycle changes.
- Fixed audit transaction behavior: generic mutation audit joins caller transaction; auth audit remains `REQUIRES_NEW`.
- Fixed audit repository to null missing actor/target user IDs instead of poisoning transactions.
- Extended unreadable-request mapping for invalid `AccessLevel`.

Completed tests:
- Added `AccessPolicyServiceTest`.
- Added `AccessFilterResolverTest`.
- Added `AccessPolicyControllerTest`.
- Added `QueryAccessFilterMapperTest`.
- Added `AuditEventWriterTest`.
- Added `IdentityAccessFlowIT` covering admin login, user creation, password change, unauthorized access, role/policy ETags, role assignment, AccessFilter resolution, last-admin visibility, and audit rows.
- Updated `RoleServiceTest` for cache invalidation.

Verification already passed:
- Targeted new/touched test set passed.
- `C:\dev\apache-maven-3.9.15\bin\mvn.cmd --% -q -pl corp-rag-app -am test` passed.
- `.\ai-service\.venv\Scripts\python.exe scripts\verify-contracts.py` failed only because plain `mvn` was not on PATH.
- `$env:MAVEN_CMD='C:\dev\apache-maven-3.9.15\bin\mvn.cmd'; .\ai-service\.venv\Scripts\python.exe scripts\verify-contracts.py` passed.
</completed_work>

<remaining_work>

- Rerun final backend verify:
  `C:\dev\apache-maven-3.9.15\bin\mvn.cmd --% -q -pl corp-rag-app -am verify`
- If verify passes, create `.planning/phases/02-identity-users-access-control/02-07-SUMMARY.md`.
- Update `.planning/ROADMAP.md` / `.planning/STATE.md` according to GSD execute-phase conventions for completing Plan 07 and Phase 2.
- Run `git status --short`.
- Commit completion docs/state.
</remaining_work>

<decisions_made>

- Access policy business rules live in `AccessPolicyService`; controllers only authorize and map contract models.
- `ResolvedAccessFilter` always includes `PUBLIC`.
- No policies or no roles resolve to PUBLIC-only visibility with wildcard departments and all doc types, keeping public docs visible while non-public access fails closed.
- Access level hierarchy is expanded downward: e.g. `CONFIDENTIAL` includes `PUBLIC`, `INTERNAL`, and `CONFIDENTIAL`.
- Empty departments remain wildcard/all departments.
- Cache TTL is 60 seconds and eviction is explicit on role assignment, role update/delete, and access-policy mutations.
- Generic audit event writes participate in the action transaction to avoid FK races with newly created users.
- Audit insert nulls missing actor/target user FK values, preserving the nullable audit schema and supporting tests/JWT subjects without seeded users.
</decisions_made>

<blockers>

- No active code blocker.
- The only unfinished verification is because the user intentionally paused during `mvn verify`.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` - machine-readable resume state.
2. `.planning/phases/02-identity-users-access-control/02-07-PLAN.md` - acceptance criteria and verification commands.
3. `backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessPolicyService.java` - main new business rules.
4. `backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessFilterResolver.java` - filter merge/cache semantics.
5. `backend/corp-rag-app/src/test/java/com/corprag/IdentityAccessFlowIT.java` - integrated acceptance flow.

## Critical Anti-Patterns
- Do not restore `AuditEventWriter.writeEvent` to `REQUIRES_NEW` for generic mutation audit. That caused FK failures when a just-created user was audited before the outer transaction committed.
- Do not assume `python` or `mvn` are on PATH in this Windows environment. Use `.\ai-service\.venv\Scripts\python.exe` and `C:\dev\apache-maven-3.9.15\bin\mvn.cmd`, or set `MAVEN_CMD` for `scripts\verify-contracts.py`.
- Do not remove audit FK nulling for missing actor/target users unless tests are changed to seed matching JWT subjects first.

## Infrastructure State
- No dev server is part of this work.
- No Maven verify process is intentionally left running.
- There is an unrelated old Java process visible from before this pause; it was not started by this workflow.
- Docker/Testcontainers were available and full `mvn test` passed using Postgres containers.

<context>
The code is in a good intermediate state. Treat this as "implemented, final verify pending", not as a failed plan. The next agent should not reimplement 02-07; it should verify, create the GSD summary/state updates, and commit completion docs.
</context>

<next_action>
Start with: `C:\dev\apache-maven-3.9.15\bin\mvn.cmd --% -q -pl corp-rag-app -am verify` from `C:\Users\maksd\IntelliJIDEA\corp-rag\backend`.
</next_action>
