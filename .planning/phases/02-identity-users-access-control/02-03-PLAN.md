---
phase: "02-identity-users-access-control"
plan: "03"
type: "execute"
wave: 3
depends_on: ["02"]
files_modified:
  - "backend/corp-rag-app/src/main/resources/db/migration/V2__create_users_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V3__create_refresh_tokens_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V4__create_audit_events_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V5__create_permissions_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V6__create_roles_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V7__create_role_permissions_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V8__create_user_roles_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V9__create_access_policies_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V10__seed_identity_defaults.sql"
  - "backend/corp-rag-app/src/main/java/com/corprag/domain"
  - "backend/corp-rag-app/src/main/java/com/corprag/repository"
  - "backend/corp-rag-app/src/test/java/com/corprag/repository/IdentitySchemaIT.java"
autonomous: true
requirements: ["AUTH-01", "AUTH-02", "AUTH-03", "AUTH-04"]
user_setup: []
must_haves:
  truths:
    - "Java-owned identity data lives in PostgreSQL through Flyway migrations, not in memory."
    - "Seeded permissions, system roles, role-permission rows, and default access policies are idempotent and match the Phase 2 decision matrix exactly."
    - "Repository code preserves optimistic concurrency inputs such as version columns and ETag material needed by later API plans."
  artifacts:
    - path: "backend/corp-rag-app/src/main/resources/db/migration"
      provides: "Phase 2 identity schema"
      contains: "users, refresh_tokens, audit_events, permissions, roles, role_permissions, user_roles, access_policies, seed data"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/domain"
      provides: "Domain records/enums for identity and access control"
      contains: "user, refresh token, audit event, role, permission, access policy, access filter values"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/repository"
      provides: "JDBC repositories"
      contains: "query/update methods needed by later auth, user, role, and policy services"
  key_links:
    - from: "Flyway migrations"
      to: "JDBC repositories"
      via: "repository tests validate all SQL against migrated PostgreSQL"
    - from: "seeded role policies"
      to: "AccessFilter resolver"
      via: "later plan resolves filters from persisted access_policies rows"
  decision_refs:
    - "D-03: refresh tokens are persisted in `refresh_tokens`."
    - "D-17: audit events use an `audit_events` table."
    - "D-18: audit schema includes nullable actor/target/entity, IP/user-agent, details JSONB, and correlation fields."
    - "D-19: audit indexes support actor, target, event type, and entity lookups."
    - "D-20: Phase 2 writes AUTH, ROLE, and ACCESS_POLICY audit events."
    - "D-21: AuditEventWriter abstraction handles transaction choice per event type."
    - "D-35: system roles are ADMIN, EMPLOYEE, VIEWER."
    - "D-36: permission wire/DB format is lower dotted strings."
    - "D-37: the exact 16 permission strings are seeded."
    - "D-38: there is no `documents.update` permission."
    - "D-39: system role permission matrix is seeded."
    - "D-40: role and user-role tables are present with assignment metadata."
    - "D-52: access policies attach to roles."
    - "D-56: accessLevels use the required hierarchy."
    - "D-57: empty departments mean wildcard/all departments."
    - "D-58: docTypes are enum values and empty docTypes are invalid."
    - "D-59: access level and doc type values use the required enum values."
    - "D-60: department codes use the required free-form format."
    - "D-63: seed policies for ADMIN, EMPLOYEE, and VIEWER."
    - "D-69: migration order uses V2 through V10 with consolidated identity seed data."
    - "D-70: audit outcomes use SUCCESS, FAILURE, and ERROR."
---

<objective>
Create the identity and access-control persistence foundation.

Purpose: Auth, user administration, role management, access policies, and audit logging need one migrated relational model before services are implemented.
Output: Flyway migrations, seeded identity defaults, domain records/enums, JDBC repositories, and schema integration tests.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-users-access-control/02-CONTEXT.md
@.planning/phases/02-identity-users-access-control/02-RESEARCH.md
@.planning/phases/02-identity-users-access-control/02-VALIDATION.md
@.planning/phases/02-identity-users-access-control/02-PATTERNS.md
@backend/corp-rag-app/src/main/resources/db/migration/V1__baseline.sql
@backend/corp-rag-app/src/main/java/com/corprag
@backend/corp-rag-app/src/test/java/com/corprag/testsupport
@CLAUDE.md

<dependency_context>
Requires Plan 02-02 test harness. This plan creates persistence and repository code only; web/security services are implemented in later plans.
</dependency_context>

<decision_coverage>
- D-03, D-17, D-18, D-19, D-20, D-21, D-35, D-36, D-37, D-38, D-39, D-40, D-52, D-56, D-57, D-58, D-59, D-60, D-63, D-69, and D-70 are covered in schema, seed data, repository methods, and integration tests using the final CONTEXT.md numbering.
</decision_coverage>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Phase 2 Flyway migrations and seed data</name>
  <files>backend/corp-rag-app/src/main/resources/db/migration</files>
  <read_first>backend/corp-rag-app/src/main/resources/db/migration/V1__baseline.sql, .planning/phases/02-identity-users-access-control/02-CONTEXT.md</read_first>
  <action>Add ordered Flyway migrations after `V1__baseline.sql`. Create tables for users, refresh tokens, audit events, permissions, roles, role_permissions, user_roles, and access_policies. Use UUID primary keys where consistent with the existing contract shape, timestamps, active/deleted flags where needed, version columns for ETag-backed resources, and unique constraints for user email, permission code, role code, role-policy one-to-one mapping, and join-table keys. Seed the exact D-37 permission strings, system roles ADMIN/EMPLOYEE/VIEWER from D-35, the D-39 role-permission matrix, and default access policies from D-63 in `V10__seed_identity_defaults.sql` per D-69 updated. Ensure access policy arrays/collections can represent additive accessLevels, departments, and docTypes and reject empty docTypes where practical at the database or service boundary.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am verify</automated>
  </verify>
  <acceptance_criteria>
    - Migrations run in D-69 order without repair/manual intervention.
    - Permissions seed exactly the 16 D-37 strings.
    - ADMIN, EMPLOYEE, and VIEWER system roles are seeded with D-38 permissions.
    - Default access policies are present for all three system roles.
  </acceptance_criteria>
  <done>Phase 2 schema and seed data migrate successfully on PostgreSQL.</done>
</task>

<task type="auto">
  <name>Task 2: Implement domain records and JDBC repositories</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/domain, backend/corp-rag-app/src/main/java/com/corprag/repository</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag, CLAUDE.md, .planning/phases/02-identity-users-access-control/02-PATTERNS.md</read_first>
  <action>Create small Java records/enums for users, refresh tokens, audit events, roles, permissions, user-role links, access policies, and resolved access filters. Implement JDBC repositories using constructor injection and named/domain-specific methods rather than exposing raw SQL to services. Include methods needed by later plans: find user by email/id, create/update user, persist and revoke refresh tokens by family/user, insert audit events, list permissions, create/update roles with version checks, replace role permissions, replace user roles, manage access policies, and resolve role policy data for a user.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test</automated>
  </verify>
  <acceptance_criteria>
    - Domain values preserve exact permission, role, access-level, and doc-type wire strings.
    - Repository methods return explicit empty results instead of null where possible.
    - Version/updated_at data needed for ETag generation is available for roles and access policies.
  </acceptance_criteria>
  <done>Services can use repository methods without embedding SQL.</done>
</task>

<task type="auto">
  <name>Task 3: Verify schema and repository behavior with PostgreSQL</name>
  <files>backend/corp-rag-app/src/test/java/com/corprag/repository/IdentitySchemaIT.java</files>
  <read_first>backend/corp-rag-app/src/test/java/com/corprag/testsupport/PostgresIntegrationTestSupport.java, .planning/phases/02-identity-users-access-control/02-VALIDATION.md</read_first>
  <action>Add migration-backed integration tests using the Plan 02-02 PostgreSQL support. Verify all Phase 2 tables exist, seed counts and seeded values match decisions, uniqueness constraints reject duplicates, repository create/update/read paths work, role and policy version data changes on update, refresh-token family revocation can be persisted, and audit event outcomes accept SUCCESS, FAILURE, and ERROR.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am verify</automated>
  </verify>
  <acceptance_criteria>
    - Integration tests fail if a required seed permission, role, or default policy is missing.
    - Integration tests cover refresh-token persistence and audit-event insertion.
    - `cd backend; mvn -q -pl corp-rag-app -am verify` exits 0.
  </acceptance_criteria>
  <done>Persistence layer is proven against real PostgreSQL migrations.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database seed data to authorization checks | Incorrect seed data can silently grant or deny permissions. |
| Repository SQL to service logic | SQL bugs can bypass version checks, revocation, or fail-safe defaults. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Elevation of Privilege | permission seed data | mitigate | Integration tests assert exact D-37 permission set and D-38 system-role matrix. |
| T-02-03-02 | Tampering | role/access policy rows | mitigate | Add version columns and repository update methods that preserve ETag material. |
| T-02-03-03 | Repudiation | audit_events | mitigate | Persist actor, action, outcome, subject, IP, user-agent, and nullable fields per D-17 through D-21. |
</threat_model>

<verification>
- [ ] `cd backend; mvn -q -pl corp-rag-app -am test` passes.
- [ ] `cd backend; mvn -q -pl corp-rag-app -am verify` passes.
- [ ] Seeded permissions, roles, and policies match Phase 2 decisions exactly.
</verification>

<success_criteria>
- Phase 2 identity schema is migrated and seeded.
- Repository methods cover the persistence needs of auth, users, roles, access policies, refresh tokens, and audit events.
- PostgreSQL integration tests prove schema and seed correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-users-access-control/02-03-SUMMARY.md`.
</output>
