---
phase: "02-identity-users-access-control"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01"]
files_modified:
  - "backend/pom.xml"
  - "backend/corp-rag-app/pom.xml"
  - "backend/corp-rag-app/src/test/java/com/corprag/testsupport/AuthTestFixtures.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/testsupport/PostgresIntegrationTestSupport.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/SecurityDependencySmokeTest.java"
autonomous: true
requirements: ["AUTH-01", "AUTH-02", "AUTH-03", "AUTH-04"]
user_setup: []
must_haves:
  truths:
    - "The Java backend has the dependencies needed for Spring Security, JWT resource-server support, security tests, and PostgreSQL integration tests before auth code is added."
    - "Phase 2 validation uses fast unit/slice tests during development and a full Maven verify path for migration-backed integration tests."
    - "Test support never embeds production JWT secrets, first-admin credentials, or reusable passwords."
  artifacts:
    - path: "backend/corp-rag-app/pom.xml"
      provides: "Phase 2 backend dependency surface"
      contains: "spring-boot-starter-security, oauth2-resource-server, spring-security-test, Testcontainers PostgreSQL support"
    - path: "backend/corp-rag-app/src/test/java/com/corprag/testsupport/AuthTestFixtures.java"
      provides: "Reusable test identities, roles, permissions, and access filters"
      contains: "deterministic fixture values only, no production secrets"
    - path: "backend/corp-rag-app/src/test/java/com/corprag/testsupport/PostgresIntegrationTestSupport.java"
      provides: "Shared Testcontainers PostgreSQL bootstrapping"
      contains: "Spring DynamicPropertySource wiring for migration-backed integration tests"
  key_links:
    - from: "backend/corp-rag-app/pom.xml"
      to: "backend/corp-rag-app/src/test/java/com/corprag/testsupport"
      via: "Phase 2 tests use one common security and database fixture layer"
    - from: "02-VALIDATION.md"
      to: "backend/corp-rag-app/pom.xml"
      via: "validation commands depend on Surefire/Failsafe and Testcontainers setup"
  decision_refs:
    - "D-01: short-lived access JWTs are security-critical and must be testable."
    - "D-02: refresh tokens are security-critical and must be testable."
    - "D-03: refresh token persistence is migration-backed."
    - "D-04: refresh token rotation and reuse detection require integration coverage."
    - "D-05: logout invalidation requires integration coverage."
    - "D-06: active session limits require deterministic test fixtures."
    - "D-07: cookie paths require MVC/security tests."
    - "D-08: dev and prod cookie flags require profile-aware tests."
    - "D-09: stale permission behavior requires JWT fixture support."
    - "D-10: Origin/Referer validation requires request-header security tests."
    - "D-11: Spring CsrfFilter remains disabled for this API strategy."
    - "D-12: SameSite Strict cookie behavior is documented and tested through headers."
    - "D-71: Phase 2 uses layered unit, controller, repository, and integration tests."
    - "D-72: JWT implementation uses Spring OAuth2 resource-server primitives and HS256 secrets."
---

<objective>
Prepare the Java backend for Phase 2 implementation and validation.

Purpose: Later plans should add auth behavior against a stable dependency and test harness rather than mixing build-system work into feature implementation.
Output: Maven dependencies, security test support, PostgreSQL integration-test support, and a smoke test proving the dependency surface is available.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-users-access-control/02-CONTEXT.md
@.planning/phases/02-identity-users-access-control/02-RESEARCH.md
@.planning/phases/02-identity-users-access-control/02-VALIDATION.md
@.planning/phases/02-identity-users-access-control/02-PATTERNS.md
@backend/pom.xml
@backend/corp-rag-app/pom.xml
@CLAUDE.md

<dependency_context>
Requires Plan 02-01 contract updates. This plan should not implement auth endpoints, migrations, users, roles, or policies; it only prepares dependencies and reusable validation support.
</dependency_context>

<decision_coverage>
- D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, and D-12 are covered by making the later session/cookie/security behavior testable.
- D-71 is covered by concrete test layers and commands.
- D-72 is covered by selecting Spring Security OAuth2 resource-server JWT dependencies rather than a handwritten JWT stack.
</decision_coverage>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Phase 2 security and integration-test dependencies</name>
  <files>backend/pom.xml, backend/corp-rag-app/pom.xml</files>
  <read_first>backend/pom.xml, backend/corp-rag-app/pom.xml, .planning/phases/02-identity-users-access-control/02-RESEARCH.md</read_first>
  <action>Update the backend Maven configuration for Phase 2. Add Spring Security, OAuth2 resource-server JWT support, Spring Security Test, and Testcontainers PostgreSQL dependencies in the existing Maven style. Keep dependency versions managed by Spring Boot or the parent where possible. Configure integration-test execution so repository and flow tests can run under `mvn verify` while unit and slice tests still run under `mvn test`. Do not add business endpoints or domain schema in this task.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test</automated>
  </verify>
  <acceptance_criteria>
    - `backend/corp-rag-app/pom.xml` includes Spring Security and OAuth2 resource-server support.
    - `backend/corp-rag-app/pom.xml` includes Spring Security Test and Testcontainers PostgreSQL test support.
    - `cd backend; mvn -q -pl corp-rag-app -am test` compiles and runs the existing test suite.
  </acceptance_criteria>
  <done>Backend dependency setup supports Phase 2 security implementation and tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add reusable Phase 2 test support</name>
  <files>backend/corp-rag-app/src/test/java/com/corprag/testsupport/AuthTestFixtures.java, backend/corp-rag-app/src/test/java/com/corprag/testsupport/PostgresIntegrationTestSupport.java, backend/corp-rag-app/src/test/java/com/corprag/SecurityDependencySmokeTest.java</files>
  <read_first>backend/corp-rag-app/src/test/java, .planning/phases/02-identity-users-access-control/02-VALIDATION.md, CLAUDE.md</read_first>
  <action>Create a small test-support package that later plans can reuse. `AuthTestFixtures` should define deterministic test users, roles, permission strings, access levels, departments, document types, cookie names, and test-only JWT secrets. `PostgresIntegrationTestSupport` should centralize PostgreSQL Testcontainers wiring using Spring test properties. Add a smoke test that verifies Spring Security and JWT classes needed by D-72 are on the classpath and that test fixtures load. Do not add tests that assert unimplemented auth behavior yet.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test</automated>
  </verify>
  <acceptance_criteria>
    - Test support compiles under Java 21.
    - No production secret or default admin password is committed.
    - The smoke test passes before later auth implementation plans run.
  </acceptance_criteria>
  <done>Later Phase 2 plans can add focused tests without duplicating fixture and container setup.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures to production code | Test-only secrets and identities must not become runtime defaults. |
| Build configuration to security implementation | Missing or incorrect dependencies can lead to ad hoc security implementations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | test fixtures | mitigate | Keep fixture secrets in test sources only and name them as test-only constants. |
| T-02-02-02 | Elevation of Privilege | dependency choices | mitigate | Use Spring Security and OAuth2 resource-server primitives required by D-72. |
| T-02-02-03 | Tampering | integration tests | mitigate | Use migration-backed PostgreSQL tests under `mvn verify` so schema drift is visible. |
</threat_model>

<verification>
- [ ] `cd backend; mvn -q -pl corp-rag-app -am test` passes.
- [ ] Test support compiles without starting unavailable external services.
- [ ] Dependency additions are scoped to the backend modules that need them.
</verification>

<success_criteria>
- Security, JWT, and security-test dependencies are available to implementation plans.
- Integration-test support is reusable and does not require production credentials.
- No Phase 2 business behavior is implemented prematurely.
</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-users-access-control/02-02-SUMMARY.md`.
</output>
