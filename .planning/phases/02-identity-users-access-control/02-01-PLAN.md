---
phase: "02-identity-users-access-control"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "contracts/openapi/api-v1.yaml"
  - "contracts/openapi/ai-service-v1.yaml"
  - "contracts/constants.yaml"
autonomous: true
requirements: ["AUTH-01", "AUTH-02", "AUTH-03", "AUTH-04"]
user_setup: []
must_haves:
  truths:
    - "The public Java API contract exposes Phase 2 auth, password, users, roles, and access-policy behavior before Java implementation."
    - "Phase 2 error codes are declared once in `contracts/constants.yaml` and are available for generated Java/Python constants."
    - "The Java-to-Python query contract keeps `QueryRequest.accessFilter` compatible with Java's access-filter resolver."
  artifacts:
    - path: "contracts/openapi/api-v1.yaml"
      provides: "Phase 2 frontend-facing REST contract"
      contains: "auth refresh, password change, reset password, users, roles, access policies, cookie paths, ETag/If-Match, ProblemDetail errors"
    - path: "contracts/openapi/ai-service-v1.yaml"
      provides: "Internal AccessFilter contract"
      contains: "accessLevels, departments, docTypes in QueryRequest.accessFilter"
    - path: "contracts/constants.yaml"
      provides: "Phase 2 generated error-code source"
      contains: "PASSWORD_CHANGE_REQUIRED, REFRESH_TOKEN_REUSED, ORIGIN_VALIDATION_FAILED, ROLE_POLICY_ALREADY_EXISTS, LAST_ADMIN_VISIBILITY_LOST"
  key_links:
    - from: "contracts/openapi/api-v1.yaml"
      to: "contracts/constants.yaml"
      via: "every new ProblemDetail error_code uses a constant declared in constants.yaml"
    - from: "contracts/openapi/api-v1.yaml"
      to: "contracts/openapi/ai-service-v1.yaml"
      via: "AccessFilter enum values and field names stay compatible for Java-to-Python QueryRequest"
  decision_refs:
    - "D-13: add `/auth/refresh`."
    - "D-14: keep flat `/users`, `/roles`, `/access-policies` paths."
    - "D-15: add Phase 2 error constants."
    - "D-16: do not add `SESSION_LIMIT_EXCEEDED`."
    - "D-36: permission wire/DB format is lower dotted strings."
    - "D-37: permission list contains exactly 16 codes."
    - "D-38: there is no `documents.update` permission."
    - "D-39: system-role permission matrix is represented in the contract."
    - "D-43: ADMIN grants require users.update and roles.update."
    - "D-44: system roles cannot be updated or deleted."
    - "D-45: custom roles can use only existing seeded permission codes."
    - "D-46: role updates use full replacement."
    - "D-47: user-role replace-set has no ETag."
    - "D-52: access policies attach to roles."
    - "D-53: duplicate policy creation returns ROLE_POLICY_ALREADY_EXISTS."
    - "D-54: roles define actions and policies define additive visibility."
    - "D-55: effective access filter resolves from assigned role policies."
    - "D-56: accessLevels use the Phase 2 hierarchy."
    - "D-57: empty departments means wildcard/all departments."
    - "D-58: docTypes are enum values and empty docTypes are invalid."
    - "D-59: access level and doc type values are fixed."
    - "D-60: department format is documented."
    - "D-61: PUBLIC documents are always visible."
    - "D-62: no roles or no policies fail safe."
    - "D-63: default role policies are seeded."
    - "D-64: system role policies are editable."
    - "D-65: access policy updates require ETag/If-Match."
    - "D-66: last-admin visibility protection is exposed."
    - "D-68: Java passes AccessFilter to Python QueryRequest"
---

<objective>
Make Phase 2 contract-first.

Purpose: Execution must not invent auth, user, role, access-policy, or error shapes inside Java.
Output: Updated OpenAPI and constants contracts that downstream generated Java code can compile against.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-users-access-control/02-CONTEXT.md
@.planning/phases/02-identity-users-access-control/02-RESEARCH.md
@.planning/phases/02-identity-users-access-control/02-VALIDATION.md
@.planning/phases/02-identity-users-access-control/02-PATTERNS.md
@contracts/openapi/api-v1.yaml
@contracts/openapi/ai-service-v1.yaml
@contracts/constants.yaml
@scripts/verify-contracts.py
@CLAUDE.md

<decision_coverage>
Implement D-13, D-14, D-15, and D-16 before Java code. Preserve D-14 flat namespace: `/users`, `/roles`, `/access-policies`; do not add `/admin/*`.
Represent D-36, D-37, D-38, D-39, D-43, D-44, D-45, D-46, D-47, D-52, D-53, D-54, D-55, D-56, D-57, D-58, D-59, D-60, D-61, D-62, D-63, D-64, D-65, D-66, and D-68 using the final CONTEXT.md numbering in contract shapes and error surfaces so later implementation plans have no ambiguity.
</decision_coverage>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend Java API contract for Phase 2 identity flows</name>
  <files>contracts/openapi/api-v1.yaml</files>
  <read_first>contracts/openapi/api-v1.yaml, .planning/phases/02-identity-users-access-control/02-CONTEXT.md, .planning/phases/02-identity-users-access-control/02-RESEARCH.md</read_first>
  <action>Update `contracts/openapi/api-v1.yaml` per D-01 through D-16 and D-22 through D-51. Add `POST /auth/refresh`, `POST /auth/password`, and `POST /users/{userId}/reset-password`. Document cookies exactly: `corp_rag_session` path `/api/v1`, `corp_rag_refresh` path `/api/v1/auth`, httpOnly, SameSite=Strict, Secure by profile. Change admin-created users so `CreateUserRequest` has no password field and the create/reset responses return a temporary password exactly once. Keep flat paths `/users`, `/roles`, `/access-policies`. Model `PUT /roles/{roleId}` and `PUT /access-policies/{policyId}` as full replacement with required `If-Match`, 428 missing precondition, 412 mismatch, and response ETag. Keep `POST /users/{userId}/roles` as replace-set without ETag. Add self-vs-other access notes for `GET/PUT /users/{id}` and the additional `roles.update` requirement when granting `ADMIN`.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/openapi/api-v1.yaml` contains `/auth/refresh`, `/auth/password`, and `/users/{userId}/reset-password`.
    - `contracts/openapi/api-v1.yaml` documents `corp_rag_session` with `Path=/api/v1` and `corp_rag_refresh` with `Path=/api/v1/auth`.
    - `CreateUserRequest` no longer requires a `password` property.
    - Role and access-policy replacement endpoints document `If-Match`, `428`, `412`, and `ETag`.
  </acceptance_criteria>
  <done>The REST contract captures the Phase 2 API boundary and passes contract verification.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 2 constants and AccessFilter compatibility checks</name>
  <files>contracts/constants.yaml, contracts/openapi/ai-service-v1.yaml</files>
  <read_first>contracts/constants.yaml, contracts/openapi/ai-service-v1.yaml, .planning/phases/02-identity-users-access-control/02-CONTEXT.md</read_first>
  <action>Extend `contracts/constants.yaml` with D-15 error codes: `PASSWORD_CHANGE_REQUIRED`, `PASSWORD_POLICY_VIOLATION`, `REFRESH_TOKEN_INVALID`, `REFRESH_TOKEN_REUSED`, `AUDIT_WRITE_FAILED`, `INVALID_PERMISSION_CODE`, `SYSTEM_ROLE_PROTECTED`, `LAST_ADMIN_PROTECTED`, `SELF_MODIFICATION_FORBIDDEN`, `ORIGIN_VALIDATION_FAILED`, `ROLE_POLICY_ALREADY_EXISTS`, and `LAST_ADMIN_VISIBILITY_LOST`. Do not add `SESSION_LIMIT_EXCEEDED` per D-16. Verify `contracts/openapi/ai-service-v1.yaml` still requires `QueryRequest.accessFilter` with `accessLevels`, `departments`, and `docTypes`, and ensure enum values match D-59 exactly: access levels `PUBLIC`, `INTERNAL`, `CONFIDENTIAL`, `RESTRICTED`; doc types `POLICY`, `REGULATION`, `GUIDE`, `REPORT`, `MANUAL`, `OTHER`.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/constants.yaml` contains all twelve D-15 Phase 2 error codes.
    - `contracts/constants.yaml` does not contain `SESSION_LIMIT_EXCEEDED`.
    - `contracts/openapi/ai-service-v1.yaml` contains `accessLevels`, `departments`, and `docTypes` under `AccessFilter`.
    - `python scripts/verify-contracts.py` exits 0.
  </acceptance_criteria>
  <done>Contract constants and AccessFilter schemas are ready for generated Java/Python surfaces.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract source to generated Java/Python code | Incorrect auth/error contracts propagate into application code. |
| Browser clients to Java API | Cookie paths, CSRF expectations, and permissions must be explicit in OpenAPI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `api-v1.yaml` | mitigate | Declare unsafe cookie-auth CSRF behavior, ETag/If-Match, and exact permission requirements before implementation. |
| T-02-01-02 | Information Disclosure | user/password responses | mitigate | Contract temporary passwords as one-time create/reset response fields only; do not add password to normal User DTOs. |
| T-02-01-03 | Elevation of Privilege | permission codes | mitigate | Declare exact 16 D-37 dotted permission strings and generated constants; reject unknown permission codes later. |
</threat_model>

<verification>
- [ ] `python scripts/verify-contracts.py` passes.
- [ ] New Phase 2 error codes generate successfully.
- [ ] OpenAPI keeps flat `/users`, `/roles`, `/access-policies` paths.
</verification>

<success_criteria>
- Phase 2 contracts are additive and verified.
- Every new API error has a matching constants entry.
- AccessFilter schema remains compatible for Python retrieval filtering.
</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-users-access-control/02-01-SUMMARY.md`.
</output>
