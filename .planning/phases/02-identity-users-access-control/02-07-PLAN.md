---
phase: "02-identity-users-access-control"
plan: "07"
type: "execute"
wave: 6
depends_on: ["05", "06"]
files_modified:
  - "backend/corp-rag-app/src/main/java/com/corprag/service/access"
  - "backend/corp-rag-app/src/main/java/com/corprag/service/audit"
  - "backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/AccessPolicyController.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/QueryAccessFilterMapper.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/service/access/AccessFilterResolverTest.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/adapter/rest/AccessPolicyControllerTest.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/service/audit/AuditEventWriterTest.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/IdentityAccessFlowIT.java"
autonomous: true
requirements: ["AUTH-01", "AUTH-02", "AUTH-03", "AUTH-04"]
user_setup: []
must_haves:
  truths:
    - "Access policies are role-scoped, additive, fail safe when absent, and resolved into Java's downstream AccessFilter shape."
    - "PUBLIC documents are always visible, while INTERNAL/CONFIDENTIAL/RESTRICTED visibility depends on resolved role policies."
    - "Phase 2 ends with full auth/user/role/access-policy flow tests and audit coverage for security-sensitive changes."
  artifacts:
    - path: "backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessPolicyService.java"
      provides: "Access policy administration"
      contains: "one-policy-per-role, full replacement, ETag/If-Match, validation, last-admin-visibility protection"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessFilterResolver.java"
      provides: "Downstream document access filter"
      contains: "merged access levels, departments, docTypes, PUBLIC always visible, fail-safe empty filter"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/QueryAccessFilterMapper.java"
      provides: "Java-to-Python contract compatibility"
      contains: "mapping from resolved filter to `QueryRequest.accessFilter` contract fields"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/service/audit"
      provides: "Phase 2 audit writer integration"
      contains: "auth, user, role, and access-policy audit events"
  key_links:
    - from: "AccessPolicyController"
      to: "AccessFilterResolver"
      via: "policy mutations evict resolver cache"
    - from: "AccessFilterResolver"
      to: "QueryAccessFilterMapper"
      via: "resolved Java filter maps to Python `QueryRequest.accessFilter` fields"
    - from: "Phase 2 services"
      to: "AuditEventWriter"
      via: "security-sensitive mutations write audit rows"
  decision_refs:
    - "D-17: audit events use a database table."
    - "D-18: audit schema includes nullable actor/target/entity, IP/user-agent, details JSONB, and correlation fields."
    - "D-19: audit indexes support actor, target, event type, and entity lookups."
    - "D-20: Phase 2 writes AUTH, ROLE, and ACCESS_POLICY events."
    - "D-21: AuditEventWriter abstraction handles transaction choice per event type."
    - "D-52: access policies attach to roles."
    - "D-53: duplicate policy creation returns ROLE_POLICY_ALREADY_EXISTS."
    - "D-54: roles define actions and policies define additive visibility."
    - "D-55: effective access filter resolves from assigned role policies."
    - "D-56: accessLevels use PUBLIC, INTERNAL, CONFIDENTIAL, RESTRICTED hierarchy."
    - "D-57: empty departments means wildcard/all departments."
    - "D-58: docTypes are enum values and empty docTypes are invalid."
    - "D-59: access level and doc type values are fixed."
    - "D-60: departments use the configured free-form code regex."
    - "D-61: PUBLIC documents are always visible."
    - "D-62: no roles or policies fail safe for non-public visibility."
    - "D-63: default policies are seeded."
    - "D-64: system role policies are editable."
    - "D-65: access policy updates require ETag/If-Match."
    - "D-66: last-admin visibility cannot be removed."
    - "D-67: access filter cache uses 60 second TTL with eviction on mutations."
    - "D-68: Java passes AccessFilter to Python QueryRequest."
    - "D-70: audit outcomes use SUCCESS, FAILURE, and ERROR."
---

<objective>
Implement access policies, access-filter resolution, audit integration, and Phase 2 end-to-end validation.

Purpose: The identity system is complete only when Java can enforce roles and resolve downstream document access filters safely.
Output: Access policy APIs, AccessFilter resolver/cache, Python contract mapper, audit integration, and full Phase 2 flow tests.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-users-access-control/02-CONTEXT.md
@.planning/phases/02-identity-users-access-control/02-RESEARCH.md
@.planning/phases/02-identity-users-access-control/02-VALIDATION.md
@.planning/phases/02-identity-users-access-control/02-PATTERNS.md
@contracts/openapi/api-v1.yaml
@contracts/openapi/ai-service-v1.yaml
@backend/corp-rag-app/src/main/java/com/corprag/service/role
@backend/corp-rag-app/src/main/java/com/corprag/service/user
@backend/corp-rag-app/src/main/java/com/corprag/repository
@backend/corp-rag-app/src/test/java/com/corprag/testsupport
@CLAUDE.md

<dependency_context>
Requires Plan 02-05 user/password behavior and Plan 02-06 role/permission behavior. This plan closes AUTH-04 and verifies the whole Phase 2 user-admin and access-control path.
</dependency_context>

<decision_coverage>
- D-17, D-18, D-19, D-20, D-21, D-52, D-53, D-54, D-55, D-56, D-57, D-58, D-59, D-60, D-61, D-62, D-63, D-64, D-65, D-66, D-67, D-68, and D-70 are covered in access policy services, cache invalidation, mapper code, audit integration, and full-flow tests.
</decision_coverage>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement access-policy administration</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/access, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/AccessPolicyController.java</files>
  <read_first>contracts/openapi/api-v1.yaml, backend/corp-rag-app/src/main/java/com/corprag/service/role, .planning/phases/02-identity-users-access-control/02-CONTEXT.md</read_first>
  <action>Implement flat `/access-policies` endpoints. Enforce one policy per role, require the relevant `access_policies.*` permissions for changes, allow guarded edits to system role policies per D-64, validate accessLevels, departments, and docTypes, treat empty departments as wildcard, reject empty docTypes, and support full replacement with required `If-Match`. Missing `If-Match` returns 428, stale ETag returns 412, duplicate role policies return `ROLE_POLICY_ALREADY_EXISTS`, and changes that remove all last-admin visibility return `LAST_ADMIN_VISIBILITY_LOST`.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test</automated>
  </verify>
  <acceptance_criteria>
    - Access policy replacement is full replacement with ETag/If-Match.
    - Exactly one policy per role is enforced.
    - Invalid department/docType/accessLevel values fail with ProblemDetail errors.
    - System role policies are editable only within D-64/D-66 safeguards.
  </acceptance_criteria>
  <done>Access policy administration matches contract and Phase 2 decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve and cache user AccessFilter values</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/access, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/QueryAccessFilterMapper.java</files>
  <read_first>contracts/openapi/ai-service-v1.yaml, backend/corp-rag-app/src/main/java/com/corprag/repository, .planning/phases/02-identity-users-access-control/02-RESEARCH.md</read_first>
  <action>Implement `AccessFilterResolver` that merges all role policies for a user into additive accessLevels, departments, and docTypes. Always include PUBLIC visibility per D-61, apply the D-56 hierarchy consistently, treat no roles or no policies as fail safe for non-public visibility per D-62, and preserve D-57 department wildcard semantics. Add a 60-second in-memory TTL cache keyed by user/security version and evict it on role assignment and access-policy mutations. Implement a mapper that produces the generated contract shape for Python `QueryRequest.accessFilter` with fields `accessLevels`, `departments`, and `docTypes` exactly.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test</automated>
  </verify>
  <acceptance_criteria>
    - Resolver returns additive filters for multi-role users.
    - PUBLIC is always visible while non-public levels require policy coverage.
    - Cache entries expire after 60 seconds and are evicted on role/policy mutations.
    - Mapper output is compatible with `contracts/openapi/ai-service-v1.yaml`.
  </acceptance_criteria>
  <done>Java can produce the downstream AccessFilter required by Python retrieval.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate audit logging and full Phase 2 flow tests</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/audit, backend/corp-rag-app/src/test/java/com/corprag/service/audit/AuditEventWriterTest.java, backend/corp-rag-app/src/test/java/com/corprag/IdentityAccessFlowIT.java</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/service/auth, backend/corp-rag-app/src/main/java/com/corprag/service/user, backend/corp-rag-app/src/main/java/com/corprag/service/role, backend/corp-rag-app/src/test/java/com/corprag/testsupport</read_first>
  <action>Finish audit writer integration for security-sensitive Phase 2 operations: login/logout, refresh reuse, user create/reset/disable, role create/update/delete, user role replacement, and access-policy changes. Add full-flow tests that bootstrap first admin, log in, create a user, force password change, assign roles, reject unauthorized access, update role and access policy with ETags, resolve AccessFilter, and verify key audit rows. Keep audit write behavior aligned with D-19 so failures are visible without accidentally committing the protected action as successful.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am verify</automated>
  </verify>
  <acceptance_criteria>
    - Full-flow integration tests cover AUTH-01, AUTH-02, AUTH-03, and AUTH-04.
    - Audit events use D-70 outcome values and include nullable actor/target/entity, IP/user-agent, and correlation fields where appropriate.
    - Unauthorized and insufficient-permission requests return 401/403 ProblemDetails.
    - `cd backend; mvn -q -pl corp-rag-app -am verify` exits 0.
  </acceptance_criteria>
  <done>Phase 2 is integrated and ready for verification.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Role policies to document retrieval | A wrong filter can leak restricted documents or hide permitted data. |
| Policy mutations to cached filters | Stale cache can preserve revoked visibility. |
| Security-sensitive actions to audit log | Missing audit rows reduce accountability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Information Disclosure | AccessFilterResolver | mitigate | Fail safe for missing roles/policies, always test PUBLIC and restricted-level behavior. |
| T-02-07-02 | Tampering | access policy updates | mitigate | Require If-Match, validate role uniqueness, and evict resolver cache on mutations. |
| T-02-07-03 | Repudiation | audit events | mitigate | Assert audit rows for auth, user, role, and policy changes in full-flow tests. |
| T-02-07-04 | Elevation of Privilege | last-admin visibility | mitigate | Reject policy changes that remove the last admin's required visibility. |
</threat_model>

<verification>
- [ ] `python scripts/verify-contracts.py` passes.
- [ ] `cd backend; mvn -q -pl corp-rag-app -am test` passes.
- [ ] `cd backend; mvn -q -pl corp-rag-app -am verify` passes.
- [ ] Full-flow tests demonstrate AUTH-01 through AUTH-04.
</verification>

<success_criteria>
- Admins can manage access policies with ETag protection and safeguards.
- Java resolves a user's AccessFilter for downstream retrieval using Phase 2 policy rules.
- Audit rows exist for security-sensitive Phase 2 actions.
- Full Phase 2 verification passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-users-access-control/02-07-SUMMARY.md`.
</output>
