---
phase: "05-retrieval-guards-query-api"
plan: "07"
type: "execute"
wave: 6
depends_on: ["02", "03", "04", "05", "06"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/uv.lock"
  - "ai-service/src/corp_rag_ai/agent"
  - "ai-service/src/corp_rag_ai/service/query"
  - "ai-service/src/corp_rag_ai/adapters/rest/query.py"
  - "ai-service/src/corp_rag_ai/main.py"
  - "ai-service/src/corp_rag_ai/config.py"
  - "ai-service/tests"
autonomous: true
requirements: ["AGT-02", "AGT-01", "AGT-03", "RET-01", "RET-02", "RET-03", "RET-04", "SEC-01"]
user_setup: []
must_haves:
  truths:
    - "LangGraph orchestrates guard, route, retrieval, rerank, synthesis, and output guard nodes."
    - "`POST /v1/query` returns the regenerated contract shape for all success/refusal paths."
    - "Internal query timeout is bounded at 30 seconds."
    - "`/diagnostics` includes query-readiness indicators."
  artifacts:
    - path: "ai-service/src/corp_rag_ai/agent/graph.py"
      provides: "compiled query graph"
      contains: "StateGraph nodes and conditional routing"
    - path: "ai-service/src/corp_rag_ai/adapters/rest/query.py"
      provides: "FastAPI query adapter"
      contains: "POST /v1/query request mapping, error mapping, response mapping"
  decision_refs:
    - "D-150"
    - "D-151"
    - "D-152"
    - "D-169"
    - "D-171"
    - "D-174"
    - "D-175"
    - "D-176"
    - "D-177"
    - "D-178"
    - "D-179"
    - "D-180"
    - "D-206"
    - "D-214"
    - "D-215"
    - "D-216"
---

<objective>
Wire the complete query pipeline through LangGraph and expose `POST /v1/query`.

Purpose: Phase 05 primitives must connect into one synchronous Python endpoint
that Java can call with an access filter and receive a contract-shaped response.
Output: LangGraph StateGraph, query service composition, FastAPI route,
diagnostics readiness, and full mocked flow tests.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md
@.planning/phases/05-retrieval-guards-query-api/RESEARCH.md
@.planning/phases/05-retrieval-guards-query-api/05-02-SUMMARY.md
@.planning/phases/05-retrieval-guards-query-api/05-03-SUMMARY.md
@.planning/phases/05-retrieval-guards-query-api/05-04-SUMMARY.md
@.planning/phases/05-retrieval-guards-query-api/05-05-SUMMARY.md
@.planning/phases/05-retrieval-guards-query-api/05-06-SUMMARY.md
@ai-service/src/corp_rag_ai/main.py
@ai-service/src/corp_rag_ai/contracts/generated/ai_service_v1.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add LangGraph dependency and typed query graph state</name>
  <files>ai-service/pyproject.toml, ai-service/uv.lock, ai-service/src/corp_rag_ai/agent/state.py, ai-service/src/corp_rag_ai/agent/graph.py, ai-service/tests/test_query_graph.py</files>
  <read_first>.planning/phases/05-retrieval-guards-query-api/RESEARCH.md, ai-service/pyproject.toml</read_first>
  <action>Add `langgraph>=0.2,<0.3` and lock the resolved version. Before implementation, verify the installed version against the `StateGraph`, `add_conditional_edges`, and `compile()` APIs referenced in `RESEARCH.md`; if upgrading outside the pinned range, re-check those signatures before coding. Define a typed query graph state containing request, guard verdict, route decision, retrieval candidates, parent context, rerank result, synthesis result, output guard result, errors, and metadata. Build a compiled `StateGraph` with conditional edges: input guard rejection ends, unsupported route ends, factual/comparison route invokes hybrid retrieval, aggregation/multi-hop route invokes graph retrieval, then common parent/rerank/synthesis/output guard/finalize nodes. Keep node functions thin wrappers around the already-tested services.</action>
  <verify>
    <automated>cd ai-service; uv run python -c "from importlib.metadata import version; from langgraph.graph import StateGraph, START, END; assert version('langgraph').startswith('0.2.'); assert StateGraph and START and END"</automated>
    <automated>cd ai-service; uv run pytest tests/test_query_graph.py</automated>
  </verify>
  <acceptance_criteria>
    - Graph compiles in tests.
    - Locked `langgraph` version is in the `0.2.x` line.
    - Guard rejection path skips all retrieval and generation nodes.
    - Unsupported path skips retrieval and generation.
    - Factual path does not invoke Neo4j unless comparison disambiguation requires it.
  </acceptance_criteria>
  <done>LangGraph orchestration is available without hiding service logic.</done>
</task>

<task type="auto">
  <name>Task 2: Compose query service and FastAPI `/v1/query` route</name>
  <files>ai-service/src/corp_rag_ai/service/query, ai-service/src/corp_rag_ai/adapters/rest/query.py, ai-service/src/corp_rag_ai/main.py, ai-service/tests/test_query_api.py</files>
  <read_first>ai-service/src/corp_rag_ai/main.py, ai-service/src/corp_rag_ai/contracts/generated/ai_service_v1.py, docs/PATTERNS.md</read_first>
  <action>Add a query service factory/composition layer in lifespan/main that wires guard, router, hybrid retriever, graph retriever, parent resolver, reranker, synthesizer, output guard, and graph. Add FastAPI route `POST /v1/query` that validates generated `QueryRequest`, maps to internal domain, enforces a 30-second timeout, invokes the service, and maps final output to generated `QueryResponse` or problem details. Do not add Java chat persistence or browser-facing behavior in this phase.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_query_api.py tests/test_query_graph.py</automated>
  </verify>
  <acceptance_criteria>
    - Contract-shaped success, refusal, guard rejection, unsupported, and dependency-error responses are tested.
    - Timeout returns a safe `answered=false`/problem response without leaking stack traces.
    - Route is registered in `main.py` and does not require AMQP consumers to be enabled.
  </acceptance_criteria>
  <done>Java can call Python `/v1/query` in mocked tests.</done>
</task>

<task type="auto">
  <name>Task 3: Extend diagnostics and full mocked pipeline coverage</name>
  <files>ai-service/src/corp_rag_ai/main.py, ai-service/src/corp_rag_ai/config.py, ai-service/tests/test_diagnostics.py, ai-service/tests/test_query_pipeline.py</files>
  <read_first>ai-service/src/corp_rag_ai/main.py, .planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md</read_first>
  <action>Extend `/diagnostics` with query readiness indicators such as `query_router`, `reranker_loaded` or `reranker_configured`, `llm_reachable` where cheaply checkable, and preserved Phase 04 fields. Add end-to-end mocked query pipeline tests for guarded rejection, out-of-scope, factual cited answer, graph aggregation/multi-hop answer, weak/no-evidence refusal, reranker degraded factual answer, and dependency failure mapping.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_diagnostics.py tests/test_query_pipeline.py tests</automated>
  </verify>
  <acceptance_criteria>
    - `/diagnostics` remains available without live external calls in default tests.
    - Full test suite passes with mocked external stores/LLM.
    - Retrieval metadata includes route, retrievers attempted/used, chunks considered/returned, reranker usage, latency, model ID, and warnings.
  </acceptance_criteria>
  <done>The complete mocked query API path is behavior-complete.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Java to Python sync HTTP | Query calls must be bounded and contract-shaped. |
| Graph orchestration to service primitives | Conditional routing must not accidentally execute unsafe branches. |
| Diagnostics to runtime state | Readiness must be useful without forcing live external calls in tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-07-01 | Denial of Service | synchronous query API | mitigate | 30-second timeout and top-k/context/reranker caps. |
| T-05-07-02 | Information Disclosure | graph routing bug | mitigate | Conditional-edge tests proving skipped nodes. |
| T-05-07-03 | Reliability | diagnostics | mitigate | Query readiness fields with no mandatory live dependency calls in default path. |
</threat_model>

<verification>
- [ ] `cd ai-service; uv run pytest tests`
- [ ] `python scripts/verify-contracts.py`
- [ ] Mocked `/v1/query` tests cover success, refusal, guard, unsupported, degraded, and failure paths.
</verification>

<success_criteria>
- AGT-02 is implemented through LangGraph.
- The Phase 05 query endpoint is ready for live UAT with seeded corpus.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retrieval-guards-query-api/05-07-SUMMARY.md`.
</output>
