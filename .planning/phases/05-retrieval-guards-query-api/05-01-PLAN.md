---
phase: "05-retrieval-guards-query-api"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "contracts/openapi/ai-service-v1.yaml"
  - "contracts/openapi/api-v1.yaml"
  - "scripts/generate_python_contracts.py"
  - "ai-service/src/corp_rag_ai/contracts/generated"
  - "ai-service/src/corp_rag_ai/config.py"
  - "ai-service/src/corp_rag_ai/main.py"
  - "ai-service/tests"
  - "infra/docker-compose.yml"
  - ".env.example"
  - "infra/.env.example"
  - "docs/ARCHITECTURE.md"
  - "docs/decisions/ADR-005-query-routing-model.md"
  - "docs/decisions/ADR-006-degraded-mode-policy.md"
  - "docs/decisions/ADR-007-citation-contract-and-refusal-rules.md"
  - "docs/decisions/ADR-008-guard-architecture.md"
  - "docs/decisions/README.md"
autonomous: true
requirements: ["RET-01", "RET-02", "RET-03", "RET-04", "AGT-01", "AGT-02", "AGT-03", "SEC-01"]
user_setup: []
must_haves:
  truths:
    - "Phase 05 starts from shared contracts and ADRs before query implementation."
    - "Citation chunk IDs are UUID child chunk IDs, not ch-NNN-MMM identifiers."
    - "Duplicate Phase 04 upload event IDs short-circuit before Qdrant, Neo4j, and OpenRouter work."
    - "python-ai local runtime contour is raised before loading the reranker."
  artifacts:
    - path: "contracts/openapi/ai-service-v1.yaml"
      provides: "aligned Python query contract"
      contains: "UUID citation chunk IDs, guard verdict objects, retrieval warnings, DeepSeek model examples"
    - path: "docs/decisions"
      provides: "Phase 05 ADR baseline"
      contains: "ADR-005 through ADR-008"
    - path: "ai-service/src/corp_rag_ai/main.py"
      provides: "wired ingestion idempotency dispatcher"
      contains: "IdempotentEventDispatcher wrapping upload/delete handlers"
  decision_refs:
    - "D-149"
    - "D-150"
    - "D-151"
    - "D-152"
    - "D-153"
    - "D-154"
    - "D-155"
    - "D-156"
    - "D-157"
    - "D-195"
    - "D-205"
    - "D-214"
    - "D-218"
    - "D-221"
    - "D-222"
---

<objective>
Prepare Phase 05's contract, ADR, runtime, and Phase 04 carry-forward baseline.

Purpose: downstream query implementation should not build against stale citation
schemas, missing ADRs, insufficient memory, or the known duplicate-redelivery
bug from Phase 04 UAT.
Output: aligned OpenAPI/generated models, ADR-005 through ADR-008, query config
defaults, raised compose memory contour, and duplicate upload idempotency
regression.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md
@.planning/phases/05-retrieval-guards-query-api/RESEARCH.md
@.planning/phases/04-python-ingestion-indexing/04-UAT-EVIDENCE.md
@contracts/openapi/ai-service-v1.yaml
@contracts/openapi/api-v1.yaml
@scripts/generate_python_contracts.py
@ai-service/src/corp_rag_ai/main.py
@ai-service/src/corp_rag_ai/adapters/amqp/consumer.py
@infra/docker-compose.yml

<dependency_context>
No Phase 05 implementation dependency. This plan must complete before any query
plan consumes generated contracts, runtime settings, or local model resources.
</dependency_context>
</context>

<tasks>
<task type="auto">
  <name>Task 1a: Update query OpenAPI contracts</name>
  <files>contracts/openapi/ai-service-v1.yaml, contracts/openapi/api-v1.yaml</files>
  <read_first>contracts/openapi/ai-service-v1.yaml, contracts/openapi/api-v1.yaml</read_first>
  <action>Update both OpenAPI files so `Citation.chunkId` is a UUID string with no `ch-NNN-MMM` pattern. Add citation display fields needed by Phase 6 source viewing: `sectionPath`, nullable `pageNumber`, short quote/snippet, score, and access level. Fix examples to use `deepseek/deepseek-v4-flash:free`. Add retrieval metadata fields for retrievers attempted/used, degradation warnings, model ID, chunks considered/returned, reranker usage, and route. Do not change Python code generation in this subtask.</action>
  <verify>
    <automated>python -c "from pathlib import Path; import yaml; files=('contracts/openapi/ai-service-v1.yaml','contracts/openapi/api-v1.yaml'); text='\n'.join(Path(p).read_text(encoding='utf-8') for p in files); [yaml.safe_load(Path(p).read_text(encoding='utf-8')) for p in files]; assert 'deepseek/deepseek-v4-flash:free' in text; assert 'ch-NNN' not in text and 'ch-001' not in text"</automated>
  </verify>
  <acceptance_criteria>
    - `Citation.chunkId` accepts UUID child chunk IDs in both Java-facing and Python-facing OpenAPI files.
    - Citation display fields and retrieval metadata are present in the contract schemas.
    - Query examples no longer mention Gemini.
    - OpenAPI YAML parses successfully before code generation changes begin.
  </acceptance_criteria>
  <done>Query OpenAPI contract surface is ready for code generation.</done>
</task>

<task type="auto">
  <name>Task 1b: Fix Python nullable/allOf codegen and regenerate models</name>
  <files>scripts/generate_python_contracts.py, ai-service/src/corp_rag_ai/contracts/generated</files>
  <read_first>scripts/generate_python_contracts.py, ai-service/src/corp_rag_ai/contracts/generated/ai_service_v1.py</read_first>
  <action>Fix Python codegen so nullable/allOf property schemas produce typed nested models, not `str | None`, then regenerate contracts from the OpenAPI files updated in Task 1a. Keep the fix narrowly scoped to schema type resolution and nullable handling.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
    <automated>python -c "from corp_rag_ai.contracts.generated.ai_service_v1 import QueryResponse, GuardVerdict; f=QueryResponse.model_fields['guardVerdict']; assert 'GuardVerdict' in str(f.annotation)"</automated>
  </verify>
  <acceptance_criteria>
    - Generated `QueryResponse.guardVerdict` and `ProblemDetail.guardVerdict` are typed as `GuardVerdict | None`.
    - Generated `ProblemDetail.guardVerdict` is also typed as `GuardVerdict | None`.
    - Contract verification passes.
  </acceptance_criteria>
  <done>Generated Python query contract models are safe to implement against.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 05 ADRs</name>
  <files>docs/decisions/ADR-005-query-routing-model.md, docs/decisions/ADR-006-degraded-mode-policy.md, docs/decisions/ADR-007-citation-contract-and-refusal-rules.md, docs/decisions/ADR-008-guard-architecture.md, docs/decisions/README.md, docs/ARCHITECTURE.md</files>
  <read_first>docs/decisions/ADR-template.md, docs/decisions/README.md, .planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md</read_first>
  <action>Create accepted ADRs for the rules-first query router, fail-loud degraded-mode matrix, child-level citation/refusal policy, and two-layer guard architecture. Update `docs/decisions/README.md` accepted ADR table and remove or supersede stale future-ADR placeholders that conflict with ADR-005 through ADR-008. Update architecture text only where it still contradicts Phase 05 decisions, especially Gemini references, sanitized-chunk exclusion, and citation ID shape.</action>
  <verify>
    <automated>rg -n "gemini|ch-NNN|ch-001|DeepSeek V3" docs contracts ai-service/src ai-service/tests --glob '!**/04-09-*' --glob '!**/04-DISCUSSION-LOG.md'</automated>
  </verify>
  <acceptance_criteria>
    - ADR-005 through ADR-008 exist and are linked from README.
    - ADRs record the alternatives rejected in `05-CONTEXT.md`: trained classifiers, silent fallback, parent-level citations, and external guard config.
    - Grep has no stale Gemini/chunk-pattern references except historical notes explicitly marked as superseded.
  </acceptance_criteria>
  <done>Phase 05 design decisions are defensible before implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Close Phase 04 duplicate-idempotency carry-forward and query runtime defaults</name>
  <files>ai-service/src/corp_rag_ai/main.py, ai-service/src/corp_rag_ai/config.py, ai-service/tests, infra/docker-compose.yml, .env.example, infra/.env.example, ai-service/README.md, infra/README.md</files>
  <read_first>ai-service/src/corp_rag_ai/main.py, ai-service/src/corp_rag_ai/adapters/amqp/consumer.py, ai-service/tests/test_amqp_consumer.py, ai-service/tests/test_ingestion_orchestrator.py, infra/docker-compose.yml</read_first>
  <action>Wrap upload and delete handlers in `main.py` with `IdempotentEventDispatcher` using the same session-bound `ProcessedEventRepository` before business service construction. Add a regression proving duplicate upload event IDs do not call Qdrant, Neo4j, MinIO, embedder, or OpenRouter/entity extraction. Extend settings and env examples with query defaults: `AI_QUERY_TIMEOUT_SECONDS=30`, `AI_ROUTER_CONFIDENCE_THRESHOLD=0.65`, `AI_RERANKER_ENABLED=true`, `AI_RERANKER_MODEL=BAAI/bge-reranker-v2-m3`, `AI_CONTEXT_TOKEN_CAP=4000`, `AI_WEAK_EVIDENCE_THRESHOLD=0.4`, and `AI_FLAGGED_CHUNK_SCORE_MULTIPLIER=0.5`. Wire the same defaults explicitly into the `python-ai` service in `infra/docker-compose.yml` as `AI_QUERY_TIMEOUT_SECONDS: ${AI_QUERY_TIMEOUT_SECONDS:-30}`, `AI_ROUTER_CONFIDENCE_THRESHOLD: ${AI_ROUTER_CONFIDENCE_THRESHOLD:-0.65}`, `AI_RERANKER_ENABLED: ${AI_RERANKER_ENABLED:-true}`, `AI_RERANKER_MODEL: ${AI_RERANKER_MODEL:-BAAI/bge-reranker-v2-m3}`, `AI_CONTEXT_TOKEN_CAP: ${AI_CONTEXT_TOKEN_CAP:-4000}`, `AI_WEAK_EVIDENCE_THRESHOLD: ${AI_WEAK_EVIDENCE_THRESHOLD:-0.4}`, and `AI_FLAGGED_CHUNK_SCORE_MULTIPLIER: ${AI_FLAGGED_CHUNK_SCORE_MULTIPLIER:-0.5}`. Raise compose `python-ai` to `mem_limit: 6g` and `mem_reservation: 4g`.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_amqp_consumer.py tests/test_ingestion_orchestrator.py</automated>
    <automated>docker compose -f infra/docker-compose.yml config</automated>
  </verify>
  <acceptance_criteria>
    - Duplicate event regression fails before the dispatcher wiring and passes after it.
    - Query defaults are documented in both env examples.
    - Query defaults are present in the rendered `python-ai` service environment.
    - Compose renders with 6 GiB limit and 4 GiB reservation for `python-ai`.
    - Existing ingestion tests still pass.
  </acceptance_criteria>
  <done>Phase 05 can start query work without inherited UAT defects or insufficient local contour.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Shared contracts to generated code | OpenAPI mismatches can silently produce wrong Python/Java types. |
| RabbitMQ redelivery to Python handlers | Duplicate messages must not repeat expensive external side effects. |
| Local model runtime to Docker host | Reranker/model memory pressure can destabilize the whole local stack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-01-01 | Tampering | citation contract | mitigate | Contract-first UUID chunk IDs plus regenerated models and contract verification. |
| T-05-01-02 | Denial of Service | duplicate upload redelivery | mitigate | Dispatcher duplicate check before business handler construction. |
| T-05-01-03 | Denial of Service | local reranker runtime | mitigate | Raise compose resource contour and keep reranker loading config-gated. |
</threat_model>

<verification>
- [ ] `python scripts/verify-contracts.py` passes.
- [ ] Generated Python guard verdict fields are typed as `GuardVerdict | None`.
- [ ] Phase 04 duplicate upload event regression proves no expensive side effects.
- [ ] `docker compose -f infra/docker-compose.yml config` passes.
</verification>

<success_criteria>
- Contract, ADR, config, and runtime prerequisites are ready for all later Phase 05 plans.
- `PH4-UAT-DEF-01` is closed by regression, not just documented.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retrieval-guards-query-api/05-01-SUMMARY.md`.
</output>
