---
phase: "05-retrieval-guards-query-api"
plan: "04"
type: "execute"
wave: 3
depends_on: ["02"]
files_modified:
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py"
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py"
  - "ai-service/src/corp_rag_ai/domain/retrieval.py"
  - "ai-service/tests/test_graph_retriever.py"
autonomous: true
requirements: ["RET-02", "RET-03", "AGT-01"]
user_setup: []
must_haves:
  truths:
    - "Graph retrieval filters through accessible Document evidence, never orphan entities."
    - "Aggregation and multi-hop routes are graph-first."
    - "Multi-hop traversal is capped at 2-3 hops; deeper queries become unsupported."
  artifacts:
    - path: "ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py"
      provides: "graph retrieval primitives"
      contains: "aggregation, multi-hop, comparison support and access-filtered Cypher"
    - path: "ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py"
      provides: "read-side graph query helpers"
      contains: "access-filter Cypher predicates without touching the Phase 04 write path"
  decision_refs:
    - "D-176"
    - "D-177"
    - "D-178"
    - "D-180"
    - "D-181"
    - "D-183"
    - "D-209"
    - "D-211"
    - "D-217"
    - "D-220"
---

<objective>
Implement access-filtered Neo4j graph retrieval for aggregation, multi-hop, and
comparison evidence.

Purpose: Phase 05 needs graph retrieval paths that can answer structured and
relationship-based questions without bypassing document visibility.
Output: graph retriever returning document-backed child evidence candidates,
with degradation metadata and tests proving access filters are embedded in
Cypher.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md
@.planning/phases/05-retrieval-guards-query-api/RESEARCH.md
@.planning/phases/05-retrieval-guards-query-api/05-02-SUMMARY.md
@ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py
@ai-service/tests/test_graph_indexer_write.py
@backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessFilterResolver.java
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add shared graph access-filter Cypher helpers</name>
  <files>ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py, ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py, ai-service/tests/test_graph_retriever.py</files>
  <read_first>ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py, ai-service/tests/test_graph_indexer_write.py, .planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md</read_first>
  <action>Create new read-side module `ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py` for graph retrieval helpers that always match candidate `Document` nodes first or join back to `Document` evidence before returning any entity/relation evidence. Build Cypher predicates for `accessLevel`, `docType`, and optional department wildcard. Use parameters, not string interpolation for user values. Orphan `Entity` nodes must be ignored because every returned candidate must have a `MENTIONED_IN` or `EVIDENCE` edge to an accessible document. Do not modify `ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py`; it remains the Phase 04 write path.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_graph_retriever.py tests/test_graph_indexer_write.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests inspect query text/params and prove `Document` access predicates are present.
    - No graph retrieval method returns entity-only evidence without document backing.
    - Empty departments mean wildcard only for department, not access level or doc type.
    - `graph_indexer.py` remains untouched by this plan.
  </acceptance_criteria>
  <done>Graph retrieval has safe access-filter building blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement aggregation and multi-hop graph retrieval</name>
  <files>ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py, ai-service/tests/test_graph_retriever.py</files>
  <read_first>.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md, ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py</read_first>
  <action>Implement graph-first retrieval for `AGGREGATION` and `MULTI_HOP`. Aggregation selects accessible document-backed entity/relation evidence for counts/lists/grouping. Multi-hop traverses entity/relation paths with a hard maximum of 2-3 hops and returns `UNSUPPORTED`/no retrieval when the routed request requires deeper paths. Returned candidates must carry child chunk IDs, parent chunk IDs, document metadata, section path, and graph path summary for synthesis.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_graph_retriever.py</automated>
  </verify>
  <acceptance_criteria>
    - Multi-hop tests prove max-hop cap is enforced.
    - Aggregation tests return citation-capable evidence, not citationless counts.
    - If Neo4j is unavailable for aggregation/multi-hop, result is explicit `graph_retrieval_unavailable`.
  </acceptance_criteria>
  <done>Graph-first routes can retrieve cited structured evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Implement comparison graph support and hybrid handoff hints</name>
  <files>ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py, ai-service/src/corp_rag_ai/domain/retrieval.py, ai-service/tests/test_graph_retriever.py</files>
  <read_first>.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md, .planning/phases/05-retrieval-guards-query-api/05-03-SUMMARY.md</read_first>
  <action>Implement comparison support that extracts compared entities/documents, uses graph only for entity disambiguation when names are ambiguous, and returns structured hints/candidate groups for later hybrid evidence merging. Do not broaden access filters to improve recall. When graph is unavailable for factual route it must not block factual hybrid retrieval; when graph is unavailable for comparison and disambiguation is required, return explicit graph degradation/failure metadata.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_graph_retriever.py</automated>
  </verify>
  <acceptance_criteria>
    - Comparison tests cover ambiguous and unambiguous entity names.
    - Factual route does not depend on graph.
    - Low/empty accessible graph evidence is a legitimate no-answer outcome.
  </acceptance_criteria>
  <done>Comparison route has graph evidence/disambiguation support.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Neo4j graph to user answer | Entity graph can contain orphan nodes and cross-document edges. |
| Query route to graph traversal | Multi-hop can become expensive without caps. |
| Graph dependency to query response | Neo4j outage must not silently disappear. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-04-01 | Information Disclosure | graph evidence | mitigate | Filter through accessible Document evidence before returning candidates. |
| T-05-04-02 | Denial of Service | multi-hop traversal | mitigate | Hard hop cap and unsupported fallback for deeper requests. |
| T-05-04-03 | Reliability | Neo4j unavailable | mitigate | Route-specific explicit failure/degradation metadata. |
</threat_model>

<verification>
- [ ] `cd ai-service; uv run pytest tests/test_graph_retriever.py`
- [ ] Tests prove access filters exist in Cypher before evidence is returned.
- [ ] Tests prove orphan entities are ignored.
- [ ] Tests cover graph dependency failure policy by route.
</verification>

<success_criteria>
- RET-03 is implemented with document-backed citations.
- Neo4j side of RET-02 is proven by storage-level filtering tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retrieval-guards-query-api/05-04-SUMMARY.md`.
</output>
