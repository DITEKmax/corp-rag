---
phase: "05-retrieval-guards-query-api"
plan: "06"
type: "execute"
wave: 5
depends_on: ["05"]
files_modified:
  - "ai-service/src/corp_rag_ai/pipeline/generation"
  - "ai-service/src/corp_rag_ai/pipeline/guards/output_guard.py"
  - "ai-service/src/corp_rag_ai/pipeline/generation/degradation_policy.py"
  - "ai-service/src/corp_rag_ai/pipeline/generation/prompts"
  - "ai-service/src/corp_rag_ai/domain/query.py"
  - "ai-service/src/corp_rag_ai/domain/guard.py"
  - "ai-service/tests"
autonomous: true
requirements: ["AGT-03", "SEC-01", "RET-04"]
user_setup: []
must_haves:
  truths:
    - "Answer generation only uses provided evidence and strict `[N]` inline citations."
    - "Output guard blocks invalid refs, uncited factual claims, unsafe evidence-only answers, and leak patterns."
    - "Degraded-mode behavior is explicit and fail-loud."
    - "Weak evidence below confidence 0.4 refuses instead of generating fragile answers."
  artifacts:
    - path: "ai-service/src/corp_rag_ai/pipeline/generation/synthesizer.py"
      provides: "DeepSeek cited answer synthesis"
      contains: "OpenRouter strict JSON schema, XML evidence boundaries, refusal handling"
    - path: "ai-service/src/corp_rag_ai/pipeline/guards/output_guard.py"
      provides: "post-generation guard"
      contains: "citation validation, unsafe evidence policy, leak pattern detection"
    - path: "ai-service/src/corp_rag_ai/pipeline/generation/degradation_policy.py"
      provides: "centralized degradation decision function"
      contains: "D-207 through D-213 matrix as a testable policy"
  decision_refs:
    - "D-158"
    - "D-166"
    - "D-167"
    - "D-168"
    - "D-196"
    - "D-197"
    - "D-198"
    - "D-199"
    - "D-200"
    - "D-201"
    - "D-202"
    - "D-203"
    - "D-204"
    - "D-206"
    - "D-207"
    - "D-208"
    - "D-209"
    - "D-210"
    - "D-211"
    - "D-212"
    - "D-213"
    - "D-215"
    - "D-216"
---

<objective>
Implement cited answer synthesis, output guard, confidence, refusal, and
degraded-mode policy.

Purpose: retrieved evidence must become a contract-shaped answer only when the
answer is supported, cited, safe, and dependency state is explicit.
Output: DeepSeek/OpenRouter synthesizer, output guard, refusal helpers, and
degraded-mode test matrix.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md
@.planning/phases/05-retrieval-guards-query-api/RESEARCH.md
@.planning/phases/05-retrieval-guards-query-api/05-05-SUMMARY.md
@ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py
@ai-service/src/corp_rag_ai/pipeline/ingestion/corpus_sanitizer.py
@ai-service/src/corp_rag_ai/contracts/generated/ai_service_v1.py
@contracts/constants.yaml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add OpenRouter synthesis client and citation schema</name>
  <files>ai-service/src/corp_rag_ai/pipeline/generation, ai-service/src/corp_rag_ai/pipeline/generation/prompts, ai-service/tests/test_synthesizer.py</files>
  <read_first>ai-service/src/corp_rag_ai/pipeline/indexing/entity_extractor.py, .planning/phases/05-retrieval-guards-query-api/RESEARCH.md, .planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md</read_first>
  <action>Implement a DeepSeek/OpenRouter synthesizer using the same OpenAI-compatible strict JSON-schema pattern as entity extraction. Prompt must isolate evidence from instructions, cite every factual claim with `[N]`, omit unsupported claims, and return structured `{answered, answer, citation_indexes, confidence_hint}`. Do not rely on raw XML tags alone: evidence content must be XML-escaped before insertion into the prompt template, or wrapped in unique per-request sentinel markers that evidence text cannot guess. Document the chosen approach in the synthesizer module docstring. `corpus_sanitizer.py` already flags `<system>` and chat-template markers, but it does not neutralize arbitrary XML-like closing tags, so prompt assembly must still handle `<...>` and `</...>` evidence text safely. The client must map auth, rate limit, timeout/connection, 5xx, and malformed JSON into query-domain dependency failures or non-retryable generation failures without leaking raw exceptions.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_synthesizer.py tests/test_entity_extractor.py</automated>
  </verify>
  <acceptance_criteria>
    - Default tests mock OpenRouter and do not require credentials.
    - Prompt includes safe evidence boundaries and strict citation instructions.
    - Tests include evidence containing fake XML closing tags and prove it cannot break prompt boundaries.
    - Generation unavailable returns `answered=false` with `generation_unavailable`.
    - Malformed structured output is rejected and tested.
  </acceptance_criteria>
  <done>Evidence can be transformed into a structured cited answer candidate.</done>
</task>

<task type="auto">
  <name>Task 2: Implement output guard and citation compliance validation</name>
  <files>ai-service/src/corp_rag_ai/pipeline/guards/output_guard.py, ai-service/tests/test_output_guard.py</files>
  <read_first>ai-service/src/corp_rag_ai/pipeline/ingestion/corpus_sanitizer.py, .planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md</read_first>
  <action>Add output guard validation for invalid citation refs, factual sentence without refs, citation indexes not present in returned citations, secret/PII-like leak patterns, and unsafe-evidence-only final context. If all final top-N evidence is flagged (`isSanitized=false`), force `answered=false` with reason `unsafe_evidence_only`. Keep exact uncited-claim heuristic simple and documented, but tests must cover obvious factual sentence failures.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_output_guard.py tests/test_query_input_guard.py</automated>
  </verify>
  <acceptance_criteria>
    - Invalid refs like `[4]` with only two citations are blocked.
    - Factual answer text without any valid refs is blocked.
    - Aggregation/count/list answers still require citations.
    - Secret-like output patterns are blocked or force refusal.
  </acceptance_criteria>
  <done>Unsafe or uncited generated answers cannot leave Python as answered=true.</done>
</task>

<task type="auto">
  <name>Task 3: Implement confidence, refusal, and degraded-mode matrix</name>
  <files>ai-service/src/corp_rag_ai/domain/query.py, ai-service/src/corp_rag_ai/pipeline/generation/degradation_policy.py, ai-service/src/corp_rag_ai/pipeline/generation, ai-service/tests/test_query_degradation_policy.py</files>
  <read_first>.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md, contracts/constants.yaml</read_first>
  <action>Add helpers that convert dependency and evidence states into contract-shaped `answered=false` responses with explicit errors/warnings. Implement the locked degraded matrix in a single module `ai-service/src/corp_rag_ai/pipeline/generation/degradation_policy.py` exposing `apply_degradation(dependency_state, evidence_state, route) -> DegradationOutcome`, so the matrix is testable as one decision function rather than scattered across synthesizer/output guard code. Matrix rows: OpenRouter generation unavailable fails; Qdrant unavailable fails for factual/comparison; Qdrant unavailable soft-degrades for aggregation/multi-hop only if Neo4j evidence exists; Neo4j unavailable does not block factual; Neo4j unavailable fails aggregation/multi-hop; reranker unavailable soft-degrades; embedding unavailable fails. Compute normal confidence from top reranker score clamped to `[0,1]`; degraded raw-score confidence must be marked as such. Force refusal when confidence is below `0.4`.</action>
  <verify>
    <automated>cd ai-service; uv run pytest tests/test_query_degradation_policy.py tests/test_synthesizer.py tests/test_output_guard.py</automated>
  </verify>
  <acceptance_criteria>
    - Every degraded matrix row from D-207 through D-213 has a named unit test.
    - Tests exercise `apply_degradation(...)` directly for each matrix row.
    - Weak evidence returns actionable refusal text, not vague "I don't know".
    - Retrieval metadata exposes route, retrievers attempted/used, chunks considered/returned, reranker usage, latency placeholder, model ID, and warnings.
  </acceptance_criteria>
  <done>Generation and refusal behavior is explicit across dependency states.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Evidence text to LLM prompt | Retrieved context may contain instructions or secrets. |
| LLM answer to Java response | Model output must not be trusted without citation and leak checks. |
| Dependency failures to user response | Silent fallback can hide missing safety or retrieval dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-06-01 | Information Disclosure | LLM prompt injection | mitigate | XML evidence boundaries and output guard validation. |
| T-05-06-02 | Tampering | unsupported citations | mitigate | Strict citation index validation after generation. |
| T-05-06-03 | Reliability | degraded dependencies | mitigate | Explicit matrix and metadata warnings/errors. |
</threat_model>

<verification>
- [ ] `cd ai-service; uv run pytest tests/test_synthesizer.py tests/test_output_guard.py tests/test_query_degradation_policy.py`
- [ ] Tests cover every D-207 through D-213 degraded-mode row.
- [ ] Tests prove invalid or uncited generated answers cannot return `answered=true`.
</verification>

<success_criteria>
- AGT-03 and output-side SEC-01 are implemented under mocked dependencies.
- Degraded-mode policy is executable and not just documented.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retrieval-guards-query-api/05-06-SUMMARY.md`.
</output>
