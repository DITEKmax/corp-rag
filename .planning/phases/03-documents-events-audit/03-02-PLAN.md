---
phase: "03-documents-events-audit"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01"]
files_modified:
  - "backend/corp-rag-app/src/main/resources/db/migration/V11__create_documents_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V12__create_outbox_events_table.sql"
  - "backend/corp-rag-app/src/main/resources/db/migration/V13__create_processed_events_table.sql"
  - "backend/corp-rag-app/src/main/java/com/corprag/domain"
  - "backend/corp-rag-app/src/main/java/com/corprag/repository"
  - "backend/corp-rag-app/src/main/java/com/corprag/security/CorrelationIdFilter.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/config/SecurityConfig.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsWriter.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/service/audit/AuditEventWriter.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/repository"
  - "backend/corp-rag-app/src/test/java/com/corprag/security"
  - "backend/corp-rag-app/src/test/java/com/corprag/service/audit"
autonomous: true
requirements: ["DOC-01", "DOC-02", "DOC-03", "EVT-01", "EVT-02", "AUD-01"]
user_setup: []
must_haves:
  truths:
    - "PostgreSQL schema supports active document visibility, soft delete, duplicate same-department SHA checks, outbox delivery, and idempotent Java consumers."
    - "Correlation ID is available through MDC before JWT handling and is reused by ProblemDetails, audit rows, and later event metadata."
    - "Repositories expose SQL-level visibility primitives instead of letting controllers filter documents in memory."
  artifacts:
    - path: "backend/corp-rag-app/src/main/resources/db/migration/V11__create_documents_table.sql"
      provides: "Document metadata/status schema"
      contains: "soft delete columns, content_sha256, storage bucket/key, status/failure/indexing metadata, partial duplicate index"
    - path: "backend/corp-rag-app/src/main/resources/db/migration/V12__create_outbox_events_table.sql"
      provides: "Transactional outbox schema"
      contains: "payload/headers JSONB, correlation_id, publish attempts, last error, next attempt, published timestamp"
    - path: "backend/corp-rag-app/src/main/resources/db/migration/V13__create_processed_events_table.sql"
      provides: "Idempotent consumer ledger"
      contains: "event_id primary key, event_type, correlation_id, processed_at cleanup index"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/security/CorrelationIdFilter.java"
      provides: "Request correlation ID lifecycle"
      contains: "X-Correlation-Id parsing/generation, MDC storage, response header, MDC cleanup"
  key_links:
    - from: "CorrelationIdFilter"
      to: "AuditEventWriter"
      via: "MDC correlationId is reused for audit rows"
    - from: "DocumentRepository"
      to: "AccessFilterResolver"
      via: "repository methods accept ResolvedAccessFilter and build SQL predicates"
    - from: "OutboxEventRepository"
      to: "future OutboxPublisher"
      via: "FOR UPDATE SKIP LOCKED ready-row polling fields"
  decision_refs:
    - "D-01: AccessFilter is the source of truth for visibility."
    - "D-02: read operations apply active row, docType, department, and accessLevel rules."
    - "D-03: list visibility is SQL/PostgreSQL before pagination and counts."
    - "D-04: invisible existing documents return 404 DOCUMENT_NOT_FOUND."
    - "D-09: no owner-bypass visibility axis."
    - "D-10: source files use corp-rag-documents bucket."
    - "D-14: original filename is preserved for UI/audit only."
    - "D-15: content_sha256 is stored as CHAR(64) NOT NULL."
    - "D-16: active duplicate detection is partial unique by content_sha256 and department."
    - "D-18: duplicates after soft delete or in another department are allowed by partial index."
    - "D-24: soft delete and document.deleted outbox event occur in one transaction."
    - "D-25: soft-deleted documents are invisible everywhere."
    - "D-28: hard delete and physical object cleanup are deferred."
    - "D-30: terminal events for soft-deleted documents must not resurrect rows."
    - "D-31: status schema supports UPLOADED, INDEXED, and INDEXING_FAILED terminal updates."
    - "D-34: retryable is recorded for audit/debug information only."
    - "D-37: upload/delete event rows are stored through outbox in the same transaction."
    - "D-38: outbox schema includes required fields."
    - "D-39: outbox schema supports row locking and batch polling."
    - "D-40: outbox schema supports attempts, last_error, and next_attempt_at."
    - "D-41: published rows are cleanable after 7 days."
    - "D-42: processed_events is keyed by event_id."
    - "D-43: consumers insert processed_events first inside the transaction."
    - "D-45: processed_events cleanup can remove rows older than 30 days."
    - "D-47: reuse and extend Phase 2 audit_events and AuditEventWriter."
    - "D-55: add CorrelationIdFilter before JWT handling."
    - "D-56: AuditEventWriter uses MDC correlationId or generated fallback."
    - "D-58: existing Phase 2 audit call sites benefit from cleanup without broad rewrites."
---

<objective>
Create the persistence, repository, and correlation foundation for Phase 3.

Purpose: Later plans need stable tables, domain records, repository primitives, and correlation IDs before implementing document services, outbox publishing, and consumers.
Output: Flyway migrations, Java domain/repository foundations, CorrelationIdFilter, and MDC-aware audit/problem details.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documents-events-audit/03-CONTEXT.md
@.planning/phases/03-documents-events-audit/03-RESEARCH.md
@.planning/phases/03-documents-events-audit/03-01-SUMMARY.md
@backend/corp-rag-app/src/main/resources/db/migration
@backend/corp-rag-app/src/main/java/com/corprag/domain
@backend/corp-rag-app/src/main/java/com/corprag/repository
@backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessFilterResolver.java
@backend/corp-rag-app/src/main/java/com/corprag/service/audit/AuditEventWriter.java
@backend/corp-rag-app/src/main/java/com/corprag/config/SecurityConfig.java
@backend/corp-rag-app/src/test/java/com/corprag/testsupport/PostgresIntegrationTestSupport.java
@CLAUDE.md

<dependency_context>
Requires Plan 03-01 contract alignment so repository/domain field names and errors match generated surfaces.
</dependency_context>

<decision_coverage>
Implement D-01, D-02, D-03, D-04, D-09, D-10, D-14, D-15, D-16, D-18, D-24, D-25, D-28, D-30, D-31, D-34, D-37 through D-45, D-47, D-55, D-56, and D-58 in schema, repository, and correlation foundations.
</decision_coverage>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add document, outbox, and processed-event migrations</name>
  <files>backend/corp-rag-app/src/main/resources/db/migration/V11__create_documents_table.sql, backend/corp-rag-app/src/main/resources/db/migration/V12__create_outbox_events_table.sql, backend/corp-rag-app/src/main/resources/db/migration/V13__create_processed_events_table.sql</files>
  <read_first>backend/corp-rag-app/src/main/resources/db/migration/V4__create_audit_events_table.sql, .planning/phases/03-documents-events-audit/03-CONTEXT.md, .planning/phases/03-documents-events-audit/03-RESEARCH.md</read_first>
  <action>Add Flyway migrations continuing after V10. Create `documents` with UUID id, title, optional description, original filename, sniffed MIME, size, access_level, department, doc_type, language, status, owner_user_id FK, storage_bucket, storage_key, content_sha256 CHAR(64), uploaded/indexed timestamps, chunk count, failure detail columns, Qdrant/Neo4j indexing metadata, deleted_at, and deleted_by FK. Add check constraints for known document statuses/access/docType/language where practical. Add partial unique index `(content_sha256, department) WHERE deleted_at IS NULL`, active visibility indexes, and uploaded_at ordering. Create `outbox_events` with D-38 fields and ready/unpublished indexes. Create `processed_events` keyed by `event_id` with processed_at cleanup index.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test</automated>
  </verify>
  <acceptance_criteria>
    - Flyway migration order is V11, V12, V13 with no filename gaps or duplicate versions.
    - `documents` has soft-delete columns and active duplicate partial unique index.
    - `outbox_events` has payload and headers JSONB plus retry/backoff columns.
    - `processed_events.event_id` is the primary key.
  </acceptance_criteria>
  <done>Phase 3 storage schema is migratable and supports later service behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add domain records and JDBC repository foundations</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/domain, backend/corp-rag-app/src/main/java/com/corprag/repository, backend/corp-rag-app/src/test/java/com/corprag/repository</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/repository/JdbcRowSupport.java, backend/corp-rag-app/src/main/java/com/corprag/repository/AccessPolicyRepository.java, backend/corp-rag-app/src/main/java/com/corprag/domain/ResolvedAccessFilter.java</read_first>
  <action>Add document/outbox/processed-event domain records and repositories using existing `JdbcClient` style. `DocumentRepository` must expose insert, find visible by id, page visible documents with total count, find active duplicate by SHA+department, soft delete visible document, mark indexed, mark indexing failed, and skip status updates for soft-deleted rows. Build SQL visibility predicates from `ResolvedAccessFilter`: active rows only, doc type IN resolved docTypes, department wildcard when resolved departments is empty, otherwise department IN list, and access_level in resolved access levels. `OutboxEventRepository` must support insert in caller transaction, polling ready unpublished rows with `FOR UPDATE SKIP LOCKED`, mark published, mark failure/backoff, and cleanup. `ProcessedEventRepository` must support insert-if-absent and cleanup.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test</automated>
  </verify>
  <acceptance_criteria>
    - Repository tests cover active duplicate uniqueness and soft-deleted duplicate re-upload allowance.
    - Repository tests cover SQL visibility for docTypes, department wildcard/non-wildcard, access levels, and deleted rows.
    - Outbox ready polling uses `FOR UPDATE SKIP LOCKED`.
    - Processed event insert returns duplicate/already-processed state without throwing.
  </acceptance_criteria>
  <done>Document, outbox, and processed-event data access is available for services.</done>
</task>

<task type="auto">
  <name>Task 3: Add correlation ID filter and MDC-aware audit/problem details</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/security/CorrelationIdFilter.java, backend/corp-rag-app/src/main/java/com/corprag/config/SecurityConfig.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsWriter.java, backend/corp-rag-app/src/main/java/com/corprag/service/audit/AuditEventWriter.java, backend/corp-rag-app/src/test/java/com/corprag/security, backend/corp-rag-app/src/test/java/com/corprag/service/audit</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/config/SecurityConfig.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsWriter.java, backend/corp-rag-app/src/main/java/com/corprag/service/audit/AuditEventWriter.java, backend/corp-rag-app/src/test/java/com/corprag/service/audit/AuditEventWriterTest.java</read_first>
  <action>Implement `CorrelationIdFilter` as a once-per-request filter. It should read `X-Correlation-Id`, accept only valid UUIDs, generate a UUID when absent/invalid, store the string in MDC key `correlationId`, write the response `X-Correlation-Id`, and clear MDC in `finally`. Register it before JWT/security filters in `SecurityConfig`. Update `ProblemDetailsWriter` to populate `ProblemDetail.correlationId` from MDC when present. Update `AuditEventWriter` so generic and auth audit rows use MDC correlationId when present and generate a fallback only for system/background jobs. Preserve existing Phase 2 audit call sites and tests.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test</automated>
  </verify>
  <acceptance_criteria>
    - Requests with a valid `X-Correlation-Id` get the same response header and ProblemDetail/audit correlation ID.
    - Requests without the header get a generated UUID response header.
    - MDC is cleared after filter completion.
    - Existing Phase 2 audit tests continue to pass without broad service rewrites.
  </acceptance_criteria>
  <done>Phase 3 can carry correlation IDs through REST, audit, outbox, and consumers.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AccessFilter to document SQL | A bad predicate can leak or hide documents before Python retrieval exists. |
| Business transaction to outbox table | Missing same-transaction event rows break at-least-once indexing delivery. |
| Request edge to audit/events | Missing correlation IDs break incident reconstruction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | DocumentRepository | mitigate | Repository tests must cover access level, department wildcard, doc type, and soft delete filters in SQL. |
| T-03-02-02 | Tampering | documents duplicate index | mitigate | Enforce duplicate content by partial unique index, not service-only checks. |
| T-03-02-03 | Repudiation | correlation/audit | mitigate | CorrelationIdFilter plus MDC-aware audit/problem details make every request traceable. |
| T-03-02-04 | Denial of Service | outbox polling | mitigate | Add ready indexes and batch fields for bounded publisher scans. |
</threat_model>

<verification>
- [ ] `cd backend; mvn -q -pl corp-rag-app -am test` passes.
- [ ] Flyway creates `documents`, `outbox_events`, and `processed_events`.
- [ ] Repository tests prove SQL visibility is applied before pagination/counts.
- [ ] Correlation ID appears in response headers, ProblemDetails, and audit rows.
</verification>

<success_criteria>
- Database schema supports all later Phase 3 document/event/audit behavior.
- Repositories provide focused methods for services without duplicating SQL predicates.
- Correlation ID behavior is installed once and reused by existing and new audit paths.
</success_criteria>

<output>
After completion, create `.planning/phases/03-documents-events-audit/03-02-SUMMARY.md`.
</output>
