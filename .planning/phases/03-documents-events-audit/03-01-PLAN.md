---
phase: "03-documents-events-audit"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "contracts/openapi/api-v1.yaml"
  - "contracts/asyncapi/events-v1.yaml"
  - "contracts/constants.yaml"
autonomous: true
requirements: ["DOC-01", "DOC-02", "DOC-03", "EVT-01", "EVT-02", "AUD-01"]
user_setup: []
must_haves:
  truths:
    - "Phase 3 REST, event, and constants contracts are updated before Java implementation."
    - "Document delete is contracted as an always-allowed soft delete for visible active documents; no delete-while-indexing 409 remains in the REST contract."
    - "Upload error codes cover unsupported sniffed MIME, duplicate active same-department content, and upload access levels above the caller's resolved effective max level."
  artifacts:
    - path: "contracts/openapi/api-v1.yaml"
      provides: "Phase 3 public REST contract"
      contains: "document upload/list/detail/raw/delete semantics, 5-minute raw URL response, duplicate/upload access errors, reserved INDEXING description, ProblemDetail details"
    - path: "contracts/asyncapi/events-v1.yaml"
      provides: "Document lifecycle event contract"
      contains: "document.uploaded/deleted/indexed/indexing.failed envelopes, metadata, correlation ID, Java/Python routing direction, content SHA metadata"
    - path: "contracts/constants.yaml"
      provides: "Generated constants source"
      contains: "INSUFFICIENT_ACCESS_LEVEL, DUPLICATE_DOCUMENT, UNSUPPORTED_FILE_TYPE plus existing document routing keys/queues/exchanges"
  key_links:
    - from: "contracts/openapi/api-v1.yaml"
      to: "contracts/constants.yaml"
      via: "every new ProblemDetail error_code uses a constants.yaml entry"
    - from: "contracts/asyncapi/events-v1.yaml"
      to: "contracts/constants.yaml"
      via: "routing keys, exchanges, queue names, and DLQs remain generated constants"
  decision_refs:
    - "D-05: raw document access returns a 5-minute MinIO presigned URL after visibility checks."
    - "D-10: source files use the corp-rag-documents bucket."
    - "D-12: MinIO object keys use yyyy/MM/documentId.ext with sniffed extension."
    - "D-13: MIME extension mapping is fixed for PDF, DOCX, HTML, Markdown, and plain text."
    - "D-15: uploads store content_sha256."
    - "D-17: duplicate same-content same-department active upload returns DUPLICATE_DOCUMENT with existingDocumentId."
    - "D-19: upload validation uses Apache Tika sniffing rather than trusting Content-Type."
    - "D-20: unsupported sniffed MIME returns UNSUPPORTED_FILE_TYPE."
    - "D-21: declared/sniffed Content-Type mismatch is audited but does not block allowed files."
    - "D-22: file size limit remains 50 MB."
    - "D-23: delete is allowed regardless of indexing status."
    - "D-31: Java applies terminal status transitions only."
    - "D-32: Java does not set INDEXING in Phase 3."
    - "D-33: INDEXING remains reserved in the contract."
    - "D-34: retryable is recorded for audit/debug only."
    - "D-35: failed documents are terminal in Phase 3."
    - "D-36: do not add reindex endpoint, permission, counters, or seed logic."
    - "D-37: upload/delete events are published through outbox."
    - "D-38: outbox schema fields are represented in implementation-facing contract assumptions."
    - "D-42: Java consumers are idempotent using processed_events."
    - "D-47: reuse and extend the Phase 2 audit system."
    - "D-48: add DOCUMENT audit event types."
    - "D-49: ordinary list/detail reads are not audited."
    - "D-59: add INSUFFICIENT_ACCESS_LEVEL."
    - "D-60: add DUPLICATE_DOCUMENT."
    - "D-61: add UNSUPPORTED_FILE_TYPE."
    - "D-62: INVALID_FILE_FORMAT remains for downstream parsing failures."
    - "D-63: do not add documents.reindex permission."
    - "D-64: department remains regex-validated free-form uppercase code."
    - "D-65: delete-while-indexing is allowed; do not implement DOCUMENT_INDEXING_IN_PROGRESS conflicts."
---

<objective>
Make Phase 3 contract-first.

Purpose: Execution must not invent document, event, or error shapes inside Java.
Output: Updated OpenAPI, AsyncAPI, and constants contracts that generated Java/Python surfaces can consume.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documents-events-audit/03-CONTEXT.md
@.planning/phases/03-documents-events-audit/03-RESEARCH.md
@contracts/openapi/api-v1.yaml
@contracts/asyncapi/events-v1.yaml
@contracts/constants.yaml
@scripts/verify-contracts.py
@CLAUDE.md

<decision_coverage>
Implement D-05, D-10, D-12, D-13, D-15, D-17, D-19, D-20, D-21, D-22, D-23, D-31, D-32, D-33, D-34, D-35, D-36, D-37, D-38, D-42, D-47, D-48, D-49, and D-59 through D-65 in the contract surface. Do not add documents.reindex, POST /documents/{id}/reindex, retry counters, or delete-while-indexing conflict behavior.
</decision_coverage>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Align document REST contract with Phase 3 decisions</name>
  <files>contracts/openapi/api-v1.yaml</files>
  <read_first>contracts/openapi/api-v1.yaml, .planning/phases/03-documents-events-audit/03-CONTEXT.md</read_first>
  <action>Update document REST contract details without adding new unsupported operations. Keep only `GET /documents`, `POST /documents`, `GET /documents/{documentId}`, `DELETE /documents/{documentId}`, and `GET /documents/{documentId}/raw` for Phase 3 document management. Remove the `409` delete-while-indexing response from `DELETE /documents/{documentId}` and document that delete is an immediate soft delete for visible active documents regardless of status. Clarify that list/detail/raw apply the same active-row visibility rules and invisible existing documents return `404 DOCUMENT_NOT_FOUND`. Clarify that raw returns a 5-minute URL and Java does not proxy bytes. Clarify that `INDEXING` remains reserved and Java sets only `UPLOADED`, `INDEXED`, and `INDEXING_FAILED` in Phase 3. Extend `ProblemDetail` with an optional `details` object so `DUPLICATE_DOCUMENT` can include `existingDocumentId`.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - `DELETE /documents/{documentId}` no longer advertises status-based 409 behavior.
    - `GET /documents/{documentId}/raw` documents a 5-minute presigned URL and no Java byte proxying.
    - `DocumentStatus` description states that `INDEXING` is reserved in Phase 3.
    - `ProblemDetail` can carry `details.existingDocumentId`.
  </acceptance_criteria>
  <done>The REST contract matches Phase 3 document visibility, upload, raw URL, and delete rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 3 error constants and REST error references</name>
  <files>contracts/constants.yaml, contracts/openapi/api-v1.yaml</files>
  <read_first>contracts/constants.yaml, contracts/openapi/api-v1.yaml, .planning/phases/03-documents-events-audit/03-CONTEXT.md</read_first>
  <action>Add `INSUFFICIENT_ACCESS_LEVEL` with HTTP 403, `DUPLICATE_DOCUMENT` with HTTP 409, and `UNSUPPORTED_FILE_TYPE` with HTTP 400 to `contracts/constants.yaml`. Wire matching OpenAPI responses/examples into `POST /documents`: unsupported sniffed MIME returns `UNSUPPORTED_FILE_TYPE`, uploading an access level above the caller's resolved max returns `INSUFFICIENT_ACCESS_LEVEL`, and active same-content same-department duplicate returns `DUPLICATE_DOCUMENT` with `details.existingDocumentId`. Leave `INVALID_FILE_FORMAT` available for downstream parsing/indexing failures. Do not add `documents.reindex` to `PermissionCode`.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/constants.yaml` contains all three new Phase 3 error codes.
    - Upload contract references the three new error codes in appropriate HTTP responses.
    - `PermissionCode` still contains only `documents.read`, `documents.upload`, and `documents.delete` for documents.
    - `INVALID_FILE_FORMAT` remains defined.
  </acceptance_criteria>
  <done>Phase 3 error surfaces are contract-backed and generated from constants.</done>
</task>

<task type="auto">
  <name>Task 3: Align document event payload contract for downstream ingestion</name>
  <files>contracts/asyncapi/events-v1.yaml</files>
  <read_first>contracts/asyncapi/events-v1.yaml, contracts/constants.yaml, .planning/phases/03-documents-events-audit/03-CONTEXT.md</read_first>
  <action>Update `DocumentUploadedPayload` so Python receives all metadata Phase 4 needs without calling Java: include `title`, `contentSha256`, `accessLevel`, `department`, `docType`, `language`, `minioBucket`, `minioObjectKey`, sniffed `mimeType`, `sizeBytes`, `originalFilename`, and `uploadedAt`. Keep event metadata `eventId`, `eventType`, `eventVersion`, `occurredAt`, `correlationId`, and `sourceService` required. Ensure all routing key, queue, exchange, and DLQ references still point to `contracts/constants.yaml` names. Keep `DocumentDeletedPayload` scoped to document id/deleted metadata; Python cleanup details remain Phase 4 implementation.</action>
  <verify>
    <automated>python scripts/verify-contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - `DocumentUploadedPayload` includes `title` and `contentSha256`.
    - Event metadata still requires `correlationId`.
    - AsyncAPI routing names remain consistent with constants.
    - Contract verification exits 0.
  </acceptance_criteria>
  <done>Document lifecycle event contracts carry the metadata Java will publish and Python will consume.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST contract to Java implementation | Incorrect upload/delete/error contracts cause incompatible frontend/admin behavior. |
| Java event contract to Python ingestion | Missing metadata forces Python to call Java or guess visibility/storage state. |
| Error constants to generated code | Hand-written or missing error codes break ProblemDetail consistency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | document REST contract | mitigate | Contract 404 for invisible documents and identical list/detail/raw visibility semantics. |
| T-03-01-02 | Tampering | document upload contract | mitigate | Contract sniffed MIME allowlist, SHA-256 duplicate handling, and access-level cap errors. |
| T-03-01-03 | Repudiation | lifecycle events | mitigate | Require event metadata correlation IDs and generated routing constants. |
</threat_model>

<verification>
- [ ] `python scripts/verify-contracts.py` passes.
- [ ] Generated constants include the new Phase 3 error codes.
- [ ] REST delete no longer exposes indexing-status conflict semantics.
- [ ] AsyncAPI `document.uploaded` includes title and content SHA-256.
</verification>

<success_criteria>
- Phase 3 contracts are additive or explicitly aligned with locked Phase 3 decisions.
- Java implementation plans can use generated DTO/constants rather than hand-written wire shapes.
- Downstream Python ingestion receives enough event metadata to index without querying Java.
</success_criteria>

<output>
After completion, create `.planning/phases/03-documents-events-audit/03-01-SUMMARY.md`.
</output>
