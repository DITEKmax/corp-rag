---
phase: "07-evaluation-observability"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/uv.lock"
  - "ai-service/src/corp_rag_ai/config.py"
  - "ai-service/src/corp_rag_ai/observability"
  - "ai-service/src/corp_rag_ai/agent/graph.py"
  - "ai-service/src/corp_rag_ai/pipeline/generation/synthesizer.py"
  - "ai-service/src/corp_rag_ai/main.py"
  - "ai-service/tests"
autonomous: true
requirements: ["OPS-01", "EVAL-02"]
user_setup:
  - "Docker verification requires Langfuse reachable at the configured local compose URL."
  - "Do not commit real Langfuse or OpenRouter secrets."
must_haves:
  truths:
    - "One root Langfuse trace is created for each `/v1/query` request when tracing is configured."
    - "Default to the legacy Langfuse Python SDK v2 line (`langfuse~=2.x`) with the existing `langfuse/langfuse:2.95.11` container."
    - "Do not upgrade the Langfuse container to v3 in Phase 7; a v3 platform upgrade and its new infrastructure belong outside this tracing task."
    - "Child spans match the real graph node names and carry retrieval metadata and guard verdicts without secrets."
    - "The synthesis LLM call is traced as a generation with prompt, output, model id, latency, and token usage where available."
    - "`/diagnostics` remains read-only and internal, with lightweight counters and Langfuse health/configuration status only."
    - "No frontend observability screen, Prometheus, or Grafana is added."
  artifacts:
    - path: "ai-service/src/corp_rag_ai/observability"
      provides: "Langfuse helper abstraction"
      contains: "no-op behavior when unconfigured plus explicit span/generation helpers"
    - path: "ai-service/src/corp_rag_ai/agent/graph.py"
      provides: "query graph tracing"
      contains: "root trace and node child spans"
    - path: "ai-service/src/corp_rag_ai/main.py"
      provides: "diagnostics metrics"
      contains: "query counters, refusal rates, mean latency, Langfuse status"
  decision_refs:
    - "D-365"
    - "D-366"
    - "D-367"
    - "D-368"
    - "D-369"
    - "D-370"
    - "D-371"
    - "D-372"
    - "D-373"
    - "D-374"
    - "D-375"
    - "D-380"
    - "D-384"
---

<objective>
Add Langfuse tracing and lightweight service metrics so every production query and later eval run has inspectable pipeline evidence.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-evaluation-observability/07-CONTEXT.md
@.planning/phases/07-evaluation-observability/07-RESEARCH.md
@docs/decisions/ADR-006-degraded-mode-policy.md
@ai-service/src/corp_rag_ai/agent/graph.py
@ai-service/src/corp_rag_ai/pipeline/generation/synthesizer.py
@ai-service/src/corp_rag_ai/main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Resolve Langfuse SDK compatibility</name>
  <files>ai-service/pyproject.toml, ai-service/uv.lock, infra/docker-compose.yml, .planning/phases/07-evaluation-observability/07-02-SUMMARY.md</files>
  <read_first>.planning/phases/07-evaluation-observability/07-RESEARCH.md, infra/docker-compose.yml, ai-service/pyproject.toml</read_first>
  <action>Before coding spans, pin the Python dependency to the legacy Langfuse SDK v2 line (`langfuse~=2.x`) and verify it against the current `langfuse/langfuse:2.95.11` compose image. Do not upgrade the Langfuse container to v3 in this phase. If the v2 SDK genuinely cannot create the required root trace, node spans, and generation against the current stack, stop and record an escalated blocker instead of silently upgrading production-like infrastructure. Record exact package/image versions in the summary.</action>
  <verify>
    <automated>uv lock --project ai-service</automated>
    <automated>docker compose -f infra/docker-compose.yml config</automated>
  </verify>
  <acceptance_criteria>
    - `ai-service` pins `langfuse~=2.x`, not SDK v3.
    - The existing `langfuse/langfuse:2.95.11` container remains unchanged.
    - The v2 client can create root traces, node spans, and generations against local Langfuse in Docker, or the summary records an escalated blocker.
    - No secrets are committed.
  </acceptance_criteria>
  <done>Langfuse client choice is proven before instrumentation spreads through the graph.</done>
</task>

<task type="auto">
  <name>Task 2: Add observability helper and root query trace</name>
  <files>ai-service/src/corp_rag_ai/observability, ai-service/src/corp_rag_ai/config.py, ai-service/src/corp_rag_ai/main.py, ai-service/src/corp_rag_ai/adapters/rest/query.py, ai-service/tests</files>
  <read_first>ai-service/src/corp_rag_ai/main.py, ai-service/src/corp_rag_ai/adapters/rest/query.py, ai-service/src/corp_rag_ai/config.py</read_first>
  <action>Create a small Langfuse helper that initializes from existing settings, no-ops safely when credentials/host are absent or local placeholders, and provides async-safe context managers. Wrap each `/v1/query` request in one root trace/span tagged with correlation id, route/session metadata where safe, model id, and eval/run tags when present.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_query_api.py ai-service/tests/test_diagnostics.py</automated>
  </verify>
  <acceptance_criteria>
    - Query behavior is identical when Langfuse is disabled or unreachable.
    - Trace metadata excludes prompts, answers, quotes, and secrets except inside explicitly configured generation observation payloads.
    - Existing `/v1/query` tests still pass.
  </acceptance_criteria>
  <done>Every query has a trace root when observability is enabled.</done>
</task>

<task type="auto">
  <name>Task 3: Instrument graph nodes and synthesis generation</name>
  <files>ai-service/src/corp_rag_ai/agent/graph.py, ai-service/src/corp_rag_ai/pipeline/generation/synthesizer.py, ai-service/tests</files>
  <read_first>ai-service/src/corp_rag_ai/agent/graph.py, ai-service/src/corp_rag_ai/pipeline/generation/synthesizer.py, docs/decisions/ADR-007-citation-contract-and-refusal-rules.md</read_first>
  <action>Add explicit span context managers around each graph node. Capture retrieval metadata fields, guard verdicts, route decisions, reranker degradation, and node latency. Trace the OpenRouter synthesis call as a generation with input prompt, output, model id, and token usage if returned by the OpenAI-compatible client. Keep strict output guard validation unchanged.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_query_graph.py ai-service/tests/test_query_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - Span names exactly match required node names.
    - Generation observation is nested under `synthesize`.
    - Citation validation behavior and refusal reasons do not change.
  </acceptance_criteria>
  <done>Langfuse shows the real query graph structure.</done>
</task>

<task type="auto">
  <name>Task 4: Add diagnostics counters</name>
  <files>ai-service/src/corp_rag_ai/main.py, ai-service/src/corp_rag_ai/observability, ai-service/tests/test_diagnostics.py, ai-service/README.md, infra/README.md</files>
  <read_first>ai-service/tests/test_diagnostics.py, ai-service/README.md</read_first>
  <action>Extend `/diagnostics` with aggregate counters: queries, answered count/rate, refused/no-evidence count, guard-blocked count, reranker-degraded count, mean latency, and Langfuse configured/reachable health. Keep the endpoint unauthenticated-internal and avoid live paid calls.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_diagnostics.py</automated>
  </verify>
  <acceptance_criteria>
    - Existing readiness booleans remain present.
    - Counters are process-local and read-only.
    - Diagnostics tests do not require Langfuse or OpenRouter network access.
  </acceptance_criteria>
  <done>Demo has lightweight query metrics without a new metrics stack.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Trace client breaks queries | Langfuse integration | Helper no-ops on missing/unreachable client and never raises into query path. |
| Sensitive data leaks | spans/generations | Store secrets never; only generation prompt/output is captured where explicitly required. |
| Incompatible Langfuse SDK | Docker self-host | Pin SDK v2 against the existing v2 container; if that fails, escalate instead of upgrading the stack. |
</threat_model>

<verification>
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_query_api.py ai-service/tests/test_query_graph.py ai-service/tests/test_query_pipeline.py ai-service/tests/test_diagnostics.py`
- [ ] Docker stack shows at least one query trace in Langfuse or summary records the exact blocker.
- [ ] `/diagnostics` returns both existing readiness flags and new counters.
</verification>

<success_criteria>
- OPS-01 observability surface is implemented without frontend scope creep.
- Traces expose per-node latency and synthesis generation evidence.
- Diagnostics provide demo-honest counters.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-observability/07-02-SUMMARY.md` with SDK/image compatibility, tests, and Docker trace evidence.
</output>
