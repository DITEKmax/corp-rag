---
phase: "07-evaluation-observability"
plan: "03"
type: "execute"
wave: 2
depends_on: ["01"]
files_modified:
  - "ai-service/eval"
  - "ai-service/eval/corpus"
  - "ai-service/eval/golden/golden_ru.meta.json"
  - "ai-service/eval/reports"
  - "ai-service/tests"
autonomous: false
requirements: ["EVAL-01", "EVAL-02"]
user_setup: []
must_haves:
  truths:
    - "The 16-document Russian demo corpus is frozen and committed before golden answers are authored."
    - "Corpus metadata includes a stable version/hash and is surfaced by all later reports."
    - "Runnable eval code and generated reports live under `ai-service/eval/`, not `.planning/`."
    - "JSON/Markdown report writers produce committed artifacts with corpus hash, model id, runner config, timestamp, and external-judge usage."
  artifacts:
    - path: "ai-service/eval/corpus"
      provides: "frozen Russian aviation/logistics demo corpus"
      contains: "16 committed source documents and manifest metadata"
    - path: "ai-service/eval/golden/golden_ru.meta.json"
      provides: "corpus binding metadata"
      contains: "corpus version/hash, generation timestamp, source manifest"
    - path: "ai-service/eval/reporting.py"
      provides: "shared report writer"
      contains: "Markdown, JSON, and CSV output helpers"
  decision_refs:
    - "D-331"
    - "D-332"
    - "D-333"
    - "D-334"
    - "D-335"
    - "D-336"
    - "D-337"
    - "D-338"
    - "D-339"
    - "D-340"
    - "D-341"
    - "D-351"
    - "D-352"
    - "D-353"
    - "D-380"
---

<objective>
Create the eval harness foundation and freeze the Russian demo corpus snapshot that all golden data and reports bind to.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-evaluation-observability/07-CONTEXT.md
@.planning/phases/07-evaluation-observability/07-RESEARCH.md
@docs/ARCHITECTURE.md
@ai-service/pyproject.toml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add eval package skeleton and schemas</name>
  <files>ai-service/eval/__init__.py, ai-service/eval/schema.py, ai-service/eval/io.py, ai-service/eval/reports/.gitkeep, ai-service/tests</files>
  <read_first>.planning/phases/07-evaluation-observability/07-CONTEXT.md, docs/ARCHITECTURE.md</read_first>
  <action>Add the `ai-service/eval/` package with typed schema helpers for golden records, corpus metadata, runner configuration, metric summaries, and report outputs. Keep runners importable without starting FastAPI or live stores.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests</automated>
  </verify>
  <acceptance_criteria>
    - Schema validates required golden fields and `expected_outcome` enum.
    - Importing eval helpers has no network side effects.
  </acceptance_criteria>
  <done>Eval code has a stable local module boundary.</done>
</task>

<task type="manual">
  <name>Task 2: Author and freeze the Russian corpus snapshot</name>
  <files>ai-service/eval/corpus, ai-service/eval/golden/golden_ru.meta.json</files>
  <read_first>.planning/phases/07-evaluation-observability/07-CONTEXT.md</read_first>
  <action>Author or refresh the 16 synthetic Russian aviation/logistics documents as domain content, then commit the frozen snapshot under `ai-service/eval/corpus/`. "Deterministic" means committed and content-hashed, not generated from a templated randomizer. Compute a corpus hash over committed document contents and store it in `golden_ru.meta.json`. Do not create golden answers in this plan.</action>
  <verify>
    <automated>cd ai-service; uv run python -m eval.validate_corpus</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 16 corpus documents are present.
    - Documents are human-authored domain content suitable for the aviation/logistics demo, not templated filler emitted by an autonomous generator.
    - Corpus manifest/hash changes if any source document content changes.
    - Metadata states that golden authoring must happen after this snapshot is indexed.
  </acceptance_criteria>
  <done>Golden authoring can bind to a committed corpus version.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared report writer</name>
  <files>ai-service/eval/reporting.py, ai-service/eval/reports/README.md, ai-service/tests</files>
  <read_first>.planning/phases/07-evaluation-observability/07-CONTEXT.md</read_first>
  <action>Add reusable report helpers that write Markdown and JSON summaries, with optional CSV detail files. Every report must include corpus hash/version, model id, eval timestamp, runner configuration, and whether external LLM judge calls were used.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_reporting.py</automated>
  </verify>
  <acceptance_criteria>
    - Reports are deterministic except for explicit timestamps/run ids.
    - Report paths stay under `ai-service/eval/reports/`.
  </acceptance_criteria>
  <done>Later eval plans share one report contract.</done>
</task>

<task type="manual">
  <name>Task 4: Index the frozen corpus for downstream plans</name>
  <files>.planning/phases/07-evaluation-observability/07-03-SUMMARY.md</files>
  <read_first>ai-service/eval/golden/golden_ru.meta.json, infra/README.md</read_first>
  <action>Run or document the exact local stack/indexing commands that load the frozen corpus into Qdrant and Neo4j. Record the corpus hash, collection/index status, document ids, and any blockers. Golden answers in Plan 07-05 must not be authored until this step is done.</action>
  <verify>
    <manual>Docker stack indexing smoke proves the 16 frozen documents are indexed.</manual>
  </verify>
  <acceptance_criteria>
    - Summary records document ids used by golden records.
    - If indexing cannot run, Plan 07-05 must be marked blocked until it can.
  </acceptance_criteria>
  <done>Frozen corpus is ready for golden question authoring.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Golden data binds to moving docs | corpus authoring | Commit corpus before golden authoring and store content hash. |
| Report drift | runner outputs | Shared writer enforces common metadata. |
| Eval code becomes production API | package boundary | Keep all runners under `ai-service/eval/` and import internals directly. |
</threat_model>

<verification>
- [ ] Corpus validator confirms exactly 16 Russian documents and manifest hash.
- [ ] Report writer tests pass.
- [ ] Summary records indexing status or a clear blocker.
</verification>

<success_criteria>
- EVAL-01 has a frozen corpus foundation.
- Later golden records and reports can cite one corpus hash/version.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-observability/07-03-SUMMARY.md`.
</output>
