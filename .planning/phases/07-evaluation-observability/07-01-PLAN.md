---
phase: "07-evaluation-observability"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "infra/docker-compose.yml"
  - "infra/.env.example"
  - "ai-service/src/corp_rag_ai/config.py"
  - "ai-service/src/corp_rag_ai/service/query"
  - "ai-service/tests"
autonomous: true
requirements: ["OPS-01", "EVAL-02", "EVAL-04"]
user_setup: []
must_haves:
  truths:
    - "Phase 7 starts with runtime stability before eval execution."
    - "The local Python AI memory contour is set from observed Docker behavior, approximately 8-9 GiB."
    - "Cold-start and model pre-warm work cannot call the external judge or change answer semantics."
    - "Access filters and strict citation validation remain unchanged while stability work is added."
  artifacts:
    - path: "infra/docker-compose.yml"
      provides: "Python AI memory contour"
      contains: "8-9 GiB local model budget if verified as needed"
    - path: "ai-service/tests"
      provides: "stability regression coverage"
      contains: "pre-warm, timeout, and diagnostics behavior"
  decision_refs:
    - "D-376"
    - "D-377"
    - "D-378"
    - "D-379"
    - "D-380"
    - "D-381"
    - "D-382"
---

<objective>
Prepare the local runtime for credible evaluation: set the Python AI memory contour, diagnose first-turn cold start, and add a safe model pre-warm path before any quality or ablation reports are generated.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-evaluation-observability/07-CONTEXT.md
@.planning/phases/07-evaluation-observability/07-RESEARCH.md
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md
@infra/docker-compose.yml
@ai-service/src/corp_rag_ai/config.py
@ai-service/src/corp_rag_ai/service/query/factory.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Measure and document the current cold-start contour</name>
  <files>.planning/phases/07-evaluation-observability/07-01-SUMMARY.md</files>
  <read_first>infra/docker-compose.yml, .planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md</read_first>
  <action>Record the existing Python AI memory limit, reranker settings, and known first-turn behavior. Use Docker diagnostics/logs if the stack is available; otherwise record the exact command sequence the executor could not run. Do not run RAGAS or golden evaluation in this plan.</action>
  <verify>
    <automated>docker compose -f infra/docker-compose.yml config</automated>
  </verify>
  <acceptance_criteria>
    - Current memory contour and cold-start risk are explicitly recorded.
    - Any unavailable Docker evidence is called out instead of guessed.
  </acceptance_criteria>
  <done>Runtime baseline is known before changes.</done>
</task>

<task type="auto">
  <name>Task 2: Raise Python AI memory contour if needed</name>
  <files>infra/docker-compose.yml, infra/.env.example, infra/README.md</files>
  <read_first>infra/docker-compose.yml, infra/.env.example</read_first>
  <action>Set the local `python-ai` memory contour to the smallest value in the 8-9 GiB range that supports bge-m3 plus reranker evaluation. Keep the change local-compose oriented, document it in infra README/env examples, and do not add production monitoring infrastructure.</action>
  <verify>
    <automated>docker compose -f infra/docker-compose.yml config</automated>
  </verify>
  <acceptance_criteria>
    - Compose validates with the new memory settings.
    - The setting remains documented as a local evaluation/runtime contour.
  </acceptance_criteria>
  <done>Docker has enough local memory for Phase 7 eval runs.</done>
</task>

<task type="auto">
  <name>Task 3: Add safe pre-warm hooks</name>
  <files>ai-service/src/corp_rag_ai/config.py, ai-service/src/corp_rag_ai/service/query, ai-service/tests</files>
  <read_first>ai-service/src/corp_rag_ai/service/query/factory.py, ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py, ai-service/src/corp_rag_ai/pipeline/indexing/embedding.py</read_first>
  <action>Add a bounded, opt-out-safe pre-warm hook for local embedding/reranker components used by query/eval. The hook must not call OpenRouter, RAGAS, Langfuse, or any external paid service. Preserve existing lazy-degraded behavior if pre-warm fails or times out.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_reranker.py ai-service/tests/test_diagnostics.py</automated>
  </verify>
  <acceptance_criteria>
    - Pre-warm failures do not prevent startup unless an existing hard dependency already would.
    - Reranker degradation metadata still works.
    - Default tests do not require model downloads.
  </acceptance_criteria>
  <done>First-turn latency is reduced without changing query safety semantics.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Eval numbers distorted by cold start | first query latency | Pre-warm local models and record runner timestamps. |
| Local machine OOM | Docker memory | Set explicit 8-9 GiB contour and document prerequisite. |
| Safety regression from stability work | query pipeline | Keep access filters and citation/output guard logic untouched. |
</threat_model>

<verification>
- [ ] `docker compose -f infra/docker-compose.yml config`
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_reranker.py ai-service/tests/test_diagnostics.py`
- [ ] Summary records whether live Docker cold-start evidence was collected.
</verification>

<success_criteria>
- Python AI local runtime has a documented memory contour suitable for Phase 7.
- First-turn model behavior is measured or explicitly deferred with commands.
- No eval reports are produced before the corpus and judge are ready.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-observability/07-01-SUMMARY.md`.
</output>
