---
phase: "07-evaluation-observability"
plan: "04"
type: "execute"
wave: 3
depends_on: ["03"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/uv.lock"
  - "ai-service/src/corp_rag_ai/pipeline/indexing/vector_indexer.py"
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/hybrid.py"
  - "ai-service/eval/retrieval_modes.py"
  - "ai-service/eval/bm25.py"
  - "ai-service/tests"
autonomous: true
requirements: ["EVAL-04", "EVAL-02"]
user_setup: []
must_haves:
  truths:
    - "Retrieval modes are eval-only and do not expose a production `/v1/query` mode switch."
    - "`bm25` is classical lexical retrieval over the same frozen chunk corpus, not bge-m3 learned sparse vectors."
    - "`sparse` is Qdrant bge-m3 learned sparse retrieval only."
    - "`dense`, `sparse`, `hybrid`, and `hybrid+reranker` apply the same access filter semantics as production retrieval."
    - "Ablation scope here is retrieval metrics only, not RAGAS across every variant."
  artifacts:
    - path: "ai-service/eval/retrieval_modes.py"
      provides: "eval-only vector retrieval runner"
      contains: "bm25, dense, sparse, hybrid, hybrid+reranker modes"
    - path: "ai-service/eval/bm25.py"
      provides: "classical lexical baseline"
      contains: "BM25 index over frozen chunk corpus"
  decision_refs:
    - "D-354"
    - "D-355"
    - "D-356"
    - "D-357"
    - "D-358"
    - "D-359"
    - "D-360"
    - "D-361"
    - "D-381"
    - "D-383"
---

<objective>
Add eval-only retrieval-mode support and a BM25 harness so the five-way retrieval ablation can run on the frozen corpus without changing production query behavior.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-evaluation-observability/07-CONTEXT.md
@.planning/phases/07-evaluation-observability/07-RESEARCH.md
@docs/decisions/ADR-001-embedding-model.md
@docs/decisions/ADR-002-vector-database.md
@ai-service/src/corp_rag_ai/pipeline/indexing/vector_indexer.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/hybrid.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add internal vector retrieval modes</name>
  <files>ai-service/src/corp_rag_ai/pipeline/indexing/vector_indexer.py, ai-service/eval/retrieval_modes.py, ai-service/tests/test_vector_indexer_query.py</files>
  <read_first>ai-service/src/corp_rag_ai/pipeline/indexing/vector_indexer.py, ai-service/tests/test_vector_indexer_query.py</read_first>
  <action>Add an internal enum or parameter that lets eval callers request dense-only, sparse-only, or hybrid RRF query construction. Keep the default production path exactly hybrid RRF. Do not add any public FastAPI, Java, or frontend mode switch.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_vector_indexer_query.py</automated>
    <manual>Docker-backed Qdrant smoke: dense-only and sparse-only eval retrieval each return at least one payload with non-empty `documentId` from the frozen indexed corpus.</manual>
  </verify>
  <acceptance_criteria>
    - Dense mode emits only the dense vector query/prefetch.
    - Sparse mode emits only the sparse vector query/prefetch.
    - Hybrid mode preserves existing dense+sparse RRF behavior.
    - Access filters are applied identically in all Qdrant modes.
    - Dense-only and sparse-only Qdrant modes return payloads containing `documentId`, so recall@k and MRR over `expected_doc_ids[]` cannot be computed against empty ids.
    - Docker verification proves dense-only and sparse-only payloads include `documentId`; mock-only verification is insufficient for this payload contract.
  </acceptance_criteria>
  <done>Eval can isolate learned dense and learned sparse retrieval.</done>
</task>

<task type="auto">
  <name>Task 2: Add BM25 baseline harness</name>
  <files>ai-service/pyproject.toml, ai-service/uv.lock, ai-service/eval/bm25.py, ai-service/tests/test_eval_bm25.py</files>
  <read_first>.planning/phases/07-evaluation-observability/07-RESEARCH.md, docs/ARCHITECTURE.md</read_first>
  <action>Add a classical BM25 implementation, preferably `bm25s` unless compatibility says otherwise. Build the index from the same frozen chunk corpus used by vector retrieval and preserve document id/chunk metadata needed for recall@k and MRR. Keep BM25 under `ai-service/eval/` only.</action>
  <verify>
    <automated>uv lock --project ai-service</automated>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_bm25.py</automated>
  </verify>
  <acceptance_criteria>
    - BM25 ranks chunks/documents without Qdrant.
    - BM25 and learned sparse are represented by distinct mode names and report columns.
    - No production dependency path imports BM25 harness code.
  </acceptance_criteria>
  <done>Classical lexical ablation baseline is available.</done>
</task>

<task type="auto">
  <name>Task 3: Add retrieval metric helpers</name>
  <files>ai-service/eval/metrics.py, ai-service/tests/test_eval_metrics.py</files>
  <read_first>.planning/phases/07-evaluation-observability/07-CONTEXT.md</read_first>
  <action>Implement deterministic retrieval metrics over `expected_doc_ids[]`: recall@5, recall@10, MRR, and optional nDCG if the schema supports graded relevance. Unit-test duplicate document handling, no-hit cases, and multi-doc expected answers.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_metrics.py</automated>
  </verify>
  <acceptance_criteria>
    - Metrics operate at document level, not hard chunk ids.
    - Expected chunk hints are ignored by metric scoring.
  </acceptance_criteria>
  <done>Cheap deterministic ablation metrics are ready.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Eval switch leaks to production | retrieval mode | Keep mode entry point in `ai-service/eval/` and default production path unchanged. |
| BM25 conflated with learned sparse | reports | Distinct implementation, mode names, and report columns. |
| Access filter regression | eval retrieval | Reuse existing Qdrant filters and test all modes. |
</threat_model>

<verification>
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_vector_indexer_query.py ai-service/tests/test_eval_bm25.py ai-service/tests/test_eval_metrics.py`
- [ ] Docker-backed smoke proves dense-only and sparse-only Qdrant retrieval return payload `documentId` values.
- [ ] Static scan confirms no `/v1/query` or Java/frontend retrieval-mode surface was added.
</verification>

<success_criteria>
- EVAL-04 code prerequisites are present.
- Retrieval metrics can be computed without LLM judge calls.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-observability/07-04-SUMMARY.md`.
</output>
