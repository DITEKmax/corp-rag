---
phase: "07-evaluation-observability"
plan: "07"
type: "execute"
wave: 5
depends_on: ["04", "05"]
files_modified:
  - "ai-service/eval/ablation_runner.py"
  - "ai-service/eval/retrieval_modes.py"
  - "ai-service/eval/reports"
  - "ai-service/tests"
autonomous: false
requirements: ["EVAL-04", "EVAL-02"]
user_setup:
  - "Ablation requires the frozen corpus indexed in Qdrant and BM25 harness built from the same corpus snapshot."
must_haves:
  truths:
    - "The five-way ablation is retrieval-only and runs on vector-routed golden records only."
    - "The runner must derive or verify the vector-routed subset from actual route/retrieval metadata, not blindly average by type."
    - "Graph-routed records are reported separately, not folded into vector retrieval recall/MRR."
    - "Metrics are document-level recall@5, recall@10, MRR, and optional nDCG over `expected_doc_ids[]`."
    - "BM25, learned sparse, dense, hybrid, and hybrid+reranker remain distinct report columns."
  artifacts:
    - path: "ai-service/eval/ablation_runner.py"
      provides: "retrieval ablation runner"
      contains: "vector subset selection, five-mode scoring, graph separate report"
    - path: "ai-service/eval/reports/ablation_ru.md"
      provides: "human-readable ablation report"
      contains: "mode comparison and graph-route section"
    - path: "ai-service/eval/reports/ablation_ru.csv"
      provides: "machine-readable per-query retrieval metrics"
      contains: "mode, rank, expected docs, retrieved docs, recall/MRR"
  decision_refs:
    - "D-348"
    - "D-351"
    - "D-352"
    - "D-353"
    - "D-354"
    - "D-355"
    - "D-356"
    - "D-357"
    - "D-358"
    - "D-359"
    - "D-360"
    - "D-361"
    - "D-362"
    - "D-363"
    - "D-364"
    - "D-380"
    - "D-381"
    - "D-383"
---

<objective>
Run the cheap retrieval ablation matrix and separate graph-route retrieval report without invoking RAGAS for every variant.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-evaluation-observability/07-CONTEXT.md
@.planning/phases/07-evaluation-observability/07-RESEARCH.md
@.planning/phases/07-evaluation-observability/07-04-SUMMARY.md
@ai-service/eval/golden/golden_ru.jsonl
@ai-service/src/corp_rag_ai/agent/graph.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py
@ai-service/eval/retrieval_modes.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build ablation subset selector</name>
  <files>ai-service/eval/ablation_runner.py, ai-service/tests/test_eval_ablation_subset.py</files>
  <read_first>ai-service/eval/golden/golden_ru.jsonl, ai-service/src/corp_rag_ai/agent/graph.py</read_first>
  <action>Implement subset selection that starts from the golden records intended for vector retrieval, then verifies route behavior through the production router or recorded `retrievalMeta.retrieversUsed`. If current routing sends a nominal type to graph, record the discrepancy in the report and exclude it from the vector matrix. Do not average graph records into vector metrics.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_ablation_subset.py</automated>
  </verify>
  <acceptance_criteria>
    - Report lists included vector ids and excluded graph ids explicitly.
    - Subset selection is deterministic and auditable.
  </acceptance_criteria>
  <done>Vector ablation scope is honest and reproducible.</done>
</task>

<task type="auto">
  <name>Task 2: Implement five-mode retrieval scoring</name>
  <files>ai-service/eval/ablation_runner.py, ai-service/eval/retrieval_modes.py, ai-service/eval/reports, ai-service/tests/test_eval_ablation_runner.py</files>
  <read_first>ai-service/eval/retrieval_modes.py, ai-service/eval/metrics.py, ai-service/eval/reporting.py</read_first>
  <action>Run `bm25`, `dense`, `sparse`, `hybrid`, and `hybrid+reranker` over the vector-routed subset. Collect retrieved chunk ids, document ids, ranks, scores where meaningful, recall@5, recall@10, MRR, runner config, and corpus hash. Keep reranker degradation visible in the report.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_ablation_runner.py ai-service/tests/test_eval_metrics.py</automated>
  </verify>
  <acceptance_criteria>
    - Learned sparse and BM25 are not conflated.
    - `hybrid+reranker` uses production-style reranker only after hybrid retrieval.
    - Deterministic metric tests do not call external LLM judges.
  </acceptance_criteria>
  <done>Vector retrieval comparison is available.</done>
</task>

<task type="auto">
  <name>Task 3: Add separate graph-route report</name>
  <files>ai-service/eval/ablation_runner.py, ai-service/eval/reports, ai-service/tests/test_eval_graph_report.py</files>
  <read_first>ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py, ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py</read_first>
  <action>Report graph-routed records separately: citeable evidence found, expected document overlap where applicable, no-evidence behavior, guard/refusal behavior, and graph retrieval warnings. Do not compute or publish a single averaged score that mixes graph and vector mechanisms.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_graph_report.py</automated>
  </verify>
  <acceptance_criteria>
    - Graph section explains it is not comparable to vector ablation modes.
    - Graph evidence remains document-text backed.
  </acceptance_criteria>
  <done>Graph retrieval quality is visible without corrupting vector ablation metrics.</done>
</task>

<task type="manual">
  <name>Task 4: Execute ablation and commit reports</name>
  <files>ai-service/eval/reports/ablation_ru.md, ai-service/eval/reports/ablation_ru.json, ai-service/eval/reports/ablation_ru.csv, .planning/phases/07-evaluation-observability/07-07-SUMMARY.md</files>
  <read_first>ai-service/eval/golden/golden_ru.jsonl, ai-service/eval/golden/golden_ru.meta.json</read_first>
  <action>Run the ablation against the indexed frozen corpus. Commit Markdown, JSON, and CSV reports. Record exact included/excluded ids, corpus hash, model ids, mode configuration, Docker/index status, and deterministic metric values.</action>
  <verify>
    <manual>Reports exist and include all five vector modes plus a separate graph section.</manual>
  </verify>
  <acceptance_criteria>
    - Report never claims RAGAS was run for all variants.
    - The vector subset is auditable by id.
    - Graph results are separate.
  </acceptance_criteria>
  <done>EVAL-04 ablation evidence is committed.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Meaningless averaged metric | route mix | Separate vector matrix and graph section. |
| Production mode leak | eval modes | Runner imports eval helpers only; no API/env production switch. |
| Retrieval recall inflated | access filters | Reuse normal access filter and document-level expected ids. |
</threat_model>

<verification>
- [ ] Ablation unit tests pass.
- [ ] `ablation_ru.md`, `ablation_ru.json`, and `ablation_ru.csv` are present or summary records blocker.
- [ ] Report shows vector subset ids and graph-route ids separately.
</verification>

<success_criteria>
- EVAL-04 has a five-way vector retrieval comparison.
- EVAL-02 retrieval metrics are reported without extra LLM-judge cost.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-observability/07-07-SUMMARY.md`.
</output>
