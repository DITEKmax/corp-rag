---
phase: "07-evaluation-observability"
plan: "06"
type: "execute"
wave: 5
depends_on: ["02", "05"]
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/uv.lock"
  - "ai-service/eval/ragas_runner.py"
  - "ai-service/eval/query_client.py"
  - "ai-service/eval/reports"
  - "ai-service/tests"
autonomous: false
requirements: ["EVAL-02", "OPS-01"]
user_setup:
  - "Live RAGAS execution requires OpenRouter/DeepSeek judge credentials and a running production `/v1/query` service."
  - "Generated reports may include paid/network LLM judge calls; do not run them as hard CI gates."
must_haves:
  truths:
    - "RAGAS runs once over the full 40-question golden set through production `/v1/query`."
    - "Do not restrict RAGAS to vector questions; graph-routed and out-of-scope records remain in the quality run."
    - "`hybrid+reranker` for RAGAS means normal production configuration with reranker enabled where the production route uses vector retrieval."
    - "RAGAS thresholds are report flags, not build failures."
    - "Reports include faithfulness, answer/response relevancy, context precision, context recall, corpus hash, model id, timestamp, and external judge usage."
  artifacts:
    - path: "ai-service/eval/ragas_runner.py"
      provides: "production quality runner"
      contains: "full 40-record `/v1/query` execution and RAGAS evaluation"
    - path: "ai-service/eval/reports/ragas_ru.md"
      provides: "human-readable quality report"
      contains: "metric summaries, deviations, full-set route mix, and corpus metadata"
    - path: "ai-service/eval/reports/ragas_ru.json"
      provides: "machine-readable quality summary"
      contains: "per-question outcomes and aggregate metrics"
  decision_refs:
    - "D-342"
    - "D-343"
    - "D-344"
    - "D-345"
    - "D-346"
    - "D-347"
    - "D-348"
    - "D-351"
    - "D-352"
    - "D-353"
    - "D-361"
    - "D-362"
    - "D-380"
    - "D-381"
    - "D-382"
---

<objective>
Build and run the production-config RAGAS quality evaluation over all 40 golden questions through `/v1/query`.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-evaluation-observability/07-CONTEXT.md
@.planning/phases/07-evaluation-observability/07-RESEARCH.md
@.planning/phases/07-evaluation-observability/07-05-SUMMARY.md
@ai-service/eval/golden/golden_ru.jsonl
@ai-service/src/corp_rag_ai/adapters/rest/query.py
@docs/decisions/ADR-004-llm-provider-deepseek-openrouter.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add RAGAS dependency and query client</name>
  <files>ai-service/pyproject.toml, ai-service/uv.lock, ai-service/eval/query_client.py, ai-service/tests/test_eval_query_client.py</files>
  <read_first>.planning/phases/07-evaluation-observability/07-RESEARCH.md, contracts/openapi/ai-service-v1.yaml</read_first>
  <action>Add RAGAS in the dependency set and create an eval query client that calls production `/v1/query` with normal retrieval options. The client must collect answers, citations, retrieved contexts, refusal outcomes, retrieval metadata, trace ids where available, and per-question latency. It must not call internal retrieval modes.</action>
  <verify>
    <automated>uv lock --project ai-service</automated>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_query_client.py</automated>
  </verify>
  <acceptance_criteria>
    - Client can be tested with mocked HTTP responses.
    - Reranker is enabled through production config, not an eval-only override.
    - Out-of-scope/refused records are preserved in the run output.
  </acceptance_criteria>
  <done>Production query sampling is available for the quality runner.</done>
</task>

<task type="auto">
  <name>Task 2: Build full-set RAGAS runner</name>
  <files>ai-service/eval/ragas_runner.py, ai-service/tests/test_eval_ragas_runner.py</files>
  <read_first>ai-service/eval/golden/golden_ru.jsonl, ai-service/eval/reporting.py</read_first>
  <action>Implement the RAGAS runner over all 40 golden records. Map successful answers to RAGAS samples with question, answer, retrieved contexts, and reference answer. For refused records, record outcome correctness and include them in report coverage even if some LLM-judge metrics are not applicable. Flag expected range deviations in the report but do not exit non-zero solely for stochastic metric misses.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_ragas_runner.py</automated>
  </verify>
  <acceptance_criteria>
    - Runner refuses to run if corpus hash/meta is missing or mismatched.
    - Report shows all 40 ids and route/outcome distribution.
    - Metric thresholds are warnings in output, not CI failures.
  </acceptance_criteria>
  <done>RAGAS quality runner is ready for live execution.</done>
</task>

<task type="manual">
  <name>Task 3: Execute live RAGAS run and commit reports</name>
  <files>ai-service/eval/reports/ragas_ru.md, ai-service/eval/reports/ragas_ru.json, .planning/phases/07-evaluation-observability/07-06-SUMMARY.md</files>
  <read_first>ai-service/eval/golden/golden_ru.jsonl, ai-service/eval/golden/golden_ru.meta.json</read_first>
  <action>Run the full 40-question RAGAS evaluation against the Docker service using DeepSeek/OpenRouter as judge. Record exact command, environment prerequisites, corpus hash, model ids, total cost/token usage if available, route mix, metric values, threshold flags, and any skipped/unjudgeable refused records. Do not tune prompts or guards solely to improve the score.</action>
  <verify>
    <manual>Live report files exist and include all 40 golden ids.</manual>
  </verify>
  <acceptance_criteria>
    - Markdown and JSON reports are committed under `ai-service/eval/reports/`.
    - The report states external LLM judge calls were used.
    - No hard CI gate is introduced for RAGAS.
  </acceptance_criteria>
  <done>Full production quality baseline is recorded.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Misleading vector-only quality score | RAGAS scope | Runner requires all 40 ids and production `/v1/query`. |
| Flaky CI from LLM judge | thresholds | Emit deviations in reports; do not gate CI. |
| Access filter loosened for quality | query client | Use production request path and existing access filter config. |
</threat_model>

<verification>
- [ ] Unit tests for query client and runner pass.
- [ ] Live `ragas_ru.md` and `ragas_ru.json` exist or summary records exact blocker.
- [ ] Report includes all 40 golden ids.
</verification>

<success_criteria>
- EVAL-02 has a production answer-quality baseline.
- OPS-01 trace/latency evidence is available during the quality run when Langfuse is configured.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-observability/07-06-SUMMARY.md`.
</output>
