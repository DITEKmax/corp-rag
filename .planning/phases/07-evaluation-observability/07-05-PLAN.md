---
phase: "07-evaluation-observability"
plan: "05"
type: "execute"
wave: 4
depends_on: ["03", "04"]
files_modified:
  - "ai-service/eval/golden/golden_ru.jsonl"
  - "ai-service/eval/golden/golden_ru.meta.json"
  - "ai-service/eval/validate_golden.py"
  - "ai-service/tests"
autonomous: false
requirements: ["EVAL-01", "EVAL-02", "EVAL-03"]
user_setup:
  - "The frozen corpus from Plan 07-03 must already be committed and indexed."
  - "Golden authoring may require reading indexed document ids and corpus content."
must_haves:
  truths:
    - "The scored golden dataset is Russian-only and monolingual."
    - "The dataset has exactly 40 records: 10 factual, 10 aggregation, 10 multi-hop, and 10 out-of-scope."
    - "Out-of-scope includes roughly 6 no-evidence records and 4 guard-refusal records."
    - "`expected_doc_ids[]` is authoritative for retrieval metrics; `expected_chunk_hint` is advisory only."
    - "No English Phase 5 smoke docs are counted in the scored dataset."
  artifacts:
    - path: "ai-service/eval/golden/golden_ru.jsonl"
      provides: "scored Russian golden dataset"
      contains: "40 records with reference answers, expected docs, outcomes, and notes"
    - path: "ai-service/eval/golden/golden_ru.meta.json"
      provides: "corpus binding"
      contains: "hash/version matching the indexed frozen corpus"
  decision_refs:
    - "D-331"
    - "D-332"
    - "D-333"
    - "D-334"
    - "D-335"
    - "D-336"
    - "D-337"
    - "D-338"
    - "D-339"
    - "D-340"
    - "D-341"
    - "D-345"
---

<objective>
Author and validate the Russian golden dataset against the frozen indexed corpus.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-evaluation-observability/07-CONTEXT.md
@.planning/phases/07-evaluation-observability/07-03-SUMMARY.md
@ai-service/eval/golden/golden_ru.meta.json
@ai-service/eval/corpus
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add golden validator</name>
  <files>ai-service/eval/validate_golden.py, ai-service/tests/test_eval_golden_schema.py</files>
  <read_first>ai-service/eval/schema.py, ai-service/eval/golden/golden_ru.meta.json</read_first>
  <action>Add a validator that enforces JSONL syntax, required schema fields, unique ids, allowed types/outcomes, 10 records per type, approximate 6/4 out-of-scope split, non-empty Russian text fields, existing expected document ids, and meta corpus hash consistency. The validator must reject exact chunk-id hard requirements.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_golden_schema.py</automated>
  </verify>
  <acceptance_criteria>
    - Validator fails on missing `expected_doc_ids`.
    - Validator permits advisory `expected_chunk_hint`.
    - Validator reports line numbers for JSONL errors.
  </acceptance_criteria>
  <done>Golden dataset can be checked before any runner consumes it.</done>
</task>

<task type="manual">
  <name>Task 2: Author 40 Russian golden records</name>
  <files>ai-service/eval/golden/golden_ru.jsonl, ai-service/eval/golden/golden_ru.meta.json</files>
  <read_first>ai-service/eval/corpus, ai-service/eval/golden/golden_ru.meta.json, .planning/phases/07-evaluation-observability/07-03-SUMMARY.md</read_first>
  <action>Write 40 Russian records against the frozen corpus: factual, aggregation, multi-hop, and out-of-scope. Reference answers must be grounded in the corpus for answerable questions. For guard-refusal records, use unsafe/refusable Russian prompts that should be blocked without weakening guard behavior. Store document ids as the hard expected citation target and use `expected_chunk_hint` only for human orientation.</action>
  <verify>
    <manual>Human pass checks that records are Russian, corpus-grounded, and bound to committed document ids.</manual>
  </verify>
  <acceptance_criteria>
    - No bilingual scored records are present.
    - Every answerable record has at least one expected doc id from the frozen corpus.
    - Every out-of-scope record has outcome `refused_no_evidence` or `refused_guard`.
  </acceptance_criteria>
  <done>Golden dataset is authored against the frozen corpus.</done>
</task>

<task type="auto">
  <name>Task 3: Validate and summarize golden coverage</name>
  <files>.planning/phases/07-evaluation-observability/07-05-SUMMARY.md</files>
  <read_first>ai-service/eval/golden/golden_ru.jsonl, ai-service/eval/golden/golden_ru.meta.json</read_first>
  <action>Run the golden validator and record counts by type/outcome, expected document coverage, corpus hash, and any advisory chunk hints. If the corpus hash differs from Plan 07-03, stop and do not repair silently.</action>
  <verify>
    <automated>cd ai-service; uv run python -m eval.validate_golden eval/golden/golden_ru.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - Validator passes.
    - Summary states the exact corpus version/hash and record counts.
  </acceptance_criteria>
  <done>Golden data is ready for RAGAS and retrieval runners.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Silent corpus drift | meta hash | Validator compares golden metadata to frozen corpus hash. |
| Brittle chunk bindings | expected citations | Score only `expected_doc_ids`; chunk hints are advisory. |
| Language dilution | dataset | Validator and manual review reject scored English records. |
</threat_model>

<verification>
- [ ] `cd ai-service; uv run python -m eval.validate_golden eval/golden/golden_ru.jsonl`
- [ ] Summary includes exact count by type and outcome.
</verification>

<success_criteria>
- EVAL-01 is satisfied with a frozen-corpus-bound Russian JSONL dataset.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-observability/07-05-SUMMARY.md`.
</output>
