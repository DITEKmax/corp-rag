---
phase: "06-chat-frontend-experience"
plan: "04"
type: "execute"
wave: 3
depends_on: ["01", "02"]
files_modified:
  - "backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ChatController.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatConversationService.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatMessageMapper.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/adapter/rest/ChatControllerTest.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/service/chat/ChatConversationServiceTest.java"
autonomous: true
requirements: ["CHAT-01"]
user_setup:
  - "Requires Plan 02 repositories and Plan 01 generated DTOs."
must_haves:
  truths:
    - "All chat REST endpoints are owned by Java and guarded by the existing `chat.query` permission unless the contract says otherwise."
    - "`POST /chat/conversations` creates a conversation for the current authenticated user; no implicit create happens inside `/chat/query`."
    - "Deleted conversations are externally gone: list excludes them, get/listMessages return 404, repeated DELETE returns 204."
    - "Conversation list rows expose title, updatedAt, and messageCount only; no last-status badge in Phase 6."
    - "Do not implement `/chat/messages/{messageId}/citations` in Phase 6."
  artifacts:
    - path: "contracts/openapi/api-v1.yaml"
      provides: "chat endpoint paths and generated DTO names"
      contains: "conversations, messages, delete, query, deferred citations detail"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/repository/ChatConversationRepository.java"
      provides: "conversation persistence operations"
      contains: "owner-scoped read/list/delete and message counts"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/repository/ChatMessageRepository.java"
      provides: "message history operations"
      contains: "list messages and soft-delete child rows"
  decision_refs:
    - "D-228"
    - "D-229"
    - "D-230"
    - "D-231"
    - "D-232"
    - "D-233"
    - "D-234"
    - "D-235"
    - "D-236"
    - "D-237"
    - "D-238"
    - "D-243"
    - "D-249"
    - "D-250"
    - "D-275"
    - "D-329"
---

<objective>
Expose Java conversation lifecycle and message-history REST endpoints over the new repositories: create/list/get/delete conversations and list visible messages with owner scoping, soft-delete semantics, and generated contract DTOs.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-frontend-experience/06-CONTEXT.md
@.planning/phases/06-chat-frontend-experience/06-RESEARCH.md
@.planning/phases/06-chat-frontend-experience/06-01-SUMMARY.md
@.planning/phases/06-chat-frontend-experience/06-02-SUMMARY.md
@contracts/openapi/api-v1.yaml
@backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/UserController.java
@backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/DocumentController.java
@backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/JwtAuthorization.java
@backend/corp-rag-app/src/main/java/com/corprag/repository/ChatConversationRepository.java
@backend/corp-rag-app/src/main/java/com/corprag/repository/ChatMessageRepository.java
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement conversation service and DTO mapping</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatConversationService.java, backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatMessageMapper.java</files>
  <read_first>contracts/openapi/api-v1.yaml, backend/corp-rag-app/src/main/java/com/corprag/repository/ChatConversationRepository.java, backend/corp-rag-app/src/main/java/com/corprag/repository/ChatMessageRepository.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/DocumentMapper.java</read_first>
  <action>Add service methods for create, list, get, delete, and list messages. Resolve the current user id from controller context and pass it through every repository call. Create uses the contracted request shape; if no first-message title exists, use the approved default placeholder and allow Plan 05 to replace it once on first query. Map message role/status/content/citations/retrievalMeta/confidence exactly into generated DTOs. Do not expose deleted rows.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatConversationService*</automated>
  </verify>
  <acceptance_criteria>
    - DTO mapper handles nullable content, nullable retrievalMeta, and null/empty citations.
    - User messages return no assistant status.
    - Non-ANSWERED assistant rows stay visible in history with their status but without fake answer text.
    - The service has no Python dependency.
  </acceptance_criteria>
  <done>Conversation/message read lifecycle is service-backed and contract-aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Add ChatController conversation endpoints</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ChatController.java</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/UserController.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/RoleController.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/DocumentController.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/JwtAuthorization.java, contracts/openapi/api-v1.yaml</read_first>
  <action>Create a REST controller for the contracted chat conversation/message endpoints except `/chat/query`, which belongs to Plan 05. Enforce authentication and `chat.query` permission. Implement `POST /chat/conversations`, list conversations with existing page/size query semantics, get conversation, list messages, and idempotent `DELETE /chat/conversations/{conversationId}` returning 204. Return 404 for missing, foreign, or deleted conversations/messages using existing ProblemDetails style.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatController*</automated>
  </verify>
  <acceptance_criteria>
    - Browser-facing endpoints are all under Java `/api/v1`.
    - No endpoint calls Python.
    - Deleted conversations get external 404 behavior except repeated DELETE 204.
    - `listConversations` sorts by `updatedAt DESC` through repository SQL.
  </acceptance_criteria>
  <done>Users can create/delete/list/get conversations and list persisted messages through Java.</done>
</task>

<task type="auto">
  <name>Task 3: Add controller/security tests for owner scoping and delete semantics</name>
  <files>backend/corp-rag-app/src/test/java/com/corprag/adapter/rest/ChatControllerTest.java, backend/corp-rag-app/src/test/java/com/corprag/service/chat/ChatConversationServiceTest.java</files>
  <read_first>backend/corp-rag-app/src/test/java/com/corprag/adapter/rest, backend/corp-rag-app/src/test/java/com/corprag/service</read_first>
  <action>Add MVC/service tests for permission denial, owner scoping, list pagination/order, idempotent delete, deleted get/listMessages 404, message status rendering, and no implementation of deferred getCitationDetails. Include at least one history response containing `REFUSED_GUARD` or `TIMEOUT` to prove failed turns remain visible to the owner.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatController*,*ChatConversationService*</automated>
  </verify>
  <acceptance_criteria>
    - A user cannot see another user's conversation.
    - Missing `chat.query` returns forbidden in the local controller pattern.
    - Delete does not remove audit-traceable rows.
    - The deferred citation details endpoint is not accidentally implemented.
  </acceptance_criteria>
  <done>Conversation lifecycle REST is covered before query orchestration adds message writes.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Cross-user conversation access | chat REST endpoints | Pass authenticated user id to every service/repository call and test foreign IDs. |
| Soft-delete bypass | get/list/listMessages | Repository and service filter `deleted_at` and return 404 for deleted rows. |
| Scope creep into source proxy | citation details endpoint | Explicitly do not implement the endpoint in Phase 6. |
| Permission desync | chat endpoints | Use existing `chat.query` permission enforcement in the controller. |
</threat_model>

<verification>
- [ ] `ChatController` exists and covers conversation lifecycle endpoints except `/chat/query`.
- [ ] `DELETE /chat/conversations/{conversationId}` returns 204 and is idempotent.
- [ ] Deleted/foreign conversations are not visible and return 404 as appropriate.
- [ ] Failed/refused assistant rows remain visible in `listMessages`.
- [ ] `cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatController*,*ChatConversationService*` passes.
</verification>

<success_criteria>
- CHAT-01 lifecycle behavior is implemented through Java REST and repositories.
- Frontend Plan 07 can rely on conversation/message endpoints for lazy create, list, history, and delete.
</success_criteria>

<output>
Write `06-04-SUMMARY.md` with endpoint list, permission behavior, delete semantics, and any deviations from the generated contract.
</output>
