---
phase: "06-chat-frontend-experience"
plan: "06"
type: "execute"
wave: 4
depends_on: ["01"]
files_modified:
  - "frontend/index.html"
  - "frontend/styles/base.css"
  - "frontend/styles/app.css"
  - "frontend/js/app.js"
  - "frontend/js/core/api-client.js"
  - "frontend/js/core/session-state.js"
  - "frontend/js/core/router.js"
  - "frontend/js/core/routes.js"
  - "frontend/js/components/**"
  - "frontend/js/pages/login-page.js"
  - "frontend/js/pages/change-password-page.js"
autonomous: true
requirements: ["UI-01"]
user_setup:
  - "Frontend stays vanilla HTML/CSS/JS. Do not install a framework, Tailwind, or a frontend build system unless later plans explicitly require it."
must_haves:
  truths:
    - "Frontend is vanilla HTML/CSS/JS with BEM; reuse existing design tokens in `frontend/styles/base.css`."
    - "All API calls go through one API client with `credentials: include`, `/api/v1` base URL, unified error mapping, and single-flight refresh."
    - "Do not manually set the `Origin` header; browser sets it and Java validates Origin/Referer."
    - "Session-first bootstrap calls `/me` before protected route content renders."
    - "One declarative route table drives both guards and navigation."
  artifacts:
    - path: "frontend/styles/base.css"
      provides: "existing design tokens"
      contains: "colors, spacing, typography, and base shell styles to reuse"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/security/OriginRefererValidationFilter.java"
      provides: "actual Java CSRF/Origin behavior"
      contains: "unsafe cookie-authenticated request Origin/Referer validation"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/service/auth/RefreshTokenService.java"
      provides: "refresh rotation behavior"
      contains: "family rotation/reuse detection requiring single-flight frontend refresh"
    - path: "contracts/openapi/api-v1.yaml"
      provides: "auth/session endpoint and permission contract"
      contains: "login, refresh, password, /me, permission codes"
  decision_refs:
    - "D-301"
    - "D-302"
    - "D-303"
    - "D-304"
    - "D-305"
    - "D-306"
    - "D-307"
    - "D-308"
    - "D-309"
    - "D-310"
    - "D-311"
    - "D-312"
    - "D-313"
    - "D-314"
    - "D-315"
    - "D-316"
    - "D-317"
    - "D-318"
    - "D-319"
    - "D-320"
    - "D-321"
    - "D-329"
---

<objective>
Build the vanilla frontend foundation: session-first bootstrap, single API client with single-flight refresh, declarative hash router, permission-gated navigation, login/change-password flows, and app shell components shared by chat and admin.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-frontend-experience/06-CONTEXT.md
@.planning/phases/06-chat-frontend-experience/06-RESEARCH.md
@.planning/phases/06-chat-frontend-experience/06-01-SUMMARY.md
@frontend/index.html
@frontend/styles/base.css
@frontend/nginx.conf
@contracts/openapi/api-v1.yaml
@backend/corp-rag-app/src/main/java/com/corprag/security/OriginRefererValidationFilter.java
@backend/corp-rag-app/src/main/java/com/corprag/security/MustChangePasswordFilter.java
@backend/corp-rag-app/src/main/java/com/corprag/service/auth/RefreshTokenService.java
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create vanilla frontend module structure and app shell</name>
  <files>frontend/index.html, frontend/styles/app.css, frontend/js/app.js, frontend/js/components/**</files>
  <read_first>frontend/index.html, frontend/styles/base.css, frontend/nginx.conf</read_first>
  <action>Convert the static shell into a vanilla module app while keeping BEM class names and design tokens. Add a root app mount, top-level shell layout, neutral loading skeleton, access-denied view, not-found view, service-unavailable retry view, and shared small components for buttons, drawers/modals, empty/error states, and nav. Avoid nested cards, marketing hero composition, Tailwind, or framework patterns. Keep text sizes stable and responsive without viewport-scaling fonts.</action>
  <verify>
    <automated>node --check frontend/js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - First paint can show a neutral loading skeleton without protected route content.
    - App shell uses BEM classes and existing CSS variables/tokens.
    - Shared components are generic enough for chat/admin but not over-abstracted.
  </acceptance_criteria>
  <done>Frontend has a real app shell for routed screens.</done>
</task>

<task type="auto">
  <name>Task 2: Implement central API client and single-flight refresh</name>
  <files>frontend/js/core/api-client.js, frontend/js/core/session-state.js</files>
  <read_first>contracts/openapi/api-v1.yaml, backend/corp-rag-app/src/main/java/com/corprag/security/OriginRefererValidationFilter.java, backend/corp-rag-app/src/main/java/com/corprag/service/auth/RefreshTokenService.java</read_first>
  <action>Implement a single API client module that prefixes `/api/v1`, sets `credentials: "include"` on every request, applies `Content-Type: application/json` when a JSON body is present, maps ProblemDetails consistently, and forbids feature modules from calling `fetch` directly. Add reactive refresh-on-401 for protected calls: one shared refresh promise at a time, waiters retry once after success, refresh failure or second 401 clears in-memory session and routes to login with return hash. Exclude login, change-password, bootstrap `/me`, and `/auth/refresh` from refresh recursion. Do not use timer-based refresh and do not set `Origin` manually.</action>
  <verify>
    <automated>node --check frontend/js/core/api-client.js</automated>
    <automated>python -c "from pathlib import Path; bad=[]; [bad.append(str(p)) for p in Path('frontend/js').rglob('*.js') if p.name!='api-client.js' and 'fetch(' in p.read_text(encoding='utf-8')]; assert not bad, bad"</automated>
  </verify>
  <acceptance_criteria>
    - Concurrent 401 tests/manual mock path cannot issue multiple simultaneous refreshes.
    - `/me` 401 goes to login; `/me` 5xx goes to service-unavailable retry.
    - Must-change-password rejection maps to change-password route, not refresh.
  </acceptance_criteria>
  <done>All frontend network behavior is centralized and safe for rotating refresh tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Implement session-first bootstrap, router, guards, and nav table</name>
  <files>frontend/js/core/session-state.js, frontend/js/core/router.js, frontend/js/core/routes.js, frontend/js/app.js</files>
  <read_first>contracts/openapi/api-v1.yaml, .planning/phases/06-chat-frontend-experience/06-CONTEXT.md</read_first>
  <action>Build a memory-only session-state singleton and a hash router whose single declarative route table contains `path`, `render`, `access`, `allowDuringMustChange` when needed, and `nav` metadata. On load, call `GET /api/v1/me` once before protected route content. Route outcomes: 401/no session -> `#/login` preserving requested route; `mustChangePassword=true` -> `#/change-password` and block all other app routes; valid session -> store user/permissions in memory and guard current hash. Missing permission renders access denied, not login. Unknown hash renders not found. Nav is built from the same route table and filtered by in-memory permissions.</action>
  <verify>
    <automated>node --check frontend/js/core/router.js</automated>
    <automated>node --check frontend/js/core/routes.js</automated>
  </verify>
  <acceptance_criteria>
    - No protected screen renders before `/me` resolves.
    - `#/change-password` is reachable during must-change and does not redirect-loop.
    - Partial admins see only routes for permissions they actually have.
    - Guard and nav cannot desync because they read the same route entry.
  </acceptance_criteria>
  <done>Route guarding and permission nav are available for chat and admin screens.</done>
</task>

<task type="auto">
  <name>Task 4: Implement login, forced password change, and shell verification</name>
  <files>frontend/js/pages/login-page.js, frontend/js/pages/change-password-page.js, frontend/js/app.js, frontend/styles/app.css</files>
  <read_first>contracts/openapi/api-v1.yaml, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/AuthController.java, backend/corp-rag-app/src/main/java/com/corprag/security/MustChangePasswordFilter.java</read_first>
  <action>Implement login and change-password pages through the central API client. Login should return to the originally requested hash after successful session bootstrap. Change-password should be the only non-login route available while `mustChangePassword` is true; after success, refresh `/me` and route through normal guards. Add logout if already contracted. Keep forms utilitarian and compact with basic errors; no visible tutorial text or marketing content.</action>
  <verify>
    <automated>node --check frontend/js/pages/login-page.js</automated>
    <automated>node --check frontend/js/pages/change-password-page.js</automated>
    <manual>Open the frontend in a browser against Java and verify `/me` bootstrap, login, must-change redirect, and logout/expired-session behavior.</manual>
  </verify>
  <acceptance_criteria>
    - Login route is public; protected routes redirect to login only when no session exists.
    - `/me` 5xx shows service unavailable, not login.
    - Must-change is honored proactively and reactively.
  </acceptance_criteria>
  <done>UI-01 core session flow is implemented and ready for chat/admin pages.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Refresh-token family self-invalidation | concurrent 401s | API client single-flight refresh and one retry only. |
| Protected content flicker/leak | bootstrap/router | `/me` resolves before route content renders. |
| Permission nav/guard desync | route config | Single route table feeds both guard and nav. |
| CSRF header misconception | fetch client | Do not set forbidden `Origin`; rely on browser and Java Origin/Referer config. |
</threat_model>

<verification>
- [ ] `node --check` passes for new frontend JS modules.
- [ ] Static grep proves feature modules do not call `fetch` directly.
- [ ] Browser smoke verifies `/me` bootstrap, login, change-password, access denied, not found, and service unavailable routes.
- [ ] CSS uses BEM and existing tokens; no Tailwind/framework dependency is added.
</verification>

<success_criteria>
- UI-01 is implemented as a session-first app shell.
- Chat/admin frontend plans can use shared API client, router, session state, nav, and guarded layouts.
</success_criteria>

<output>
Write `06-06-SUMMARY.md` with module map, route table shape, API-client refresh behavior, and verified CSRF/origin assumptions.
</output>
