---
phase: "06-chat-frontend-experience"
plan: "05"
type: "execute"
wave: 4
depends_on: ["01", "02", "03", "04"]
files_modified:
  - "backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ChatController.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryService.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatTitleService.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatHistoryAssembler.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/service/chat/**"
  - "backend/corp-rag-app/src/test/java/com/corprag/adapter/rest/ChatControllerTest.java"
autonomous: true
requirements: ["CHAT-01", "CHAT-02"]
user_setup:
  - "Requires Plans 02-04. Python can be mocked in tests; live Python UAT is deferred to Plan 09."
must_haves:
  truths:
    - "`POST /chat/query` requires `conversationId`; it never implicitly creates a conversation."
    - "429 is checked before guard/Python/DB; it creates no chat rows and does not bump `updated_at`."
    - "Every processed query writes exactly one user row and one paired assistant row in a single transaction after the final outcome is known."
    - "Python history is the last N=10 successful ANSWERED pairs; drop the whole failed/degraded pair and never send a dangling user message."
    - "BL-02 auto-retry is internal to one `/chat/query` request and results in one persisted pair with the final outcome."
    - "Do not weaken Python output guard. Missing-citation failures surface as `DEGRADED` if the internal retry also fails."
  artifacts:
    - path: "backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatRateLimiter.java"
      provides: "pre-persistence 429 decision"
      contains: "per-user in-memory limiter and retryAfterSeconds"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryAuditService.java"
      provides: "query audit event helper"
      contains: "event methods for every final query outcome"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/adapter/ai/PythonQueryClient.java"
      provides: "typed Python `/v1/query` client"
      contains: "normal, guard, degraded, timeout, unavailable outcome mapping"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/repository/ChatMessageRepository.java"
      provides: "paired persistence and answered-pair history"
      contains: "transactional append and last-10 answered pair query"
  decision_refs:
    - "D-228"
    - "D-230"
    - "D-231"
    - "D-237"
    - "D-239"
    - "D-240"
    - "D-241"
    - "D-242"
    - "D-243"
    - "D-244"
    - "D-245"
    - "D-246"
    - "D-247"
    - "D-248"
    - "D-256"
    - "D-257"
    - "D-258"
    - "D-261"
    - "D-262"
    - "D-267"
    - "D-268"
    - "D-269"
    - "D-270"
    - "D-271"
    - "D-272"
    - "D-278"
    - "D-279"
    - "D-280"
    - "D-326"
    - "D-327"
---

<objective>
Implement Java `/chat/query` orchestration: rate-limit before work, validate conversation ownership, assemble answered-pair history, call Python through Java, handle BL-02 internal retry, persist paired user/assistant outcome rows, audit every significant outcome, and return contract-aligned responses.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-frontend-experience/06-CONTEXT.md
@.planning/phases/06-chat-frontend-experience/06-RESEARCH.md
@.planning/phases/06-chat-frontend-experience/06-01-SUMMARY.md
@.planning/phases/06-chat-frontend-experience/06-02-SUMMARY.md
@.planning/phases/06-chat-frontend-experience/06-03-SUMMARY.md
@.planning/phases/06-chat-frontend-experience/06-04-SUMMARY.md
@contracts/openapi/api-v1.yaml
@contracts/openapi/ai-service-v1.yaml
@backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ChatController.java
@backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessFilterResolver.java
@backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/QueryAccessFilterMapper.java
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement query orchestration skeleton and pre-work gates</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryService.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ChatController.java</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatRateLimiter.java, backend/corp-rag-app/src/main/java/com/corprag/repository/ChatConversationRepository.java, backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessFilterResolver.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/QueryAccessFilterMapper.java, contracts/openapi/api-v1.yaml</read_first>
  <action>Add the `/chat/query` endpoint to `ChatController` and a `ChatQueryService`. Enforce authentication and `chat.query`; require `conversationId`; verify the conversation belongs to the current user and is not deleted. Check the per-user rate limiter before Python, guard handling, or any DB write. On denied limiter result, emit light audit and return 429 ProblemDetails with `Retry-After`; do not persist rows or update conversation activity. Resolve the user's access filter in Java for allowed requests.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatQueryService*,*ChatController*</automated>
  </verify>
  <acceptance_criteria>
    - Missing/foreign/deleted conversation returns existing 404/403 style.
    - 429 path has zero chat repository writes and an audit event.
    - `conversationId` remains required and no lazy create occurs inside query endpoint.
  </acceptance_criteria>
  <done>Pre-work query gates are correct before Python orchestration is added.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble Python request with answered-pair history and title derivation</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatHistoryAssembler.java, backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatTitleService.java, backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryService.java</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/repository/ChatMessageRepository.java, contracts/openapi/ai-service-v1.yaml</read_first>
  <action>Build the Python `QueryRequest` using the current `conversationId`, current user id, generated/request correlation id, user message text, resolved access filter, and last 10 successful answered pairs from the repository. Represent each eligible pair as user message plus assistant answer in order. Exclude entire pairs whose assistant status is not `ANSWERED`. If the conversation still has the placeholder title and this is its first user message, derive a bounded immutable title from the user text and update the title once before/within final persistence.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatHistoryAssembler*,*ChatTitleService*,*ChatQueryService*</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if a user-only dangling history message is sent to Python.
    - Last N means last 10 answered pairs, not last 10 messages after filtering.
    - Placeholder-to-first-message title mutation happens once and later queries cannot rename.
  </acceptance_criteria>
  <done>Python receives only clean answered context and immutable conversation title behavior is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Map Python outcomes, BL-02 internal retry, and strict guard behavior</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryService.java, backend/corp-rag-app/src/test/java/com/corprag/service/chat/ChatQueryServiceTest.java</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/adapter/ai/PythonQueryClient.java, ai-service/src/corp_rag_ai/adapters/rest/query.py, .planning/phases/05.1-phase-5-uat-fix-wave/05.1-05-SUMMARY.md</read_first>
  <action>Map Python normal and problem responses into assistant statuses: answered true with valid citations -> `ANSWERED`; answered false/no evidence -> `NO_EVIDENCE`; guard 422 -> `REFUSED_GUARD`; missing-citation/output-guard BL-02 -> perform one internal retry through the existing Python `/v1/query` contract without inventing a new retry field; if retry succeeds persist the final answer as one pair, if retry also fails persist one `DEGRADED` assistant row with no answer text and no citations; timeout -> `TIMEOUT`; 503/connection failure -> `AI_UNAVAILABLE`. Do not weaken the Python output guard or accept invalid citations as displayable answer text. If BL-02 requires a Python-side change, harden the synthesis prompt only and keep output-guard validation strict.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatQueryService*</automated>
  </verify>
  <acceptance_criteria>
    - BL-02 retry makes at most one extra Python call inside the same Java request.
    - External history sees one user+assistant pair for BL-02 auto-retry, not two.
    - `DEGRADED` has null content and null/empty citations.
    - `REFUSED_GUARD` and `NO_EVIDENCE` do not show retry eligibility in backend DTO hints if such hints exist.
  </acceptance_criteria>
  <done>All query outcomes map to the locked assistant status model without weakening guard behavior.</done>
</task>

<task type="auto">
  <name>Task 4: Persist paired rows transactionally and audit final outcome</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryService.java, backend/corp-rag-app/src/test/java/com/corprag/service/chat/ChatQueryServiceTest.java</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/repository/ChatMessageRepository.java, backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryAuditService.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsWriter.java</read_first>
  <action>After the final outcome is known, write the user row and assistant row in one transaction with the same `correlation_id`. Store citations JSONB exactly as returned only on `ANSWERED`; store retrievalMeta JSONB whenever Python returned it for assistant outcomes; store nullable confidence. Bump conversation `updated_at` for any persisted outcome. Emit the audit event after/around persistence with message ids where available. Return a contract-aligned success response for 200 outcomes and ProblemDetails for guard/unavailable errors where the contract uses problem responses; the persisted assistant row must still be visible through `listMessages`.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatQueryService*,*ChatController*</automated>
  </verify>
  <acceptance_criteria>
    - Every processed non-429 request produces exactly two rows.
    - User and assistant rows share one correlation id.
    - Manual retry produces a new pair and new correlation id.
    - `updated_at` bumps for refused/no-evidence/degraded/timeout/unavailable outcomes.
    - Audit rows are separate from chat history rows.
  </acceptance_criteria>
  <done>`/chat/query` has durable paired history and traceable audit for all final outcomes.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Rate-limit writes pollute history | 429 path | Limiter runs before repositories and tests assert no chat writes. |
| Unsafe answer text shown after output guard failure | BL-02 path | `DEGRADED` stores no answer text/citations; UI retries manually if needed. |
| Follow-up prompt contamination | history assembler | Last 10 answered pairs only, dropping failed/degraded pairs as pairs. |
| Lost traceability for failures | persistence/audit | Persist paired failure rows and write audit events with correlation id. |
</threat_model>

<verification>
- [ ] 429 creates no chat rows, no updatedAt bump, and one light audit event.
- [ ] Every processed non-429 outcome writes one user row plus one assistant row in one transaction.
- [ ] Python history builder returns last 10 successful answered pairs only.
- [ ] BL-02 auto-retry is internal and creates one persisted pair with the final outcome.
- [ ] Strict output guard is not weakened.
- [ ] `cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatQueryService*,*ChatController*` passes.
</verification>

<success_criteria>
- CHAT-02 is implemented through Java as the browser-facing orchestrator.
- Query outcomes, history, audit, rate limiting, and Python calls match the locked discussion decisions.
</success_criteria>

<output>
Write `06-05-SUMMARY.md` with endpoint behavior, status mapping, audit events emitted, history-pair query semantics, and any known live-Python UAT prerequisites for Plan 09.
</output>
