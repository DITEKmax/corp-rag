---
phase: "06-chat-frontend-experience"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01"]
files_modified:
  - "backend/corp-rag-app/src/main/resources/db/migration/V14__add_chat_conversations_messages.sql"
  - "backend/corp-rag-app/src/main/java/com/corprag/domain/chat/**"
  - "backend/corp-rag-app/src/main/java/com/corprag/repository/ChatConversationRepository.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/repository/ChatMessageRepository.java"
  - "backend/corp-rag-app/src/test/java/com/corprag/repository/**"
autonomous: true
requirements: ["CHAT-01", "CHAT-02"]
user_setup:
  - "Requires Plan 01 generated contract DTOs to compile."
must_haves:
  truths:
    - "Chat persistence is PostgreSQL only in Java-owned Flyway migrations; no Redis, MongoDB, or separate chat datastore."
    - "Use one `chat_messages` stream table. Do not split user/assistant tables and do not add `pair_id`."
    - "Soft-delete conversations by setting `deleted_at` on the parent and explicitly setting `deleted_at` on child messages."
    - "Python history query returns last N=10 successful ANSWERED pairs, never filtered dangling messages."
    - "The only allowed title mutation is placeholder title to first-message-derived title; after derivation, no rename behavior exists."
  artifacts:
    - path: "backend/corp-rag-app/src/main/resources/db/migration"
      provides: "Flyway schema convention"
      contains: "existing V1-V13 migrations and table naming/FK style"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/repository"
      provides: "JDBC repository patterns"
      contains: "document/user/audit repository examples"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/domain/chat/**"
      provides: "new chat persistence domain"
      contains: "conversation, message role/status, citation/retrieval snapshot value objects"
  decision_refs:
    - "D-225"
    - "D-226"
    - "D-227"
    - "D-230"
    - "D-231"
    - "D-232"
    - "D-233"
    - "D-234"
    - "D-235"
    - "D-236"
    - "D-237"
    - "D-238"
    - "D-239"
    - "D-240"
    - "D-244"
    - "D-245"
    - "D-249"
    - "D-250"
    - "D-251"
    - "D-252"
    - "D-253"
    - "D-254"
    - "D-255"
    - "D-256"
    - "D-257"
    - "D-258"
    - "D-259"
    - "D-260"
    - "D-322"
    - "D-330"
---

<objective>
Add the Java-owned chat persistence foundation: Flyway schema, domain records, repositories, JSONB snapshot mapping, soft-delete semantics, conversation ordering, and answered-pair history selection.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-frontend-experience/06-CONTEXT.md
@.planning/phases/06-chat-frontend-experience/06-RESEARCH.md
@.planning/phases/06-chat-frontend-experience/06-VALIDATION.md
@.planning/phases/06-chat-frontend-experience/06-01-SUMMARY.md
@backend/corp-rag-app/src/main/resources/db/migration
@backend/corp-rag-app/src/main/java/com/corprag/repository
@backend/corp-rag-app/src/main/java/com/corprag/domain
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify migration conventions and FK targets</name>
  <files>backend/corp-rag-app/src/main/resources/db/migration/V14__add_chat_conversations_messages.sql</files>
  <read_first>backend/corp-rag-app/src/main/resources/db/migration, backend/corp-rag-app/src/main/java/com/corprag/repository/UserRepository.java, backend/corp-rag-app/src/main/java/com/corprag/domain/UserAccount.java</read_first>
  <action>Inspect existing Flyway file names, timestamp/default styles, FK naming, UUID handling, JSONB usage, and ownership FK targets. Confirm the next migration version and that conversation owner FK should target `users(id)`. Keep the `chat_` prefix unless existing migrations prove a different naming style is mandatory.</action>
  <verify>
    <automated>rg -n "CREATE TABLE (users|documents|audit_events|outbox_events)|jsonb|REFERENCES users|V13" backend/corp-rag-app/src/main/resources/db/migration backend/corp-rag-app/src/main/java/com/corprag/repository</automated>
  </verify>
  <acceptance_criteria>
    - Summary records the exact current highest migration version.
    - FK target and table naming are verified against real migrations.
    - No schema work proceeds from remembered table names alone.
  </acceptance_criteria>
  <done>D-322 is resolved with concrete repo evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add chat Flyway migration</name>
  <files>backend/corp-rag-app/src/main/resources/db/migration/V14__add_chat_conversations_messages.sql</files>
  <read_first>.planning/phases/06-chat-frontend-experience/06-CONTEXT.md, backend/corp-rag-app/src/main/resources/db/migration</read_first>
  <action>Create the `chat_conversations` and `chat_messages` tables exactly from the locked schema. `chat_conversations` has `id uuid primary key`, `user_id uuid not null references users(id)`, `title varchar(200) not null`, `created_at`, `updated_at`, and nullable `deleted_at`. `chat_messages` has `id uuid primary key`, `conversation_id uuid not null references chat_conversations(id)`, `role varchar(16) not null`, nullable `status varchar(32)`, nullable `content text`, nullable `citations jsonb`, nullable `retrieval_meta jsonb`, nullable `confidence numeric(4,3)`, non-null `correlation_id uuid`, `created_at`, and nullable `deleted_at`. Add check constraints for role/status, citations only on answered assistant rows when practical, and confidence bounds. Add indexes `(user_id, deleted_at, updated_at desc)`, `(conversation_id, deleted_at, created_at asc)`, and `(correlation_id)`.</action>
  <verify>
    <automated>rg -n "CREATE TABLE chat_conversations|CREATE TABLE chat_messages|chat_messages_.*status|retrieval_meta jsonb|correlation_id|updated_at DESC" backend/corp-rag-app/src/main/resources/db/migration/V14__add_chat_conversations_messages.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration uses only PostgreSQL/Flyway; no new datastore or cache.
    - `content` is nullable.
    - `deleted_at` exists on both tables.
    - `correlation_id` is non-null and indexed.
    - No FK cascade hard-deletes chat rows.
  </acceptance_criteria>
  <done>Database can store Phase 6 chat conversations/messages and snapshots.</done>
</task>

<task type="auto">
  <name>Task 3: Add chat domain records and repository operations</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/domain/chat/**, backend/corp-rag-app/src/main/java/com/corprag/repository/ChatConversationRepository.java, backend/corp-rag-app/src/main/java/com/corprag/repository/ChatMessageRepository.java</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/repository/DocumentRepository.java, backend/corp-rag-app/src/main/java/com/corprag/repository/AuditEventRepository.java, backend/corp-rag-app/src/main/java/com/corprag/repository/UserRepository.java</read_first>
  <action>Implement repository/domain code using existing JDBC/object-mapper patterns. Support create conversation with placeholder title, one-time first-message title derivation, list owner conversations ordered by `updated_at DESC`, owner-scoped get, idempotent soft-delete with child message `deleted_at`, append a user+assistant pair in one transactional operation, list non-deleted messages by `created_at ASC`, count non-deleted messages per conversation for list rows, and map citation/retrieval JSONB snapshots through structured serialization.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -DskipTests=false</automated>
  </verify>
  <acceptance_criteria>
    - Conversation list excludes deleted rows and exposes message counts from non-deleted messages.
    - `updated_at` is bumped for every persisted pair/outcome, including non-ANSWERED outcomes.
    - Repeated delete is idempotent.
    - Citation snapshots are stored only for `ANSWERED` assistant rows.
    - Retrieval metadata is stored for assistant rows when available and null otherwise.
  </acceptance_criteria>
  <done>Repositories expose the persistence behavior needed by chat services without controller logic.</done>
</task>

<task type="auto">
  <name>Task 4: Add repository tests for invariants and answered-pair history</name>
  <files>backend/corp-rag-app/src/test/java/com/corprag/repository/**</files>
  <read_first>backend/corp-rag-app/src/test/java/com/corprag/repository, backend/corp-rag-app/src/test/java/com/corprag/**</read_first>
  <action>Add tests covering the locked persistence invariants: placeholder to first-message title mutation happens once, no later title rename occurs; list ordering is SQL `updated_at DESC`; soft-deleted conversations/messages disappear from read APIs; content may be null for non-ANSWERED assistant rows; citations/retrieval meta JSON round-trip; user+assistant rows share one `correlation_id`; answered-pair history returns the last 10 successful pairs and drops entire failed/degraded pairs.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*Chat*Repository*</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if history query returns a dangling user message.
    - Tests fail if delete hard-removes chat rows instead of setting `deleted_at`.
    - Tests fail if a 429-style non-persisted action can affect conversation ordering.
  </acceptance_criteria>
  <done>Persistence invariants are pinned before chat services/controllers are added.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Cross-user history leakage | repository owner filters | Every conversation/message query takes owner user id and filters deleted rows. |
| Audit trace loss | delete behavior | Soft-delete rows and keep correlation ids. |
| Unsafe follow-up context | Python history query | Select complete `ANSWERED` pairs only; failed/degraded pairs are excluded as pairs. |
| Snapshot access bypass later | citations JSONB | Snapshot is display-only; plan keeps full-content viewer deferred and live access recheck in backlog. |
</threat_model>

<verification>
- [ ] Migration version and FK target verified against real migrations.
- [ ] `chat_conversations` and `chat_messages` schema matches locked shape.
- [ ] Repository tests cover soft-delete, ordering, nullable content, JSONB snapshots, and correlation id sharing.
- [ ] Answered-pair history query returns the last 10 successful pairs and no dangling user messages.
- [ ] `cd backend; mvn -q -pl corp-rag-app -am test` passes.
</verification>

<success_criteria>
- Java has durable, owner-scoped chat storage for conversations and messages.
- Persistence decisions are encoded in schema constraints, repositories, and tests before REST/query orchestration.
</success_criteria>

<output>
Write `06-02-SUMMARY.md` with migration name, schema choices, repository methods, and any verified naming/FK notes for downstream services.
</output>
