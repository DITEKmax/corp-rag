---
phase: "06-chat-frontend-experience"
plan: "03"
type: "execute"
wave: 2
depends_on: ["01"]
files_modified:
  - "backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatRateLimiter.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryAuditService.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/adapter/ai/PythonQueryClient.java"
  - "backend/corp-rag-app/src/main/java/com/corprag/config/**"
  - "backend/corp-rag-app/src/test/java/com/corprag/service/chat/**"
  - "backend/corp-rag-app/src/test/java/com/corprag/adapter/ai/**"
autonomous: true
requirements: ["CHAT-02"]
user_setup:
  - "Requires Plan 01 contract DTOs. Can run in parallel with Plan 02 because it does not modify the chat repository files."
must_haves:
  truths:
    - "Java owns browser-facing auth, access-filter resolution, rate limiting, persistence coordination, and query audit."
    - "Rate limiting is in-memory per Java instance, per authenticated user id, for `POST /chat/query` only."
    - "429 is counted before guard/Python/DB, emits a light audit event, and creates no chat rows."
    - "Query audit rows use the existing `audit_events` table and `AuditEventWriter`; they are separate from user-visible chat history."
    - "Python query calls are Java-to-Python only and always include `conversationId`, `userId`, `correlationId`, resolved access filter, message, and answered-pair history when applicable."
  artifacts:
    - path: "backend/corp-rag-app/src/main/java/com/corprag/service/audit/AuditEventWriter.java"
      provides: "existing audit insertion mechanism"
      contains: "writeEvent with MDC correlation id and JSONB details"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsWriter.java"
      provides: "ProblemDetails response conventions"
      contains: "errorCode, correlationId, details, RFC7807 fields"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessFilterResolver.java"
      provides: "owner access policy resolution"
      contains: "Java-side access filter source for Python queries"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/QueryAccessFilterMapper.java"
      provides: "contract DTO mapper for Python access filters"
      contains: "existing access-filter shape mapping"
  decision_refs:
    - "D-241"
    - "D-261"
    - "D-262"
    - "D-263"
    - "D-264"
    - "D-265"
    - "D-266"
    - "D-267"
    - "D-268"
    - "D-269"
    - "D-270"
    - "D-271"
    - "D-272"
    - "D-321"
    - "D-324"
    - "D-330"
---

<objective>
Build the cross-cutting chat-query foundation before the query controller: thread-safe in-memory rate limiting, concrete query audit event writing, and a Java-to-Python query client with explicit timeout/unavailable behavior.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-frontend-experience/06-CONTEXT.md
@.planning/phases/06-chat-frontend-experience/06-RESEARCH.md
@.planning/phases/06-chat-frontend-experience/06-VALIDATION.md
@.planning/phases/06-chat-frontend-experience/06-01-SUMMARY.md
@backend/corp-rag-app/src/main/java/com/corprag/service/audit/AuditEventWriter.java
@backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsWriter.java
@backend/corp-rag-app/src/main/java/com/corprag/service/access/AccessFilterResolver.java
@backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/QueryAccessFilterMapper.java
@ai-service/src/corp_rag_ai/adapters/rest/query.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define and implement query audit event helper</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatQueryAuditService.java, backend/corp-rag-app/src/test/java/com/corprag/service/chat/ChatQueryAuditServiceTest.java</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/service/audit/AuditEventWriter.java, backend/corp-rag-app/src/main/java/com/corprag/domain/AuditEventEntry.java, backend/corp-rag-app/src/main/java/com/corprag/repository/AuditEventRepository.java, backend/corp-rag-app/src/main/resources/db/migration/*audit*</read_first>
  <action>Add a small chat audit service around `AuditEventWriter` with concrete event methods for `CHAT_QUERY_ANSWERED`, `CHAT_QUERY_REFUSED_GUARD`, `CHAT_QUERY_NO_EVIDENCE`, `CHAT_QUERY_DEGRADED`, `CHAT_QUERY_TIMEOUT`, `CHAT_QUERY_AI_UNAVAILABLE`, and `CHAT_QUERY_RATE_LIMITED`. Use category `CHAT`; set entity type to conversation for persisted outcomes and user/rate-limit context for 429 when no conversation row is written. Details JSON should include correlation id through MDC, conversation id when available, message ids after persistence, status, route, retrievers used, citation count, retry-after seconds for 429, and upstream error classification without storing prompt/answer text in audit details.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatQueryAudit*</automated>
  </verify>
  <acceptance_criteria>
    - Audit helper writes one significant event per final `/chat/query` outcome.
    - 429 audit is supported without chat message ids.
    - Chat audit does not duplicate full answer content or citation quote text.
  </acceptance_criteria>
  <done>Query audit shape is designed early and reusable by the orchestrator.</done>
</task>

<task type="auto">
  <name>Task 2: Add in-memory per-user chat rate limiter</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/service/chat/ChatRateLimiter.java, backend/corp-rag-app/src/test/java/com/corprag/service/chat/ChatRateLimiterTest.java</files>
  <read_first>.planning/phases/06-chat-frontend-experience/06-CONTEXT.md, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsWriter.java, contracts/constants.yaml</read_first>
  <action>Implement a thread-safe in-memory token bucket or equivalent per-user limiter for `POST /chat/query`: 30 requests/minute, counted before guard/Python/DB. Return a decision object with allowed/denied and `retryAfterSeconds`. Include lazy or periodic stale entry cleanup. Do not add Redis, Postgres counters, or global rate limits. Add a controller-facing helper to write 429 ProblemDetails with `Retry-After` through existing conventions; the actual endpoint integration lands in Plan 05.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatRateLimiter*</automated>
  </verify>
  <acceptance_criteria>
    - Concurrent calls for the same user cannot overshoot because of racey counters.
    - Users have independent buckets.
    - 429 has a deterministic positive `Retry-After`.
    - No inactive-user unbounded map growth.
  </acceptance_criteria>
  <done>Rate-limit behavior is testable before the `/chat/query` controller exists.</done>
</task>

<task type="auto">
  <name>Task 3: Add Java-to-Python query client and DTO mapping</name>
  <files>backend/corp-rag-app/src/main/java/com/corprag/adapter/ai/PythonQueryClient.java, backend/corp-rag-app/src/main/java/com/corprag/config/**, backend/corp-rag-app/src/test/java/com/corprag/adapter/ai/**</files>
  <read_first>contracts/openapi/ai-service-v1.yaml, backend/corp-rag-contracts/pom.xml, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/QueryAccessFilterMapper.java, ai-service/src/corp_rag_ai/adapters/rest/query.py</read_first>
  <action>Add a Python query adapter using generated AI-service DTOs where available and existing Spring HTTP-client style or a minimal `RestClient` configuration if none exists. It must call only `POST /v1/query`, never chunk detail, and include required `conversationId`, `userId`, `correlationId`, message, access filter, and conversation history pairs supplied by the orchestrator. Map normal responses, 422 guard ProblemDetails, missing-citation/degraded ProblemDetails, 503/unavailable, and transport timeout into typed Java outcomes for Plan 05. Configure a bounded timeout; do not let frontend requests hang indefinitely.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*PythonQueryClient*</automated>
  </verify>
  <acceptance_criteria>
    - Client never calls Python source/chunk detail endpoints.
    - `conversationId` is required in outbound request construction.
    - Timeout and unavailable map to distinct typed outcomes.
    - Guard/degraded responses preserve problem details needed for persisted statuses and UI wording.
  </acceptance_criteria>
  <done>Plan 05 can orchestrate query outcomes without embedding HTTP mechanics.</done>
</task>

<task type="auto">
  <name>Task 4: Add focused foundation tests and documentation notes</name>
  <files>backend/corp-rag-app/src/test/java/com/corprag/service/chat/**, backend/corp-rag-app/src/test/java/com/corprag/adapter/ai/**, .planning/phases/06-chat-frontend-experience/06-03-SUMMARY.md</files>
  <read_first>.planning/phases/06-chat-frontend-experience/06-RESEARCH.md, backend/corp-rag-app/src/test/java/com/corprag/**</read_first>
  <action>Ensure focused unit tests prove the critical behaviors: no distributed limiter dependency, 429 pre-persistence decision object, audit event details by outcome, Python client required fields, no direct chunk detail path, timeout/unavailable classification, and generated DTO compatibility. Record any exact config property names for AI base URL/timeout in the summary.</action>
  <verify>
    <automated>cd backend; mvn -q -pl corp-rag-app -am test -Dtest=*ChatRateLimiter*,*ChatQueryAudit*,*PythonQueryClient*</automated>
  </verify>
  <acceptance_criteria>
    - Plan 05 can wire these components without changing their public contracts.
    - Summary states the exact audit event names and details schema.
  </acceptance_criteria>
  <done>Cross-cutting query foundation is stable before REST orchestration.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Burst requests bypass limit | in-memory limiter | Thread-safe per-user bucket and focused concurrency tests. |
| Horizontal scaling false guarantee | limiter design | Document single-instance limitation; no Redis in Phase 6. |
| Query audit omitted for denials/failures | orchestrator outcomes | Audit helper exposes every significant final outcome, including 429. |
| Python outage blocks UI indefinitely | Python client | Bounded timeout maps to `TIMEOUT`/`AI_UNAVAILABLE` outcomes. |
</threat_model>

<verification>
- [ ] `ChatRateLimiter` unit tests pass, including concurrent calls and stale cleanup.
- [ ] `ChatQueryAuditService` tests pass and use existing `AuditEventWriter`.
- [ ] `PythonQueryClient` tests pass and prove required fields/outcome mapping.
- [ ] No Redis/shared cache/Postgres rate counter is introduced.
- [ ] `cd backend; mvn -q -pl corp-rag-app -am test` passes or all failures are unrelated and documented.
</verification>

<success_criteria>
- Query audit design is available before `/chat/query` orchestration.
- Rate limiting can reject 429 before persistence.
- Java can call Python `/v1/query` through a typed, bounded adapter.
</success_criteria>

<output>
Write `06-03-SUMMARY.md` with audit event names/details, limiter behavior, Python client config/outcome mappings, and any dependencies for Plan 05.
</output>
