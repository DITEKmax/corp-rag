---
phase: "06-chat-frontend-experience"
plan: "07"
type: "execute"
wave: 5
depends_on: ["04", "05", "06"]
files_modified:
  - "frontend/js/pages/chat-page.js"
  - "frontend/js/api/chat-api.js"
  - "frontend/js/components/chat/**"
  - "frontend/js/components/source-modal.js"
  - "frontend/styles/chat.css"
  - "frontend/js/core/routes.js"
autonomous: true
requirements: ["CHAT-01", "CHAT-02", "UI-02"]
user_setup:
  - "Requires Java chat lifecycle/query endpoints from Plans 04 and 05 and the frontend shell/API client from Plan 06."
must_haves:
  truths:
    - "The browser talks only to Java `/api/v1`; chat UI must never call Python."
    - "First user message uses lazy create: `POST /chat/conversations`, then `POST /chat/query` with the returned `conversationId`."
    - "Manual retry posts the same message text and same conversationId as a new `/chat/query`, preserving the original failed/degraded pair."
    - "DEGRADED from BL-02 renders a compact retry-focused bubble with no answer text and no citation chips."
    - "Source modal renders only already-returned citation quote/snippet fields; no Python chunk call and no deferred citation-details endpoint."
  artifacts:
    - path: "frontend/js/core/api-client.js"
      provides: "single Java API client"
      contains: "credentials, refresh, ProblemDetails mapping"
    - path: "frontend/js/core/routes.js"
      provides: "guarded route table"
      contains: "chat route access and nav metadata"
    - path: "contracts/openapi/api-v1.yaml"
      provides: "chat response/message/citation schemas"
      contains: "Message status, citations, retrievalMeta, quote, snippet"
    - path: "backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ChatController.java"
      provides: "browser-facing chat endpoints"
      contains: "conversation lifecycle, messages, query"
  decision_refs:
    - "D-224"
    - "D-228"
    - "D-229"
    - "D-230"
    - "D-231"
    - "D-238"
    - "D-243"
    - "D-244"
    - "D-245"
    - "D-246"
    - "D-247"
    - "D-248"
    - "D-273"
    - "D-274"
    - "D-275"
    - "D-277"
    - "D-278"
    - "D-279"
    - "D-280"
    - "D-281"
    - "D-282"
    - "D-283"
    - "D-284"
    - "D-285"
    - "D-326"
    - "D-327"
    - "D-328"
    - "D-329"
---

<objective>
Build the browser chat experience on top of Java: conversation list, persisted message history, lazy-create input flow, outcome-specific assistant bubbles, manual retry, citation chips, quote-only source modal, qualitative confidence, and collapsed retrieval diagnostics.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-frontend-experience/06-CONTEXT.md
@.planning/phases/06-chat-frontend-experience/06-RESEARCH.md
@.planning/phases/06-chat-frontend-experience/06-04-SUMMARY.md
@.planning/phases/06-chat-frontend-experience/06-05-SUMMARY.md
@.planning/phases/06-chat-frontend-experience/06-06-SUMMARY.md
@contracts/openapi/api-v1.yaml
@frontend/js/core/api-client.js
@frontend/js/core/router.js
@frontend/js/core/routes.js
@frontend/styles/base.css
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add chat API wrapper and route integration</name>
  <files>frontend/js/api/chat-api.js, frontend/js/core/routes.js, frontend/js/pages/chat-page.js</files>
  <read_first>frontend/js/core/api-client.js, frontend/js/core/routes.js, contracts/openapi/api-v1.yaml</read_first>
  <action>Create a thin chat API wrapper that uses only the central API client for list/create/delete conversations, list messages, and query. Add the chat route to the declarative route table with the correct auth/permission access. Do not call `/chat/messages/{messageId}/citations`; do not call Python; do not call `fetch` directly.</action>
  <verify>
    <automated>node --check frontend/js/api/chat-api.js</automated>
    <automated>python -c "from pathlib import Path; bad=[]; [bad.append(str(p)) for p in Path('frontend/js').rglob('*.js') if p.name!='api-client.js' and 'fetch(' in p.read_text(encoding='utf-8')]; assert not bad, bad"</automated>
  </verify>
  <acceptance_criteria>
    - Chat wrapper exposes Java API methods only.
    - Chat route is guarded by the shared route/permission mechanism.
    - No deferred citation detail endpoint is used.
  </acceptance_criteria>
  <done>Chat feature has a contract-aligned Java API boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement conversation list, message history, and input flow</name>
  <files>frontend/js/pages/chat-page.js, frontend/js/components/chat/**, frontend/styles/chat.css</files>
  <read_first>frontend/styles/base.css, frontend/js/components, contracts/openapi/api-v1.yaml</read_first>
  <action>Build the chat page as the first screen for the chat route: a conversation list ordered by backend `updatedAt DESC`, message history, and composer. On an empty/new chat, submit first user text by creating a conversation first, then posting `/chat/query` with the returned `conversationId`. Existing conversations post directly. After query success or ProblemDetails outcome, refresh the relevant conversation messages so persisted status rows are the source of truth. Deleted conversations disappear from the list. Keep layout dense and operational, not a marketing landing page.</action>
  <verify>
    <automated>node --check frontend/js/pages/chat-page.js</automated>
  </verify>
  <acceptance_criteria>
    - No implicit create inside `/chat/query`.
    - Duplicate user texts from manual retry remain visible.
    - Conversation list shows only title, updatedAt, and messageCount.
    - Empty/error/loading states are basic but coherent.
  </acceptance_criteria>
  <done>Users can browse conversations and send messages through Java chat endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Render assistant outcomes and manual retry behavior</name>
  <files>frontend/js/components/chat/message-list.js, frontend/js/components/chat/message-bubble.js, frontend/js/pages/chat-page.js, frontend/styles/chat.css</files>
  <read_first>contracts/openapi/api-v1.yaml, .planning/phases/06-chat-frontend-experience/06-CONTEXT.md</read_first>
  <action>Render bubble variants from assistant status. `ANSWERED` shows answer text, qualitative confidence, citation chips, and optional diagnostics. `REFUSED_GUARD` shows a distinct guard refusal without retry. `NO_EVIDENCE` explains no supported answer was found without retry. `DEGRADED` shows compact wording that the answer could not be formatted with proper source references plus retry; do not render answer text or citation chips. `TIMEOUT` and `AI_UNAVAILABLE` show retry-focused service failure states. Retry button appears only for `DEGRADED`, `TIMEOUT`, and `AI_UNAVAILABLE` and resubmits the same text as a new query pair.</action>
  <verify>
    <automated>node --check frontend/js/components/chat/message-bubble.js</automated>
  </verify>
  <acceptance_criteria>
    - BL-02 degraded state is not an empty bubble and does not display rejected answer text.
    - REFUSED_GUARD and NO_EVIDENCE have no retry button.
    - Retry does not overwrite the original failed/degraded pair.
    - Failed/refused rows remain visible in history.
  </acceptance_criteria>
  <done>Outcome-specific rendering matches backend status semantics and safety decisions.</done>
</task>

<task type="auto">
  <name>Task 4: Add citation chips, quote-only source modal, and diagnostics</name>
  <files>frontend/js/components/chat/citation-chip.js, frontend/js/components/source-modal.js, frontend/js/components/chat/diagnostics-panel.js, frontend/styles/chat.css</files>
  <read_first>contracts/openapi/api-v1.yaml, .planning/phases/05.1-phase-5-uat-fix-wave/05.1-05-SUMMARY.md</read_first>
  <action>For `ANSWERED` messages, map inline `[N]` to `citations[N-1]` for chips. Chips preview `snippet`; clicking opens a modal from returned citation fields only: `documentTitle`, `sectionPath`, `quote`, `pageNumber`, and `accessLevel`. Modal body shows document text only; guard against displaying `entity:`/graph marker strings if bad data slips through by treating it as a rendering error rather than normal source text. Add a collapsed diagnostics block that consumes nullable `retrievalMeta` from fresh responses and history using one component. Display confidence as a qualitative label, not a raw number.</action>
  <verify>
    <automated>node --check frontend/js/components/source-modal.js</automated>
    <automated>node --check frontend/js/components/chat/diagnostics-panel.js</automated>
  </verify>
  <acceptance_criteria>
    - Source modal makes zero network calls.
    - Citation chip indexes resolve against the returned citation array.
    - Source modal never shows `entity:X` markers as document text.
    - Diagnostics are hidden by default and omitted when `retrievalMeta` is null.
  </acceptance_criteria>
  <done>UI-02 source inspection is complete without full-content viewer scope creep.</done>
</task>

<task type="auto">
  <name>Task 5: Browser-smoke chat workflow</name>
  <files>frontend/js/pages/chat-page.js, frontend/styles/chat.css, .planning/phases/06-chat-frontend-experience/06-07-SUMMARY.md</files>
  <read_first>.planning/phases/06-chat-frontend-experience/06-06-SUMMARY.md</read_first>
  <action>Run static checks and a browser smoke against Java when available. Verify list, lazy create, query, message refresh, degraded/timeout retry states with mocked or forced backend responses if needed, source modal rendering from quote/snippet, and reload history retaining citations/retrievalMeta. Record any endpoints that must be live for Plan 09.</action>
  <verify>
    <automated>python -c "from pathlib import Path; bad=[]; [bad.append(str(p)) for p in Path('frontend/js').rglob('*.js') if p.name!='api-client.js' and 'fetch(' in p.read_text(encoding='utf-8')]; assert not bad, bad"</automated>
    <manual>Open the frontend in a browser and verify chat list/history/query/source modal against Java.</manual>
  </verify>
  <acceptance_criteria>
    - Chat UI can recover from refresh/reload using `listMessages`.
    - Source modal works from persisted citation snapshots.
    - No direct Python URL appears in frontend code.
  </acceptance_criteria>
  <done>Chat frontend is ready for full end-to-end UAT.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Frontend bypasses Java access checks | chat API wrappers | Central Java-only API client and static no-direct-fetch/no-Python checks. |
| Rejected BL-02 text displayed | degraded bubble | `DEGRADED` renders no content/citations, only retry copy. |
| Source viewer leaks arbitrary chunks | citation modal | Uses returned quote/snippet only; no chunk detail request. |
| Snapshot mistaken for live access | source modal | Modal is history display only; no full-content viewer or live chunk fetch in Phase 6. |
</threat_model>

<verification>
- [ ] `node --check` passes for chat modules.
- [ ] Static grep proves no feature module calls `fetch` directly and no frontend code references Python URLs.
- [ ] Chat route uses shared route guard/nav.
- [ ] Browser smoke covers lazy create, query, history reload, retry states, citation chips, source modal, and diagnostics.
</verification>

<success_criteria>
- UI-02 is implemented on Java chat endpoints.
- Users can create/continue conversations, see persisted history, open cited source text, and retry degraded/unavailable outcomes.
</success_criteria>

<output>
Write `06-07-SUMMARY.md` with chat UI module map, status rendering rules, source modal scope, and browser-smoke evidence or explicit blockers.
</output>
