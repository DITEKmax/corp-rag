---
phase: "06-chat-frontend-experience"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "contracts/openapi/api-v1.yaml"
  - "contracts/openapi/ai-service-v1.yaml"
  - "contracts/constants.yaml"
  - "scripts/verify-contracts.py"
autonomous: true
requirements: ["CHAT-01", "CHAT-02", "UI-01", "UI-02", "UI-03"]
user_setup:
  - "On Windows, use the local Maven binary if plain `mvn` is unavailable: set `MAVEN_CMD=C:\\dev\\apache-maven-3.9.15\\bin\\mvn.cmd` before running `scripts/verify-contracts.py`."
  - "Do not start Java chat implementation until this contract-first wave passes contract verification."
must_haves:
  truths:
    - "Contract-first wave must run before Java implementation: OpenAPI DTO changes drive ChatController, Message mapping, and frontend history rendering."
    - "The browser-facing contract remains Java-only; no frontend or Java source-viewer path may call Python directly."
    - "Non-ANSWERED assistant outcomes need nullable message content so the UI can distinguish no answer text from an empty answer."
    - "The `/chat/messages/{messageId}/citations` detail path is deferred in Phase 6; quote-only source viewing is the implemented scope."
  artifacts:
    - path: "contracts/openapi/api-v1.yaml"
      provides: "frontend-facing Java REST contract"
      contains: "Chat conversations/messages/query schemas, Message.retrievalMeta, deferred getCitationDetails description, rate-limit response"
    - path: "contracts/openapi/ai-service-v1.yaml"
      provides: "Python query boundary contract"
      contains: "required QueryRequest.conversationId and real chunk detail path text"
    - path: "scripts/verify-contracts.py"
      provides: "contract lint, Java DTO generation/compile, Python contract generation"
      contains: "the required verification command before backend work"
  decision_refs:
    - "D-223"
    - "D-224"
    - "D-228"
    - "D-240"
    - "D-255"
    - "D-267"
    - "D-273"
    - "D-274"
    - "D-275"
    - "D-276"
    - "D-281"
    - "D-283"
    - "D-317"
    - "D-321"
    - "D-324"
    - "D-326"
    - "D-327"
    - "D-328"
    - "D-329"
    - "D-330"
---

<objective>
Align Phase 6 contracts before implementation: message outcomes, nullable history fields, retrieval diagnostics in `Message`, quote-only source viewer scope, and the `/chat/query` rate-limit problem response must be explicit and verified before Java DTOs or frontend components are written.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-frontend-experience/06-CONTEXT.md
@.planning/phases/06-chat-frontend-experience/06-RESEARCH.md
@.planning/phases/06-chat-frontend-experience/06-VALIDATION.md
@contracts/openapi/api-v1.yaml
@contracts/openapi/ai-service-v1.yaml
@contracts/constants.yaml
@scripts/verify-contracts.py
@backend/corp-rag-app/src/main/java/com/corprag/security/OriginRefererValidationFilter.java
@backend/corp-rag-app/src/main/java/com/corprag/service/auth/RefreshTokenService.java
@backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsWriter.java
@backend/corp-rag-app/src/main/java/com/corprag/service/audit/AuditEventWriter.java
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify first-wave backend anchors by reading actual code paths</name>
  <files>contracts/openapi/api-v1.yaml, contracts/openapi/ai-service-v1.yaml, contracts/constants.yaml</files>
  <read_first>backend/corp-rag-app/src/main/java/com/corprag/security/OriginRefererValidationFilter.java, backend/corp-rag-app/src/main/java/com/corprag/config/SecurityConfig.java, backend/corp-rag-app/src/main/java/com/corprag/config/AppSecurityProperties.java, backend/corp-rag-app/src/main/java/com/corprag/service/auth/RefreshTokenService.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsWriter.java, backend/corp-rag-app/src/main/java/com/corprag/adapter/rest/ProblemDetailsExceptionHandler.java, backend/corp-rag-app/src/main/java/com/corprag/service/audit/AuditEventWriter.java</read_first>
  <action>Use `rg`/file view to confirm exact class names and paths before editing contract text. Confirm Java uses Origin/Referer validation and no double-submit CSRF token, confirm refresh-token rotation/single-flight implications, confirm ProblemDetails field names, and confirm `RATE_LIMIT_EXCEEDED` already exists or add it through `contracts/constants.yaml` in the existing style only if missing.</action>
  <verify>
    <automated>rg -n "OriginRefererValidationFilter|csrf\\(|RefreshTokenService|ProblemDetailsWriter|AuditEventWriter|RATE_LIMIT_EXCEEDED" backend contracts</automated>
  </verify>
  <acceptance_criteria>
    - The implementation notes in this plan's summary name exact verified classes/paths, not inferred names.
    - No frontend CSRF-token work is planned unless a real double-submit token path is found.
    - Rate-limit ProblemDetails uses existing `errorCode`/`correlationId`/`details` conventions.
  </acceptance_criteria>
  <done>Verification flags D-321 and D-324 have concrete evidence before contract edits proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Align chat/message OpenAPI schemas for persisted outcomes</name>
  <files>contracts/openapi/api-v1.yaml</files>
  <read_first>contracts/openapi/api-v1.yaml, .planning/phases/06-chat-frontend-experience/06-CONTEXT.md</read_first>
  <action>Update the chat schemas so generated Java DTOs can represent Phase 6 storage and UI behavior. Add an assistant outcome/status enum with `ANSWERED`, `REFUSED_GUARD`, `NO_EVIDENCE`, `DEGRADED`, `TIMEOUT`, and `AI_UNAVAILABLE`. Add nullable `retrievalMeta` to `Message` using the existing `RetrievalMeta` schema. Make `Message.content` nullable or remove it from required fields so non-ANSWERED assistant rows are not forced to carry placeholder text. Add nullable/optional citation snapshots to `Message` for answered assistant history. Align `ChatQueryResponse` with outcomes: successful 200 responses must expose enough status data for `ANSWERED`, `NO_EVIDENCE`, and `DEGRADED`; guard/timeouts/unavailable may still use ProblemDetails while their persisted rows are visible through `listMessages`.</action>
  <verify>
    <automated>python -c "from pathlib import Path; text=Path('contracts/openapi/api-v1.yaml').read_text(encoding='utf-8'); required=['retrievalMeta','ANSWERED','REFUSED_GUARD','NO_EVIDENCE','DEGRADED','TIMEOUT','AI_UNAVAILABLE']; missing=[t for t in required if t not in text]; assert not missing, missing"</automated>
  </verify>
  <acceptance_criteria>
    - `Message.retrievalMeta` is nullable and reuses `RetrievalMeta`.
    - `Message.content` can be absent/null for non-ANSWERED assistant rows.
    - Assistant statuses are represented in the contract with the same names used by schema and frontend.
    - The contract preserves `conversationId` as required on `ChatQueryRequest`.
  </acceptance_criteria>
  <done>Generated Java DTOs can model Phase 6 message history without ad hoc handwritten DTO drift.</done>
</task>

<task type="auto">
  <name>Task 3: Fix/defer source-viewer detail contract and rate-limit response</name>
  <files>contracts/openapi/api-v1.yaml, contracts/openapi/ai-service-v1.yaml, contracts/constants.yaml</files>
  <read_first>contracts/openapi/api-v1.yaml, contracts/openapi/ai-service-v1.yaml, contracts/constants.yaml</read_first>
  <action>In `api-v1.yaml`, mark `/chat/messages/{messageId}/citations` / getCitationDetails as deferred/not implemented in Phase 6 and fix the stale Python dependency text from `GET /chunks/{chunkId}` to `GET /v1/documents/{documentId}/chunks/{chunkId}`. Do not add Java or Python chunk proxy semantics to the Java contract. Add or verify a `429` response on `POST /chat/query` using RFC7807 `ProblemDetail`, existing `RATE_LIMIT_EXCEEDED` error-code style, `correlationId`, and a `Retry-After` response header. If `ai-service-v1.yaml` contains the same stale source-viewer wording, fix the text only without changing implemented paths.</action>
  <verify>
    <automated>python -c "from pathlib import Path; api=Path('contracts/openapi/api-v1.yaml').read_text(encoding='utf-8'); assert 'GET /chunks/{chunkId}' not in api; assert '/v1/documents/{documentId}/chunks/{chunkId}' in api; assert 'Retry-After' in api; assert 'RATE_LIMIT_EXCEEDED' in Path('contracts/constants.yaml').read_text(encoding='utf-8')"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 6 contract describes quote/snippet modal scope and deferred full-content viewer honestly.
    - The source-viewer description uses the real Python chunk detail path when discussing deferred work.
    - `POST /chat/query` documents `429` with `Retry-After` and project ProblemDetails fields.
  </acceptance_criteria>
  <done>Contract debt D-276 and rate-limit shape D-267 are fixed before implementation.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate and verify contract outputs</name>
  <files>contracts/openapi/api-v1.yaml, contracts/openapi/ai-service-v1.yaml, contracts/constants.yaml</files>
  <read_first>scripts/verify-contracts.py, backend/corp-rag-contracts/pom.xml</read_first>
  <action>Run the repository contract verifier so Java generated DTOs, constants, and Python generated contract modules compile from the updated sources. Do not proceed to backend plans if this fails.</action>
  <verify>
    <automated>$env:MAVEN_CMD='C:\dev\apache-maven-3.9.15\bin\mvn.cmd'; uv run --project ai-service --group dev python scripts/verify-contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - OpenAPI/AsyncAPI YAML parses.
    - Java contract generation and compile pass.
    - Python generated contract smoke imports pass.
    - No generated code is committed if project conventions ignore it.
  </acceptance_criteria>
  <done>Contract-first gate is green for all downstream Phase 6 plans.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Invalid UI state from under-specified outcomes | `Message` and `ChatQueryResponse` schemas | Status enum and nullable content make degraded/refused/failure rows explicit. |
| Accidental Python source-viewer bypass | citation detail contract | Mark detail endpoint deferred; source modal uses returned quote/snippet only. |
| Contract drift before Java implementation | generated DTOs | Run `verify-contracts` before backend code starts. |
| CSRF mechanism guessed incorrectly | frontend API-client assumptions | Task 1 reads actual Java filter/config before contract and frontend work rely on it. |
</threat_model>

<verification>
- [ ] Exact Java security/problem/audit anchors verified with file reads.
- [ ] `api-v1.yaml` includes nullable `Message.retrievalMeta`.
- [ ] `api-v1.yaml` supports assistant outcome/status and nullable message content.
- [ ] `api-v1.yaml` marks getCitationDetails deferred for Phase 6 and fixes the real Python chunk path text.
- [ ] `POST /chat/query` documents `429` ProblemDetails and `Retry-After`.
- [ ] `$env:MAVEN_CMD='C:\dev\apache-maven-3.9.15\bin\mvn.cmd'; uv run --project ai-service --group dev python scripts/verify-contracts.py` passes.
</verification>

<success_criteria>
- Contract changes required by D-276 and D-283 are complete before Java implementation.
- Generated DTO surfaces can represent Phase 6 chat persistence outcomes.
- No Java or frontend plan downstream must guess response/status shapes.
</success_criteria>

<output>
Write `06-01-SUMMARY.md` with verified class paths, contract changes, verifier result, and any contract-generation caveats for downstream backend/frontend plans.
</output>
