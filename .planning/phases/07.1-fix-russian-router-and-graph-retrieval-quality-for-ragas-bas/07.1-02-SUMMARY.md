---
phase: 07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas
plan: "02"
subsystem: eval-maintenance
tags: [neo4j, qdrant, postgres, rabbitmq, russian-golden-corpus]

requires:
  - phase: 07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas
    provides: 07.1-01 router and graph retrieval code fixes
provides:
  - Testable graph corpus state verifier for frozen golden metadata.
  - Live Neo4j document set repaired to exactly the 16 Russian golden document IDs.
  - Missing crew rostering document graph rebuilt under UUID aa939285-1629-450d-b1f4-0e63ec6ba1c6.
  - Java Postgres neo4j_entity_count reconciled for all 16 golden documents.
affects: [ragas, graph-retrieval, eval-harness, phase-8]

tech-stack:
  added: []
  patterns:
    - Eval maintenance helpers accept injected rows and can run without live databases.
    - Live graph cleanup uses the existing document-scoped DETACH DELETE shape.

key-files:
  created:
    - ai-service/eval/graph_corpus_state.py
    - ai-service/tests/test_eval_graph_corpus_state.py
  modified:
    - .planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-02-SUMMARY.md

key-decisions:
  - Used RabbitMQ document.uploaded replay for aa939285 so normal ingestion components rebuilt Qdrant, parent chunks, entity extraction, and Neo4j graph state.
  - Used a targeted Java Postgres reconciliation update for aa939285 because Java markIndexed intentionally updates only UPLOADED documents and this baseline document was already INDEXED.
  - Did not run /v1/query or RAGAS during this maintenance plan.

patterns-established:
  - Graph corpus verification compares live Document IDs against golden_ru.meta.json and reports missing, extra, and zero-entity sets.
  - Live corpus repair evidence records before/after store counts and exact document IDs touched.

requirements-completed: [ING-06, RET-02, RET-03, EVAL-02]

duration: "in-session"
completed: 2026-06-01
---

# Phase 07.1 Plan 02 Summary

**Frozen Russian graph corpus state repaired for post-fix RAGAS comparison.**

## Accomplishments

- Added `eval.graph_corpus_state`, a read-only verifier that loads expected IDs from `golden_ru.meta.json` and reports `missing`, `extra`, and `entity_count_zero`.
- Removed exactly six stale non-golden Neo4j `Document` nodes by UUID/title using the locked cleanup shape `MATCH (d:Document {id: $documentId}) DETACH DELETE d`.
- Replayed a `document.uploaded` event for `aa939285-1629-450d-b1f4-0e63ec6ba1c6` from existing Java metadata and MinIO object; normal ingestion rebuilt Qdrant, parent chunks, entity extraction, and Neo4j graph data.
- Reconciled Java Postgres `documents.neo4j_entity_count` for `aa939285...` from 0 to the rebuilt Neo4j count 8.

## Commits

1. `c2550f6` - `feat(07.1-02): add graph corpus state verifier`

## Verification

- `uv run --project ai-service --group dev pytest ai-service/tests/test_eval_graph_corpus_state.py` -> 3 passed.
- `uv run --project ai-service --group dev pytest ai-service/tests` -> 278 passed, 12 skipped, 1 warning.
- `uv run --project ai-service --group dev python -m eval.graph_corpus_state --neo4j-json ...` -> `ok=true`, `expected_count=16`, `neo4j_count=16`, no missing/extra/zero-entity docs.

## Live Repair Evidence

Before repair:

| Store | Evidence |
| --- | --- |
| Neo4j labels | `Document=21`, `Entity=176`, `RelationMention=137` |
| Neo4j relationships | `EVIDENCE=121`, `MENTIONED_IN=182`, `SOURCE=137`, `TARGET=137` |
| Neo4j stale docs | `68b7e695...` Acme Remote Work Policy; `ffa5329d...` TechCorp Approved Vendor List; `e78f4686...` TechCorp Phase 5 Query Policy; `73e1071b...` TechCorp Q1 2026 Security Incident Report; `d4d80f41...` Politica; `0bf68dd1...` Title |
| Neo4j missing golden | `aa939285-1629-450d-b1f4-0e63ec6ba1c6` Политика планирования экипажей |
| Java golden row for aa939285 | `INDEXED`, `neo4j_entity_count=0`, MinIO key `2026/06/aa939285-1629-450d-b1f4-0e63ec6ba1c6.txt` |

Actions:

- Deleted exactly the six stale Neo4j documents listed above; no golden UUID was deleted.
- Published `document.uploaded` replay event `0655f0a3-3397-44e5-8040-8286f10603dd` with correlation `1b91916a-7340-4c1e-8b50-2cee1aa055c2`.
- Python ingestion processed the replay and Java processed resulting `document.indexed` event `21ec274e-75ba-4776-8e5c-3d1e88489c36`.
- Java `DocumentRepository.markIndexed` returned no data update because the row was already `INDEXED` and the method only updates `status='UPLOADED'`; reconciled `neo4j_entity_count` with a targeted SQL update for `aa939285...` only.

After repair:

| Store | Evidence |
| --- | --- |
| Neo4j verifier | `ok=true`, `expected_count=16`, `neo4j_count=16`, `missing=[]`, `extra=[]`, `entity_count_zero=[]` |
| Neo4j labels | `Document=16`, `Entity=183`, `RelationMention=143` |
| Neo4j relationships | `EVIDENCE=106`, `MENTIONED_IN=158`, `SOURCE=143`, `TARGET=143` |
| Rebuilt aa939285 graph | `entity_count=8`, `relation_count=6`, `mention_count=8` |
| Java golden rows | `16/16` golden docs `INDEXED` with `neo4j_entity_count > 0`; `zero_or_null=0` |
| Qdrant | `pointCount=16`, `documentCount=16`, `missing=[]`, `extra=[]` versus `golden_ru.meta.json` |
| RabbitMQ | `ai.document.uploaded=0 ready / 0 unacked`, `backend.document.indexed=0 ready / 0 unacked` |

RabbitMQ note: `backend.document.indexed.dlq` still contains one pre-existing May 18 stale Acme event (`50dd6edd...`), unchanged by this repair.

## Deviations

The preferred replay path was used. One targeted Java Postgres reconciliation update was necessary because the Java consumer intentionally refuses to update already `INDEXED` rows. This kept Java and Neo4j entity counts consistent without weakening contracts or changing code.

## Guardrails

- No `/v1/query` call was made.
- No RAGAS run was executed.
- No golden metadata, corpus file, or expected UUID was changed.
- No Qdrant, Neo4j, or Postgres store was recreated.
- Output guard, citation validation, access filtering, and production retrieval defaults were not changed.

## Next

Plan 07.1-03 can run the single clean post-fix RAGAS comparison with topK=10, parent-context extraction, bge-m3 embeddings, DeepSeek judge, and concurrency=1. The post-fix report should remain uncommitted until user validation.

---
*Phase: 07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas*
*Completed: 2026-06-01*
