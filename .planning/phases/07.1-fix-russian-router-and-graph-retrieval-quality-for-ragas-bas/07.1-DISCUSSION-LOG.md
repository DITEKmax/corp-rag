# Phase 07.1: Fix Russian router and graph retrieval quality for RAGAS baseline - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-01
**Phase:** 07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas
**Areas discussed:** Scope split, router fix, graph aggregation, Neo4j corpus repair, eval metadata, invariants

---

## Scope Split

| Option | Description | Selected |
|--------|-------------|----------|
| Fix all four now | Router, aggregation, multi-hop Cypher, and graph index repair all land in Phase 07.1. | |
| Narrow 07.1, defer multi-hop redesign | Fix router, Cyrillic aggregation, graph corpus state, and eval diagnostics now; defer full text-conditioned multi-hop Cypher to Phase 8. | ✓ |
| Diagnose only | Produce more findings without remediation. | |

**User's choice:** Open Phase 07.1 as a fix phase, but treat multi-hop Cypher as potentially too large.
**Notes:** The captured decision is to keep 07.1 narrow. Multi-hop graph retrieval is a real issue, but full semantic path ranking/entity linking is deferred to Phase 8.

---

## Router Fix

| Option | Description | Selected |
|--------|-------------|----------|
| Retrieve despite unsupported | Change `UNSUPPORTED` so it can still retrieve evidence for answerable-looking questions. | |
| Add Russian-aware routing before LLM fallback | Keep pre-retrieval `UNSUPPORTED`, but prevent false unsupported outcomes by adding Russian route coverage. | ✓ |
| Replace router with trained classifier | Build or train a new route classifier. | |

**User's choice:** Fix the router side for five false `UNSUPPORTED` answerable records.
**Notes:** The invariant from Phase 5 remains: true `UNSUPPORTED` short-circuits before retrieval. The Phase 07.1 fix targets Russian aggregation/multi-hop questions that should never have fallen through English-only rules into low-confidence unsupported classification.

---

## Graph Aggregation

| Option | Description | Selected |
|--------|-------------|----------|
| Unicode-aware tokenization | Replace ASCII-only term extraction so Cyrillic query terms are retained. | ✓ |
| Bypass terms for Russian | If Russian is detected, ignore query terms and return broad graph evidence. | |
| Move aggregation to hybrid only | Stop using graph aggregation for the Russian corpus. | |

**User's choice:** Fix Cyrillic query-term extraction.
**Notes:** Access predicates and document-backed citation requirements stay unchanged. The specific failure is `[A-Za-z0-9]+` producing empty or near-empty terms for Russian questions, causing aggregation retrieval to miss relevant graph evidence.

---

## Neo4j Corpus Repair

| Option | Description | Selected |
|--------|-------------|----------|
| Recreate Neo4j from scratch | Drop/recreate the graph database and rebuild all documents. | |
| Targeted cleanup and one-doc graph rebuild | Delete six extra `Document` subgraphs by document ID and rebuild graph for the missing golden document while preserving its UUID. | ✓ |
| Leave graph state as finding only | Document the mismatch and defer repair. | |

**User's choice:** Clean and reindex graph state pointwise, not by recreating collections/databases.
**Notes:** Qdrant was already cleaned in 07-06; this phase targets Neo4j. The missing golden document is `Политика планирования экипажей` (`aa939285-1629-450d-b1f4-0e63ec6ba1c6`). The fix must preserve the UUID used by `golden_ru.meta.json`.

---

## Eval Metadata

| Option | Description | Selected |
|--------|-------------|----------|
| Keep current report shape | Continue storing only route and outcome. | |
| Add route source/reason to eval cache | Persist `route_source` and `route_reason` in query-phase JSON records for future diagnosis. | ✓ |
| Add frontend UI metadata | Surface route reason/source in the browser diagnostics panel. | |

**User's choice:** Add eval route-source diagnostics.
**Notes:** The previous read-only diagnosis could not reconstruct whether `UNSUPPORTED` came from rules, low confidence, or classifier fallback because `ragas_ru.json` did not store route source/reason. This is eval/report metadata, not a new frontend feature.

---

## Invariants

| Option | Description | Selected |
|--------|-------------|----------|
| Tune guards/access for eval | Loosen filters or guards to increase answered count. | |
| Preserve production safety contracts | Improve retrieval/routing while keeping output guard, access filters, and production contracts intact. | ✓ |
| Change golden data | Modify expected docs/questions to better match current behavior. | |

**User's choice:** Preserve guard/access/contract invariants and compare against the committed 07-06 baseline.
**Notes:** The post-fix RAGAS run is required for comparison, but generated reports should not be committed until the user validates the run.

---

## the agent's Discretion

- Exact Russian router patterns and Unicode tokenization implementation.
- Exact targeted graph rebuild mechanism, as long as it preserves the golden document UUID and Java/Neo4j consistency.
- Exact eval-only placement of `route_source` and `route_reason`.

## Deferred Ideas

- Full text-conditioned multi-hop graph retrieval and semantic path ranking for Phase 8.
- Frontend/admin UI for route diagnostics.
- Golden dataset mutation or new expected document UUIDs.
