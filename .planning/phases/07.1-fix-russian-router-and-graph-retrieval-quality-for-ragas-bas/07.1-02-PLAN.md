---
phase: "07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas"
plan: "02"
type: "maintenance"
wave: 2
depends_on: ["01"]
files_modified:
  - "ai-service/eval"
  - "ai-service/tests"
  - ".planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-02-SUMMARY.md"
autonomous: false
requirements: ["ING-06", "RET-02", "RET-03", "EVAL-02"]
user_setup:
  - "Live maintenance requires the local Docker stack with Postgres, Neo4j, RabbitMQ, MinIO, Java backend, and python-ai healthy."
  - "This plan may mutate live Neo4j and Java/Python indexing state for the frozen corpus only. Do not run `/v1/query` or RAGAS here."
  - "Do not recreate Neo4j, Qdrant, or Postgres. All mutations are targeted by document ID."
must_haves:
  truths:
    - "D-399: Neo4j cleanup is by `documentId`, not database recreation."
    - "D-400: Use the existing document-backed graph cleanup shape for the six extra documents and do not touch the 16 golden document IDs."
    - "D-401: Rebuild graph data for `aa939285-1629-450d-b1f4-0e63ec6ba1c6` using normal ingestion graph-building components, not hand-written synthetic Cypher."
    - "D-402: The missing golden document must keep its existing UUID."
    - "D-403: Prefer a targeted maintenance/reindex path that replays or reconstructs a `document.uploaded` event for existing Java document metadata and MinIO object."
    - "D-404: If direct Python maintenance is used, it must still use existing parser/chunker/entity-extractor/graph-indexer components and keep Java/Postgres `neo4j_entity_count` consistent."
    - "D-405: Verification must prove Neo4j contains exactly the 16 document IDs from `golden_ru.meta.json`, with no missing or extra docs, and Java Postgres has non-zero `neo4j_entity_count` for all golden docs unless explicitly documented."
    - "D-413: Do not weaken output guard, citation validation, prompt-injection checks, or weak-evidence thresholds."
    - "D-414: Do not broaden or bypass Qdrant/Neo4j access filtering."
    - "D-415: Do not change production `RetrievalOptions.topK` defaults or add production retrieval-mode switches."
    - "D-416: Do not mutate the frozen corpus, golden questions, reference answers, or expected document UUIDs."
  artifacts:
    - path: "ai-service/eval/golden/golden_ru.meta.json"
      provides: "authoritative 16 golden document IDs"
      contains: "manifest-to-live UUID mapping used for Neo4j verification"
    - path: "ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py"
      provides: "targeted graph cleanup and replace primitives"
      contains: "`cleanup_document` and `replace_document_graph` implementation to reuse or call through"
    - path: ".planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-02-SUMMARY.md"
      provides: "live data repair evidence"
      contains: "before/after Neo4j and Java Postgres counts, exact document IDs touched, and commands used"
  decision_refs:
    - "D-399"
    - "D-400"
    - "D-401"
    - "D-402"
    - "D-403"
    - "D-404"
    - "D-405"
    - "D-413"
    - "D-414"
    - "D-415"
    - "D-416"
---

<objective>
Repair live Neo4j corpus state for the frozen Russian golden corpus: remove six stale non-golden document subgraphs and rebuild the missing `Политика планирования экипажей` graph under its existing UUID.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-CONTEXT.md
@.planning/phases/07-evaluation-observability/07-05-SUMMARY.md
@.planning/phases/07-evaluation-observability/07-06-SUMMARY.md
@ai-service/eval/golden/golden_ru.meta.json
@ai-service/eval/corpus/manifest.json
@ai-service/eval/corpus/documents/ru-crew-rostering-policy.md
@ai-service/src/corp_rag_ai/pipeline/ingestion/orchestrator.py
@ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py
@backend/corp-rag-app/src/main/java/com/corprag/service/outbox/EventEnvelopeFactory.java
@backend/corp-rag-app/src/main/java/com/corprag/service/document/DocumentIndexingResultService.java
@infra/docker-compose.yml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add targeted corpus-state verification helper</name>
  <files>ai-service/eval, ai-service/tests</files>
  <read_first>ai-service/eval/golden/golden_ru.meta.json, ai-service/eval/validate_golden.py, ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py</read_first>
  <action>Add a small eval/maintenance verification helper or testable module that compares live Neo4j `Document` IDs with `golden_ru.meta.json`, reports missing and extra document IDs/titles, and optionally cross-checks Java Postgres `documents.neo4j_entity_count`. Keep it read-only unless an explicit cleanup/reindex command mode is selected in later tasks.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests</automated>
  </verify>
  <acceptance_criteria>
    - Verification logic is testable without live Neo4j/Postgres by injecting or mocking rows.
    - The helper prints or returns explicit `missing`, `extra`, and `entity_count_zero` sets.
    - It reads expected IDs from `golden_ru.meta.json` rather than duplicating them in code.
  </acceptance_criteria>
  <done>Live graph corpus state can be checked reproducibly before and after repair.</done>
</task>

<task type="manual">
  <name>Task 2: Snapshot live before-state and delete six extra Neo4j documents</name>
  <files>.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-02-SUMMARY.md</files>
  <read_first>ai-service/eval/golden/golden_ru.meta.json, .planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-CONTEXT.md</read_first>
  <action>With the local Docker stack healthy, record before-state counts by Neo4j label, relationship type, and per-document entity/relation counts. Delete exactly the six known non-golden `Document` subgraphs from Neo4j by document ID/title: `Acme Remote Work Policy`, `TechCorp Approved Vendor List`, `TechCorp Phase 5 Query Policy`, `TechCorp Q1 2026 Security Incident Report`, `Politica`, and `Title`. Use targeted `MATCH (d:Document {id: ...}) DETACH DELETE d` or the existing cleanup helper. Do not touch Qdrant or the 16 golden document IDs.</action>
  <verify>
    <manual>Run a live Neo4j query showing the six titles are gone.</manual>
    <manual>Run the corpus-state verification helper and record remaining missing/extra docs.</manual>
  </verify>
  <acceptance_criteria>
    - Summary records exact before-state counts and the six removed document IDs/titles.
    - Neo4j no longer contains the six stale document titles.
    - No golden document ID is deleted.
  </acceptance_criteria>
  <done>Neo4j stale non-golden document subgraphs are removed.</done>
</task>

<task type="manual">
  <name>Task 3: Rebuild graph for the missing golden document with preserved UUID</name>
  <files>ai-service/eval, .planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-02-SUMMARY.md</files>
  <read_first>ai-service/eval/corpus/documents/ru-crew-rostering-policy.md, ai-service/src/corp_rag_ai/pipeline/ingestion/orchestrator.py, ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py, backend/corp-rag-app/src/main/java/com/corprag/service/outbox/EventEnvelopeFactory.java</read_first>
  <action>Rebuild graph data for `aa939285-1629-450d-b1f4-0e63ec6ba1c6` (`Политика планирования экипажей`) using the normal ingestion components. Preferred path: reconstruct or replay a `document.uploaded` event for the existing Java document metadata and MinIO object, so Python re-parses/chunks/extracts/upserts and Java receives an updated indexed event. Alternate path: a direct Python maintenance command is allowed only if it uses existing parser/chunker/entity extractor/graph indexer components and then keeps Java Postgres `neo4j_entity_count` consistent with Neo4j. Do not create a replacement document UUID and do not edit golden metadata.</action>
  <verify>
    <manual>Neo4j per-document query shows `aa939285-1629-450d-b1f4-0e63ec6ba1c6` exists with non-zero entities and relation/evidence counts or an explicit documented reason if zero is legitimate.</manual>
    <manual>Java Postgres `documents.neo4j_entity_count` for `aa939285-1629-450d-b1f4-0e63ec6ba1c6` is non-zero and matches the rebuilt graph entity count.</manual>
  </verify>
  <acceptance_criteria>
    - The document UUID remains `aa939285-1629-450d-b1f4-0e63ec6ba1c6`.
    - Graph data is produced by normal extraction/indexing components, not hand-authored Cypher entities.
    - Java Postgres and Neo4j do not disagree about entity count.
  </acceptance_criteria>
  <done>The missing golden document is present in Neo4j and consistent with Java metadata.</done>
</task>

<task type="manual">
  <name>Task 4: Verify final corpus state without running query</name>
  <files>.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-02-SUMMARY.md</files>
  <read_first>ai-service/eval/golden/golden_ru.meta.json, .planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-CONTEXT.md</read_first>
  <action>Run final live verification for Neo4j, Java Postgres, Qdrant sanity, and RabbitMQ queue drain. Neo4j must contain exactly the 16 golden document IDs from `golden_ru.meta.json`, no extras, and no missing golden documents. Java Postgres must show all 16 golden documents indexed with non-zero `neo4j_entity_count` unless explicitly justified. Qdrant should still contain the 16 golden documents after the prior 07-06 cleanup. Do not run `/v1/query` or RAGAS in this plan.</action>
  <verify>
    <manual>Neo4j document ID set exactly equals `golden_ru.meta.json.indexed_document_ids`.</manual>
    <manual>Java Postgres document status/count query passes for all 16 golden IDs.</manual>
    <manual>RabbitMQ `ai.document.uploaded` queue has 0 ready and 0 unacknowledged messages.</manual>
    <manual>Qdrant `documents_chunks` document IDs still match the 16 golden IDs.</manual>
  </verify>
  <acceptance_criteria>
    - Summary includes before/after tables for Neo4j document count, entity count, relation count, and Java `neo4j_entity_count`.
    - Summary states no `/v1/query` or RAGAS run was executed.
    - Live state is ready for one clean post-fix RAGAS comparison run.
  </acceptance_criteria>
  <done>Frozen corpus graph state is clean and ready for RAGAS comparison.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Accidental deletion of golden graph data | Neo4j cleanup | Delete only explicit non-golden IDs/titles and verify expected ID set before/after. |
| Golden ID drift | Reindex path | Preserve existing Java document UUID and never re-upload as a new document. |
| Hand-authored graph hides extraction weakness | Rebuild path | Use existing ingestion graph-building components only. |
| Reported corpus state diverges across stores | Java/Neo4j/Qdrant | Cross-check all three stores and queue drain before RAGAS. |
</threat_model>

<verification>
- [ ] Targeted corpus-state helper tests pass if a helper is added.
- [ ] Neo4j no longer contains the six stale non-golden document titles.
- [ ] Neo4j contains exactly the 16 IDs from `ai-service/eval/golden/golden_ru.meta.json`.
- [ ] Java Postgres `documents.neo4j_entity_count` is non-zero for all 16 golden documents unless a documented exception is accepted.
- [ ] Qdrant still contains exactly the 16 golden document IDs.
- [ ] RabbitMQ upload queue is drained.
- [ ] No `/v1/query` or RAGAS live query run is executed in this plan.
</verification>

<success_criteria>
- Live Neo4j graph state is synchronized with the frozen golden corpus.
- The missing crew-rostering golden document is graph-indexed under its original UUID.
- Six stale Phase 5/UAT documents are absent from Neo4j.
- Java and Neo4j entity counts are consistent enough for the final comparison run.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-02-SUMMARY.md`.
</output>
