---
phase: "07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas"
plan: "03"
type: "eval"
wave: 3
depends_on: ["01", "02"]
files_modified:
  - "ai-service/eval/reports"
  - ".planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-03-SUMMARY.md"
autonomous: false
requirements: ["EVAL-02", "OPS-01"]
user_setup:
  - "Requires local Docker stack healthy, reranker enabled, OpenRouter/DeepSeek judge credentials in `infra/.env`, and the cleaned Neo4j corpus state from 07.1-02."
  - "Run exactly one clean full RAGAS comparison pass unless the user explicitly authorizes another."
  - "Do not commit generated post-fix RAGAS report files until the user validates the run."
must_haves:
  truths:
    - "D-409: After fixes, rerun one clean full RAGAS pass over the same frozen 40-record Russian golden dataset for comparison against baseline commit `4d23614`."
    - "D-410: Comparison reports answered count, `reranker_degraded_count`, faithfulness, answer relevancy, context precision, context recall, outcome accuracy, citation document recall, and route mix."
    - "D-411: Post-fix run uses production `/v1/query`, local `BAAI/bge-m3` RAGAS embeddings, DeepSeek judge from `infra/.env`, topK 10, and concurrency 1 unless a concrete blocker is documented."
    - "D-412: Post-fix report is a comparison artifact and must not overwrite/hide the committed 07-06 baseline story."
    - "D-413: Do not weaken output guard, citation validation, prompt-injection checks, or weak-evidence thresholds."
    - "D-414: Do not broaden or bypass Qdrant/Neo4j access filtering."
    - "D-415: Do not change production `RetrievalOptions.topK` defaults or add production retrieval-mode switches."
    - "D-416: Do not mutate the frozen corpus, golden questions, reference answers, or expected document UUIDs."
    - "D-417: Do not commit generated post-fix RAGAS reports until the user validates that the run is clean and comparable."
  artifacts:
    - path: "ai-service/eval/reports/ragas_ru.json"
      provides: "baseline and/or post-fix machine-readable RAGAS report"
      contains: "per-record query cache, metrics, route mix, and route diagnostics after 07.1-01"
    - path: "ai-service/eval/reports/ragas_ru.md"
      provides: "human-readable RAGAS report"
      contains: "post-fix metrics and comparison notes after user validation"
    - path: ".planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-03-SUMMARY.md"
      provides: "comparison summary"
      contains: "before/after metrics against commit `4d23614`, diagnostics delta, and whether reports were intentionally left uncommitted"
  decision_refs:
    - "D-409"
    - "D-410"
    - "D-411"
    - "D-412"
    - "D-413"
    - "D-414"
    - "D-415"
    - "D-416"
    - "D-417"
---

<objective>
Run one clean post-fix production RAGAS comparison over the same 40-record Russian golden dataset and present before/after metrics against baseline commit `4d23614` without committing generated reports before user validation.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-CONTEXT.md
@.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-01-SUMMARY.md
@.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-02-SUMMARY.md
@.planning/phases/07-evaluation-observability/07-06-SUMMARY.md
@ai-service/eval/golden/golden_ru.jsonl
@ai-service/eval/golden/golden_ru.meta.json
@ai-service/eval/query_client.py
@ai-service/eval/ragas_runner.py
@ai-service/eval/reports/ragas_ru.json
@infra/.env.example
</context>

<tasks>
<task type="manual">
  <name>Task 1: Establish clean pre-run diagnostics and baseline comparison source</name>
  <files>.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-03-SUMMARY.md</files>
  <read_first>.planning/phases/07-evaluation-observability/07-06-SUMMARY.md, ai-service/eval/reports/ragas_ru.json</read_first>
  <action>Confirm the Docker stack is healthy, reranker is not degraded, Neo4j/Qdrant corpus state matches the 16 golden IDs, and `/diagnostics` counters are captured before the run. Extract baseline metrics from the committed 07-06 report at commit `4d23614` or from the current committed report if unchanged, not from assumptions: answered count 15, `reranker_degraded_count` delta 0, faithfulness 0.9167, answer relevancy 0.8561, context precision 0.9889, context recall 1.0, outcome accuracy 0.55, citation document recall 0.50. Treat `outcome_accuracy=0.55` as the committed report's all-record outcome-match metric whose failed status reflects weak answerability, including only 15/30 answerable records answered; this is expected to improve after the router fix. Record exact baseline source.</action>
  <verify>
    <manual>`/diagnostics` before-run snapshot captured with `reranker_degraded_count`, `answered_count`, `query_count`, and `mean_latency_ms`.</manual>
    <manual>Baseline report source is identified by path and commit/hash.</manual>
  </verify>
  <acceptance_criteria>
    - Summary records before-run diagnostics and baseline metric source.
    - Summary states that `outcome_accuracy=0.55` and `citation_doc_recall=0.50` are extracted from the committed 07-06 report/RAGAS JSON, not computed ad hoc during 7.1 planning.
    - If stack or corpus state is dirty, stop and report the blocker instead of running RAGAS.
  </acceptance_criteria>
  <done>Pre-run state is clean and comparison baseline is explicit.</done>
</task>

<task type="manual">
  <name>Task 2: Run one full post-fix RAGAS pass</name>
  <files>ai-service/eval/reports/ragas_ru.md, ai-service/eval/reports/ragas_ru.json, ai-service/eval/reports/ragas_ru.csv</files>
  <read_first>ai-service/eval/ragas_runner.py, ai-service/eval/query_client.py, ai-service/eval/golden/golden_ru.jsonl</read_first>
  <action>Run the full production `/v1/query` RAGAS evaluation once with the same conditions as the valid baseline: topK 10, parent-context extraction enabled, local `BAAI/bge-m3` embeddings for RAGAS answer relevancy, DeepSeek judge from `infra/.env`, and concurrency 1. The query phase must persist the full cache immediately after collection and before scoring. Do not tune prompts, guards, access filters, or golden records during the run.</action>
  <verify>
    <manual>Generated JSON report contains all 40 golden IDs and non-skipped RAGAS metrics for answered rows.</manual>
    <manual>`reranker_degraded_count` delta is 0 or any non-zero delta is treated as a dirty run requiring user review before acceptance.</manual>
  </verify>
  <acceptance_criteria>
    - Exactly one clean run is performed unless the user authorizes another.
    - Report runner config shows `top_k: 10`, `concurrency: 1`, `parent_context_enabled: true`, and `embedding_model_id: BAAI/bge-m3`.
    - Query cache includes `route_source` and `route_reason` for diagnostics.
  </acceptance_criteria>
  <done>Post-fix report files are generated locally for review but not committed.</done>
</task>

<task type="manual">
  <name>Task 3: Compare before/after metrics and route outcomes</name>
  <files>.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-03-SUMMARY.md</files>
  <read_first>ai-service/eval/reports/ragas_ru.json, .planning/phases/07-evaluation-observability/07-06-SUMMARY.md</read_first>
  <action>Produce a comparison table in the summary and in the response to the user. Include answered count, refused answerable count, `reranker_degraded_count` delta, faithfulness, answer relevancy, context precision, context recall, outcome accuracy, citation document recall, route mix, and the specific before/after outcomes for the five false-unsupported records. Also summarize any remaining graph multi-hop refusals as Phase 8 debt rather than trying to fix them inside this plan.</action>
  <verify>
    <manual>Comparison table is present in summary and can be read without inspecting raw JSON.</manual>
  </verify>
  <acceptance_criteria>
    - Comparison explicitly references baseline commit `4d23614`.
    - Five false-unsupported record outcomes are listed by id.
    - Remaining multi-hop weakness, if any, is reported as deferred Phase 8 debt.
  </acceptance_criteria>
  <done>Before/after RAGAS quality delta is ready for user validation.</done>
</task>

<task type="manual">
  <name>Task 4: Hold generated reports pending user validation</name>
  <files>.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-03-SUMMARY.md</files>
  <read_first>.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-CONTEXT.md</read_first>
  <action>Do not commit generated `ai-service/eval/reports/ragas_ru.*` post-fix report files until the user confirms the run is valid and comparable. Commit only code/data-repair summaries if appropriate. If reports remain uncommitted, state that clearly in the summary and final response, including paths to review.</action>
  <verify>
    <manual>`git status --short` is inspected before final response and generated report files are not committed without explicit user approval.</manual>
  </verify>
  <acceptance_criteria>
    - User sees metrics and diagnostics before any post-fix report commit.
    - Summary states whether report files are pending validation or committed after approval.
  </acceptance_criteria>
  <done>Post-fix RAGAS evidence is presented for review without prematurely committing reports.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| Dirty comparison due to reranker degradation | Live RAGAS | Capture before/after diagnostics and require degraded delta 0 for clean acceptance. |
| Baseline overwritten without audit trail | Reports | Reference baseline commit `4d23614`; hold generated reports pending user validation. |
| Eval tuning mutates safety behavior | Run procedure | No guard/access/golden changes during run; only previously planned fixes are allowed. |
| Scoring failure loses query phase | Eval harness | Preserve Plan 07-06 cache-before-scoring behavior. |
</threat_model>

<verification>
- [ ] Before-run `/diagnostics` snapshot captured.
- [ ] Docker stack healthy and corpus state verified before RAGAS.
- [ ] One full RAGAS run uses topK 10, parent contexts, bge-m3 embeddings, DeepSeek judge, concurrency 1.
- [ ] Query-phase cache persists before scoring.
- [ ] `reranker_degraded_count` delta is 0 for a clean accepted run.
- [ ] Comparison table against `4d23614` is produced.
- [ ] Generated post-fix report files are not committed until user validates the run.
</verification>

<success_criteria>
- A clean before/after RAGAS comparison exists for Phase 07.1.
- The comparison shows whether router/aggregation/corpus repair improved answered outcomes without reranker degradation.
- The user can validate the generated report before it is committed.
- Remaining multi-hop graph retrieval weaknesses are clearly separated as Phase 8 debt.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-03-SUMMARY.md`.
</output>
