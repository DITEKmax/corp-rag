---
phase: "07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas"
plan: "01"
type: "hotfix"
wave: 1
depends_on: []
files_modified:
  - "ai-service/src/corp_rag_ai/pipeline/routing/query_router.py"
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py"
  - "ai-service/eval/query_client.py"
  - "ai-service/eval/ragas_runner.py"
  - "ai-service/tests/test_query_router.py"
  - "ai-service/tests/test_graph_retriever.py"
  - "ai-service/tests/test_eval_query_client.py"
  - "ai-service/tests/test_eval_ragas_runner.py"
autonomous: false
requirements: ["AGT-01", "RET-03", "EVAL-02", "OPS-01"]
user_setup:
  - "This plan is code/test only. Do not run `/v1/query` or RAGAS during this plan."
  - "Use the committed Phase 07 Plan 06 baseline report only as input evidence."
must_haves:
  truths:
    - "D-386: Phase 07.1 fixes Russian router classification, Cyrillic aggregation query terms, and live Neo4j corpus synchronization; this plan owns the code-only portion."
    - "D-387: Eval/report records need `route_source` and `route_reason` so future false-UNSUPPORTED cases are diagnosable."
    - "D-388: Full text-conditioned multi-hop Cypher redesign is deferred to Phase 8."
    - "D-389: This plan may only make minimal multi-hop-adjacent changes required by router or metadata fixes."
    - "D-390: Russian-aware routing rules must cover frozen-corpus aggregation and multi-hop question shapes before LLM fallback."
    - "D-391: The public route set remains exactly `FACTUAL`, `AGGREGATION`, `MULTI_HOP`, `COMPARISON`, `UNSUPPORTED`."
    - "D-392: True `UNSUPPORTED` remains a pre-retrieval short-circuit."
    - "D-393: Tests must cover the five false-unsupported baseline records."
    - "D-394: Router remains rules-first with DeepSeek/OpenRouter JSON fallback; no trained classifier."
    - "D-395: Aggregation query-term extraction must retain Cyrillic/Unicode tokens."
    - "D-396: Graph aggregation remains access-filtered through Neo4j `Document` evidence."
    - "D-397: Aggregation answers still require document-backed citations."
    - "D-398: Regression tests must prove Russian aggregation terms are not empty by default."
    - "D-406: Eval query-phase records and JSON reports include `route_source` and `route_reason`."
    - "D-407: Route diagnostics are eval/report metadata, not a new frontend UI."
    - "D-408: Query-phase cache must still persist before RAGAS scoring and support score-only."
    - "D-413: Do not weaken output guard, citation validation, prompt-injection checks, or weak-evidence thresholds."
    - "D-414: Do not broaden or bypass Qdrant/Neo4j access filtering."
    - "D-415: Do not change production `RetrievalOptions.topK` defaults or add production retrieval-mode switches."
    - "D-416: Do not mutate the frozen corpus, golden questions, reference answers, or expected document UUIDs."
  artifacts:
    - path: "ai-service/src/corp_rag_ai/pipeline/routing/query_router.py"
      provides: "Russian-aware route rules"
      contains: "rules-first classification that prevents false `UNSUPPORTED` on known Russian answerable question shapes"
    - path: "ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py"
      provides: "Unicode aggregation terms"
      contains: "Cyrillic-safe token extraction for graph aggregation"
    - path: "ai-service/eval/query_client.py"
      provides: "diagnostic query cache records"
      contains: "`route_source` and `route_reason` when available, while preserving pre-scoring cache persistence"
  decision_refs:
    - "D-386"
    - "D-387"
    - "D-388"
    - "D-389"
    - "D-390"
    - "D-391"
    - "D-392"
    - "D-393"
    - "D-394"
    - "D-395"
    - "D-396"
    - "D-397"
    - "D-398"
    - "D-406"
    - "D-407"
    - "D-408"
    - "D-413"
    - "D-414"
    - "D-415"
    - "D-416"
---

<objective>
Fix the code defects that caused Russian answerable records to route as `UNSUPPORTED`, caused Russian graph aggregation terms to be empty, and hid route source/reason from eval diagnostics.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-CONTEXT.md
@.planning/phases/07-evaluation-observability/07-06-SUMMARY.md
@ai-service/eval/reports/ragas_ru.json
@ai-service/eval/golden/golden_ru.jsonl
@ai-service/src/corp_rag_ai/pipeline/routing/query_router.py
@ai-service/src/corp_rag_ai/agent/graph.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py
@ai-service/eval/query_client.py
@ai-service/eval/ragas_runner.py
@docs/decisions/ADR-005-query-routing-model.md
@docs/decisions/ADR-007-citation-contract-and-refusal-rules.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Russian router regression coverage</name>
  <files>ai-service/tests/test_query_router.py</files>
  <read_first>ai-service/eval/reports/ragas_ru.json, ai-service/eval/golden/golden_ru.jsonl, ai-service/src/corp_rag_ai/pipeline/routing/query_router.py</read_first>
  <action>Add focused router tests for the five Phase 07 Plan 06 false-UNSUPPORTED records: `ru-aggregation-003`, `ru-multihop-002`, `ru-multihop-003`, `ru-multihop-005`, and `ru-multihop-006`. Tests must prove those questions receive supported routes before any retrieval happens. Also add at least one true unsupported Russian/out-of-corpus case proving `UNSUPPORTED` still short-circuits for genuinely unsupported prompts.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_query_router.py</automated>
  </verify>
  <acceptance_criteria>
    - The five false-unsupported record ids are explicitly named in test cases or parametrization.
    - Tests include at least 2-3 Russian aggregation/multi-hop questions that are not present in the golden dataset and prove the router generalizes from linguistic cues rather than matching fixed golden wording.
    - Supported routing patterns are based on linguistic signals such as enumeration, count/list forms (`сколько`, `какие`), comparison forms (`чем отличается`), causal links, and multi-document relationship cues, not on record IDs or exact golden question text.
    - At least one true `UNSUPPORTED` regression stays green.
    - Tests do not call OpenRouter; the supported routes should come from rules or deterministic local logic.
  </acceptance_criteria>
  <done>Router failure is reproduced and guarded by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Russian-aware router rules without changing route contracts</name>
  <files>ai-service/src/corp_rag_ai/pipeline/routing/query_router.py, ai-service/tests/test_query_router.py</files>
  <read_first>docs/decisions/ADR-005-query-routing-model.md, ai-service/src/corp_rag_ai/pipeline/routing/query_router.py</read_first>
  <action>Add conservative Russian routing patterns for aggregation and multi-hop corporate-document questions. Keep existing English rules and fallback behavior. Do not retrieve on unsupported questions, do not lower the LLM confidence threshold, and do not introduce new route enum values. Prefer specific patterns around Russian question forms observed in the golden set, such as company/list/count aggregation and relationship/causal/conflict/multi-document questions.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_query_router.py</automated>
  </verify>
  <acceptance_criteria>
    - Known false-unsupported Russian questions route to `AGGREGATION` or `MULTI_HOP` as appropriate.
    - Router rules do not hardcode the five golden question strings; they match reusable Russian aggregation and multi-hop linguistic forms.
    - Existing English routing tests still pass.
    - True unsupported questions still return `UNSUPPORTED` before retrieval.
  </acceptance_criteria>
  <done>Russian answerable questions no longer fall through to false unsupported routing.</done>
</task>

<task type="auto">
  <name>Task 3: Make graph aggregation query terms Unicode-aware</name>
  <files>ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py, ai-service/tests/test_graph_retriever.py</files>
  <read_first>ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py, ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py</read_first>
  <action>Replace ASCII-only aggregation token extraction with Unicode-aware tokenization that preserves Cyrillic words and meaningful numeric/Latin tokens. Keep stopword filtering conservative and avoid broadening access filtering. Add or extend tests proving Russian aggregation questions produce non-empty, normalized query terms and that the Cypher params passed to Neo4j include those terms.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_graph_retriever.py</automated>
  </verify>
  <acceptance_criteria>
    - Russian aggregation terms are not empty for frozen-corpus examples.
    - Latin identifiers such as `P1`, `REST`, and aircraft IDs remain usable where relevant.
    - Neo4j access predicates remain in the generated aggregation query path.
  </acceptance_criteria>
  <done>Graph aggregation can lexical-match Russian corpus terms instead of returning empty-term no-evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Persist route source and reason in eval query cache</name>
  <files>ai-service/eval/query_client.py, ai-service/eval/ragas_runner.py, ai-service/tests/test_eval_query_client.py, ai-service/tests/test_eval_ragas_runner.py</files>
  <read_first>ai-service/eval/query_client.py, ai-service/eval/ragas_runner.py, ai-service/src/corp_rag_ai/agent/graph.py, contracts/openapi/ai-service-v1.yaml</read_first>
  <action>Propagate route diagnostics into eval records. If production response metadata already exposes `route_source` and `route_reason`, map them directly. If they are only internal, add a narrow eval-safe enrichment path that does not require a public frontend contract change. Preserve existing query-phase cache write-before-scoring and score-only behavior.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_eval_query_client.py ai-service/tests/test_eval_ragas_runner.py</automated>
  </verify>
  <acceptance_criteria>
    - Cached query JSON records include `route_source` and `route_reason` fields, nullable only when genuinely unavailable.
    - Existing report schema still contains all Plan 07-06 fields: question, answer, retrieved_contexts, expected_doc_ids, outcome, reranker_used, degradation_warnings.
    - Score-only mode can read a freshly collected cache with the new fields.
  </acceptance_criteria>
  <done>Future diagnostics can distinguish rules, LLM, low-confidence, and fallback routing decisions.</done>
</task>

<task type="auto">
  <name>Task 5: Run focused code-fix verification</name>
  <files>.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-01-SUMMARY.md</files>
  <read_first>ai-service/tests/test_query_router.py, ai-service/tests/test_graph_retriever.py, ai-service/tests/test_eval_query_client.py, ai-service/tests/test_eval_ragas_runner.py</read_first>
  <action>Run focused tests plus the full ai-service suite if feasible. Do not run `/v1/query` or RAGAS in this plan. Record exact test commands and results in the summary.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_query_router.py ai-service/tests/test_graph_retriever.py ai-service/tests/test_eval_query_client.py ai-service/tests/test_eval_ragas_runner.py</automated>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests pass.
    - Full ai-service suite passes or the summary records a concrete unrelated blocker.
    - No generated RAGAS report is committed by this plan.
  </acceptance_criteria>
  <done>Code-only remediation is ready for live data repair.</done>
</task>
</tasks>

<threat_model>
| Threat | Surface | Mitigation |
|--------|---------|------------|
| False positives route unrelated Russian prompts into retrieval | Router rules | Keep patterns corpus-oriented and preserve true-unsupported tests. |
| Eval score improves by weakening refusal policy | Router/guard | Preserve `UNSUPPORTED` short-circuit for genuine unsupported and do not change output guard thresholds. |
| Access filtering regresses while fixing graph aggregation | Graph Cypher | Reuse existing graph access predicate and test route-specific retrieval behavior. |
| Report schema drift breaks score-only recovery | Eval cache | Add tests for old required fields plus new route diagnostic fields. |
</threat_model>

<verification>
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_query_router.py`
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_graph_retriever.py`
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_eval_query_client.py ai-service/tests/test_eval_ragas_runner.py`
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests`
- [ ] No `/v1/query` or RAGAS live run is executed in this plan.
</verification>

<success_criteria>
- The five known false-unsupported Russian golden records no longer route to `UNSUPPORTED`.
- Russian aggregation query-term extraction is Unicode-aware and tested.
- Eval query cache includes `route_source` and `route_reason` while preserving pre-scoring persistence and score-only behavior.
- Output guard, access predicates, route enum values, golden data, and production topK defaults are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-fix-russian-router-and-graph-retrieval-quality-for-ragas-bas/07.1-01-SUMMARY.md`.
</output>
