---
phase: "05.1-phase-5-uat-fix-wave"
plan: "05"
type: "hotfix"
wave: 5
depends_on: ["04"]
files_modified:
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/parent_resolver.py"
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py"
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py"
  - "ai-service/tests/test_parent_resolver.py"
  - "ai-service/tests/test_graph_retriever.py"
  - "ai-service/tests/test_query_pipeline.py"
  - ".planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md"
  - ".planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md"
  - "ai-service/Dockerfile"
autonomous: false
requirements: ["RET-04", "AGT-03", "SEC-01"]
user_setup:
  - "Live validation requires the Phase 5.1 Docker stack, paid `deepseek/deepseek-v4-flash`, `AI_RERANKER_ENABLED=true`, and the current vendor corpus."
  - "Do not commit secrets from `infra/.env`."
  - "Do not let Phase 6 chat/source-viewer work start until DEF-A is fixed and re-UAT evidence passes."
must_haves:
  truths:
    - "05.1-04 live evidence found graph `mention.text`/`evidence.text` is NULL because indexing does not write text properties on graph relationships."
    - "Graph aggregation must not rely on Neo4j relationship text for citeable document text."
    - "Graph candidates must be document-text-backed before reranking, otherwise the cross-encoder scores `entity:X` markers near zero and weak-evidence refusal fires."
    - "Final answers must keep the 05.1-03 invariant: every `[N]` maps to the final returned citations array."
    - "Graph citation quote/snippet fields must show document text, not `entity:X` markers."
    - "The unpinned Docker Hub uv base image is an info-level workaround, not the primary blocker."
  artifacts:
    - path: ".planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md"
      provides: "live evidence for PH5.1-DEF-A and PH5.1-DEF-B"
      contains: "reranker success, graph weak-evidence refusal, missing relationship text, and Docker base workaround"
    - path: "ai-service/src/corp_rag_ai/pipeline/retrieval/parent_resolver.py"
      provides: "parent chunk document text before reranking and context packing"
      contains: "ParentResolution and citation candidate flow"
    - path: "ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py"
      provides: "Neo4j graph retrieval Cypher"
      contains: "`mention.text AS documentText` and `evidence.text AS documentText` references to remove or replace"
  decision_refs:
    - "D-148"
    - "D-190"
    - "D-193"
    - "D-196"
    - "D-198"
    - "D-199"
    - "D-205"
    - "D-212"
    - "D-215"
---

<objective>
Fix PH5.1-DEF-A so graph aggregation answers are both functional and safe: Probe B/C must return `answered=true` with non-empty document-text citations, valid inline citation indexes, and no `entity:X` user-facing snippets. Track PH5.1-DEF-B as an info-level Docker reproducibility follow-up without blocking the graph fix.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-03-SUMMARY.md
@.planning/phases/05-retrieval-guards-query-api/PHASE5-UAT-FIX-REPORT.md
@ai-service/src/corp_rag_ai/agent/graph.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/parent_resolver.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/context_packer.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py
@ai-service/src/corp_rag_ai/pipeline/indexing/graph_indexer.py
@ai-service/Dockerfile
@ai-service/Dockerfile.bak
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add DEF-A regression coverage for graph candidates without relationship text</name>
  <files>ai-service/tests/test_parent_resolver.py, ai-service/tests/test_query_pipeline.py, ai-service/tests/test_graph_retriever.py</files>
  <read_first>ai-service/src/corp_rag_ai/agent/graph.py, ai-service/src/corp_rag_ai/pipeline/retrieval/parent_resolver.py, ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py, ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py</read_first>
  <action>Add failing tests that reproduce the live DEF-A path. A graph candidate should arrive from Neo4j with valid `chunkId`/`parentChunkId` and `graphPath=entity:CloudSec Inc`, but no `documentText`. Parent resolution must supply document-backed text before reranking so the reranker sees the source passage, not the entity marker. Add a query-pipeline regression where a graph aggregation answer with parent-backed text returns `answered=true`, non-empty citations, document-text `quote`/`snippet`, and valid inline refs. Add a Cypher regression proving graph queries no longer reference missing `mention.text` or `evidence.text` relationship properties.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_parent_resolver.py ai-service/tests/test_query_pipeline.py ai-service/tests/test_graph_retriever.py</automated>
  </verify>
  <acceptance_criteria>
    - The regression fails before implementation because graph candidates reaching rerank still contain only `entity:X`/graph marker text.
    - A graph candidate with parent context is enriched to document text before reranker scoring.
    - `graph_query_helpers.py` tests prove the generated Cypher does not read missing relationship `text` properties.
  </acceptance_criteria>
  <done>DEF-A is reproduced by automated tests before the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve graph citation text from parent chunks before reranking</name>
  <files>ai-service/src/corp_rag_ai/pipeline/retrieval/parent_resolver.py, ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py, ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py</files>
  <read_first>ai-service/src/corp_rag_ai/domain/retrieval.py, ai-service/src/corp_rag_ai/pipeline/retrieval/parent_resolver.py, ai-service/src/corp_rag_ai/pipeline/retrieval/context_packer.py, ai-service/src/corp_rag_ai/pipeline/retrieval/graph_query_helpers.py</read_first>
  <action>Choose option (b) by least disruption: reuse the existing parent chunk store instead of requiring Neo4j relationship text or a corpus reindex. During parent resolution, replace graph candidates that lack document text or whose content/snippet is an internal graph marker with document-backed parent text while preserving `chunkId`, `parentChunkId`, metadata, retriever type, and access fields. Ensure `ParentResolution.citation_candidates` and `ResolvedParentContext.children` contain the enriched candidates so reranking, context packing, and citations all use document text. Remove the `mention.text AS documentText` and `evidence.text AS documentText` reads from graph Cypher to stop `UnknownPropertyKeyWarning`; keep `GraphRetriever` tolerant of a future `documentText` field if one is ever provided. If a graph candidate has no resolvable parent context, keep the existing explicit no-evidence/weak-evidence refusal path rather than answering with marker text.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_parent_resolver.py ai-service/tests/test_context_packer.py ai-service/tests/test_graph_retriever.py ai-service/tests/test_query_pipeline.py ai-service/tests/test_query_graph.py</automated>
  </verify>
  <acceptance_criteria>
    - No graph reranker input is only `entity:X` when parent document text exists.
    - Graph citation `quote` and `snippet` are document text and do not start with `entity:`.
    - Probe B/C-style graph aggregation can pass weak-evidence gating because reranker scores source text, not internal graph markers.
    - Existing hybrid parent-resolution behavior and 05.1-03 final citation invariant do not regress.
  </acceptance_criteria>
  <done>Graph retrieval uses parent-backed text for scoring and citation assembly.</done>
</task>

<task type="auto">
  <name>Task 3: Verify DEF-A fix with unit, full, and live graph probes</name>
  <files>.planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md, .planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md</files>
  <read_first>.planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md, .planning/phases/05-retrieval-guards-query-api/05-UAT.md</read_first>
  <action>Run the focused tests, the do-not-break gate, and the full ai-service test suite. Then rebuild `python-ai` and rerun Scenario 4 Probe B (`How many vendors are approved in total?`) and Probe C (`How many vendors does the Compliance department approve?`) against the current or freshly reindexed vendor corpus. Record exact response fields, rerankerUsed, confidence, citations, inline-ref validity, and quote/snippet text in `05.1-UAT-EVIDENCE.md`. Add a short follow-up note/link in the original Phase 5 evidence file.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_parent_resolver.py ai-service/tests/test_context_packer.py ai-service/tests/test_graph_retriever.py ai-service/tests/test_query_pipeline.py ai-service/tests/test_query_graph.py</automated>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_embedding_adapter.py ai-service/tests/test_ingestion_orchestrator.py ai-service/tests/test_query_input_guard.py ai-service/tests/test_hybrid_retriever.py ai-service/tests/test_query_pipeline.py</automated>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests</automated>
    <manual>docker compose --env-file infra/.env -f infra/docker-compose.yml up -d --build python-ai</manual>
    <manual>Run Scenario 4 Probe B and Probe C from the Phase 5 UAT runbook.</manual>
  </verify>
  <acceptance_criteria>
    - Probe B and Probe C return `answered=true`, `route=AGGREGATION`, non-empty citations, and `retrievalMeta.rerankerUsed=true`.
    - Every inline `[N]` satisfies `1 <= N <= len(citations)`.
    - Returned citation `quote`/`snippet` values are human-readable document text, not `entity:X`.
    - F-06 and the answerable-aggregation half of PH5-UAT-DEF-04 are marked closed only after live evidence satisfies the checks.
  </acceptance_criteria>
  <done>DEF-A is closed by automated and live evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Track or restore the pinned uv base image workaround</name>
  <files>ai-service/Dockerfile, ai-service/Dockerfile.bak, .planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md</files>
  <read_first>ai-service/Dockerfile, ai-service/Dockerfile.bak, .planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md</read_first>
  <action>Handle PH5.1-DEF-B as an info-level reproducibility item. If the Docker daemon can pull `ghcr.io/astral-sh/uv:0.5.26-python3.12-bookworm`, restore the pinned ghcr.io base image and remove the local backup artifact. If ghcr.io is still unreachable from the daemon, keep the Docker Hub workaround only as an explicitly documented temporary state and do not treat it as closing DEF-B. Do not let DEF-B block DEF-A or Phase 6 if the graph fix passes; do not commit `Dockerfile.bak` unless it is intentionally converted into documented evidence.</action>
  <verify>
    <manual>docker build path used by `docker compose --env-file infra/.env -f infra/docker-compose.yml up -d --build python-ai` succeeds after the chosen Dockerfile state.</manual>
  </verify>
  <acceptance_criteria>
    - Final summary states whether the uv base image is pinned again or DEF-B remains open due to registry reachability.
    - Python package versions remain pinned through `uv.lock` either way.
    - No accidental secret or local-only backup artifact is committed.
  </acceptance_criteria>
  <done>DEF-B is either fixed by a pinned base image revert or explicitly deferred as info-level.</done>
</task>
</tasks>

<verification>
- [ ] Focused DEF-A tests fail before the fix and pass after the fix.
- [ ] `graph_query_helpers.py` no longer reads missing `mention.text` or `evidence.text` relationship properties.
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_parent_resolver.py ai-service/tests/test_context_packer.py ai-service/tests/test_graph_retriever.py ai-service/tests/test_query_pipeline.py ai-service/tests/test_query_graph.py`
- [ ] Do-not-break gate: `uv run --project ai-service --group dev pytest ai-service/tests/test_embedding_adapter.py ai-service/tests/test_ingestion_orchestrator.py ai-service/tests/test_query_input_guard.py ai-service/tests/test_hybrid_retriever.py ai-service/tests/test_query_pipeline.py`
- [ ] Before writing `05.1-05-SUMMARY.md`, run or explicitly defer with reason: `uv run --project ai-service --group dev pytest ai-service/tests`
- [ ] Rebuilt `python-ai` includes the DEF-A fix.
- [ ] Live Probe B and Probe C return `answered=true`, valid citation indexes, document-text quote/snippet fields, and no `entity:X` snippets.
- [ ] `05.1-UAT-EVIDENCE.md` and the original Phase 5 evidence link/section record closure or explicit deferral.
</verification>

<success_criteria>
- PH5.1-DEF-A is closed by code and live evidence.
- F-06 is verified closed: graph citation quote/snippet fields are document text.
- PH5-UAT-DEF-04 is fully closed for answerable graph aggregation: no lying refs and no deterministic weak refusal when citeable graph evidence exists.
- Phase 6 may build source-viewer/chat behavior on graph-cited answers only after this plan passes.
- PH5.1-DEF-B is either fixed or clearly left open as info-level Docker registry reproducibility debt.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-phase-5-uat-fix-wave/05.1-05-SUMMARY.md`.
</output>
