---
phase: "05.1-phase-5-uat-fix-wave"
plan: "04"
type: "hotfix-verification"
wave: 4
depends_on: ["03"]
files_modified:
  - ".planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md"
  - ".planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md"
  - "infra/.env"
autonomous: false
requirements: ["RET-04", "AGT-03", "SEC-01"]
user_setup:
  - "Requires live Docker stack, OpenRouter API key, paid `deepseek/deepseek-v4-flash` model id, and fresh Phase 5 corpus."
  - "Set `AI_RERANKER_ENABLED=true` only after 05.1-01 and 05.1-02 complete."
  - "Do not commit `OPENROUTER_API_KEY` or other secrets from `infra/.env`."
must_haves:
  truths:
    - "05.1-04 is human/live verification, not an autonomous unit-test-only plan."
    - "The python-ai image must be rebuilt after dependency lock changes."
    - "Live re-UAT must close P4 and PH5-UAT-DEF-02/03/04 with observed values."
    - "Scenario 3 must use the reranker successfully, not degraded reranker mode."
    - "Scenario 4 Probe B/C must return citation arrays where every inline `[N]` maps to a returned citation."
  artifacts:
    - path: ".planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md"
      provides: "follow-up live UAT evidence"
      contains: "diagnostics, P4, Scenario 3, Scenario 4 Probe B/C, and non-regression results"
    - path: ".planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md"
      provides: "original Phase 5 UAT baseline"
      contains: "P4 not completed and PH5-UAT-DEF-02/03/04 findings to close"
  decision_refs:
    - "D-94"
    - "D-95"
    - "D-148"
    - "D-180"
    - "D-190"
    - "D-193"
    - "D-196"
    - "D-198"
    - "D-199"
    - "D-205"
    - "D-206"
    - "D-212"
    - "D-215"
---

<objective>
Run the live Phase 5.1 re-UAT on the real stack with `AI_RERANKER_ENABLED=true`, then record evidence that P4 and PH5-UAT-DEF-02/03/04 are closed and that previously passing scenarios did not regress.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-CONTEXT.md
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-01-SUMMARY.md
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-02-SUMMARY.md
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-03-SUMMARY.md
@.planning/phases/05-retrieval-guards-query-api/05-UAT.md
@.planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md
@.planning/phases/05-retrieval-guards-query-api/PHASE5-UAT-FIX-REPORT.md
@infra/docker-compose.yml
@infra/.env.example
</context>

<tasks>
<task type="human">
  <name>Task 1: Rebuild python-ai and bring up the live stack</name>
  <files>infra/.env, infra/docker-compose.yml</files>
  <read_first>.planning/phases/05-retrieval-guards-query-api/05-UAT.md, .planning/phases/05-retrieval-guards-query-api/PHASE5-UAT-FIX-REPORT.md</read_first>
  <action>Set the live UAT configuration after 05.1-01/02 land: keep `DEEPSEEK_MODEL_ID=deepseek/deepseek-v4-flash`, keep the local UAT query timeout such as `AI_QUERY_TIMEOUT_SECONDS=120`, and set `AI_RERANKER_ENABLED=true`. Rebuild the `python-ai` image, bring up the full Docker stack, and confirm `/diagnostics` reports all expected readiness fields true. Do not use the `:free` model for multi-document live UAT because it 429-rate-limited indexing during Phase 5 UAT. Do not commit `OPENROUTER_API_KEY` or any secret value from `infra/.env`.</action>
  <verify>
    <manual>docker compose --env-file infra/.env -f infra/docker-compose.yml up -d --build python-ai</manual>
    <manual>docker compose --env-file infra/.env -f infra/docker-compose.yml up -d</manual>
    <manual>Invoke-RestMethod http://localhost:8000/diagnostics | ConvertTo-Json -Depth 10</manual>
  </verify>
  <acceptance_criteria>
    - `python-ai` image includes the 05.1-01 through 05.1-03 code and lockfile changes.
    - Diagnostics show query service, router, reranker configuration, vector, graph, AMQP, and LLM configured/readiness fields as expected by `05-UAT.md`.
    - Fresh Phase 5 corpus is indexed and available before query scenarios run.
  </acceptance_criteria>
  <done>Live stack is ready for reranker-enabled re-UAT.</done>
</task>

<task type="human">
  <name>Task 2: Close P4 and PH5-UAT-DEF-02 with Scenario 3</name>
  <files>.planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md</files>
  <read_first>.planning/phases/05-retrieval-guards-query-api/05-UAT.md, .planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md</read_first>
  <action>Run the P4 reranker memory check and Scenario 3 (`What is the vacation policy?`) with `AI_RERANKER_ENABLED=true`. Record memory before/after first successful factual query and the response fields. Scenario 3 must return `answered=true`, `route=FACTUAL`, non-empty citations, `retrievalMeta.rerankerUsed=true`, no `reranker_disabled`, no `reranker_unavailable`, and complete within the configured query timeout.</action>
  <verify>
    <manual>Run P4 commands from `.planning/phases/05-retrieval-guards-query-api/05-UAT.md`.</manual>
    <manual>Run Scenario 3 commands from `.planning/phases/05-retrieval-guards-query-api/05-UAT.md`.</manual>
  </verify>
  <acceptance_criteria>
    - P4 memory check is completed and recorded.
    - PH5-UAT-DEF-02 is closed by observed successful reranker scoring.
    - PH5-UAT-DEF-03 is not reproduced by Scenario 3; no reranker-only query timeout occurs.
  </acceptance_criteria>
  <done>Factual query path is verified live with reranker enabled.</done>
</task>

<task type="human">
  <name>Task 3: Close PH5-UAT-DEF-04 and F-06 with graph probes</name>
  <files>.planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md</files>
  <read_first>.planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md, .planning/phases/05.1-phase-5-uat-fix-wave/05.1-03-SUMMARY.md</read_first>
  <action>Run Scenario 4 Probe B (`How many vendors are approved in total?`) and Probe C (`How many vendors does the Compliance department approve?`) against the live corpus. Both must return `answered=true`, `route=AGGREGATION`, non-empty `citations`, and every inline `[N]` in `answer` must satisfy `1 <= N <= len(citations)`. Also inspect returned citation `quote`/`snippet` values and record that they are human-readable document text, not `entity:X` markers.</action>
  <verify>
    <manual>Run Scenario 4 Probe B and Probe C with the vendor corpus from the Phase 5 UAT setup.</manual>
    <manual>For each response, enumerate inline refs and compare them to the returned citation array length.</manual>
  </verify>
  <acceptance_criteria>
    - Probe B no longer fails with `missing_citations` when citeable graph evidence exists.
    - Probe C never returns `[4]` with a one-element citation array.
    - Graph citation snippets/quotes no longer expose `entity:X` markers.
  </acceptance_criteria>
  <done>Graph route is live-verified for citation indexes and user-facing citation snippets.</done>
</task>

<task type="human">
  <name>Task 4: Run quick non-regression on previously passing scenarios</name>
  <files>.planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md</files>
  <read_first>.planning/phases/05-retrieval-guards-query-api/05-UAT.md, .planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md</read_first>
  <action>Rerun Scenario 1 prompt-injection rejection, Scenario 2 out-of-scope rejection, Scenario 5 no-evidence refusal, and Scenario 6 degraded Qdrant-off aggregation. Confirm the behaviors that passed during Phase 5 UAT remain true with the reranker re-enabled. Scenario 6 should record the current vector-degraded behavior exactly; F-07 remains deferred unless 05.1 implementation intentionally changed it.</action>
  <verify>
    <manual>Run Scenario 1, 2, 5, and 6 commands from `.planning/phases/05-retrieval-guards-query-api/05-UAT.md`.</manual>
  </verify>
  <acceptance_criteria>
    - Input guard prompt-injection and out-of-scope paths still short-circuit before retrieval.
    - Access-filtered no-evidence refusal still says no accessible documents discuss the topic and does not broaden filters.
    - Graph-only aggregation still works with Qdrant stopped when accessible graph evidence exists.
  </acceptance_criteria>
  <done>Previously passing UAT scenarios remain green.</done>
</task>

<task type="human">
  <name>Task 5: Write follow-up UAT evidence</name>
  <files>.planning/phases/05.1-phase-5-uat-fix-wave/05.1-UAT-EVIDENCE.md, .planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md</files>
  <read_first>.planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md, .planning/phases/05-retrieval-guards-query-api/PHASE5-UAT-FIX-REPORT.md</read_first>
  <action>Create `05.1-UAT-EVIDENCE.md` with the observed diagnostics, corpus status, P4 memory values, Scenario 3 response summary, Scenario 4 Probe B/C citation checks, and quick non-regression results. Either update the original `05-UAT-EVIDENCE.md` with a short "Follow-up Phase 5.1 evidence" section or link to the new evidence file. Mark P4 complete and PH5-UAT-DEF-02/03/04 closed only when the live observations satisfy the acceptance criteria.</action>
  <verify>
    <manual>Review `05.1-UAT-EVIDENCE.md` against `PHASE5-UAT-FIX-REPORT.md` Suggested validation items 1, 3, 4, 6, and 8.</manual>
  </verify>
  <acceptance_criteria>
    - Evidence includes concrete values, not only "passed" labels.
    - P4 and PH5-UAT-DEF-02/03/04 closure is traceable to live responses.
    - Deferred F-07 behavior is recorded without silently changing the decision contract.
  </acceptance_criteria>
  <done>Phase 5.1 has live evidence comparable to the original Phase 5 UAT standard.</done>
</task>
</tasks>

<verification>
- [ ] Before live re-UAT, run or explicitly defer with reason: `uv run --project ai-service --group dev pytest ai-service/tests`
- [ ] `python-ai` rebuilt after dependency changes.
- [ ] `/diagnostics` ready fields confirmed.
- [ ] P4 reranker memory check completed.
- [ ] Scenario 3 returns `answered=true`, `rerankerUsed=true`, and no reranker degradation warnings.
- [ ] Scenario 4 Probe B/C return valid citation indexes and document-text snippets.
- [ ] Scenarios 1, 2, 5, and 6 still pass their previous behavior.
- [ ] `05.1-UAT-EVIDENCE.md` records observed values and closure status.
</verification>

<success_criteria>
- P4 is no longer `NOT COMPLETED`.
- PH5-UAT-DEF-02, PH5-UAT-DEF-03, and PH5-UAT-DEF-04 are closed by live evidence.
- F-06 graph citation snippet behavior is verified for the graph probes.
- The do-not-break list from `PHASE5-UAT-FIX-REPORT.md` remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-phase-5-uat-fix-wave/05.1-04-SUMMARY.md`.
</output>
