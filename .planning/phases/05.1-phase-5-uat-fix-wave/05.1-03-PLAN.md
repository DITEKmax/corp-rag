---
phase: "05.1-phase-5-uat-fix-wave"
plan: "03"
type: "hotfix"
wave: 3
depends_on: ["02"]
files_modified:
  - "ai-service/src/corp_rag_ai/agent/graph.py"
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/context_packer.py"
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py"
  - "ai-service/src/corp_rag_ai/pipeline/generation/synthesizer.py"
  - "ai-service/src/corp_rag_ai/pipeline/guards/output_guard.py"
  - "ai-service/tests/test_output_guard.py"
  - "ai-service/tests/test_context_packer.py"
  - "ai-service/tests/test_graph_retriever.py"
  - "ai-service/tests/test_query_pipeline.py"
  - "ai-service/tests/test_query_graph.py"
  - "ai-service/tests/test_synthesizer.py"
autonomous: true
requirements: ["AGT-03", "SEC-01", "RET-04"]
user_setup: []
must_haves:
  truths:
    - "F-03 runs after F-01 and F-02."
    - "Every final answer `[N]` must map to the final returned `citations` array."
    - "Graph aggregation/list/count answers still require citations."
    - "Out-of-range refs must be blocked or repaired before `answered=true`."
    - "Graph citation quote/snippet fields must show document text, not user-facing `entity:X` markers."
  artifacts:
    - path: "ai-service/src/corp_rag_ai/agent/graph.py"
      provides: "final QueryResult assembly"
      contains: "output guard invocation, citation selection, metadata"
    - path: "ai-service/src/corp_rag_ai/pipeline/retrieval/context_packer.py"
      provides: "prompt evidence and citation index space"
      contains: "packed citations and source/citation numbering"
    - path: "ai-service/src/corp_rag_ai/pipeline/guards/output_guard.py"
      provides: "post-generation validation"
      contains: "invalid/missing citation checks"
  decision_refs:
    - "D-167"
    - "D-196"
    - "D-198"
    - "D-199"
    - "D-205"
---

<objective>
Fix F-03 and the graph-citation portion of F-06: graph-route answers must not cite nonexistent indexes, answerable aggregation queries must return citeable evidence instead of false `missing_citations` refusals, and graph citations must expose document text in `quote`/`snippet` rather than `entity:X` markers.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-CONTEXT.md
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-02-SUMMARY.md
@.planning/phases/05-retrieval-guards-query-api/PHASE5-UAT-FIX-REPORT.md
@.planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md
@.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md
@ai-service/src/corp_rag_ai/agent/graph.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/context_packer.py
@ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py
@ai-service/src/corp_rag_ai/pipeline/generation/synthesizer.py
@ai-service/src/corp_rag_ai/pipeline/guards/output_guard.py
@ai-service/tests/test_output_guard.py
@ai-service/tests/test_query_pipeline.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add final-response regression for selected citation remapping</name>
  <files>ai-service/tests/test_query_pipeline.py, ai-service/tests/test_query_graph.py, ai-service/tests/test_output_guard.py</files>
  <read_first>ai-service/src/corp_rag_ai/agent/graph.py, ai-service/tests/test_query_pipeline.py, ai-service/tests/test_output_guard.py</read_first>
  <action>Add tests that reproduce the UAT class: packed context has multiple citations, synthesis returns an answer containing `[4]`, and final `QueryResult.citations` would otherwise contain only one selected citation. The final result must either return a citations array where `[4]` is valid, or compact citations and rewrite the answer so it cites `[1]`. Include at least one test where the reranker, real or mocked, reorders candidates and reduces them to a smaller final top-N before context packing or final assembly; this is the live-use condition that can shrink the final citation set. Add a second test where `[2]` is referenced with only one available packed citation and assert the output guard blocks it.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_query_pipeline.py ai-service/tests/test_query_graph.py ai-service/tests/test_output_guard.py</automated>
  </verify>
  <acceptance_criteria>
    - The regression fails on the current `_select_citations(...)` behavior before the fix.
    - The fixed final `QueryResult` never contains an out-of-range inline citation.
    - Citation remapping is proven on the reranked/reduced path, not only raw retrieval order.
  </acceptance_criteria>
  <done>The exact final-response citation mismatch is covered.</done>
</task>

<task type="auto">
  <name>Task 2: Make prompt citation indexes and returned citation indexes the same contract</name>
  <files>ai-service/src/corp_rag_ai/agent/graph.py, ai-service/src/corp_rag_ai/pipeline/retrieval/context_packer.py, ai-service/src/corp_rag_ai/pipeline/generation/synthesizer.py, ai-service/tests/test_context_packer.py, ai-service/tests/test_synthesizer.py</files>
  <read_first>ai-service/src/corp_rag_ai/agent/graph.py, ai-service/src/corp_rag_ai/pipeline/retrieval/context_packer.py, ai-service/src/corp_rag_ai/pipeline/generation/synthesizer.py</read_first>
  <action>Choose one contract and implement it consistently: either return the full `packed.citations` index space used by the prompt, or compact selected citations and rewrite inline refs to the compact 1-based indexes before returning. Prefer the approach that keeps the final API simple for Phase 6 while preserving source-viewer correctness. Update prompt/evidence formatting so graph evidence exposes citeable indexes clearly, not only parent/source indexes. Validate after final assembly, not only before citation selection.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_context_packer.py ai-service/tests/test_synthesizer.py ai-service/tests/test_query_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - For any final `answered=true`, every `[N]` in `answer` satisfies `1 <= N <= len(citations)`.
    - `citation_indexes` and inline refs cannot describe different final citation spaces.
    - Phase 6 can use `citations[N-1]` for every inline ref without special mapping.
  </acceptance_criteria>
  <done>Prompt, guard, and final response share one citation index space.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure graph aggregation has citeable document-text evidence for answerable counts/lists</name>
  <files>ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py, ai-service/src/corp_rag_ai/pipeline/retrieval/context_packer.py, ai-service/tests/test_graph_retriever.py, ai-service/tests/test_query_pipeline.py</files>
  <read_first>ai-service/src/corp_rag_ai/pipeline/retrieval/graph.py, ai-service/src/corp_rag_ai/pipeline/retrieval/context_packer.py, ai-service/tests/test_graph_retriever.py</read_first>
  <action>For graph aggregation results, ensure candidates carry child chunk IDs and enough document-backed evidence to become citations. If graph records provide only `graphPath`, keep that as internal metadata and resolve the citeable quote/snippet from document-backed chunk/parent evidence where available. Pull F-06 into this task: graph-route citation `quote` and `snippet` must contain human-readable document chunk text, not user-facing `entity:X` graph markers. If document text is unavailable for an otherwise graph-supported answer, return an explicit refusal rather than `answered=true` with unciteable or entity-marker citations.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_graph_retriever.py ai-service/tests/test_context_packer.py ai-service/tests/test_query_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - Probe B-style total-count graph query can produce non-empty citations.
    - Probe C-style department-count graph query cannot return `[4]` with a one-element final citations array.
    - Graph citation `quote`/`snippet` fields contain document text and do not start with `entity:`.
    - If graph evidence is genuinely unciteable, the response is `answered=false` and the reason is explicit.
  </acceptance_criteria>
  <done>Graph-route answers are either cited correctly or refused explicitly.</done>
</task>
</tasks>

<verification>
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_output_guard.py ai-service/tests/test_context_packer.py ai-service/tests/test_graph_retriever.py ai-service/tests/test_query_pipeline.py ai-service/tests/test_query_graph.py ai-service/tests/test_synthesizer.py`
- [ ] Scenario 4 Probe B-style query returns `answered=true` with non-empty citations when citeable graph evidence exists.
- [ ] Scenario 4 Probe C-style query never returns `[N]` where `N > len(citations)`.
- [ ] Reranked/reduced final top-N path preserves the final citation-index invariant.
- [ ] Graph citation snippets/quotes are document text, not `entity:X` markers.
- [ ] Final REST JSON preserves the same citation-index invariant as internal `QueryResult`.
- [ ] Do-not-break gate: `uv run --project ai-service --group dev pytest ai-service/tests/test_embedding_adapter.py ai-service/tests/test_ingestion_orchestrator.py ai-service/tests/test_query_input_guard.py ai-service/tests/test_hybrid_retriever.py ai-service/tests/test_query_pipeline.py`
- [ ] Before writing `05.1-03-SUMMARY.md`, run or explicitly defer with reason: `uv run --project ai-service --group dev pytest ai-service/tests`
</verification>

<success_criteria>
- F-03 and F-06 acceptance criteria from `PHASE5-UAT-FIX-REPORT.md` are satisfied for graph citations.
- `D-196`, `D-198`, `D-199`, and `D-205` hold for graph routes and final responses.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-phase-5-uat-fix-wave/05.1-03-SUMMARY.md`.
</output>
