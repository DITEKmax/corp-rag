---
phase: "05.1-phase-5-uat-fix-wave"
plan: "01"
type: "hotfix"
wave: 1
depends_on: []
files_modified:
  - "ai-service/pyproject.toml"
  - "ai-service/uv.lock"
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py"
  - "ai-service/src/corp_rag_ai/pipeline/indexing/embedding.py"
  - "ai-service/tests/test_reranker.py"
  - "ai-service/tests/test_embedding_adapter.py"
  - "docs/decisions/ADR-004-llm-provider-deepseek-openrouter.md"
autonomous: true
requirements: ["RET-04"]
user_setup:
  - "Rebuild the python-ai image after dependency lock changes."
must_haves:
  truths:
    - "F-01 is first priority for Phase 5.1."
    - "Reranker remains local `BAAI/bge-reranker-v2-m3` through FlagEmbedding/FlagReranker."
    - "bge-m3 dense+sparse embedding behavior must not regress."
    - "Dependency changes must be minimal and documented."
  artifacts:
    - path: "ai-service/pyproject.toml"
      provides: "direct Python dependency constraints"
      contains: "FlagEmbedding, transformers-compatible constraints, qdrant-client constraints"
    - path: "ai-service/uv.lock"
      provides: "resolved Python dependency versions"
      contains: "currently verified flagembedding==1.4.0 and transformers==5.8.1"
    - path: "ai-service/tests/test_reranker.py"
      provides: "reranker behavior tests"
      contains: "mocked ranking/degradation tests and new live self-skipping smoke"
  decision_refs:
    - "D-148"
    - "D-190"
---

<objective>
Fix F-01: make the local `bge-reranker-v2-m3` reranker actually score query/passage pairs under the resolved dependency set, while proving bge-m3 embeddings still work.

Purpose: Phase 5 UAT had to disable reranking because `FlagReranker.compute_score(...)` failed at tokenizer/scoring time. Phase 6 must not inherit a query path that only works in degraded mode.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-CONTEXT.md
@.planning/phases/05-retrieval-guards-query-api/PHASE5-UAT-FIX-REPORT.md
@.planning/phases/05-retrieval-guards-query-api/05-UAT-EVIDENCE.md
@.planning/phases/04-python-ingestion-indexing/04-CONTEXT.md
@.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md
@ai-service/pyproject.toml
@ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py
@ai-service/src/corp_rag_ai/pipeline/indexing/embedding.py
@ai-service/tests/test_reranker.py
@ai-service/tests/test_embedding_adapter.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Determine and apply the minimal compatible dependency set</name>
  <files>ai-service/pyproject.toml, ai-service/uv.lock</files>
  <read_first>ai-service/pyproject.toml, ai-service/uv.lock, .planning/phases/05.1-phase-5-uat-fix-wave/05.1-CONTEXT.md</read_first>
  <action>Evaluate both compatibility strategies explicitly before choosing a dependency set. Option A: try pinning `transformers` down to a version where `XLMRobertaTokenizer.prepare_for_model` exists and `flagembedding==1.4.0` reranker scoring works. Before choosing Option A, run a scratch `uv lock` attempt and inspect whether the downgrade conflicts with or forces downgrades of unrelated dependency families that currently work with the transformers 5.x stack, including LangGraph/OpenAI SDK/Qdrant client/sentence-transformers/tokenizers-related constraints; reject Option A if it fails to resolve cleanly or downgrades unrelated families. Option B: try upgrading FlagEmbedding to a version whose reranker path works with the current transformers 5.x tokenizer API, then verify it still exposes the same bge-m3 dense+sparse embedding behavior used in Phase 4 (`BGEM3FlagModel`, dense length 1024, non-empty sparse lexical weights). Choose by least disruption: the selected option must resolve cleanly without downgrading unrelated dependency families and must stay closest to the current stack, which is currently on `transformers==5.8.1`. Option B is likely cleaner because the project is already on transformers 5.x, but confirm empirically rather than assuming. Update `ai-service/pyproject.toml` and regenerate `ai-service/uv.lock`. Do not change unrelated packages.</action>
  <verify>
    <automated>uv lock --project ai-service</automated>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_reranker.py ai-service/tests/test_embedding_adapter.py</automated>
  </verify>
  <acceptance_criteria>
    - The lockfile records the exact chosen FlagEmbedding/transformers versions.
    - No unrelated dependency family is bumped except transitive changes required by the chosen compatibility set.
    - The executor documents why the chosen version set is compatible.
    - The summary records both evaluated options, the chosen option, exact final versions for FlagEmbedding, transformers, tokenizers, and any other moved dependency family, the rejected alternative and why, and confirmation that no unrelated dependency family was downgraded.
  </acceptance_criteria>
  <done>Dependency resolution no longer selects a reranker-incompatible tokenizer stack.</done>
</task>

<task type="auto">
  <name>Task 2: Add a self-skipping live reranker scoring smoke</name>
  <files>ai-service/tests/test_reranker.py</files>
  <read_first>ai-service/tests/test_reranker.py, ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py</read_first>
  <action>Add an integration-marked test that loads `FlagReranker('BAAI/bge-reranker-v2-m3')` and calls `compute_score([('vacation policy', 'Employees request annual vacation after manager approval.')], normalize=True)`. The test must self-skip unless explicitly enabled and the model is cached or live model download is allowed, matching the existing live-smoke pattern. Assert the returned score can be interpreted as a float in `[0, 1]`.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_reranker.py -m integration -q -s</automated>
  </verify>
  <acceptance_criteria>
    - Default unit tests still do not download heavyweight models.
    - The live smoke proves the actual UAT failure path, not only mocked behavior.
  </acceptance_criteria>
  <done>The exact reranker scoring path has executable coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve bge-m3 embedding behavior and document the version choice</name>
  <files>ai-service/tests/test_embedding_adapter.py, docs/decisions/ADR-004-llm-provider-deepseek-openrouter.md</files>
  <read_first>ai-service/tests/test_embedding_adapter.py, docs/decisions/ADR-004-llm-provider-deepseek-openrouter.md</read_first>
  <action>Run and, if needed, extend embedding tests so bge-m3 still produces dense 1024 vectors and non-empty sparse lexical weights under the chosen dependency set. Make the assertions strict: dense vector length must be exactly 1024; sparse output must contain plausible integer token-id to positive numeric weight pairs, not just any truthy object. If feasible without making default tests flaky or network-bound, add a known query/passage non-degenerate similarity smoke that would fail on all-zero or garbage vectors. If the dependency pin is architecturally meaningful, add a short note to ADR-004 or an implementation note in the summary; do not invent a new ADR number unless the repo already has an accepted ADR slot for it.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_embedding_adapter.py ai-service/tests/test_reranker.py</automated>
  </verify>
  <acceptance_criteria>
    - bge-m3 dense vectors have length exactly 1024.
    - bge-m3 sparse vectors are non-empty and contain plausible token-id to weight pairs.
    - Embedding output is not all-zero or structurally degenerate under the chosen dependency set.
    - The summary records exact dependency versions and why they were chosen.
  </acceptance_criteria>
  <done>Reranker compatibility is restored without breaking Phase 4 embeddings.</done>
</task>
</tasks>

<verification>
- [ ] `uv lock --project ai-service`
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_reranker.py ai-service/tests/test_embedding_adapter.py`
- [ ] Optional/live: reranker scoring smoke returns a normalized score with `AI_RERANKER_ENABLED=true`.
- [ ] Rebuild `python-ai` after lockfile changes.
- [ ] Do-not-break spot check: embedding adapter tests remain strict enough to catch dense/sparse regressions after the transformers/FlagEmbedding change.
- [ ] Do-not-break gate: `uv run --project ai-service --group dev pytest ai-service/tests/test_embedding_adapter.py ai-service/tests/test_ingestion_orchestrator.py ai-service/tests/test_query_input_guard.py ai-service/tests/test_hybrid_retriever.py ai-service/tests/test_query_pipeline.py`
- [ ] Before writing `05.1-01-SUMMARY.md`, run or explicitly defer with reason: `uv run --project ai-service --group dev pytest ai-service/tests`
</verification>

<success_criteria>
- F-01 acceptance criteria from `PHASE5-UAT-FIX-REPORT.md` are satisfied.
- The local reranker can score at least one query/passage pair.
- bge-m3 embedding tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-phase-5-uat-fix-wave/05.1-01-SUMMARY.md`.
</output>
