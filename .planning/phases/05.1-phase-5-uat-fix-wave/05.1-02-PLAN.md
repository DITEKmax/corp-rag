---
phase: "05.1-phase-5-uat-fix-wave"
plan: "02"
type: "hotfix"
wave: 2
depends_on: ["01"]
files_modified:
  - "ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py"
  - "ai-service/src/corp_rag_ai/config.py"
  - "ai-service/src/corp_rag_ai/service/query/factory.py"
  - "ai-service/src/corp_rag_ai/agent/graph.py"
  - "ai-service/tests/test_reranker.py"
  - "ai-service/tests/test_query_pipeline.py"
  - "ai-service/tests/test_query_degradation_policy.py"
  - "infra/docker-compose.yml"
  - "infra/.env.example"
  - ".env.example"
  - "ai-service/README.md"
autonomous: true
requirements: ["RET-04", "AGT-03"]
user_setup: []
must_haves:
  truths:
    - "F-02 runs after F-01."
    - "Reranker disabled and reranker runtime failure must have distinguishable metadata warnings."
    - "Reranker runtime failure must not turn an otherwise answerable query into `query_timeout`."
    - "The reranker-step timeout must be shorter than `AI_QUERY_TIMEOUT_SECONDS`."
  artifacts:
    - path: "ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py"
      provides: "reranker load/scoring adapter"
      contains: "exception handling, semaphore, warnings, and new step timeout"
    - path: "ai-service/src/corp_rag_ai/config.py"
      provides: "query/reranker settings"
      contains: "new reranker-step timeout setting"
    - path: "ai-service/src/corp_rag_ai/agent/graph.py"
      provides: "query orchestration metadata"
      contains: "reranker unavailable dependency state and final metadata warnings"
  decision_refs:
    - "D-193"
    - "D-206"
    - "D-212"
    - "D-215"
---

<objective>
Fix F-02: reranker exceptions, slow model loading, or scoring hangs must soft-degrade to raw retrieval order with explicit metadata instead of consuming the full query timeout.
</objective>

<execution_context>
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/IntelliJIDEA/corp-rag/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-CONTEXT.md
@.planning/phases/05.1-phase-5-uat-fix-wave/05.1-01-SUMMARY.md
@.planning/phases/05-retrieval-guards-query-api/PHASE5-UAT-FIX-REPORT.md
@.planning/phases/05-retrieval-guards-query-api/05-CONTEXT.md
@ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py
@ai-service/src/corp_rag_ai/config.py
@ai-service/src/corp_rag_ai/service/query/factory.py
@ai-service/src/corp_rag_ai/agent/graph.py
@ai-service/tests/test_reranker.py
@ai-service/tests/test_query_pipeline.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add bounded reranker load/scoring timeout</name>
  <files>ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py, ai-service/src/corp_rag_ai/config.py, ai-service/src/corp_rag_ai/service/query/factory.py</files>
  <read_first>ai-service/src/corp_rag_ai/pipeline/retrieval/reranker.py, ai-service/src/corp_rag_ai/config.py, ai-service/src/corp_rag_ai/service/query/factory.py</read_first>
  <action>Add a configurable reranker-step timeout, for example `AI_RERANKER_TIMEOUT_SECONDS`, and wire it into `LocalReranker`. Use an explicit default of 25 seconds unless implementation evidence shows a better local CPU value; document the rationale in the summary. Bound both lazy model load and `compute_score(...)` waits. Enforce or warn on the invariant that the reranker-step timeout must be less than `AI_QUERY_TIMEOUT_SECONDS`, so misconfiguration cannot recreate the whole-query timeout failure. If the timeout fires or any exception is raised, return raw candidates sliced to `top_n`, `reranker_used=false`, and the existing runtime-failure warning `reranker_unavailable` unless the executor intentionally renames all related constants/tests. Keep `reranker_disabled` only for the explicit disabled path.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_reranker.py</automated>
  </verify>
  <acceptance_criteria>
    - A fake model factory that hangs or raises during load soft-degrades.
    - A fake model whose `compute_score` hangs or raises soft-degrades.
    - A configuration test asserts or warns/clamps when reranker-step timeout is not below the overall query timeout.
    - A normal warm reranker path does not trip the timeout and still reports `reranker_used=true`.
    - Disabled reranker still reports `reranker_disabled`, not runtime failure.
  </acceptance_criteria>
  <done>Reranker cannot consume the whole query budget by itself.</done>
</task>

<task type="auto">
  <name>Task 2: Prove query pipeline answers with reranker runtime failure</name>
  <files>ai-service/tests/test_query_pipeline.py, ai-service/tests/test_query_degradation_policy.py, ai-service/src/corp_rag_ai/agent/graph.py</files>
  <read_first>ai-service/tests/test_query_pipeline.py, ai-service/tests/test_query_degradation_policy.py, ai-service/src/corp_rag_ai/agent/graph.py</read_first>
  <action>Add or update pipeline tests so an answerable factual query with a failing or timed-out reranker still returns `answered=true`, preserves raw retrieval order, sets `retrieval_meta.reranker_used=false`, and includes `reranker_unavailable` in `degradation_warnings`. Confirm it does not become `route=UNSUPPORTED` and does not produce the REST-level `query_timeout` warning solely because of reranker failure.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_query_pipeline.py ai-service/tests/test_query_degradation_policy.py ai-service/tests/test_query_api.py</automated>
  </verify>
  <acceptance_criteria>
    - The F-02 regression is covered at `QueryResult` level, not just `LocalReranker` level.
    - Metadata remains explicit and fail-loud per `D-206`.
  </acceptance_criteria>
  <done>Reranker degradation is visible in the final query response.</done>
</task>

<task type="auto">
  <name>Task 3: Document the reranker timeout knob in local runtime docs</name>
  <files>infra/docker-compose.yml, infra/.env.example, .env.example, ai-service/README.md</files>
  <read_first>infra/docker-compose.yml, infra/.env.example, .env.example, ai-service/README.md</read_first>
  <action>Expose and document the new reranker-step timeout in compose and env examples. Keep `AI_QUERY_TIMEOUT_SECONDS` as the overall request timeout; the reranker-step timeout must be clearly described as a smaller internal budget that triggers soft degradation. State the default and its rationale, including that it should be comfortably below the overall query timeout while still large enough for a warm CPU cross-encoder scoring pass.</action>
  <verify>
    <automated>uv run --project ai-service --group dev pytest ai-service/tests/test_main_idempotent_dispatch.py ai-service/tests/test_query_api.py</automated>
  </verify>
  <acceptance_criteria>
    - Local env examples include the new setting with a sensible default.
    - README distinguishes overall query timeout from reranker-step timeout.
    - The summary records the chosen default and why it should not fire on the warm reranker path.
  </acceptance_criteria>
  <done>Runtime operators can tune reranker degradation without changing code.</done>
</task>
</tasks>

<verification>
- [ ] `uv run --project ai-service --group dev pytest ai-service/tests/test_reranker.py ai-service/tests/test_query_pipeline.py ai-service/tests/test_query_degradation_policy.py ai-service/tests/test_query_api.py`
- [ ] With an injected failing reranker, an answerable factual query returns `answered=true`, `rerankerUsed=false`, and `reranker_unavailable`.
- [ ] No reranker-only failure produces `query_timeout`.
- [ ] A warm successful reranker path remains `rerankerUsed=true`.
- [ ] Timeout configuration cannot exceed or equal the overall query timeout without an explicit validation failure, warning, or clamp.
- [ ] Do-not-break gate: `uv run --project ai-service --group dev pytest ai-service/tests/test_embedding_adapter.py ai-service/tests/test_ingestion_orchestrator.py ai-service/tests/test_query_input_guard.py ai-service/tests/test_hybrid_retriever.py ai-service/tests/test_query_pipeline.py`
- [ ] Before writing `05.1-02-SUMMARY.md`, run or explicitly defer with reason: `uv run --project ai-service --group dev pytest ai-service/tests`
</verification>

<success_criteria>
- F-02 acceptance criteria from `PHASE5-UAT-FIX-REPORT.md` are satisfied.
- `D-193`, `D-206`, `D-212`, and `D-215` stay true in final response metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-phase-5-uat-fix-wave/05.1-02-SUMMARY.md`.
</output>
